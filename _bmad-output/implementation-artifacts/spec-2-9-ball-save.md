---
title: 'Story 2.9: Ball save'
type: 'feature'
created: '2026-09-07'
status: 'done'
baseline_revision: '75eef7aa8934d2b99a226b310495f66c7fd96117'
baseline_commit: '75eef7aa8934d2b99a226b310495f66c7fd96117'
review_loop_iteration: 0
followup_review_recommended: true
context: []
warnings: ['oversized']
deferred:
  - summary: >-
      `test/contracts.test.ts`'s exhaustive-switch test's remaining pre-existing
      `case` arms (`ball_missing`, `eject_failed`, `broken`, `device_overflow`, etc.)
      still have their string bodies unexercised by any assertion; only this
      story's own three ball-save arms were closed (QA pass, see below).
    evidence: |-
      Found by the verification-gap reviewer during Story 2.9's review pass: the
      `describeEvent()` helper's arms exist only to satisfy the `const neverEvent:
      never` exhaustiveness gate at compile time, and a wrong field reference inside
      one would type-check and ship silently. The QA pass (2026-09-07) closed the
      gap for this story's own three new arms (`ball_save_enabled` /
      `ball_save_timer_started` / `ball_saved`) with three `expect(describeEvent(...))`
      calls using distinct `untilTick`/`tick` and `player`/`tick` values so a field
      swap reddens; mutation-tested by swapping `event.untilTick` for `event.tick`
      in the `ball_save_timer_started` arm and observing the new assertion fail,
      then reverting from a saved copy (`git diff --stat` confirmed unchanged
      afterward). The remaining pre-existing arms for OTHER event variants are an
      unchanged, wider pattern this story did not introduce and QA did not expand
      scope to cover -- left deferred as originally recorded.
    location: >-
      test/contracts.test.ts:209-244 (now :209-273 after the QA closure)
    severity: low
  - summary: >-
      The new `enableBallSave` arming gate (`sources.includes(BALL_SAVE_SOURCE)`)
      interacts with grace-lapse in a way nothing in this story discusses or tests:
      once a window's grace fully lapses without a drain, `hasGraceLapsed()` clears
      `sources` entirely, so ball-save eligibility is lost for the rest of that ball.
    evidence: |-
      Found by blind-hunter during rework iteration 1's review pass, and confirmed
      against `src/sim/rules/ball-save.ts`'s `hasGraceLapsed()` (resets the WHOLE
      `machine.ballSave` to `EMPTY_BALL_SAVE`, `sources` included) and its call site
      in `ball-controller.ts`'s `step()`, evaluated every tick before device events
      are read. Before this rework, arming had no gate on `sources` at all, so this
      specific consequence is new. Currently dormant: nothing in Epic 2 produces a
      second mid-ball `ball_launched` outside a save's own re-serve (which this
      rework's fix correctly excludes from arming anyway), so the gap is unreachable
      today. Story 3.7 (Quick multiball, the first second ball-save arming source
      per this story's own Design Notes) is the first story positioned to make it
      reachable and should account for it explicitly.
    location: >-
      src/sim/rules/ball-controller.ts:533 (the `sources.includes(BALL_SAVE_SOURCE)`
      arming gate; was :470 before rework iteration 1, corrected to :513 by
      rework iteration 2 itself, which was ALSO wrong -- that correction was
      computed before rework iteration 2's own ~48-line comment block above the
      `awaitingSaveRelaunch` declaration was added, and nobody re-walked the
      pointer afterward. Build-auto review pass (2026-09-08) confirmed :533 by
      `grep -n` against the tree as it now stands.) interacting with
      src/sim/rules/ball-save.ts's `hasGraceLapsed()`
    severity: medium
  - summary: >-
      `awaitingSaveRelaunch`'s rework-iteration-2 bounded-lifetime fix is a
      TIMEOUT, not a true per-pulse causal binding: any `ball_launched` arriving
      inside the bounded window is still consumed as the deferred pulse's own
      relaunch, with no correlation identifier distinguishing it from an
      unrelated genuine plunge landing in the same window.
    evidence: |-
      Found by blind-hunter and edge-case-hunter during the build-auto review
      pass (2026-09-08) and confirmed against the code's own inline comment in
      `src/sim/rules/ball-controller.ts` ("this is a TIMEOUT, not a true causal
      binding -- there is still no per-pulse identity check"). Safe today only
      because `bd_shooter` holds one ball and `s_shooter_lane` is a single
      physical switch, so between the deferred pulse firing and its own
      resulting launch there is exactly one possible open edge -- a manual
      plunge in that gap would produce the SAME `ball_launched` the pulse would
      have, not a second, distinct event for a true correlation check to
      disambiguate. Edge-case-hunter's concrete trigger: a genuine manual plunge
      landing inside the bounded window is silently swallowed as the relaunch,
      denying that ball a fresh save window it should have gotten. A genuine
      multi-ball path through `bd_shooter` (Story 3.7, the first story to add a
      second ball-save arming source per this story's own Design Notes) would
      need real per-pulse correlation, not just a time bound, and should account
      for this explicitly.
    location: >-
      src/sim/rules/ball-controller.ts:447 (the bounded-lifetime expiry check)
      and its consumption site (the `ball_launched` handling in `step()`)
    severity: medium
  - summary: >-
      `awaitingSaveRelaunch`'s bounded-lifetime fix reuses `ballSaveGraceTicks`
      -- a tunable authored for a DIFFERENT concern (how long a drain stays
      saved past the displayed expiry) -- to also bound the physical
      pulse-to-shooter-lane travel time the record actually needs to outlive.
    evidence: |-
      Found by blind-hunter and edge-case-hunter during the build-auto review
      pass (2026-09-08), and disclosed candidly in the code's own inline
      comment in `src/sim/rules/ball-controller.ts`: "safe today only because
      production `ballSaveGraceMs` (2000ms) vastly exceeds any realistic
      coil-pulse travel time; a future tuning pass that shrinks it well below
      that travel time would need a fresh look here." Edge-case-hunter's
      concrete trigger: if `ballSaveGraceMs` were tuned down near or below the
      real pulse-to-relaunch delay, `awaitingSaveRelaunch` could expire before
      its own genuine relaunch arrives, mis-arming a second ball-save window
      for what is actually the same re-serve. Currently unreachable -- no
      tuning pass in this epic touches `ballSaveGraceMs` downward -- but the
      coupling is real and worth a dedicated look whenever `ballSaveGraceMs` is
      next retuned.
    location: >-
      src/sim/rules/ball-controller.ts:447 (the bounded-lifetime expiry check,
      reusing the `ballSaveGraceTicks` local resolved from `ballSaveGraceMs`)
    severity: low
---

<intent-contract>

## Intent

**Problem:** `machine.ballSave` exists in `GameState` (`src/sim/contracts/state.ts:35-43`) but is completely inert — written twice as a reset, read nowhere. An early drain therefore always ends the ball, so a plunge that dribbles straight down the outlane costs a ball with no recourse, and PRD FR-19's enable/timer-start/hurry-up/grace vocabulary has no implementation at all.

**Approach:** Make `machine.ballSave` a live device owned by the ball controller (AD-18) with a rules-internal `arm({ ticks, source })` / `disarm(source)` surface: enabled at `ball_starting` with the timer stopped, started on `ball_launched`, projected to a new `l_ball_save` insert through the existing `lampsOf()` seam, and consulted at the drain so a save re-serves instead of ending the ball. Three new `…Ms` tunables and one new lamp move `header.gameStart.tuning` and `tableHash`, so the story also owes a **header-only** refresh of all five replay goldens.

## Boundaries & Constraints

**Always:**
- **AD-18 is the shape.** One machine-scoped device; the ball controller alone pulses `c_trough_eject` and `c_autolaunch` and alone mutates `ballsInPlay`; sources stack; the longest live window wins; Tilt disarms all.
- **AD-3 / AD-15.** The three durations are authored in ms **only** at the top level of `src/sim/table/tuning.ts`, each with a `source` string and a `confidence`. Every rules-side reader reaches ticks through `shotWindowTicks('<key>Ms', tuning)` — never `TICK_HZ`, never a hand-derived `'…Ticks'` name, never a nested key (`assertNoNestedMsKeys` throws).
- **AD-19.** The ball controller consumes `DeviceEvent`s only (`ball_launched`, `device_ball_entered/_left`); it never reaches for a `SwitchEvent`.
- **AD-9 / AD-16.** No colour anywhere under `src/sim/`; no `s_`/`c_`/`l_`/`bd_`-prefixed string literal outside `src/sim/table/dragonwar.ts` and `test/**` — reach every name through `TABLE` property access or a `TABLE`-derived expression.
- **The window comparison is inclusive `<=`, matching the project's only two existing tick windows** (`src/sim/rules/devices/shots.ts:67`, `src/sim/rules/devices/index.ts:258,296-300`). Expiry is evaluated **before** this tick's own device events are read, the same ordering both of those use.
- **`BallSaveState`'s serialized field set stays exactly `{ untilTick, sources }`.** It is inside the hashed `GameState`, so adding or renaming a field moves `expectedHash` and `expectedGameStateHash` on all five goldens — which is neither a header-only refresh nor a granted trajectory re-record.
- **Arming must be unreachable in `phase: 'attract'`.** All five goldens plunge (`ball_launched` fires) but never press `s_start`; if the timer could start outside a game, `machine.ballSave.untilTick` would change and every golden's state hash would go stale.
- `assets/src/dragonwar.blend` and `public/assets/*` are re-generated together with the `TABLE` change in one commit — `tools/export.py:194` rejects an `l_` node that is not a `TABLE.lamps` key, and `lamp-driver.ts:343` throws on a `TABLE.lamps` key with no glb node.
- `export BLENDER="C:/Users/Josh/tools/blender-5.2.1-windows-x64/blender.exe"` in every shell before any test or export command. Unset, the Blender-gated cases **skip** rather than fail, which reads as success.

**Block If:**
- Any golden's `expectedHash`, `expectedGameStateHash`, `expectedCheckpointHashes`, `transitions`, `coilPrologue`, `durationTicks` or `assetHash` moves. That is not a header refresh — **HALT and report**; do not re-record around it and do not switch a parity check off. A trajectory re-record needs the author's explicit grant and is out of this story's budget.
- The acceptance criteria cannot be met inside `BallSaveState`'s existing `{ untilTick, sources }` shape. Widening it costs five state hashes — **HALT** with the specific AC that forces it and the field you would add.
- The suite baseline moves in a way this story does not explain (files, tests, skips, `lint:boundaries` file count, `check:ad7`'s exact passing count of 3).

**Never:**
- Do not build tilt. Story 2.11 owns `s_tilt_bob`, `s_slam_tilt`, the warning ladder and the coil-disable batch. This story reads `machine.tilt.tilted` and ships the guard; it emits no tilt event and closes no tilt switch.
- Do not build a second arming source. No Epic 2 mode arms ball save; Quick multiball (Story 3.7) is the first.
- Do not add a Backglass row, a `ShowCommand`, a flasher, audio, or a `ModeView` for ball save. Story 2.6 delivered the DMD and chartered no ball-save field.
- Do not touch `INSERT_EMISSIVE_LEVEL`, the light budget, exposure or tonemapping — Story 4.1/4.2 own the scene's headroom (AD-12 clause 3), and Epic 2 must not pre-empt them.
- Do not decide DW-212 (the Lock insert's attract behaviour) or DW-204 (the skill shot's lane freeze). Both are the author's.
- Do not make `disarm` a no-op on the last source: an empty `sources` must return `untilTick` to `null`.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Enable, timer stopped | `ball_starting` fires in `phase: 'game'` | `ball_save_enabled` emitted; `machine.ballSave.sources` contains the controller's source; `untilTick` stays `null`; `l_ball_save` projects `{ role: 'off', step: 0 }` | No error expected |
| Timer starts | `ball_launched` at tick `L`, save enabled | `ball_save_timer_started { untilTick, tick }`; `untilTick === L + ballSaveTicks`; `l_ball_save` projects `{ role: 'lit', step: 1 }` | No error expected |
| Hurry-up | current tick is within the last `ballSaveHurryUpTicks` of the window | `l_ball_save` projects `{ role: 'lit', step: 3 }`; no extra command traffic per blink (presentation times the cadence) | No error expected |
| Displayed expiry | current tick `> untilTick` | `l_ball_save` projects `{ role: 'off', step: 0 }` — the insert follows the *displayed* window, and the grace that follows is deliberately invisible (PRD FR-19 defines grace as *past the displayed expiry*) | No error expected |
| Grace lapsed | current tick `> untilTick + graceTicks` | The device is disarmed; a drain from here ends the ball | No error expected |
| Drain inside the window | drain (parking entry taking `ballsInPlay` to 0) at `tick <= untilTick` | `ball_saved { player, tick }`; **no** `ball_ended`; ball number, `currentPlayer` and `modes[]` unchanged; `c_trough_eject` pulsed | No error expected |
| Drain inside grace | `untilTick < tick <= untilTick + graceTicks` | Saved exactly as above — grace is past the **displayed** expiry, so the insert is already off | No error expected |
| Drain past grace | `tick > untilTick + graceTicks` | Normal Story 2.5 drain: `ball_ended`, teardown, rotation or game over | No error expected |
| Autolaunch of the saved ball | served ball reaches the shooter lane (`device_ball_entered { device: bd_shooter }`) after a save | `c_autolaunch` pulsed **then**, not in the drain tick's batch | A pulse into an empty lane launches nothing (`src/sim/physics/devices.ts:489`), so same-tick pulsing silently fails FR-19 |
| Enabled but never launched | drain while `sources` non-empty and `untilTick === null` | **Not** saved — the ball ends normally | No error expected |
| Two sources | `arm({ ticks: a, source: 'x' })` then `arm({ ticks: b, source: 'y' })` at ticks `t1`, `t2` | `untilTick === max(t1 + a, t2 + b)`; both names in `sources` | No error expected |
| Disarm one of two | `disarm('x')` while `'y'` is live | `'x'` removed, `'y'` remains, device still armed, `untilTick` non-`null` | No error expected |
| Disarm the last source | `disarm('y')` leaving `sources` empty | `untilTick` returns to `null`; `l_ball_save` off | No error expected |
| Disarm an unknown source | `disarm('never-armed')` | No change; same object returned | Must not throw (Conventions: step paths never throw) |
| Tilt | `machine.tilt.tilted === true` with a live window | No save on drain **and** `l_ball_save` projects off — the device is inert while tilted | No error expected |
| Attract | `phase: 'attract'`, `ball_launched` fires (every golden does this) | Nothing arms; `machine.ballSave` stays `{ untilTick: null, sources: [] }`; no state-hash movement | No error expected |
| Between balls | `ball_will_start` | `ballSave` reset to `{ untilTick: null, sources: [] }` (existing Story 2.5 behaviour, unchanged), then the SAME `startBall()` call's own `ball_starting` immediately re-enables it, so the observable value at the end of the tick is `{ untilTick: null, sources: ['ball-controller'] }` (AC 1) — the reset is real but not independently observable; `l_ball_save` off | No error expected |

</intent-contract>

## Code Map

**Rules — the device and its owner**
- `src/sim/contracts/state.ts:35-43` — `BallSaveState { untilTick: number | null; sources: readonly string[] }`, field at `:67`. **Shape is frozen** (see Boundaries). Its doc comment already states "sources stack and the longest live window wins".
- `src/sim/rules/ball-controller.ts` — the owner. `startBall()` at `:221-258` resets `ballSave` at `:234` and queues the trough pulse at `:248`; `step()` at `:260-366`; the **drain branch at `:319-363`** is the interception point (`parkingEntryThisTick && machine.ballsInPlay === 0`). `HARDWARE_COILS` at `:169`; `ballServingCoils()` at `:145-158` is the existing precedent for reaching a coil name through `TABLE.ballDevices[*].ballSearchOrder` with no literal.
- `src/sim/rules/index.ts:229` calls `applyDeviceEvents` (decrements `ballsInPlay`) **before** `:236` calls `ballController.step(...)`. The controller therefore sees the post-decrement count; `state.machine.ballsInPlay` is still the pre-decrement value at `:229-236` if a "would reach 0" reading is wanted there.
- `src/sim/rules/devices/index.ts:283` emits `ball_launched` (on the **open** edge of `s_shooter_lane`); `:291-293` emits `device_ball_entered`. `bd_shooter` also emits `device_ball_entered` on arrival — the hook for the deferred autolaunch.
- `src/sim/contracts/events.ts:180-190` — the ten-member `SemanticEvent` union; `EventName` is derived at `:86`. Template shape at `:117-130`. **`ball_save_*` is absent entirely** — three new members.
- `src/sim/table/dragonwar.ts:377-390` — `bd_shooter`, `kind: 'non-parking'`, **no `ejectCoil`**; its launch coil is the first `pulse` in `ballSearchOrder` (`c_autolaunch`, `:384`). `c_autolaunch` declared at `:299`. `c_autolaunch` is already excluded from `HARDWARE_COILS` by construction, so a `disable` batch cannot swallow the pulse.
- `src/sim/physics/devices.ts:478-479,489+` — `launch()` resolves a ball resting in the entry zone; **a pulse into an empty shooter lane launches nothing**. This is why the autolaunch must be deferred.

**Tunables**
- `src/sim/table/tuning.ts` — `entry()` factory at `:49-51`; the Story 2.4 window trio at `:312-334` is the closest precedent (top-level scalars, doc-commented block, `'unverified'`). `Confidence` is `'high' | 'medium' | 'low' | 'unverified'` (`dragonwar.ts:54`). `shotWindowTicks()` at `:815-822` is generic over any top-level `…Ms` key — Story 2.4 already used it for the non-shot `lockCaptureWindowMs` (`src/sim/rules/devices/index.ts:236-239`). `msToTicks()` at `:672` uses `Math.round` and is **not exported**.
- `tools/boundary-lint.mjs:511-514,541-552` — `sim-no-literal-ms`: any identifier ending `Ms`/`_MS` bound to a numeric literal anywhere under `src/sim/` except `tuning.ts`. `:533` — `sim-one-tick-constant` bans naming `TICK_HZ` outside `contracts/time.ts` and `tuning.ts`.

**Lamps**
- `src/sim/table/dragonwar.ts:219-222` — `LampSubject`, a three-arm union, module-private. `:638-653` — the fourteen inserts; each entry is `{ channel: 'insert', group: 'lg_inserts', subject: {...} satisfies LampSubject }` and the key **is** the glb node name.
- `src/sim/rules/lamps.ts:70-91` — `projectLamp()`. The `lock` arm at `:76-78` resolves **above** the `!player` guard at `:79-81`; `letter` and `lane` resolve below it. Constants at `:44-47` — **no `LIT_STEP_3` exists yet**. `lampsOf()` at `:101-110` iterates `TABLE.lamps` generically. **Only exhaustiveness failure: `:86` reads `subject.lane`** after excluding `lock`/`letter`, so a fourth arm fails `pnpm typecheck` there; there is no `assertNever`.
- `src/sim/loop/index.ts:439-447` — the per-lamp diff; `:301` seeds `previousLamps` at boot. Both generic; no change needed.
- `src/presentation/lighting/grammar.ts:33-40,56` — `lit`/step 3 is already a supported `(role, step)` (`intensity 1.8`, `blinkPeriodMs 160`). No presentation change is required.

**Assets**
- `tools/make-placeholder-blend.py:3324-3330` — `add_insert(name, cx, cy, half_lens, half_cup)`; z constants at `:3316-3318` (lens top at table z = **−0.3 mm**, cup to −7.0 mm); `LANE_INSERT_HALF_MM`/`LANE_CUP_HALF_MM` at `:3319-3322`; `l_lock`'s single call at `:3359` is the closest template. Every `cx/cy` is derived from an existing local, never a re-typed literal (DW-149).
- `tools/export.py:446-486,597-603` — the collision doc is built from `col_`, `sw_` and `ballDevices` only, so **an `l_` node cannot move `assetHash`** (already pinned by `test/asset-contract.test.ts:2706-2710`). `:194` requires every `l_` node to be a known `TABLE.lamps` key.

**Replay / goldens**
- `src/sim/loop/replay.ts:145-147` `tableHash()` hashes the whole `TABLE`; `:150-152` `assetHash(doc)`; `:248-283` `assertHeaderMatchesLiveEnvironment` runs five checks first thing in `runReplay` (`:339`) and throws `StaleReplayHeaderError` (`:240`); `:275-282` compares `canonicalize(resolveTuning())` against the header — **`source` prose and `confidence` included**.
- `test/replays/*.golden.json` — all five currently `tableHash "672573c"`, `assetHash "ab163ff"`, `physicsVersion "v1-ce6772ef"` (**measured at this tree, 2026-09-07**). `GoldenFile` shape at `test/replay-goldens.test.ts:98-121`. `roll-and-drain` alone carries `checkpointTicks` / `expectedCheckpointHashes`. `test/replay-goldens.test.ts:64` records that none of the five presses `s_start`.

**Tests in the blast radius (read these before editing them)**
- `test/rules-lifecycle.test.ts:88-100,111-127,131-148` — launch at tick 10, drain at tick 20. **Inside any real save window → these break.**
- `test/rules-lifecycle.test.ts:152-187` — seeds `ballSave: { untilTick: 500, sources: ['test'] }` **and** `tilt: { tilted: true }`, then drains at tick 1. Under the Tilt guard this stays green unmodified; it becomes an accidental pin of "Tilt disarms all".
- `test/rules-lifecycle.test.ts:190-305` — game-over cases; `untilTick: null` and no launch, so unaffected.
- `test/rules-modes.test.ts:478-495` — `threeBallDrawSequence()`, launches at 7 and drains at 10. **Breaks.**
- `test/table.test.ts:405-415` — a hard fourteen-name literal array; a fifteenth lamp reddens it. `:417-445` are both-directions lane/letter checks a `ball_save` subject passes.
- `test/asset-contract.test.ts:2607-2615` `subjectSwitchName()` — a fourth kind **falls through to the Lock's switch with no type error**, and `:2646-2662` then demands `l_ball_save`'s centre sit inside `sw_lock_lane`. Needs a structural fix, not an edit.
- `test/contracts.test.ts:209-244` — exhaustive `switch` with `const neverEvent: never`; three new events need three new arms or `pnpm typecheck` fails.
- `test/lighting-integration.test.ts:189-203` — **no tick may carry more than two `LampCommand`s.** Keeping `l_ball_save` off until the timer starts keeps this satisfied; lighting it at `ball_starting` (the same tick the base mode lights lanes) risks breaching it.
- `test/rules-devices-headless.test.ts:193-201` `ENTRY_FILES` — a new `test/rules-*.test.ts` is **required** by the ratchet at `:215-236` and must be hand-added, and its module closure may not touch `sim/physics`, `sim/loop`, `@babylonjs` or `node:fs`.
- `test/util/switch-script.ts` — `close/open/at/build` (`:44-81`), `runRulesScript(script, { durationTicks, initialState?, tuning?, adjustments? })` (`:228`), results at `:181-192` (`statesByTick`, `events`, `coilCommands`). `assertTicksInRange` (`:91-97`) requires `durationTicks >= ` the largest scripted tick. Measured: 2321 scripted ticks across 60 runner calls cost 79 ms of test time, so multi-thousand-tick runs are cheap.

## Tasks & Acceptance

**Execution:**

1. `src/sim/table/tuning.ts` — add three top-level scalars in one doc-commented block, following the `:312-334` precedent: `ballSaveMs: entry(8000, …, 'unverified')`, `ballSaveHurryUpMs: entry(2000, …, 'unverified')`, `ballSaveGraceMs: entry(2000, …, 'unverified')`. `ballSaveGraceMs`'s `source` **cites PRD FR-19's named default of 2 s `[ASSUMPTION]`** (a transcription); the other two `source` strings state plainly that no artifact names a figure and that the value is authored and adjustable until Epic 3's playtest freeze. — *AD-3/AD-15: the only place a literal ms may be bound.*

2. `src/sim/contracts/events.ts` — add `BallSaveEnabledEvent { type: 'ball_save_enabled', tick }`, `BallSaveTimerStartedEvent { type: 'ball_save_timer_started', untilTick, tick }` and `BallSavedEvent { type: 'ball_saved', player, tick }` to the union at `:180-190`, in the file's existing one-interface-per-event shape. — *AD-9 payload-completeness; the Conventions row already reserves `_enabled` vs `_timer_started` as distinct.*

3. `src/sim/rules/ball-save.ts` (new, under `src/sim/rules/`) — pure helpers over `BallSaveState`: `armBallSave(state, { ticks, source }, tick)` (adds the source; `untilTick = max(existing, tick + ticks)`), `disarmBallSave(state, source)` (removes the name; `untilTick` returns to `null` when `sources` empties; unknown source is a no-op returning the same object), `enableBallSave(state, source)` (records the source with `untilTick` left `null`), and predicates for *running*, *within hurry-up* and *within grace*, each taking `tick` and the resolved tick counts. Inclusive `<=` throughout. — *Keeps AD-7's "plain data, mutated only inside `rules.step`" while giving the device a testable surface; a separate file keeps `ball-controller.ts`'s pinned `applyDeviceEvents` contract untouched.*

4. `src/sim/rules/ball-controller.ts` — wire the device: enable at `ball_starting` inside `startBall()` and emit `ball_save_enabled`; on `ball_launched` (in `step()`, gated on `phase === 'game'`) call `armBallSave` with `shotWindowTicks('ballSaveMs', tuning)` and emit `ball_save_timer_started`; in the drain branch at `:319-363`, when the save is live and `machine.tilt.tilted` is false, emit `ball_saved` and pulse the trough-eject coil **instead of** running the teardown/`ball_ended`/rotation path; disarm once the grace has lapsed. Resolve `c_autolaunch` through `TABLE.ballDevices.bd_shooter.ballSearchOrder`'s first `pulse` step — no literal. The controller must now receive the resolved tuning (mirror `createBallController(adjustments)`'s existing construction-time resolution). — *AD-18: this module alone pulses both coils and alone mutates `ballsInPlay`.*

5. `src/sim/rules/ball-controller.ts` — pulse `c_autolaunch` when the re-served ball reaches the shooter lane (`device_ball_entered { device: bd_shooter }`) following a save, **not** in the drain tick's command batch. — *`src/sim/physics/devices.ts:489` resolves a ball resting in the entry zone; a same-tick pulse fires into an empty lane and launches nothing, so the same-tick reading cannot satisfy FR-19's "a saved ball is auto-launched".*

6. `src/sim/table/dragonwar.ts` — add a fourth `LampSubject` arm `{ readonly kind: 'ball_save' }` at `:219-222` and a fifteenth entry `l_ball_save: { channel: 'insert', group: 'lg_inserts', subject: { kind: 'ball_save' } satisfies LampSubject }` at the end of the `lamps` block. — *AD-9: lamp state is a projection; the key is also the glb node name.*

7. `src/sim/rules/lamps.ts` — add a `LIT_STEP_3` constant and a `ball_save` branch in `projectLamp()` **beside the `lock` arm at `:76-78`, above the `!player` guard** (the subject is machine-scoped): off when not running or when `machine.tilt.tilted`; `lit`/1 while running; `lit`/3 within the last `ballSaveHurryUpTicks`; off from the displayed expiry onward (the grace window is deliberately invisible — PRD FR-19 defines grace as *past the displayed expiry*). `lampsOf` needs the resolved tick counts; thread them the same way the projection already receives what it needs, without adding a field to `GameState`. Add an explicit `never` exhaustiveness tail so a fifth kind fails to compile rather than falling through. — *Closes the `:86` narrowing hole this story would otherwise widen.*

8. `tools/make-placeholder-blend.py` — one `add_insert('l_ball_save', …)` call using `LANE_INSERT_HALF_MM`/`LANE_CUP_HALF_MM`, placed at the **bottom centre** of the playfield: centre x within ±40 mm of `playfieldMm.w / 2`, centre y below `playfieldMm.h * 0.2`, cx/cy derived from an existing local rather than a fresh literal (DW-149). Then re-build the blend and re-export (`pnpm export:assets`, `BLENDER` exported). — *AD-11: Blender owns placement; the insert must not overlap any existing cup and its lens top must stay at z ≤ −0.3 mm (DW-47).*

9. `test/table.test.ts:405-415` — extend the lamp-name array to fifteen. — *The only hard-coded lamp inventory in the suite; deliberately literal so a lamp cannot appear unnoticed.*

10. `test/asset-contract.test.ts:2607-2662` — make `subjectSwitchName()` return `string | null` for a subject with no `sw_` zone and skip the containment assertion for those, **and add a derived count assertion** that every zone-bearing subject was actually checked (so the loop cannot go silently vacuous). Add a replacement placement pin for `l_ball_save`: inside the playfield rectangle and within the bottom-centre bounds of task 8, measured off the exported glb. — *Ball Save has no switch; the fall-through would otherwise demand its centre sit inside `sw_lock_lane`.*

11. `test/contracts.test.ts:209-244` — add the three new `case` arms to the exhaustive event switch. — *`const neverEvent: never` makes this a `pnpm typecheck` failure otherwise.*

12. `test/rules-ball-save.test.ts` (new) — headless rules tests: the enable/timer-start distinction; the lamp step ladder; the four-probe boundary run (task 15); multi-source arbitration and `disarm`; the Tilt guard; the attract no-op. Then add its path to `ENTRY_FILES` in `test/rules-devices-headless.test.ts:193-201`. — *Rule 8 + the headless ratchet: the file is required by name the moment it exists, and its closure must not reach `sim/physics`, `sim/loop`, `@babylonjs` or `node:fs`.*

13. `test/rules-lifecycle.test.ts`, `test/rules-modes.test.ts` — move the drains in the launch-then-drain scripts (`rules-lifecycle:88-100,111-127,131-148`; `rules-modes:478-495`) past `untilTick + graceTicks`, or disarm explicitly, **preserving exactly what each test pins** (rotation, the Hot-seat window's two conjuncts, the tick-20 event ordering, the three-ball sequence). Do not weaken an assertion to make it pass. — *These tests were written before a drain could be saved; the behaviour genuinely changed.*

14. `test/replays/*.golden.json` — **header-only** refresh of all five: rewrite `header.tableHash` and `header.gameStart.tuning`, **append** to `notes`, and leave `assetHash`, `transitions`, `coilPrologue`, `durationTicks`, `expectedHash`, `expectedGameStateHash` and `expectedCheckpointHashes` byte-identical. Build the recording harness **in the scratchpad, never under `test/`** (a `test/**/*.test.ts` file silently joins `pnpm test`), over the shipped `runReplay` / `buildHeader` / `tableHash()` / `assetHash(doc)` surface with a scratchpad vitest config naming the absolute harness path. Trace BEFORE and AFTER on a tick stride into full-precision per-ball TSV and diff them — expect no output; ~1,132 sampled rows across the five (80 / 32 / 371 / 384 / 265) is the known corpus, and a materially different row count means the instrument is wrong. **Serialize `gameStart.tuning` in `resolveTuning()`'s natural declaration order with `value`/`source`/`confidence` per entry** — DW-213's `reopen_if` fires if the order flips back to sorted/`confidence`-first, turning a `wontfix-accepted` into live work. — *Story 2.4 (`b9f5431`) is the precedent: 32 insertions / 2 deletions per golden, header-only, no author grant needed.*

15. `test/rules-ball-save.test.ts` — the boundary run must probe **four** ticks, not three: `untilTick − 1` (saved), `untilTick + graceTicks − 1` (saved), **`untilTick + graceTicks` exactly (saved)**, and `untilTick + graceTicks + 1` (ends). Use an override tuning built from **independently authored small literals** (e.g. 100 ms / 30 ms at 1 kHz) and script the drains at absolute ticks written out from those literals — never read back from `shotWindowTicks()`. — *The epic's own three probes cannot catch a `<=` → `<` mutation: `untilTick + grace − 1` passes under both. The exact-boundary probe is the one that reddens, exactly as Story 2.4's own boundary mutation did. Satisfies AC 11.*

**Acceptance Criteria:**

- **AC 1 — enable is not a start.** Given a game in progress, when `ball_starting` fires, then `ball_save_enabled` is emitted, `machine.ballSave.untilTick` is still `null`, and `l_ball_save` projects `{ role: 'off', step: 0 }`.
- **AC 2 — the timer starts at the plunge and the insert follows it.** Given the save is enabled, when `ball_launched` fires at tick `L`, then `ball_save_timer_started` is emitted, `untilTick === L + shotWindowTicks('ballSaveMs', tuning)`, and sampling `lampsOf()` across the whole window yields a **non-empty** step-1 span followed by a **non-empty** step-3 span, with every step-3 tick later than every step-1 tick, and `off` from the displayed expiry onward.
- **AC 3 — a save re-serves instead of ending the ball.** Given a live window, when a parking entry would take `ballsInPlay` to 0, then `ball_saved { player }` is emitted, **no** `ball_ended` is emitted, `currentPlayer`, `players[i].ballNumber` and `modes[]` are unchanged, and a `pulse` on `TABLE.ballDevices.bd_trough.ejectCoil` appears in that tick's `coilCommands`.
- **AC 4 — the autolaunch is deferred to the served ball's arrival.** Given a save has re-served, when `device_ball_entered { device: bd_shooter }` arrives, then and only then does a `pulse` on `bd_shooter`'s `ballSearchOrder` launch coil appear in `coilCommands` — and it is absent from the drain tick's batch.
- **AC 5 — arbitration.** Given `arm({ ticks: a, source: 'x' })` at `t1` and `arm({ ticks: b, source: 'y' })` at `t2` with `t1 + a !== t2 + b`, when both are live, then `untilTick === max(t1 + a, t2 + b)`; `disarm('x')` leaves `'y'` armed with `untilTick` non-`null`; `disarm('y')` then returns `untilTick` to `null`; and `disarm` of a never-armed source returns the same object without throwing.
- **AC 6 — Tilt makes the device inert.** Given a live window and `machine.tilt.tilted === true`, when a drain occurs, then the ball ends normally and `l_ball_save` projects off. (Story 2.11 will additionally call `disarm(source)` on the tilt event; this story ships the read-side guard, which is the only half testable at this tree.)
- **AC 7 — the goldens moved for exactly one reason.** Given the change is complete, when `git diff test/replays/` is inspected, then `header.tableHash`, `header.gameStart.tuning` and `notes` are the only fields that moved on any of the five, and `pnpm test` passes with no `StaleReplayHeaderError`.
- **AC 8 (Integration AC) — the device drives a real consumer through the real seam.** Given a game started through `createLoop`, when the ball is launched and the window expires, then `FrameOutput.commands` carries `LampCommand { lamp: 'l_ball_save', role: 'lit', step: 1 }`, later `step: 3`, and finally `{ role: 'off', step: 0 }` — asserted on the loop's own command stream, not by reading `machine.ballSave`, and with no tick of the run carrying more than two `LampCommand`s.
- **AC 9 — the tunables are pinned by consequence, not by re-import.** Given the **production** tuning, when a launch is followed by a drain 3,000 ticks later, then the ball is saved; when it is followed by a drain 30,000 ticks later, then the ball ends; and `0 < ballSaveHurryUpMs < ballSaveMs` and `ballSaveGraceMs > 0` hold on the resolved tuning. No expectation in these assertions may be computed from the tunable under test.
- **AC 10 — nothing arms outside a game.** Given `phase: 'attract'`, when `ball_launched` fires, then `machine.ballSave` remains `{ untilTick: null, sources: [] }` and all five goldens' `expectedGameStateHash` values are unchanged.
- **AC 11 — the drain boundary is straddled on both sides.** Given a switch-script test under an override tuning of independently authored literals, when drains occur at `untilTick − 1`, `untilTick + graceTicks − 1`, `untilTick + graceTicks` and `untilTick + graceTicks + 1`, then the first three save and the fourth ends the ball. (The epic's stated three probes are the first, second and fourth; the third is added because none of the three can distinguish `<=` from `<` at the boundary — see the Rule 19 mutation below.)

### Rework iteration 1 (2026-09-07) -- the author's ball-save re-arm decision

- [x] **[Review][DW-218] A save's own re-serve must NOT re-arm the ball-save window.** The author decided **option 2** of the four put to them: **a *player* plunge arms the window; a save's own re-serve does not.** Today `ball-controller.ts` arms on *every* `ball_launched`, and the save's `c_autolaunch` re-serve opens `s_shooter_lane` and emits exactly that event, so each save restarts a fresh full window. Distinguish the two launch causes at the arming site -- the story already carries a per-ball flag of exactly this kind (`awaitingSaveLaunch`) held in the controller closure, and that is the pattern to follow: **a closure-held discriminator costs zero state hashes, whereas a new `GameState` field would move `expectedHash`/`expectedGameStateHash` on all five goldens and is forbidden by this spec's own Block If.** Do not change `BallSaveState`'s serialized shape.
- [x] **[Review] Wire the `enableBallSave` gate -- a defect fix, not an open question.** `armBallSave` never checks whether the controller's source is present in `sources`, so AC 1's enable-versus-start distinction exists in the event stream and **nowhere in behaviour**. That is a bug under any re-arm semantics; the author's decision settles what the gate now guards. Give it a mutation that reddens: today, deleting the enable step entirely leaves the arming path working.
- [x] **[Review] Prove the fix with the real-physics harness that found the defect, not with unit tests alone.** Re-run 120,000 ticks at **production** tuning, seed 0, no input, over a real `createLoop` + real physics, and show `ball_ended` now occurs at a natural cadence with **at most one save window per ball**. **Keep the control**: the short-window run (`ballSaveMs` 500 / grace 100) must still give **zero saves and `ball_ended` at tick 4273** -- without it the new result is a null measurement from a possibly-blind harness, which is precisely how this defect survived the first pass. Add the passing form of this as a real test so the property is pinned, not merely observed once.
- [x] **[Review] Do not regress the golden budget.** All five goldens are mid-story at the header-only refresh (`tableHash` `bac5816c`). Any movement in `transitions`, `coilPrologue`, `durationTicks`, `expectedHash`, `expectedGameStateHash`, `expectedCheckpointHashes` or `assetHash` is a **HALT**, not a re-record.

#### Rework iteration 1 -- implemented (2026-09-07)

**The fix.** `src/sim/rules/ball-controller.ts` gains a second closure-held cross-tick flag, `awaitingSaveRelaunch` (alongside the existing `awaitingSaveLaunch`), following the exact pattern the checklist named: set `true` only when the deferred-autolaunch coil pulse genuinely fires (arrival at `bd_shooter` while `awaitingSaveLaunch` and not tilted), consumed by the very next `ball_launched` -- which is that same pulse's own `s_shooter_lane` opening one or more ticks later. When consumed, that `ball_launched` does **not** call `armBallSave`. Reset at `ball_will_start` for the identical stale-flag reason `awaitingSaveLaunch` already is. `BallSaveState`'s serialized `{ untilTick, sources }` shape is untouched -- zero state-hash movement, as required.

**The `enableBallSave` gate.** The controller's own `ball_launched` arming branch is now additionally gated on `nextState.machine.ballSave.sources.includes(BALL_SAVE_SOURCE)` -- i.e. genuinely enabled by `ball_starting` first. This is a call-site gate, not a change to `armBallSave()` itself: `armBallSave()` stays a general-purpose helper with no enablement precondition, so AC 5's headless arbitration test (which arms sources `'x'`/`'y'` directly, deliberately bypassing `enableBallSave`) is unaffected.

**Proof (implementer's own harness).** A temporary scratchpad harness (`test/fixtures/dw218-verify-tmp/`, deleted after use -- never joined `pnpm test`) reproduced the review's measurement at this tree: production tuning, seed 0, no player input beyond a mechanical plunge per ball. Result: `ball_saved` at ticks 4276/8575 (ball 1), 17120/21419 (ball 2), 29964/34263 (ball 3); `ball_ended` at 12849/25693/38537; `game_over` reached. Pre-fix (reverted for comparison via the same harness): `finalPhase` never left `'game'`. The control (`ballSaveMs` 500 / `ballSaveGraceMs` 100) gave zero saves and a natural `ball_ended` at tick 4276, confirming the harness itself was not blind. The **passing form** of the main property is now a permanent test, `test/rules-ball-save-integration.test.ts`'s new `"DW-218 regression guard"` case: production tuning, real `createLoop`, 60,000-tick budget. **[Assertion set superseded twice — corrected at code review, iteration 2, 2026-09-08.]** As written here it asserted `finalPhase === 'game_over'`, `ended.length === 3` and `saved.length < 10`; this rework's own review then tightened it to the exact measured `saved`/`ended` tick arrays; code review iteration 2 replaced *those* with structural invariants (see `### Rework iteration 2 (2026-09-08) -- four MEDs from code review pass 2

All four are MED, in-story, fix-risk low, dispositioned NOT deferrable under Rule 15. The lead independently re-measured the first two before accepting them.

- [x] **[Review][DW-224+DW-225] Give `awaitingSaveRelaunch` a causal binding and a timeout -- one fix closes both.** It is a cross-tick latch with neither. The decisive consequence is worse than either ledger entry records and is why this is not deferrable: **if the deferred autolaunch resolves to `eject_failed`, the saved ball rests in the shooter lane with `ballsInPlay` at 0, so no parking entry can occur, so no `ball_ended` and no `ball_will_start` -- the game HANGS in `phase: 'game'` with no recovery until ball search arrives in Story 2.12.** The sharp corollary: the latch's only reset is at `ball_will_start`, so **in the very scenario its own reset comment cites as justification, the reset can never run.** Follow the in-file, in-layer precedent that needs no new tunable: `pendingLockLaneClosure` (`sim/rules/devices/index.ts:243,258-260`) is a `{ startTick }` record that self-clears off an already-resolved tick count, and the ball controller already resolves `ballSaveGraceTicks`. **Pin the hang itself with a test** -- a latch that expires is worth little if nothing proves the game recovers.
- [x] **[Review] AC 10's `phase === 'game'` gate ships unprotected -- restore a mutation that actually reddens.** Rework 1 added a second conjunct (`sources.includes(BALL_SAVE_SOURCE)`) to the exact line AC 10's mutation targets, and the mutation block was never re-walked. **The lead re-measured this independently: deleting `nextState.phase === 'game' && ` leaves 29/29 ball-save tests AND 52/52 golden tests green.** No golden presses `s_start`, so `sources` is `[]` throughout and the second conjunct blocks arming by itself; AC 10's own attract test starts from that same empty-`sources` state and so passes for the wrong reason. Fix: a case seeding `phase !== 'game'` with `sources` **already holding** `BALL_SAVE_SOURCE` -- the state reachable after `game_over`, where the controller does not call `startBall()`. **AD-6 clause (1) has already been corrected in the spine** to say the `sources` conjunct is what does the work and the phase test is defence-in-depth; keep the spec consistent with that.
- [x] **[Review] `ballSaveHurryUpMs` has no production-magnitude pin -- epic vacuity #43's exact shape, one tunable over.** **The lead re-measured: setting it to `1` leaves all 29 ball-save tests green**, because every behavioural hurry-up assertion runs under an override tuning (20 or 40 ticks), so the production value is never on the path; `0 < 1 < 8000` satisfies the relational assertion. The only red anywhere is a `StaleReplayHeaderError` on the golden headers, whose remedy is a header refresh -- so a 1 ms hurry-up could ship green. Fix: two production-tuning probes with offsets **authored at the probe**, mirroring what AC 9 already does for the grace. Do not derive an expectation from the tunable.
- [x] **[Review] Re-walk every `mutation:` line in `## Verification` at the FINAL tree.** Pass 2 found one false (AC 10) and two carrying incidental false claims. The lesson generalises and is the reason this item exists: **a mutation genuinely observed red can be silently invalidated by a later edit to the same block, and nothing re-checks it unless someone re-walks it.** Every line must name a mutation someone actually ran and observed at the tree as it now stands.

**Golden discipline is unchanged and remains a HALT:** all five carry this story's header-only refresh (`tableHash` `bac5816c`); the rework has moved none. Any movement in `transitions`, `coilPrologue`, `durationTicks`, `expectedHash`, `expectedGameStateHash`, `expectedCheckpointHashes` or `assetHash` is a HALT, not a re-record.

### Review Findings`, iteration 2). The current, shipped assertion set is: `finalPhase === 'game_over'`, **`timerStarted.length === 3`** (one arming per ball — the DW-218 mechanism stated directly), `ended.length === 3`, `saved.length > 0` (non-vacuity), and every `ball_saved` falling inside some armed window plus its grace.

**Proof (independent re-verification, build-auto stage, same day).** The lead independently re-ran the exact protocol this checklist item names -- 120,000 ticks, production tuning, seed 0, no input, over a real `createLoop` + the real collision document, via a throwaway scratchpad vitest harness (never under `test/`, deleted after use, never committed) -- and got tick-for-tick the same numbers as the implementer's own scratchpad run: `saved=[4276,8575,17120,21419,29964,34263]`, `ended=[12849,25693,38537]`, `game_over` reached well inside the 120,000-tick budget (last event at tick 38537). The **control** was also independently re-run at the full protocol: `ballSaveMs` 500 / `ballSaveGraceMs` 100 gave zero saves and `ball_ended` at tick 4276 -- the exact same tick the production run's first `ball_saved` landed, proving both runs observed the identical real-physics drain and only the window length changed the outcome (the original code review's own control measured tick 4273 under a slightly different call sequence; the 3-tick difference is harness setup, not a physics or fix discrepancy).

Unlike the implementer's own scratchpad, **the control is now also a committed, permanent test** -- `test/rules-ball-save-integration.test.ts`'s new `"DW-218 regression control"` case, added at this stage per the checklist's own "Add the passing form as a real test in the suite, so the property is pinned rather than observed once" (read to cover both halves of the measurement, not only the production-tuning half): it runs the production tuning and the 500ms/100ms tuning back-to-back against the identical seed-0 setup and asserts `control.savedTick === -1` and `control.endedTick === production.savedTick`, so a future harness regression that silently stopped delivering physics events would fail this test rather than passing vacuously.

Two further headless tests were added to `test/rules-ball-save.test.ts` to pin the same properties cheaply and precisely: one drives a full drain-relaunch cycle through `runRulesScript()` and asserts `machine.ballSave` is byte-identical to what the original plunge set (no second `ball_save_timer_started`); one drives `createBallController()` directly (mirroring the file's own `awaitingSaveLaunch`-reset precedent) to prove `awaitingSaveRelaunch` itself resets at `ball_will_start`, so a stale flag from an abandoned save cannot swallow a later, genuine plunge. A third pins the `enableBallSave` gate: a `ball_launched` scripted against an initial state with `sources: []` (simulating "the enable step never ran") does not arm.

**Rule 19.** All three new headless assertions and the new integration regression-guard assertion were verified red under the mutation described in the checklist above (reverting `ball-controller.ts`'s `ball_launched` handling to the pre-fix unconditional `if (nextState.phase === 'game') { armBallSave(...) }`), independently re-applied and re-confirmed red by the build-auto stage itself (not merely trusting the implementer's self-report): 3 of 29 ball-save tests failed for the expected reason (`DW-218 regression guard`'s `finalPhase` stayed `'game'`; both new headless discriminator tests flipped `false`→`true`), the new control test stayed green throughout (by design -- it validates the harness, not the fix, since a single first drain's outcome does not depend on the re-arm discriminator). Restored from a saved copy (never `git checkout --`/`git stash`); `md5sum` and `git diff --stat -- src/sim/rules/ball-controller.ts` confirmed the restored file was byte-identical to the fix both before and after the revert-and-restore cycle.

**Suite.** `pnpm typecheck` clean across all three tsconfigs. `pnpm test`: **114 files / 1862 tests / 0 failed / 0 skipped** (5 more than the pre-rework 1857: three new headless tests plus two new integration tests -- the regression guard and its control). `pnpm lint:boundaries`: `OK -- 105 .ts file(s)` (unchanged file count -- no new `src/` file). `pnpm check:headers` / `pnpm check:attributions`: both clean. `pnpm check:ad7`: exactly 3 passing. `pnpm check:corridor`: 1 passing. `pnpm check:reachability`: 1 passing (~113 s). `git diff --stat` confirms no golden (`test/replays/`), asset (`public/assets/`), tuning, table, event, or lamp file moved -- this rework touches only `src/sim/rules/ball-controller.ts`, `test/rules-ball-save.test.ts`, `test/rules-ball-save-integration.test.ts` and this spec file.

### Rework iteration 2 (2026-09-08) -- code review findings

Four MED findings, none deferrable under Rule 15 (all in-story, all fix-risk low). Full evidence for each is in `### Review Findings` below; this is the actionable list.

- [x] **[Review][DW-224 + DW-225] Give `awaitingSaveRelaunch` a causal binding and a bounded lifetime -- one fix closes both.** It is a cross-tick latch whose only reset is `ball_will_start`, and in the very scenario its own reset comment cites as justification (the deferred autolaunch resolving to `eject_failed`) that reset **can never run**: the saved ball rests in the shooter lane with `ballsInPlay` at 0, so no parking entry, no `ball_ended`, no `ball_will_start` -- the game hangs in `phase: 'game'` until Story 2.12's ball search exists. Follow the in-file, in-layer precedent that needs **no new tunable**: `pendingLockLaneClosure` (`sim/rules/devices/index.ts:243,258-260`) holds `{ startTick }` and self-clears off an already-resolved tick count, and the ball controller already resolves `ballSaveGraceTicks`. Do NOT add a `GameState` field (five state hashes) and do NOT add a tunable if an already-resolved count will do. Give it a mutation that reddens: with the fix in place, a latch left set past its window must not swallow a later genuine `ball_launched`.
- [x] **[Review] Pin AC 10's `phase === 'game'` gate -- it currently ships with no test discriminating it.** Measured at review: deleting `nextState.phase === 'game' && ` leaves 77/77 green, because rework iteration 1's `sources.includes(BALL_SAVE_SOURCE)` conjunct masks it and no golden ever presses `s_start`. Add a case that isolates the gate from the source gate: seed `phase !== 'game'` **with** `sources` already holding `BALL_SAVE_SOURCE` -- the state genuinely reachable after `game_over`, where the controller does not call `startBall()` -- and assert no `ball_save_timer_started` and `untilTick` still `null`. Then re-run the AC 10 mutation and confirm it reddens that new case; correct the `## Verification` AC 10 entry to record the mutation actually observed.
- [x] **[Review] Give `ballSaveHurryUpMs` a production-magnitude consequence pin.** Measured at review: setting it to `1` leaves all 29 ball-save tests green (it satisfies `0 < 1 < 8000`, and every behavioural hurry-up assertion runs under an override tuning that replaces the production value). This is epic vacuity #43's shape and is the identical gap the previous review closed for `ballSaveGraceMs` -- mirror that fix: two production-tuning probes with offsets **authored at the probe**, never read back from the tunable under test.
- [x] **[Review] Re-walk all eleven `## Verification` mutation lines against the tree at the end of the rework.** AC 10's line went stale precisely because rework iteration 1 changed the line a mutation targeted and nobody re-ran the block; two further lines carried incidental false claims (AC 6's "without being edited", and pre-story line numbers contradicting the spec's own Matrix Test Audit). Every line must name a mutation someone actually ran and observed **at the final tree**, not at the tree it was first written against. Note also that the rework's own new behaviour (the `sources.includes` gate and the `awaitingSaveRelaunch` discriminator) belongs to no AC and has no `## Verification` line at all.

#### Rework iteration 2 -- implemented (2026-09-08)

**The fix (DW-224 + DW-225).** `src/sim/rules/ball-controller.ts`'s `awaitingSaveRelaunch` is widened from a plain boolean to the exact `{ startTick }` shape `pendingLockLaneClosure` already uses one file over (`sim/rules/devices/index.ts`): set to `{ startTick: tick }` at the moment the deferred-autolaunch pulse genuinely fires (unchanged trigger site), and expired at the TOP of `step()` -- BEFORE this tick's own device events are read, the same ordering `hasGraceLapsed()` immediately above it and `pendingLockLaneClosure` itself both already use -- once `tick > awaitingSaveRelaunch.startTick + ballSaveGraceTicks`. Reuses the already-resolved `ballSaveGraceTicks` local; no new tunable, no new `GameState` field (the record lives in the same controller-instance closure `awaitingSaveLaunch`/`awaitingSaveRelaunch` already occupied -- zero state-hash movement). This bounds how long a `ball_launched` may be trusted as "caused by MY pulse" (DW-224's causal-binding *language*) and guarantees the latch eventually releases so a later, genuinely unrelated `ball_launched` arms normally instead of being silently swallowed for the rest of the ball (DW-225's same-ball recovery) -- it does not, and cannot from `sim/rules` alone, un-strand a ball physically resting in an empty shooter lane after a real `eject_failed`; that recovery remains Story 2.12's ball search, exactly as both findings named. **[Precision correction, build-auto review pass, 2026-09-08]** — this is a bounded TIMEOUT, not a true per-pulse causal binding: nothing here checks that the consumed `ball_launched` was actually caused by this pulse, only that it arrived inside a bounded window after it. The fix's own inline code comment says so plainly and explains why it is sufficient today (single ball, single `s_shooter_lane` switch, so between the pulse firing and its own resulting launch there is exactly one possible open edge -- not a second, distinct event a true correlation check would need to disambiguate); Story 3.7's multiball would need real per-pulse correlation, not just a bound. See the frontmatter `deferred:` entries this pass added for both this and the reused-tunable coupling below.

**AC 10's isolating test.** `test/rules-ball-save.test.ts`'s `AC 10` describe block gains a second case: `phase: 'game_over'` seeded with `sources` already holding `BALL_SAVE_SOURCE` (the state genuinely reachable after `game_over`, since the drain branch's own `gameOver` path sets `phase: 'game_over'` without calling `startBall()` again) asserts a `ball_launched` still does not arm. **Mutation demonstrated:** deleting `nextState.phase === 'game' && ` off the real arming branch reddens this new case (`expected true to be false` on the `ball_save_timer_started` check) while leaving the pre-existing attract-phase case green, exactly isolating the phase gate from the already-covered sources gate; reverted from a saved copy, `git diff --stat` confirmed `ball-controller.ts` unchanged afterward.

**`ballSaveHurryUpMs`'s production-magnitude pin.** A new test in the `AC 9` describe block calls `lampsOf()` directly with `hurryUpTicks = shotWindowTicks('ballSaveHurryUpMs', PRODUCTION_TUNING)` -- the real production wiring, so a value change in `tuning.ts` genuinely flows through -- and asserts `lit`/3 at 500 ticks before the displayed expiry and `lit`/1 at 5,000 ticks before it, both offsets authored at the probe and independent of the resolved value. **Mutation demonstrated:** setting `ballSaveHurryUpMs` to `1` reddens the new test (`expected step 1 to equal step 3` at the 500-tick probe) while leaving the untouched pre-existing hurry-up assertions (all under override tunings) green; reverted from a saved copy, `git diff --stat` confirmed `tuning.ts` unchanged afterward.

**The bounded-lifetime fix's own mutation.** A new test in a `Rework iteration 2 (DW-224 + DW-225)` describe block drives `createBallController()` directly: fires the deferred-autolaunch pulse (arming the record), lets the record's own window fully elapse with no resulting `ball_launched` (simulating a same-ball `eject_failed`, chosen well inside the still-live `untilTick + graceTicks` span so the unrelated whole-device `hasGraceLapsed()` reset stays out of the way), then delivers a later, genuinely unrelated `ball_launched` and asserts it arms. **Mutation demonstrated:** short-circuiting the new expiry check (`if (false && awaitingSaveRelaunch && ...)`) reddens this test (`expected false to be true` on the `ball_save_timer_started` check); reverted from a saved copy, `git diff --stat` confirmed `ball-controller.ts` unchanged afterward.

**The `## Verification` re-walk.** All eleven AC mutation lines were reviewed against this diff. AC 1 through AC 9 and AC 11 target code this rework does not touch (`armBallSave`/`disarmBallSave`/`isRunning`/`isWithinHurryUp`/`isWithinGrace`/`hasGraceLapsed` in `ball-save.ts`; `projectLamp`/`LIT_STEP_3` in `lamps.ts`; the drain interception, the deferred-autolaunch timing, and the `phase === 'game' && sources.includes(...)` arming line itself, unchanged by this rework's diff) -- `git diff --stat` confirms this rework touches only `ball-controller.ts` and `test/rules-ball-save.test.ts`, and neither AC 1-9/11's own target lines nor their own dedicated tests moved, so their prior claims (already re-walked once by code review iteration 2 itself) stand unchanged. AC 10's own line is corrected below to record the gap's closure.

**Suite.** `pnpm typecheck` clean across all three tsconfigs. `pnpm test`: **114 files / 1867 tests / 0 failed / 0 skipped** (5 more than the pre-rework 1862: the AC 10 isolating case, the hurry-up production probe, the DW-224/225 bounded-lifetime recovery case, and its own two boundary-straddle probes). **[Corrected, build-auto review pass, 2026-09-08]** — the count first written here (1865 / "3 more") undercounted by two: the `Rework iteration 2 (DW-224 + DW-225)` describe block holds three `it()` cases, not one, and the two boundary probes were omitted from this bullet, from the Spec Change Log entry, and from the Auto Run Result. Re-measured directly (`pnpm test`): `Test Files 114 passed (114)`, `Tests 1867 passed (1867)` — matching the cycle log's own independent count taken before this finalize pass ran. `pnpm lint:boundaries`: `OK -- 105 .ts file(s)` (unchanged file count -- no new `src/` file). `pnpm check:headers` / `pnpm check:attributions`: both clean. `pnpm check:ad7`: exactly 3 passing. `git diff --stat` confirms this rework touches only `src/sim/rules/ball-controller.ts`, `test/rules-ball-save.test.ts` and this spec file -- no golden, no tuning, no table, no event, no lamp file moved.

### Review Findings

**Code review, 2026-09-07 (iteration 1, `bmad-code-review`; review tier: full — `blind-hunter`, `edge-case-hunter`, `verification-gap`, `acceptance-auditor`, all four returned, no model override since `_bmad/custom/model-overrides.yaml` does not exist on this project).** Suite re-run by the reviewer with `BLENDER` exported: **114 files / 1854 tests / 0 failed / 0 skipped** before patching, **1857 passed** after. Golden discipline re-verified independently and **structurally** (parsed JSON at `411da33` vs HEAD, never substring grep): only `header.tableHash` (`672573c`→`bac5816c`), `header.gameStart.tuning` (exactly six added keys, zero removals, zero value changes, declaration order preserved so DW-213's `reopen_if` is not tripped) and `notes` (a strict append on all five) moved. `expectedHash`, `expectedGameStateHash`, `expectedCheckpointHashes`, `checkpointTicks`, `transitions`, `coilPrologue`, `durationTicks` and `assetHash` are byte-identical on all five. **No Block-If condition was hit; no HALT on the goldens.**

#### HIGH — RESOLVED in rework iteration 1 (`DW-218`)

**Resolution note (build-auto stage, 2026-09-07, review of rework iteration 1):** the finding below is preserved verbatim as the historical record of what code review measured and why it was filed `decision-pending` rather than resolved at that time. It is **no longer unresolved or blocking** — the author's chosen fix (a player plunge arms the window; a save's own re-serve does not) is implemented, independently re-verified at the full 120,000-tick protocol (see `#### Rework iteration 1 -- implemented (2026-09-07)` above), and pinned by two new committed tests. The `enableBallSave` gating gap this finding also names is likewise closed (the checklist's second item, above). Flagged by this rework's own `blind-hunter` review pass as a stale-looking contradiction against the now-checked-off checklist; recorded here rather than deleted so the finding's own measurement and reasoning remain intact as history.

- **A ball save re-arms a full new window on the saved ball's own auto-relaunch, so the ball can never end.** `ball_launched` arms unconditionally in `phase: 'game'`, and a save's re-serve auto-launches through `c_autolaunch`, which opens `s_shooter_lane` and emits another `ball_launched`. **Measured** at review with a scratchpad harness over a real `createLoop` + real physics, production tuning, seed 0, no player input: `ball_saved` at ticks 4273, 8547, 12846 … 119996 — **28 saves in 120,000 ticks, `ball_ended` never once emitted.** Identical with the hazard coils enabled, so it is not a harness artefact. **Control:** `ballSaveMs` 500 / grace 100 yields `ball_ended` at the same tick 4273 with zero saves — so the loop is caused by the effective window (8,000 + 2,000 ticks) exceeding the ~4,275-tick natural drain interval of the shipped table, combined with the unconditional re-arm. Ball 2, the end-of-ball bonus, rotation, game over and Match are all unreachable. The suite was 1854 green throughout. This is the shape the implement-stage review considered and rejected as AD-6-mandated ("the ball-save timer re-arms on every subsequent plunge") — that rejection reasoned from AD-6's text and never measured the consequence. **The fix is a product call** (one window per ball / no re-arm on a save's own autolaunch / re-arm with the remainder), so it is filed `decision-pending` with `note=human=` rather than resolved here; the defect is unambiguous under any of them. Note also that `enableBallSave` currently has **no gating effect at all** — `armBallSave` never consults whether the controller's source is in `sources` — so AC 1's enable/start distinction is observable in the event stream but nowhere in behaviour.

#### Resolved at review (patched, suite green)

- **`[med]` The asset-contract non-vacuity guard could not fail for the reason it existed.** Task 10 required "a derived count assertion that every zone-bearing subject was actually checked (so the loop cannot go silently vacuous)". As written it compared `checked` against a second call of `subjectSwitchName()` — the very helper the `continue` consults — so a regression returning `null` for a genuinely zone-bearing subject lowered both sides together and still passed; only `toBeGreaterThan(0)` survived. This is epic vacuity #43's shape (an expectation computed from the value under test) in a new dress. Now compared against `Object.keys(TABLE.lamps).length - 1`, derived from the lamp registry alone. Found by `blind-hunter` and `acceptance-auditor` independently.
- **`[med]` The hurry-up span was pinned by nothing.** "Non-empty step-3 span" did not constrain the span's *length*: collapsing `isWithinHurryUp` to `tick > untilTick - 1` — a one-tick hurry-up warning — left all 1854 tests green, so epics.md AC 2's "moves to step 3 for the last `ballSaveHurryUpMs`" was unverified. AC 2's ladder now asserts the exact step-3 span and its first and last tick from the authored literal (20). **Mutation demonstrated:** `tick > untilTick - 1` → RED (2 tests) where it was previously green; reverted from a saved copy, `src/sim/rules/ball-save.ts` confirmed byte-identical to HEAD afterwards.
- **`[med]` The hurry-up's OPENING boundary was straddled by nothing.** `isWithinHurryUp`'s lower bound is exclusive, and no probe sat at exactly `untilTick - hurryUpTicks`; flipping that `>` to `>=` moved the step-1/step-3 transition with the whole suite green — the same shape as the epic's own AC 11 amendment, one function over. Probes added at 90 and 91. **Mutation demonstrated:** `>` → `>=` → RED (2 tests), previously green; reverted from a saved copy, tree verified unchanged.
- **`[med]` `ballSaveGraceMs` had no consequence pin at its production magnitude** — only `> 0`. Setting it to 1 left every assertion in the "pinned by consequence, not by re-import" block green (0 < 1); the sole red anywhere was `test/replay-goldens.test.ts`'s `StaleReplayHeaderError`, whose remedy is exactly the header-only golden refresh this story just performed, so a 1 ms grace could ship green. Two production-tuning probes added with offsets authored at the probe (a drain 500 ticks past the displayed expiry is saved; one 5,000 ticks past it is not), never read back from the tunable.
- **`[med]` Nothing proved a saved ball actually comes back.** The headless AC 3/AC 4 test asserts only that two `CoilCommand` objects are in the right tick's batch, and it *scripts* the re-served ball's arrival with a hand-written `close('s_shooter_lane')`; the AC 8 integration test never drained. Since the re-serve pulse is issued on the drain tick itself — the same tick the drained ball parks and `deriveDeviceSlots()` updates — a save that suppressed `ball_ended` and then served nothing would have hung the game at `ballsInPlay: 0` with the suite green. The reviewer **falsified that hypothesis empirically** (the chain works: `ball_saved` at tick 4273, no `ball_ended`, `ballsInPlay` back to 1) and then closed the gap with a real-physics test in `test/rules-ball-save-integration.test.ts`, costing ~310 ms. **Mutation demonstrated:** delete the `c_trough_eject` pulse from the save branch → RED (it also reddens the headless assertion, which asserts the same pulse); reverted from a saved copy, `src/sim/rules/ball-controller.ts` verified byte-identical to HEAD. Found by `verification-gap` and `acceptance-auditor` independently.
- **`[med]` AC 2 computed its `untilTick` expectation from the resolver under test.** `expect(started).toMatchObject({ untilTick: 3 + shotWindowTicks('ballSaveMs', OVERRIDE_TUNING) })` put the same function on both sides of the assertion, against the spec's own Boundaries ("no expectation in this story may be computed from `ballSaveMs`…") and task 15's discipline, which AC 11 follows and AC 2 did not. Now spelled out from the authored literal as `untilTick: 103`.
- **`[low]` The AC 8 integration test counted every `PresentationCommand` while its message and AC 8 both say `LampCommand`s** — stricter than the criterion, so no false green, but the failure message was untrue and an unrelated Gi/Show command landing on a lane-rotation tick would redden a ball-save test for a reason it does not intend to constrain. Now filtered to `cmd.type === 'lamp'`, matching the established sibling idiom in `test/lighting-integration.test.ts`. Flagged by three layers independently.
- **`[low]` `ballLaunchedTick` was asserted non-null and then never used**, so "a lit/1 LampCommand must appear **at the plunge**" only proved one existed somewhere in a ~520-tick run. Now anchored to the real plunge tick the loop reported.
- **`[low]` The grace lapse's full reset was pinned only on `untilTick`.** The lapse resets the whole device, dropping the controller's own enable record from `sources` for the rest of the ball — but the "drain past grace" test cannot pin that (its drain ends the ball in the same tick, and the rotation's own `startBall()` immediately re-enables the source). A dedicated test now observes the lapse in isolation, with no drain at all.
- **`[low]` `enableBallSave`'s preservation of a live `untilTick` was asserted by nothing** — its only production caller passes `EMPTY_BALL_SAVE`, where `untilTick` is already `null`, so a mutation hard-coding `null` survived the suite. Story 3.7's second source is the first caller that can reach it.
- **`[low]` `src/sim/contracts/commands.ts` still described `lampsOf()` as "a pure function of `GameState` alone"**, which this story falsified by threading `ballSaveHurryUpTicks` in as a second argument. Corrected in place.
- **`[med]` / `[low]` Two false claims in this spec, corrected in place:** the anti-vacuity paragraph's assertion that AC 2 pins `ballSaveHurryUpMs` (refuted by the lead's own M5 record — one red, not two, because AC 2 runs under an override that replaces the production value), and the I/O matrix "Between balls" row, which stated an output the fused reset-and-enable does not produce and which `test/rules-lifecycle.test.ts` was amended away from.

#### Routed / escalated (recorded, not blocking)

- **`DW-219` `[med]` `routed` → `3-7-quick-multiball-fight-the-monster`.** `disarm()` of the source that set the maximum leaves `untilTick` at the departed source's deadline, so **AD-18's "the longest LIVE window wins" stops holding after any disarm** — and the test only ever disarms the non-max-setter, so the failing direction is exercised by nothing. **On the lead's explicit question:** against the named authority — amended `epics.md` AC 5 — the implementation **does** satisfy the criterion as written: its Given/When scopes to "when both are live", where the max genuinely wins, and "`disarm(source)` of one leaves the other" is literally true. Against **AD-18's Rule read as a standing invariant** it does **not**, in a state unreachable in Epic 2 (one source, zero production `disarm` callers). Separately, **the spec's stated cost for fixing it is wrong**: no golden ever reaches `phase: 'game'` (verified structurally — no `s_start` in any golden's `transitions` or `coilPrologue`), and a per-source deadline map held in the controller closure — the pattern this same story already uses for `awaitingSaveLaunch`, and for the same stated reason — moves no state hash at all. The limitation is defensible; the justification recorded for it is not, and Story 3.7 should not inherit the belief that the fix is expensive.
- **`DW-220` `[med]` `escalated` → epic decision sheet.** Rule 20: this story changed the `lampsOf()` seam (now `lampsOf(state, ballSaveHurryUpTicks)`, a time-dependent lamp input threaded from the loop's resolved tuning rather than added to the hashed `GameState` or re-derived inside `sim/rules`) and narrowed AD-6's unconditional plunge clause with a `phase === 'game'` gate. Both are recorded only in this spec's prose; the spine's AD-9 and AD-6 still state the pre-2.9 text. Spine edits are the lead's under Rule 20, not the reviewer's.
- **`DW-221` `[med]` `routed` → `3-2-locking-balls-and-the-lock-arbiter`.** A `bd_lock` capture that takes `ballsInPlay` to 0 is treated as a drain (Story 2.5's deliberately device-agnostic detection), but the save branch re-serves from `bd_trough` unconditionally — so locking your last ball inside the window emits a `ball_saved` the player did not earn and serves a second ball while the first stays locked. Reachable today; DW-171 records the parking path.
- **`DW-222` `[med]` `routed` → `2-11-tilt-warnings-tilt-and-slam-tilt`.** A Tilt engaging between a save's re-serve and the re-served ball's arrival suppresses the autolaunch **and** consumes `awaitingSaveLaunch`, stranding that ball in the shooter lane with no `ball_ended` and no recovery until ball search lands in 2.12. Unreachable until 2.11 ships a tilt event; 2.11 also owns what a Tilt should do to a ball already saved.

#### Closed at emission

- **`by-design`** — `lit`/step 3 rather than the declared-but-unused `hurryup` role: epics.md AC 2 names step 3 explicitly and AD-9 lists "ball-save hurry-up" under the step progression. — `l_ball_save`'s loose placement bounds (±40 mm x, y below 20% of the playfield): those are task 8's own stated tolerances. — The read-side-only Tilt guard: AC 6 and the Design Notes scope it explicitly to Story 2.11.
- **`wontfix-accepted`** — `lampsOf(state, ballSaveHurryUpTicks = 0)`'s silent-zero default degrades rather than failing; flagged by three layers. Both production call sites pass it and AC 8's integration test reddens if either drops it, and making the parameter required would touch ~36 single-argument call sites (past a two-way door). `reopen_if` = a third `lampsOf(` call site appears under `src/` without the ticks argument. — The four verbatim `NO_BALL_SAVE_TUNING` blocks; `reopen_if` = a fifth file needs the same override.
- **`wontfix-theoretical`** — `EMPTY_BALL_SAVE`'s shared unfrozen `sources` array (no code path mutates it in place; `readonly` blocks it at compile time). — `armBallSave`'s same-object early return, unreachable with one source. — The `never` exhaustiveness tail returning the subject at runtime under an unsound cast. — `SHOOTER_LAUNCH_COIL`'s module-load throw (mirrors the pre-existing `HARDWARE_COILS` derivation over a `deepFreeze`d `TABLE`). — A second parking entry while `awaitingSaveLaunch` is pending emitting a second `ball_saved` (needs a second ball in play — Story 3.7). — The save pulsing an empty trough (the drained ball is itself in the trough, so it is never empty at save time). — `ballSaveMs: 1` in the collateral overrides leaving a one-tick live window (no collateral script drains within one tick of its plunge).
- **Rejected with evidence** — "`ball_will_start` clearing `sources` is unpinned": the AC 5 lifecycle test seeds `sources: ['test']` / `untilTick: 500` and asserts the exact post-state, so removing the reset reddens it. — "arming while tilted survives into play": tilt clears only at `ball_will_start`, which resets `ballSave` in the same call, so a tilt-armed window can never outlive the tilt. — "the `kind === 'non-parking'` discriminator is untested": AC 4's positive assertion at tick 5 reddens if it is inverted (the negative direction is separately recorded as theoretical above).

#### Rules checked

- **Rule 1 (Integration AC):** satisfied — AC 8 is a genuine consumer-tier criterion asserted on `FrameOutput.commands`, and the drain path now has a second real-runtime test.
- **Rule 3 (real-runtime evidence):** satisfied — `test/rules-ball-save-integration.test.ts` drives a real `createLoop`, the real collision doc and real physics. The spec's disclosure that no test here observes a rendered pixel is accurate and, checked deliberately, **has not been upgraded anywhere into a claim about appearance** (`verification-gap` reports the same negative result independently).
- **Rule 5 (NFR tripwire):** no hit — no NFR was worked around with code comments plus a ledger entry.
- **Rule 6 (AD conformance):** AD-6, AD-19, AD-3/AD-15, AD-9, AD-7 and AD-11 all verified conforming (`ballSaveGraceMs`'s `source` string checked verbatim against PRD FR-19, `[ASSUMPTION]` marker included). AD-18 carries the `DW-219` residual above. **AD-12 untouched**, as required.
- **Rule 19 (falsifiability):** four previously-green mutations are now red (see Resolved above), each applied, observed, and reverted from a saved copy — never `git checkout --`, never `git stash` — with the tree verified byte-identical afterwards.

---

**Code review, 2026-09-08 (iteration 2, `bmad-code-review`; review tier: full — `blind-hunter`, `edge-case-hunter`, `verification-gap`, `acceptance-auditor`, all four returned, no model override since `_bmad/custom/model-overrides.yaml` does not exist on this project).** Reviewed the WHOLE story across both implement passes (`git diff 411da33`, 35 files / 2,261 changed lines), not just the rework. Suite re-run by the reviewer with `BLENDER` exported: **114 files / 1862 tests / 0 failed / 0 skipped**, before and after patching. `pnpm typecheck` clean across all three tsconfigs; `lint:boundaries` `OK -- 105 .ts file(s)`; `check:headers`, `check:attributions` clean; `check:ad7` exactly 3.

**Golden discipline re-verified independently and structurally** (parsed JSON per field at `411da33` vs HEAD, never substring grep) and **independently again by the acceptance-auditor**: on all five, the only movements are `header.tableHash` (`672573c`→`bac5816c`), `header.gameStart.tuning` (exactly six added keys — the three `…Ms` plus their three `…Ticks` siblings; zero removals, zero value changes, shared-key declaration order preserved so DW-213's `reopen_if` is not tripped) and `notes` (strict append). `expectedHash`, `expectedGameStateHash`, `expectedCheckpointHashes`, `checkpointTicks`, `transitions`, `coilPrologue`, `durationTicks` and `assetHash` byte-identical on all five. `git diff --stat 4f733dc..HEAD -- test/replays/` empty — the rework moved no golden at all. **No Block-If condition hit; no HALT.**

#### MED — RESOLVED in rework iteration 2 (three of four; `DW-226` stays routed, not in-story)

**Resolution note (2026-09-08, rework iteration 2):** the three `[rework]`-triaged findings below (AC 10's gate, `ballSaveHurryUpMs`'s consequence pin, `DW-224`+`DW-225`) are **no longer unresolved or blocking** — each is implemented, verified with a Rule 19 mutation observed red then reverted (see `#### Rework iteration 2 -- implemented (2026-09-08)` above), and the full suite is green (114 files / 1865 tests / 0 failed / 0 skipped). `DW-226` (the fourth) was already dispositioned `routed` → Story 3.7 at the time this section was first written, not `rework`-triaged for THIS story, so it is unaffected and remains open on its routed destination. Findings preserved verbatim below as the historical record of what code review measured.

- **`[med]` AC 10's `phase === 'game'` arming gate is shipped unprotected, and the mutation recorded for it reddens nothing.** Rework iteration 1 added a second conjunct to the exact line AC 10's mutation targets, and the eleven-line mutation block was never re-walked. **Measured:** deleting `nextState.phase === 'game' && ` leaves **77/77 green** (AC 10's own test and all five goldens). No golden presses `s_start`, so `sources` is `[]` throughout and the second conjunct blocks arming by itself; AC 10's attract test starts from the same empty-`sources` state, so it passes for the wrong reason. Reverted from a saved copy, tree verified byte-identical. Rule 19 → MED, in-story, fix-risk low. Found by `verification-gap` and by the reviewer's own measurement independently. **Fix:** a case seeding `phase !== 'game'` with `sources` already holding `BALL_SAVE_SOURCE` — the state reachable after `game_over`, where the controller does not call `startBall()`. Full record on the AC 10 line in `## Verification`.
- **`[med]` `ballSaveHurryUpMs` has no production-magnitude consequence pin — epic vacuity #43's exact shape, one tunable over from the one the last review closed.** The previous pass fixed precisely this gap for `ballSaveGraceMs` (two authored-offset probes) and left the hurry-up on the relational assertion alone (`0 < hurryUp < ballSaveMs`). **Measured:** setting `ballSaveHurryUpMs` to **1** satisfies `0 < 1 < 8000` and leaves **all 29 ball-save tests green** — every behavioural hurry-up assertion runs under an override tuning (20 or 40 ticks), so the production value is never on the path. The only red anywhere is `StaleReplayHeaderError` on the golden headers, whose remedy is a header refresh — so a 1 ms hurry-up could ship green. Reverted from a saved copy, `tuning.ts` verified byte-identical. Found by `blind-hunter`, confirmed by the reviewer's own measurement. Rule 19 → MED, in-story, fix-risk low. **Fix:** two production-tuning probes with offsets authored at the probe, mirroring what AC 9 already does for the grace.
- **`[med]` `DW-224` + `DW-225` — `awaitingSaveRelaunch` is a cross-tick latch with no causal binding and no timeout.** Dispositioned under Rule 15 rather than deferred; see the dedicated note below.
- **`[med]` `DW-226` (new) — the grace-lapse × `enableBallSave` asymmetry was recorded in frontmatter `deferred:` but never harvested into the ledger.** Two defects with one root cause. (a) **The harvest gap:** of rework iteration 1's three `deferred:` entries, two were harvested (`DW-224`, `DW-225`) and this one was not — Rule 17's "an item that never reaches the ledger is invisible to the drain". (b) **The asymmetry itself:** `hasGraceLapsed()` resets the whole device to `EMPTY_BALL_SAVE`, emptying `sources`, and the new arming gate reads `sources` — so a plunge *before* a lapse extends the window by a full `ballSaveMs` while a plunge *after* one cannot arm at all. The decided semantics ("a player plunge arms the window") therefore hold or not depending on whether the previous window happened to expire, with no test on either side. Found independently by `blind-hunter` and `edge-case-hunter`. Ledgered and routed to `3-7-quick-multiball-fight-the-monster` — the first second arming source, and the story that makes it reachable — following `DW-219`'s precedent.

#### Rule 15 disposition of `DW-224` and `DW-225` — NOT deferrable

Both are MED, both in-story (`src/sim/rules/ball-controller.ts`), and they share one root cause: **a cross-tick discriminator with no causal binding and no timeout.** Rule 15's bar for a MED, in-footprint, fix-risk ≤ med is *fix now* — and that is the honest reading here. Recorded plainly, in both directions:

- **Against the findings.** Today's *consequence* is narrower than either entry states. `ball_launched` is emitted on the **open edge of a single switch** (`s_shooter_lane`, `sim/rules/devices/index.ts:283`), and `bd_shooter` holds one ball. Between the autolaunch pulse and its resulting launch there is exactly **one** open edge, so a manual plunge in that gap produces the *same* `ball_launched` the pulse would have — not a second, distinct one to misclassify. DW-224's stated scenario is therefore not reachable in single-ball play, and where it does fire the "wrong" outcome (no fresh window) coincides with the author's ratified semantics anyway. The lead's check closed the `wontfix-theoretical` escape on the *plunger's existence*; it does not establish a distinct misclassifiable event.
- **For fixing now, which is decisive.** `edge-case-hunter` found a materially worse consequence than either ledger entry records: if the deferred autolaunch resolves to `eject_failed`, the saved ball rests in the shooter lane with `ballsInPlay` at 0, **no parking entry can ever occur, so no `ball_ended` and no `ball_will_start` — the game hangs in `phase: 'game'` with no recovery until ball search lands in Story 2.12.** `blind-hunter` independently noted the sharp corollary: `awaitingSaveRelaunch`'s only reset is at `ball_will_start`, so **in the very scenario its reset comment cites as justification, the reset can never run.** The stated mitigation is unreachable in the case it names.
- **Fix-risk: low, not med.** The in-file, in-layer precedent already exists and needs no new tunable — `pendingLockLaneClosure` (`sim/rules/devices/index.ts:243,258-260`) is a `{ startTick }` record self-clearing off an already-resolved tick count, and the ball controller already resolves `ballSaveGraceTicks`. ~10 lines in one file, one module, with the author's decision and the measurement both still fresh. **One fix closes both.**
- **Disposition: both stay `open` on `2-9-ball-save`, fixed in rework iteration 2.** Not `escalated` — the fix-risk is not high. Not `wontfix-theoretical` — a reachable game-hang is not theoretical hardening. Deferring a latent state-machine defect that Stories 2.12 and 3.7 are both positioned to activate, while the file is open and the fix is ten lines, is exactly the trade this epic already lost once on DW-218 by reasoning from text instead of measuring.

#### Resolved at review (patched, suite green, `typecheck` clean)

- **`[med]` The DW-218 regression guard pinned this harness's own setup, not the property it exists to protect.** Rework iteration 1 tightened it from a loose `saved.length < 10` to the exact measured `saved`/`ended` tick arrays. Those nine numbers are trajectory results of the collision doc, geometry, seed, production tuning and the test's own `plungeInNTicks = 1` re-plunge cadence — none of which is the DW-218 property, and the test never observed `ball_save_timer_started`, the one event that *is* the mechanism. **The decisive evidence is in the story's own record:** the same fixed code, exercised through a different but equally legitimate stimulus sequence, produced `saved = [4273, 8547, 17217, 21516, 30262, 34561]` / `ended = [12846, 25790, 38835]` against this file's `[4276, 8575, 17120, 21419, 29964, 34263]` / `[12849, 25693, 38537]` — only ball 1's first two saves agreed. Two correct runs disagree on the numbers. And the repair path is the real hazard: `tuning.ts` marks all three ball-save durations `'unverified'` and "adjustable until Epic 3's playtest freeze", so a planned tuning pass will redden this by design, and the obvious repair — paste the newly measured array — silently accepts a concurrent partial re-arm regression. **Patched to structural invariants:** `finalPhase === 'game_over'`, `timerStarted.length === 3` (one arming per ball, the mechanism stated directly), `ended.length === 3`, `saved.length > 0` (non-vacuity — without it the test would pass with ball save switched off entirely), and every `ball_saved` inside some armed window plus grace. The measured ticks are kept in the comment as the record of measurement, not as an assertion. **Verified both ways:** green at HEAD (29/29 ball-save tests), and **red under the DW-218 mutation** (`awaitingSaveRelaunch` disabled → `finalPhase` stayed `'game'`), with the control green throughout by design; reverted from a saved copy, `ball-controller.ts` `md5sum`-verified byte-identical. Found independently by `blind-hunter`, `verification-gap` and the reviewer.
- **`[low]` Stale "fourteen"-insert prose left in *production* source** while the test files were updated — `src/presentation/lighting/lamp-driver.ts:12` ("the glb's fourteen inserts all share ONE `mat_insert` datablock") and `:269` ("re-resolves all fourteen nodes on every render frame"), both present-tense claims about the shipped glb, now fifteen. The previous pass fixed this wording in two *test* files and missed the production file. Fixed. (The remaining "fourteen" mentions under `src/` and `tools/` are accurate historical references to Story 2.8's own fourteen inserts and were left alone.) Found by `blind-hunter`.
- **`[low]` `shotWindowTicks()`'s doc comment was falsified by this story.** It claimed to be "the ONE function a caller under `sim/rules/devices/**` may use to reach a tick count from a `TABLE.shots[*].windowMs` key"; this story added three non-shot call sites outside that directory (`ball-controller.ts` for `ballSaveMs`/`ballSaveGraceMs`, `sim/loop/index.ts` for `ballSaveHurryUpMs`), and Story 2.4 had already used it for the non-shot `lockCaptureWindowMs`. The helper is and always was generic over any top-level `…Ms` key (`ShotWindowMsKey = TuningMsKey<typeof TUNING>`); only the identifier still says "shot". Corrected in place. Found by `blind-hunter`.
- **`[low]` `BALL_SAVE_TICKS` was declared in the AC 2 block and never used, while the assertion it documents hard-coded `103`** — against that block's own stated discipline ("the expectations below are spelled out from the AUTHORED literals above"), and inconsistent with the AC 11 block one file over, which derives `UNTIL_TICK = PLUNGE_TICK + BALL_SAVE_TICKS` correctly. `noUnusedLocals` is not enabled, so nothing caught it. Fixed by mirroring AC 11's derivation; still never read back through `shotWindowTicks()`. Found by `blind-hunter`.
- **`[low]` The test's launch-coil constant was derived differently from production's, making a negative assertion vacuity-prone.** Tests used `TABLE.ballDevices.bd_shooter.ballSearchOrder[0]!.coil`; production uses `.find(step => step.action === 'pulse')`. If step 0 ever stopped being the pulse, the test constant would become `undefined` and AC 4's negative assertion ("the autolaunch coil must be ABSENT from the drain tick's own batch") would pass trivially against a coil name that does not exist. Fixed to mirror production's derivation, throwing loudly instead. Found by `blind-hunter`.
- **`[low]` `tools/make-placeholder-blend.py`'s non-overlap justification named the wrong neighbours** — "the nearest x-neighbours in this band are `l_inlane_r` and `l_outlane_r`", both right-side, which cannot be the nearest pair to a dead-centre insert; the band's four inserts are placed symmetrically, so the nearest are the two inlanes. Comment-only, no geometry change, and the independent `no two inserts' cup footprints overlap` assertion covered it regardless. Corrected. Found by `acceptance-auditor`.
- **`[low]` Four false or stale claims in this spec's own record, corrected in place:** the rework writeup's description of the regression guard's assertions (written against the pre-tightening form, then superseded again by this pass); AC 6's "reddens *without being edited*" (this story did edit that test) and its pre-story line reference `:152-187`, which contradicted the spec's own Matrix Test Audit citation of `:173-215`; and AC 10's mutation line (measured false — see above). Found by `acceptance-auditor` and `verification-gap`.

#### Routed / recorded (not blocking this story)

- **`DW-227` `[med]` `routed` → `burndown`.** `test/export-py-skip-visibility.test.ts` is a load-dependent coin-flip: its nested vitest spawn takes ~103–118 s against its own `RUN_TIMEOUT_MS = 120_000`, so under full-suite load it is killed and reports `result.status === null`. **Three of the four review layers independently observed it actually fail** (`blind-hunter` red on run 1 and green on run 2; `verification-gap` and `acceptance-auditor` each one red), while both of the reviewer's own full-suite runs were green — genuinely intermittent. Pre-existing (last touched by Story 2.5) and untouched by this diff, but it is the very test that proves the "0 skipped" claim every stage of this story reports against, so a flake there degrades the whole suite's evidentiary value. Distinct root cause from `DW-179` (an unexplained, non-reproducing flake in a different file), so filed separately rather than as an occurrence.
- **`DW-221` occurrence appended** — `edge-case-hunter` and `blind-hunter` both re-derived the `bd_lock`-capture-treated-as-a-drain path from this diff alone. Already routed to `3-2-locking-balls-and-the-lock-arbiter`; recorded as an occurrence, not re-filed.
- **`DW-222` occurrence appended** — `edge-case-hunter` re-derived the Tilt-between-eject-and-arrival stranding, with the added observation that the stranded ball leaves `ballsInPlay` at 0 and so hangs the game rather than merely losing the launch. Already routed to `2-11-tilt-warnings-tilt-and-slam-tilt`.

#### Closed at emission

- **Pre-adjudicated, re-raised by a layer with no knowledge of the earlier pass, not re-filed** — Tilt "disarms all" being read-side only (AC 6 + Design Notes scope it to Story 2.11, `by-design`); `lampsOf`'s `= 0` default (`wontfix-accepted`; its `reopen_if` re-checked and **not** tripped — still exactly two `lampsOf(` call sites under `src/`); `EMPTY_BALL_SAVE`'s unfrozen `sources` and `projectLamp`'s `never` tail returning rather than throwing (both `wontfix-theoretical`); the four verbatim `NO_BALL_SAVE_TUNING` blocks (`wontfix-accepted`); `disarm()` never shrinking `untilTick` (`DW-219`); a duplicate `ball_saved` from a second parking entry, and the save pulsing an empty trough (both `wontfix-theoretical` — the drained ball is itself in the trough).
- **`by-design`** — the three new `SemanticEvent`s having no non-test consumer (this spec's own "Never" list forbids a Backglass row, a `ShowCommand`, a flasher and a `ModeView` for ball save); the integration test's ≤2-`LampCommand`-per-tick cap (AC 8's own explicit wording, not a borrowed invariant); the goldens carrying three tunables they cannot observe (AD-15's ratified and memlogged consequence — a tunable's `source`/`confidence` strings are part of the hashed contract).
- **`wontfix-theoretical`** — `armBallSave` with `ticks <= 0`, a negative `graceTicks`, and `ballSaveHurryUpMs >= ballSaveMs`: all three are authored constants in `tuning.ts` with AC 9's relational assertions over them, not user input.
- **Rejected with evidence** — "AC 2's *off from the displayed expiry onward* is contradicted by the inclusive `<=`": the I/O matrix's "Displayed expiry" row defines it as `tick > untilTick`, which is exactly what ships; AC 2's prose is loose, the behaviour is not, and the lamp ladder asserts the inclusive last tick deliberately. — "the collateral tests moving to `NO_BALL_SAVE_TUNING` lost drain-to-rotation coverage at shipped values": the DW-218 regression guard now exercises exactly that, end to end, at production tuning across all three balls. — "`test/contracts.test.ts`'s `.type` read-back cannot fail": true but it is that file's established idiom for an assignability check, and the QA pass's three `describeEvent` assertions beside it are genuinely falsifiable (M8 observed red). — "`review_loop_iteration` should have been bumped": same conflation rejected in iteration 1.

#### Rules checked

- **Rule 1 (Integration AC):** satisfied — AC 8 is a genuine consumer-tier criterion asserted on `FrameOutput.commands`, and the drain path carries a second real-runtime test.
- **Rule 3 (real-runtime evidence):** satisfied — `test/rules-ball-save-integration.test.ts` drives a real `createLoop`, the real collision document and real physics. The spec's disclosure that no test here observes a rendered pixel is accurate and **has not been upgraded anywhere into a claim about appearance** — checked independently by `verification-gap` (full grep of every added line and every assertion message) and `acceptance-auditor`, both reporting the same negative result, and confirmed by the reviewer.
- **Rule 5 (NFR tripwire):** no hit.
- **Rule 6 (AD conformance):** **AD-6** and **AD-9**, both amended 2026-09-07 for this story, verified conforming against their *current* text — arming is gated on `phase === 'game'` and on the source being enabled, `awaitingSaveRelaunch` is set only when the pulse genuinely fires and never while tilted, and the shipped seam is `lampsOf(state, hurryUpTicks)` with exactly two call sites. **AD-18**, **AD-19**, **AD-3/AD-15**, **AD-7**, **AD-11** conforming; AD-18 carries the recorded `DW-219` residual. **AD-12 untouched.** No Rule 6 finding.
- **Rule 19 (falsifiability):** three mutations run this pass, each stated before running, applied, observed, and reverted from a saved copy — never `git checkout --`, never `git stash` — with `md5sum` and `git diff --stat` confirming byte-identity after each. Two exposed unpinned behaviour (AC 10's phase gate, `ballSaveHurryUpMs`); one confirmed the patched regression guard still reddens on the real defect. A fourth (`isRunning`'s inclusive `<=` → `<`, a boundary none of the ten known mutations covered) was **run and correctly reddened two tests**, so no finding.

## Spec Change Log

- **2026-09-08 -- rework iteration 2 closed: all four MEDs resolved, independently re-verified.** `src/sim/rules/ball-controller.ts`'s `awaitingSaveRelaunch` is widened from a plain boolean to `pendingLockLaneClosure`'s own `{ startTick }` shape, self-clearing off the already-resolved `ballSaveGraceTicks` -- no new tunable, no new `GameState` field, closing `DW-224` (a bounded timeout carrying causal-binding *language*, not a true per-pulse correlation check -- see the build-auto correction below) and `DW-225` (same-ball recovery) with one fix, per the Rule 15 disposition above. `test/rules-ball-save.test.ts` gained five new tests: an AC 10 case isolating the `phase === 'game'` gate from the already-covered `sources` gate (seeded `phase: 'game_over'` with `sources` already holding `BALL_SAVE_SOURCE`, the state genuinely reachable after `game_over`); a production-magnitude consequence pin for `ballSaveHurryUpMs` (mirroring the previous pass's own `ballSaveGraceMs` fix -- authored 500/5,000-tick offsets, `hurryUpTicks` itself read through the real `shotWindowTicks()` wiring so a tuning change genuinely flows through); and three cases in a new `Rework iteration 2 (DW-224 + DW-225)` describe block -- one proving the bounded-lifetime fix itself (a latch left set past its own window does not swallow a later, genuinely unrelated `ball_launched`) and two boundary-straddle probes distinguishing `>` from `>=` at the expiry tick. All mutations named in the rework checklist were run, observed red for the stated reason, and reverted from a saved copy with `git diff --stat` confirming byte-identity afterward. The `## Verification` block's AC 10 entry is corrected to record the closure; a new subsection records the mutations that belong to no numbered AC. `DW-226` (the fourth MED-triaged finding) was already `routed` to Story 3.7, not `rework`-triaged for this story, and is unaffected. Suite: **114 files / 1867 tests / 0 failed / 0 skipped** (5 more than the pre-rework 1862). `pnpm typecheck` clean across all three tsconfigs; `pnpm lint:boundaries` `OK -- 105 .ts file(s)` (unchanged -- no new `src/` file); `pnpm check:headers`/`pnpm check:attributions` clean; `pnpm check:ad7` exactly 3 passing. `git diff --stat` confirms no golden, tuning value, table, event or lamp file moved -- this rework touches only `src/sim/rules/ball-controller.ts`, `test/rules-ball-save.test.ts` and this spec file.

- **2026-09-08 -- build-auto review pass (resuming an `in-review` spec at its own review step, per the completion contract): five patches applied, two findings deferred.** Review layers blind-hunter, edge-case-hunter, verification-gap and intent-alignment ran in parallel against the diff since `baseline_revision`. Patched (mechanical, no design change, no code behaviour change except the one Rule 19 mutation exercise): (1) the `DW-226` deferred-entry and AC 10 `## Verification` line both cited `ball-controller.ts:513` for the arming conjunct; the true line at this tree is `:533` -- the prior correction was computed before rework iteration 2's own ~48-line comment addition and never re-walked, corrected here by direct `grep -n`. (2) the "three new tests" / "1865 tests" counts in the implemented writeup, this change log, and the Auto Run Result all undercounted by two -- the `Rework iteration 2 (DW-224 + DW-225)` describe block holds three `it()` cases (the recovery case plus two boundary probes), not one; true count is **114 files / 1867 tests**, matching a direct `pnpm test` run and the cycle log's own independent measurement. (3) the two boundary-straddle probes had no Rule 19 mutation of their own recorded in `## Verification` -- found by `verification-gap`; a mutation (`tick > ...` to `tick >= ...` at `ball-controller.ts:447`) was run, observed to redden exactly the "EXACTLY" boundary case, and reverted from a saved copy, tree confirmed byte-identical, then recorded. (4) "closing DW-224 (causal binding)" overclaimed relative to the code's own honest inline comment ("this is a TIMEOUT, not a true causal binding"); softened in the implemented writeup, this entry, and the Auto Run Result to match the code's own framing. Deferred to frontmatter `deferred:` (real, narrow, currently-unreachable-or-low-reachability, both pointing at Story 3.7 as the story that would make them live): the timeout-not-correlation gap itself (a manual plunge landing inside the bounded window is still silently swallowed as the relaunch -- found by blind-hunter and edge-case-hunter, already disclosed candidly in the code's own comment), and the reused-`ballSaveGraceTicks` coupling two different concerns (drain-grace duration vs. pulse-travel time -- found the same way, also already disclosed in-code). Intent-alignment's finding that no test constructs the literal `eject_failed` event or asserts on `ballsInPlay`/`ball_ended`/`ball_will_start` was reviewed and accepted as already correctly and honestly scoped: the Residual Risks section already states plainly that the physical stranding is unfixed pending Story 2.12's ball search, and the new tests instead pin what this story's layer (`sim/rules`) actually owns -- that the RULES-side latch is no longer the thing permanently blocking recovery once a later `ball_launched` does arrive, however it arises. No design change, no latch revert, no re-litigated decision. Full suite re-run clean after all patches: **114 files / 1867 tests / 0 failed / 0 skipped**, `pnpm typecheck` clean.

- **2026-09-08 -- reopened for rework iteration 2 (code review pass 2, four MEDs).** Two are new vacuities, taking the epic's count to **49**, and the lead re-measured both independently before accepting them. **#48:** AC 10's `phase === 'game'` mutation, genuinely observed red in iteration 1, was silently invalidated when the rework added a second conjunct to the same line -- deleting the phase gate now leaves 29/29 ball-save and 52/52 golden tests green. **#49:** `ballSaveHurryUpMs` can be set to `1` with all 29 ball-save tests green, vacuity #43's shape one tunable over from the gap the previous pass closed for `ballSaveGraceMs`. `DW-224`/`DW-225` were dispositioned NOT deferrable on a consequence worse than either recorded -- an `eject_failed` on the save's re-serve hangs the game outright -- and `DW-226` records both a harvest gap and a real grace-lapse asymmetry. The regression guard's exact-tick-array assertions were replaced with structural invariants after the lead and the reviewer independently got different tick values from the *same* fixed code, confirming those numbers encoded harness setup rather than the property under test. **AD-6 clause (1) was corrected in the spine** -- against a premise the lead had written there the previous day.

- **2026-09-07 -- reopened for rework iteration 1 on the author's decision (DW-218).** Code review found, and the lead independently reproduced with a from-scratch harness, that a ball save re-arms a fresh full window on its own re-serve: at production tuning over 120,000 ticks, **28 `ball_saved` and zero `ball_ended`** -- the ball never ends, putting ball 2, the bonus, lane rotation, game over and Match out of reach. The control (`ballSaveMs` 500 / grace 100 -> zero saves, `ball_ended` at the same tick 4273) is what makes that null result evidence rather than a blind harness. Four options were put to the author; **option 2 was chosen: a player plunge arms the window, a save's own re-serve does not.** Behaviourally identical to one-window-per-ball in single-ball play, while keeping AC 5's multi-source arbitration meaningful for Story 3.7's multiball arming. Option 4 (shorten `ballSaveMs`) was rejected as non-viable: the loop is structural, so a tuning-only fix re-breaks the moment the table's drain interval changes. **AD-6 amended** to record both this decision and the pre-existing `phase === 'game'` gate; **AD-9 amended** to correct the lamp projection signature to `lampsOf(state, hurryUpTicks)` (DW-220, closed). `lint_spine.py` clean, memlog entry 76, both amendments so no `spine-next-id` claim.
- **2026-09-07 -- rework iteration 1 closed: DW-218 fixed and independently re-verified.** `src/sim/rules/ball-controller.ts` gained a second closure-held cross-tick discriminator, `awaitingSaveRelaunch` (alongside the existing `awaitingSaveLaunch`), set only when the deferred-autolaunch coil pulse genuinely fires and consumed by the very `ball_launched` it causes, so a save's own re-serve no longer re-arms. The controller's arming branch is now also gated on `machine.ballSave.sources.includes(BALL_SAVE_SOURCE)`, closing the `enableBallSave` gap the HIGH finding also named. `BallSaveState`'s serialized shape is untouched; no golden moved (verified: `git diff --stat -- test/replays/` empty). Re-verified at the full 120,000-tick protocol by the build-auto stage, independently of the implementer's own scratchpad: production tuning reaches `game_over` with all 3 balls ending (`saved=[4276,8575,17120,21419,29964,34263]`, `ended=[12849,25693,38537]`); the control (`ballSaveMs` 500 / `ballSaveGraceMs` 100) gives zero saves and `ball_ended` at tick 4276 -- the same tick production's first save landed. Both the regression guard and the control are now committed tests in `test/rules-ball-save-integration.test.ts`. Rule 19 mutation (revert to the pre-fix unconditional arm) re-applied and re-confirmed red by the build-auto stage, then restored from a saved copy with the tree confirmed byte-identical. Review pass (blind-hunter, edge-case-hunter, verification-gap, intent-alignment) found three mechanical documentation patches (applied: this entry, the HIGH-finding resolution note above, and tightening the regression guard's `saved`/`ended` assertions from a loose bound to the exact measured arrays) and three narrow residual-risk findings deferred to frontmatter `deferred:` (the discriminator's causality assumption, `awaitingSaveRelaunch` staleness on a same-ball `eject_failed`, and the `enableBallSave` gate's dormant interaction with grace-lapse) -- none reintroduce the measured DW-218 defect (infinite saves, zero `ball_ended`).

## Review Triage Log

### 2026-09-07 — Review pass
- intent_gap: 0
- bad_spec: 0
- patch: 3 (medium 1, low 2)
- defer: 1 (low 1)
- reject: 5
- addressed_findings:
  - `[medium]` `[patch]` `awaitingSaveLaunch` (the deferred-autolaunch flag, `ball-controller.ts`) had no reset point tied to the ball lifecycle -- unlike `machine.ballSave` itself (reset at `ball_will_start`, AD-7) and unlike this file's own cited precedent `pendingLockLaneClosure` (self-clears on a tick timeout). Found independently by blind-hunter and edge-case-hunter. If a save's re-serve never reached `bd_shooter` (a real, reachable `eject_failed`, or any future ball-search/multiball interaction intervening first), the stale `true` would silently auto-launch the NEXT ball's own first, unrelated arrival at the shooter lane. Fixed: `startBall()` now resets the flag at `ball_will_start`, the same boundary AD-7 already resets `ballSave`/`tilt`/`multiball` at. Two new tests added to `test/rules-ball-save.test.ts` drive `createBallController()` directly (the flag has no `GameState` analogue `runRulesScript()`'s initial-state surface can seed) and were confirmed red against a saved-copy revert of the fix before being restored green.
  - `[low]` `[patch]` The deferred autolaunch's coil pulse (`ball-controller.ts`, the `awaitingSaveLaunch` arrival branch) had no Tilt guard, unlike the drain branch a few lines below it (`!nextState.machine.tilt.tilted`). Found by edge-case-hunter. A Tilt engaging between a save's trough-eject and the re-served ball's own arrival at `bd_shooter` would still fire the launch coil, in tension with AC 6's "the device is inert while tilted." Fixed: the arrival branch now checks `!nextState.machine.tilt.tilted` before pushing the coil pulse (still consuming the flag either way, so a later non-tilted arrival cannot also fire it) -- this stays inside the read-side guard this story is scoped to (no tilt event is emitted or consumed). Covered by the same two new tests above.
  - `[low]` `[patch]` `test/lighting-scene.test.ts` (6 spots) and `test/lighting-integration.test.ts` (3 spots) still described "fourteen" lit lamps/inserts in comments and assertion messages, made stale by this story's fifteenth lamp (`l_ball_save`). Found by blind-hunter. The underlying assertions are all derived from `Object.keys(TABLE.lamps)` and were never actually wrong, so this was cosmetic only. Fixed: updated the prose to "fifteen" in both files; no assertion logic changed.

### 2026-09-07 — Review pass (rework iteration 1, DW-218)
- intent_gap: 0
- bad_spec: 0
- patch: 3 (low 3)
- defer: 3 (medium 3)
- reject: 5
- addressed_findings:
  - `[low]` `[patch]` The spec's own pre-rework `#### HIGH — unresolved, blocking (DW-218, decision-pending)` section contradicted the now-checked-off rework checklist above it, reading as "unresolved" immediately after "done." Found by blind-hunter. Fixed: heading changed to `RESOLVED in rework iteration 1`, with a resolution note pointing to the implementation writeup; the original finding text is preserved verbatim underneath as historical record.
  - `[low]` `[patch]` `## Spec Change Log` had no closing entry for this rework -- only the "reopened" entry existed, leaving the one section meant to be an append-only history silent on the fix itself. Found by blind-hunter. Fixed: appended a closing entry summarising the fix, the independent re-verification, and this review pass's own disposition.
  - `[low]` `[patch]` `test/rules-ball-save-integration.test.ts`'s `"DW-218 regression guard"` test bounded `saved.length` only loosely (`< 10`) despite the exact value (6) being already known and measured identically twice. Found independently by blind-hunter and verification-gap. A partial regression reintroducing a few extra re-arms (7-9 saves) would have passed undetected. Fixed: tightened to `toEqual()` against the exact measured `saved`/`ended` tick arrays (deterministic under seed 0, no player input -- confirmed by two independent measurements at this tree).
  - `[medium]` `[defer]` The `awaitingSaveRelaunch` discriminator consumes blindly whichever `ball_launched` arrives next without verifying it was actually caused by the deferred-autolaunch pulse -- a player's manual plunge landing in the same narrow window could be misclassified. Found independently by blind-hunter and edge-case-hunter. Does not reintroduce the DW-218 defect (the still-live original window continues either way); narrow reachability; no test exercises the manual-plunger input path. Recorded in frontmatter `deferred:`.
  - `[medium]` `[defer]` `awaitingSaveRelaunch` has no same-ball recovery if the deferred-autolaunch pulse resolves to `eject_failed` -- the flag stays stuck `true` for the rest of that ball. Found independently by blind-hunter and edge-case-hunter. Low reachability today (a ball normally visits `bd_shooter` once); relevant once Story 2.12 (ball search) or a future story gives a ball a path back through `bd_shooter` mid-ball. Recorded in frontmatter `deferred:`.
  - `[medium]` `[defer]` The new `enableBallSave` arming gate loses ball-save eligibility for the rest of a ball once grace fully lapses without a drain (`hasGraceLapsed()` clears `sources` too). Found by blind-hunter. Dormant today (nothing produces a second mid-ball `ball_launched` outside a save's own re-serve); Story 3.7 (the first second arming source) is positioned to make it reachable. Recorded in frontmatter `deferred:`.
  - Rejected (5): "`review_loop_iteration` should have been bumped" -- conflates this workflow's own internal bad-spec-loopback counter (correctly `0`; no loopback occurred in this run) with the spec body's separate "Rework iteration 1" labelling, a different concept. "The committed control test's relative comparison (`control.endedTick === production.savedTick`) is weaker than a hardcoded tick literal" -- the relative form is deliberately the stronger, more maintainable invariant: it cannot pass vacuously (guarded by `production.savedTick > 0`), directly encodes the causal claim the control exists to prove, and does not need updating on an unrelated future physics/geometry change the way a hardcoded literal would. "The 3-tick gap between the original review's measured tick 4273 and this rework's measured 4276 is unexplained" -- adequately explained (harness call-sequence difference) and corroborated by two independent modern measurements both landing on 4276. "The regression guard's hard-coded one-tick inter-ball re-plunge delay (`plungeInNTicks = 1`) is fragile to future physics changes" -- already empirically exercised successfully by two independent 60,000/120,000-tick runs against the real physics engine; the same category of measured-and-hardcoded timing assumption is already established practice elsewhere in this suite (e.g. the pre-existing AC 2 integration test's 320-tick travel window). Intent-alignment audit's observations (the discriminator lives in a JS closure invisible to the state-hash/golden surface) -- this is the explicitly author-mandated design (a closure-held discriminator was the pattern specified to avoid a state-hash cost), not a defect; no action taken.

### 2026-09-08 — Review pass (iteration 2, whole story across both implement passes)

- intent_gap: 0
- bad_spec: 0
- patch: 7 (medium 1, low 6)
- rework: 4 (medium 4 — none deferrable under Rule 15; story stays `in-progress`)
- routed/recorded: 4 (2 new ledger entries `DW-226`/`DW-227`; 2 occurrences appended to `DW-221`/`DW-222`)
- reject: 14 (pre-adjudicated, `by-design`, or `wontfix-theoretical` — itemised under "Closed at emission")
- mutations_demonstrated: 4 (3 new + 1 re-confirmation), each stated before running, applied, observed, reverted from a saved copy, byte-identity confirmed by `md5sum` and `git diff --stat`
- addressed_findings:
  - `[medium]` `[patch]` The DW-218 regression guard's exact `saved`/`ended` tick arrays pinned this harness's own re-plunge cadence rather than the property. Found independently by `blind-hunter`, `verification-gap` and the reviewer. Replaced with structural invariants centred on `timerStarted.length === 3` — the DW-218 mechanism stated directly. Verified green at HEAD and **red under the DW-218 mutation**.
  - `[medium]` `[rework]` AC 10's `phase === 'game'` gate ships unprotected; its recorded mutation reddens nothing (**measured: 77/77 green with the gate deleted**). Rework iteration 1's second conjunct masks it.
  - `[medium]` `[rework]` `ballSaveHurryUpMs` has no production-magnitude consequence pin (**measured: value `1` → 29/29 ball-save tests green**) — epic vacuity #43's shape, one tunable over from the gap the previous pass closed for `ballSaveGraceMs`.
  - `[medium]` `[rework]` `DW-224` + `DW-225`, one root cause: `awaitingSaveRelaunch` is a latch with no causal binding and no timeout. Dispositioned NOT deferrable under Rule 15 (MED, in-story, fix-risk **low** — `pendingLockLaneClosure`'s in-file precedent needs no new tunable). `edge-case-hunter` established a reachable **game hang**; `blind-hunter` established that the flag's only reset cannot run in the scenario its own comment cites.
  - `[medium]` `[routed]` `DW-226` (new) — the grace-lapse × `enableBallSave` asymmetry was in frontmatter `deferred:` but **never harvested into the ledger** (its two siblings were). Harvest gap closed; routed to Story 3.7, which makes it reachable.
  - `[medium]` `[routed]` `DW-227` (new) — `test/export-py-skip-visibility.test.ts` is a load-dependent coin flip (~103–118 s against its own 120 s budget). **Three of four layers independently observed it actually fail**; both of the reviewer's full-suite runs were green. Pre-existing, but it is the test that proves the "0 skipped" claim every stage reports against.
  - `[low]` `[patch]` ×6 — stale "fourteen"-insert prose in *production* source (`lamp-driver.ts`, missed when the test files were updated); `shotWindowTicks()`'s doc comment falsified by this story's three non-shot call sites; `BALL_SAVE_TICKS` declared-and-unused while its assertion hard-coded `103`; the test launch-coil constant derived as `ballSearchOrder[0]` where production uses the first `pulse` step (a negative assertion could pass trivially against `undefined`); the blend comment naming two right-side inserts as the nearest neighbours of a dead-centre one; and four false or stale claims in this spec's own record.

### 2026-09-08 — Review pass (build-auto, resuming `in-review` at its own review step)
- intent_gap: 0
- bad_spec: 0
- patch: 4 (medium 3, low 1)
- defer: 2 (medium 2)
- reject: 2 (low 1, unranked 1)
- addressed_findings:
  - `[low]` `[patch]` Stale line-number citation (`src/sim/rules/ball-controller.ts:513`, the true line at this tree is `:533`) in the frontmatter `DW-226` deferred entry's `location:` and the AC 10 historical note in `## Verification`. Found independently by `blind-hunter` and `verification-gap`; the prior correction (`:470` → `:513`) had itself gone stale after rework iteration 2's own ~48-line comment addition and was never re-walked. Confirmed by direct `grep -n` against the tree as it now stands; corrected in both places.
  - `[medium]` `[patch]` Test-count undercount: "three new tests" / "1865 tests" was claimed in five locations (the implemented writeup, the Spec Change Log, and three spots in the Auto Run Result), but `test/rules-ball-save.test.ts`'s new `Rework iteration 2 (DW-224 + DW-225)` describe block holds three `it()` cases, not one -- true count is five new tests, **114 files / 1867 tests**. Found independently by `blind-hunter` and `intent-alignment`; confirmed by a direct `pnpm test` run, matching the cycle log's own independent count taken before this stage started. Corrected in all five locations.
  - `[medium]` `[patch]` The two new boundary-straddle tests (`startTick + ballSaveGraceTicks` EXACTLY / `+1`) had no Rule 19 `mutation:` line recorded in `## Verification`'s "Rework iteration 2's own new mutations" subsection -- only two mutations were listed there, covering the hurry-up probe and the full expiry short-circuit, none targeting the boundary comparison itself. Found by `verification-gap`. Fixed: ran the mutation (`tick > awaitingSaveRelaunch.startTick + ballSaveGraceTicks` changed to `tick >= ...` at `ball-controller.ts:447`), observed it redden exactly the "EXACTLY" case while the "+1" case and the rest of the suite stayed green, reverted from a saved copy (`ball-controller.ts.bak`), confirmed byte-identical by both `git diff --stat` and a direct `diff` against the backup, then recorded a third mutation entry.
  - `[medium]` `[patch]` "Closing `DW-224` (causal binding)" overclaimed relative to the fix's own inline code comment, which states plainly: "this is a TIMEOUT, not a true causal binding -- there is still no per-pulse identity check." Found by `blind-hunter`. Softened in the implemented writeup, the Spec Change Log, and the Rework iteration 2 Auto Run Result summary to match the code's own framing (a bounded timeout, sufficient today given `bd_shooter` holds one ball and `s_shooter_lane` is one switch; a true per-pulse correlation check is Story 3.7's, per the code's own comment).
  - `[medium]` `[defer]` The bounded-lifetime fix is a timeout, not a true per-pulse causal binding: any `ball_launched` arriving inside the bounded window is still consumed as the deferred pulse's own relaunch, so a genuine manual plunge landing in that window is silently swallowed instead of getting its own fresh window. Found independently by `blind-hunter` and `edge-case-hunter`; already disclosed candidly in the code's own inline comment, and safe today only because `bd_shooter` holds one ball. Added to frontmatter `deferred:`, pointing at Story 3.7.
  - `[medium]` `[defer]` The bounded-lifetime fix reuses `ballSaveGraceTicks` -- a tunable authored for a different concern (how long a drain stays saved past the displayed expiry) -- to also bound the physical pulse-to-shooter-lane travel time the record needs to outlive. Found independently by `blind-hunter` and `edge-case-hunter`; already disclosed in the code's own comment as safe today only because production `ballSaveGraceMs` (2000ms) vastly exceeds realistic travel time. Added to frontmatter `deferred:`, pointing at Story 3.7 (the next time `ballSaveGraceMs` is retuned).
  - `[low]` `[reject]` `blind-hunter`'s minor finding that `AwaitingSaveRelaunch` is declared inside `createBallController()`'s function body while the precedent it's compared to, `PendingLockLaneClosure`, is declared at module scope (confirmed by `grep -n`). No functional consequence -- erased at compile time, `tsc` clean on all three tsconfigs -- and the code comment's claim is about the shared `{ startTick }` field shape, not lexical scope. Noise, not a defect.
  - `[reject]` `intent-alignment`'s observation that no test constructs a literal `eject_failed` event or asserts on `ballsInPlay`/`ball_ended`/`ball_will_start` -- the checklist item's literal "pin the hang itself" language. Reviewed and accepted as already honestly and correctly scoped, not a gap: the Residual Risks section already states plainly that the physical stranding remains unfixed pending Story 2.12's ball search, and this story's own layer (`sim/rules`) cannot construct that full scenario without it. The new tests instead pin what this story does own -- that the latch itself no longer permanently blocks recovery once a later `ball_launched` arrives, however it arises.

## Design Notes

**Governing architecture decisions (Rule 6).** **AD-18** is the primary — it names `machine.ballSave` as one machine device owned by the ball controller, with `arm({ ticks, source })` / `disarm(source)`, stacking sources, the longest live window winning, Tilt disarming all, and the ball controller alone pulsing `c_trough_eject` / `c_autolaunch` and mutating `ballsInPlay`. **AD-6** supplies the plunge semantics: the *opening* of `s_shooter_lane` is the one event meaning "plunged", "on which the ball controller increments `ballsInPlay`, **starts the ball-save timer** and arms the skill shot" — this story is the one that makes the middle clause true. **AD-19** is the boundary the device sits inside: the controller consumes device events, never a raw `SwitchEvent`. **AD-3** and **AD-15** govern the three tunables (ms in `tuning.ts`, converted once, `source` + `confidence`, and the whole serialized output hashed into every golden header). **AD-9** governs the new lamp (`step ∈ {0,1,2,3}` is the only progression rules may express, and it names "ball-save hurry-up" explicitly) and the payload-completeness of the three new events. **AD-7** keeps `ballSave` machine-scoped and reset at `ball_will_start`. **AD-12** is read-only here: clause 3 records that a lit insert's dynamic light contributes 0.00 luma in the current scene, so the new insert inherits a grammar carried by the emissive material alone — Epic 2 must not touch the levers Story 4.1/4.2 own. **No AC contradicts any AD Rule.**

**Consumes:** Story 2.4 (`ball_launched`, `device_ball_entered/_left` from the devices layer) · Story 2.5 (the ball controller, the `ball_will_start` → `ball_starting` → `ball_started` lifecycle, `ballsInPlay`, `HARDWARE_COILS`) · Story 2.3/2.1d (`bd_shooter`, `c_autolaunch`, the physics eject/launch path) · Story 2.8 (`lampsOf()`, the `sim/loop` lamp diff, `grammar.ts`'s `lit`/step 3 row) · AD-15's tuning resolver (`shotWindowTicks()`).

**Consumed-by:** **Story 2.11 (Tilt)** — calls `disarm(source)` for every source on the tilt event; this story ships the `machine.tilt.tilted` read-side guard, which is the only half testable before 2.11 exists. **Story 3.7 (Quick multiball)** — the first *second* arming source (PRD FR-19: "multiball Modes set their own Ball save"; PRD:227). **Story 2.6's Backglass** is deliberately **not** a consumer: no ball-save row is chartered.

**Integration ACs (Rule 1).** AC 8 is the integration criterion: `lampsOf()` + the `sim/loop` diff read the device and produce an observable `LampCommand` stream on `FrameOutput`, asserted at the loop tier rather than by inspecting `machine.ballSave`. AC 3/AC 4 are the second consumer — the drain path — asserted on emitted events and `coilCommands`. **`arm({ ticks, source })` has no second caller in this story; the first will be Story 3.7**, so AC 5 exercises the arbitration through direct calls to the rules-internal surface in the headless test.

**Why DW-212 does not gate this story.** DW-212 asks whether `l_lock` should stay lit between balls and in Attract, because `lampsOf()` resolves the `lock` subject *above* the "no base mode ⇒ all off" guard (`src/sim/rules/lamps.ts:76-81`). The `ball_save` subject is machine-scoped too and sits in the same tier — but its answer is **independent of DW-212's**, and provably so: `machine.ballSave` is `{ untilTick: null }` at boot (`src/sim/loop/index.ts:217`) and is reset at every `ball_will_start` (`ball-controller.ts:234`), whereas `bd_lock`'s occupancy persists. So `l_ball_save` projects **off** in Attract and between balls under *either* guard placement, satisfying the frozen intent contract by construction. AC 1 and AC 10 assert that off-ness directly. Whichever way the author decides the Lock, nothing in this story changes. The story therefore neither builds on nor pre-empts the pending decision. **DW-204** (the skill shot's lane-freeze question) is untouched — this story reads no lane state.

**Why `BallSaveState`'s shape is frozen, and what it costs.** `machine.ballSave` is inside the hashed `GameState` (`src/sim/loop/replay.ts:115-122`), so adding a field would move `expectedHash` **and** `expectedGameStateHash` on all five goldens — a state-hash change, which is neither the header-only refresh this story is budgeted for nor a granted trajectory re-record. The consequence is that `sources` carries names only, with a single `untilTick` holding the *effective* (maximum) deadline. Disarming the source that set the maximum therefore leaves the deadline where it was rather than shrinking it to the next-longest source's. That satisfies AC 5's own wording ("`disarm(source)` of one leaves the other") and matches the shipped contract's own doc comment, which already pairs a name list with a single `untilTick`. It is recorded here as a decided limitation, not an oversight; the per-source form becomes affordable the moment a story is already re-recording state hashes.

**Anti-vacuity plan for three new tunables (epic vacuity #43).** `INSERT_EMISSIVE_LEVEL` could be set to 0 — blackening every insert — with the suite 1824 green, because every expectation was `colour × INSERT_EMISSIVE_LEVEL`. The same trap is live here three times over. So: **no expectation in this story may be computed from `ballSaveMs`, `ballSaveHurryUpMs` or `ballSaveGraceMs`.** AC 9 pins them by *observable consequence* with independently authored probe offsets (3,000 and 30,000 ticks) that bracket the window behaviourally, plus relational invariants that need no re-import. AC 2 pins the hurry-up *mechanism* structurally — a non-empty step-1 span, a non-empty step-3 span, step 3 strictly later. **[CORRECTED at code review 2026-09-07]** This paragraph originally claimed AC 2 also pins the *tunable*, reddening if `ballSaveHurryUpMs` is 0 or ≥ `ballSaveMs`. It does not: AC 2's describe block builds its own `OVERRIDE_TUNING` that replaces `ballSaveHurryUpMs.value` with 20, so the production value is not on the path at all — which is why the lead's own M5 (`ballSaveHurryUpMs` 2000→0) reddened exactly **one** test, AC 9's relational assertion, and not two. The structural form was also weaker than claimed in a second way: "non-empty" did not constrain the span's *length*, so collapsing `isWithinHurryUp` to a single tick left all 1854 tests green. Code review closed both: AC 2's ladder now asserts the exact step-3 span and its first tick from the authored literal (20), and AC 9 gained two production-tuning consequence probes for the grace (a drain 500 ticks past the displayed expiry is saved, one 5,000 ticks past it is not) with offsets authored at the probe, never read back from the tunable. AC 5's boundary run uses an override tuning of authored literals and scripts its probes at absolute ticks spelled out from those literals, never read back through `shotWindowTicks()`. The relational assertions are the weakest of the three and are deliberately the *secondary* pin, following `test/lighting-scene.test.ts:561-565`'s model (measure against something else) rather than `:500,508`'s (`toBe(CONSTANT)`).

**What the lamp tests prove, and what they cannot (AD-15/epic vacuity #42).** Every lamp assertion in this story is a **lamp-state** claim — the `{ role, step }` a pure projection yields, and the `LampCommand`s the loop's diff emits. Those are honestly testable headlessly. **None of them proves a pixel changed.** `NullEngine` rasterises nothing, and AD-12 clause 3 records that in the current scene a lit insert's dynamic light moves the composited pixel by 0.00 luma. So a green suite here means "the projection and the command stream are correct", never "the ball-save insert is visible". Confirming the insert renders at all belongs to the lead's browser smoke; confirming it is *visibly* lit belongs to Story 4.1, whose own criterion is that Story 2.8's A/B goes non-zero.

**Golden budget, measured at this tree (2026-09-07).** All five goldens carry `tableHash 672573c` / `assetHash ab163ff` / `physicsVersion v1-ce6772ef` — matching the epic baseline with no drift. Adding `l_ball_save` moves `tableHash` (it hashes the whole `TABLE`); the three tunables add **six** header keys (each `…Ms` gains a derived `…Ticks` sibling carrying the same `source` and `confidence`) and move `header.gameStart.tuning`. `assetHash` does **not** move: an `l_` node matches none of `tools/export.py`'s three collision-builder prefixes. Story 2.4's `b9f5431` is the exact precedent — 32 insertions / 2 deletions per golden for the same reason — so this is routine and needs no author grant. **What would need a grant, and must instead HALT, is any movement in a body field or a state hash.**

**Ledger inbox (Rule 17).** `bash _bmad/scripts/ledger.sh _bmad-output/implementation-artifacts/deferred-work.md slice 2-9-ball-save` returns empty at this tree — verified during planning. This story owns no ledger entries; there is nothing to address or decline.

## Verification

**Commands** (export `BLENDER="C:/Users/Josh/tools/blender-5.2.1-windows-x64/blender.exe"` first, in every shell — unset, the Blender-gated cases skip and the counts change):
- `pnpm typecheck` — expected: exits 0 across all three tsconfigs. A missing `case` arm in `test/contracts.test.ts` or a missing `never` tail in `projectLamp()` fails here first.
- `pnpm test` — expected: 0 failing, **0 skipped**. Baseline before this story was 112 files / 1824 tests; report the counts as measured and account for the delta (one new test file plus the new cases). 23 skips means `BLENDER` was not exported.
- `pnpm lint:boundaries` — expected: `OK -- N .ts file(s)`; N grows by the new `src/sim/rules/ball-save.ts`. Any `sim-no-literal-ms`, `sim-one-tick-constant`, `no-device-name-literal` or `sim-no-colour` violation is a failure.
- `pnpm check:headers` and `pnpm check:attributions` — expected: exit 0; the new source file carries the GPL-3.0 header.
- `pnpm export:assets` — expected: exit 0, `l_ball_save` accepted by `tools/export.py:194`, `public/assets/dragonwar.glb` regenerated.
- `pnpm check:ad7` — expected: exit 0 with **exactly 3** passing tests. `pnpm check:corridor` and `pnpm check:reachability` — expected: exit 0 (reachability over its 52 cases, ~2 minutes).
- **Golden hash-identity gate:** `git diff test/replays/ | grep -E 'expected(Hash|GameStateHash|CheckpointHashes)|assetHash|"transitions"|coilPrologue|durationTicks'` — expected: **no output**. Any line here is the Block-If condition; HALT.
- `git diff --stat -- public/assets/dragonwar.collision.json` — expected: **empty** (an `l_` node cannot reach the collision doc).
- `git ls-files --others --exclude-standard test/` — expected: only the intended new test file; the re-record harness must never appear under `test/`.

**Rule 19 mutations — one per acceptance criterion, each applied, observed red, then reverted from a saved copy (never `git checkout --`, never `git stash`), with `git status --short` and `git diff --stat` confirmed unchanged afterwards. State the expected red BEFORE running each one; a mutation harness can itself be vacuous.**
- **AC 1** — make the `ball_starting` enable also set `untilTick` → the enable-is-not-a-start assertion in `test/rules-ball-save.test.ts` reddens.
- **AC 2** — set `LIT_STEP_3` back to step 1 in `projectLamp()`'s hurry-up branch → the "non-empty step-3 span" assertion reddens while the step-1 span stays green.
- **AC 3** — remove the save interception so the drain branch always runs → the `ball_saved`/no-`ball_ended` assertions redden.
- **AC 4** — move the `c_autolaunch` pulse into the drain tick's batch → the "absent from the drain tick" assertion reddens.
- **AC 5** — change `armBallSave`'s `max` to "last write wins" → the two-source arbitration assertion reddens.
- **AC 6** — drop the `machine.tilt.tilted` guard → `test/rules-lifecycle.test.ts:152-187` (which seeds `tilted: true` with `untilTick: 500` and drains at tick 1) reddens without being edited.
  - **[Two corrections, code review iteration 2, 2026-09-08 — the mutation's substance stands; two incidental claims around it do not.]** (1) **"without being edited" is false.** This story *did* edit that test: its `machine.ballSave` expectation moved from `{ untilTick: null, sources: [] }` to `{ untilTick: null, sources: [BALL_SAVE_SOURCE] }` to accommodate the fused reset-then-enable (the same amendment this spec already acknowledges under "Two false claims in this spec, corrected in place"). The mutation still reddens it — dropping the guard saves the tick-1 drain and flips the `ball_ended` / `modes` / `modesPlayed` assertions — so only the "unmodified" clause was wrong. (2) **The line reference is pre-story.** `:152-187` is the numbering at `411da33`; post-diff the describe sits at **`:173-215`**, which is what this spec's own Matrix Test Audit cites — the two sections contradicted each other. Found by the acceptance-auditor.
- **AC 7** — revert one golden's `header.tableHash` to `672573c` → `test/replay-goldens.test.ts` reddens with a named `StaleReplayHeaderError`.
- **AC 8** — drop the per-lamp diff guard in `src/sim/loop/index.ts:439-447` so the whole projection is pushed → the ≤2-commands-per-tick assertion reddens (fifteen commands on the arming tick).
- **AC 9** — set `ballSaveMs` to `1` → the "drain 3,000 ticks later is saved" probe reddens; set it to `60000` → the "drain 30,000 ticks later ends the ball" probe reddens. Run both.
- **AC 10** — remove the `phase === 'game'` gate on arming → all five goldens redden on `expectedGameStateHash`, which is exactly the state-hash movement the Block-If forbids.
  - **[FALSE AT THIS TREE — corrected at code review, iteration 2, 2026-09-08; CLOSED — rework iteration 2, 2026-09-08.]** This mutation was true of the code it was originally run against, and became untrue once rework iteration 1 added a second conjunct to the very line it targets (`nextState.phase === 'game' && nextState.machine.ballSave.sources.includes(BALL_SAVE_SOURCE)`, now `ball-controller.ts:533` — rework iteration 2's own correction said `:513`, which was itself already stale by the time rework iteration 2 finished, per the build-auto review pass, 2026-09-08) — no golden ever presses `s_start`, so `sources` stays `[]` for every golden's whole duration and the **second** conjunct alone blocked arming, leaving the phase gate itself **shipped unprotected** (measured at review: deleting `nextState.phase === 'game' && ` gave 77/77 passing). **Closed by rework iteration 2:** `test/rules-ball-save.test.ts`'s `AC 10` describe block gained a second case seeding `phase: 'game_over'` **with** `sources` already holding `BALL_SAVE_SOURCE` — the state genuinely reachable after `game_over`, since the drain branch's own `gameOver` path sets `phase: 'game_over'` without calling `startBall()` again — asserting no `ball_save_timer_started` and `untilTick` stays `null`. **Re-measured at this tree:** deleting `nextState.phase === 'game' && ` now reddens exactly this new case (`expected true to be false`) while the pre-existing attract-phase case and all five goldens stay green, correctly isolating the phase gate from the already-covered sources gate. Reverted from a saved copy; `git diff --stat` confirmed `ball-controller.ts` byte-identical afterward.
- **AC 11** — change the grace comparison from `<=` to `<` → **only** the exact-`untilTick + graceTicks` probe (`test/rules-ball-save.test.ts`'s "AC 11" describe block) reddens; the epic's own three stated probes all stay green under it, which is precisely why task 15 requires a fourth. Run it and record which test went red. (Story 2.4's boundary mutation took the identical form.)
  - **QA correction (2026-09-07):** as originally written, this claim was an overclaim — the mutation actually reddened **two** tests: the exact-boundary probe above, and `test/rules-ball-save.test.ts`'s "drain inside grace (past the displayed expiry but before the grace deadline)" test, because that test's drain tick (`10 + graceTicks`) *was itself* the exact grace deadline, not a genuinely interior grace tick as its name claimed. This was accidental, not deliberate: the test's own name promised a tick "before the grace deadline," and the boundary-run's dedicated exact-deadline probe (with its explicit "distinguishes `<=` from `<`" rationale) already existed as the correct owner of that exact case. QA re-pointed the "drain inside grace" test at `10 + Math.floor(graceTicks / 2)` — strictly interior to the grace span, touching neither the displayed-expiry edge nor the deadline edge — so it now exercises a genuinely different case (a non-boundary mid-grace drain) instead of duplicating the boundary probe. Re-ran the mutation after the fix: it now reddens **only** the exact-boundary probe, exactly as this line originally claimed, making the claim true again rather than merely restated. Reverted from a saved copy; `git status --short`/`git diff --stat` confirmed unchanged on `src/sim/rules/ball-save.ts` afterward.

**Rework iteration 2's own new mutations (2026-09-08) — none belongs to a numbered AC, per the checklist's own note that "the rework's own new behaviour... belongs to no AC and has no `## Verification` line at all":**
- **`ballSaveHurryUpMs`'s production-magnitude pin** — set `ballSaveHurryUpMs` to `1` in `tuning.ts` → the new `AC 9` describe-block test ("production tuning: lampsOf() projects lit/3... 500 ticks before the displayed expiry...") reddens (`expected step 1 to equal step 3`), while every OTHER hurry-up assertion in the file (all under override tunings) stays green. Reverted from a saved copy; `git diff --stat` confirmed `tuning.ts` byte-identical afterward.
- **`awaitingSaveRelaunch`'s bounded lifetime (DW-224 + DW-225)** — short-circuit the new expiry check at the top of `step()` (`if (false && awaitingSaveRelaunch && ...)`) → the new `Rework iteration 2 (DW-224 + DW-225)` describe-block test reddens (`expected false to be true` — the later, genuinely unrelated `ball_launched` stays silently swallowed instead of arming). Reverted from a saved copy; `git diff --stat` confirmed `ball-controller.ts` byte-identical afterward.
- **The bounded-lifetime expiry's own exact boundary (`>` vs `>=`) — added by the build-auto review pass, 2026-09-08, closing a gap `verification-gap` found: the two boundary-straddle probes had no recorded mutation of their own.** Changed `tick > awaitingSaveRelaunch.startTick + ballSaveGraceTicks` to `tick >= ...` at `src/sim/rules/ball-controller.ts:447` → reddens exactly the "startTick + ballSaveGraceTicks EXACTLY" case (`expected true to be false` — the record now clears one tick early, so the boundary-tick `ball_launched` wrongly arms instead of being swallowed as the relaunch), while the "+1" case and the rest of the suite (114 files / 1867 tests) stay green — mirroring AC 11's identical-shaped `<=`-vs-`<` boundary one function over. Reverted from a saved copy (`ball-controller.ts.bak`, taken before the mutation); `git diff --stat` and a byte-for-byte `diff` against the backup both confirmed the tree unchanged afterward.

**Manual checks:**
- The re-record trace diff (`trace-BEFORE` vs `trace-AFTER`, tick-stride sampled, full precision): expect **no output**, and a row count of the order of 1,132 across the five (80 / 32 / 371 / 384 / 265). A materially different row count means the instrument, not the change, is wrong.
- Inspect each golden's `notes`: **appended**, never rewritten, with the existing `DW-70` and `deviceSlots` provenance intact.
- Inspect the `gameStart.tuning` diff: only the six new blocks should appear. A four-figure reordering diff means the harness flipped the key order and has tripped DW-213's `reopen_if`.
- **Presentation limits (stated plainly, AD-15):** no automated test in this story can observe a rendered pixel. The lamp assertions prove the projection and the command stream; they cannot prove the ball-save insert is visible on the playfield. That is the lead's browser smoke, and its *visible brightness* is Story 4.1's criterion, not this story's.

## QA Pass (2026-09-07)

No new test files were warranted — the story's existing coverage (headless + integration + goldens) already pins every I/O & Edge-Case Matrix row and every AC with a falsifiable test. QA's contribution was two targeted corrections to existing tests, both investigated per lead-supplied leads, both confirmed and fixed:

1. **AC 11 Verification overclaim (fixed).** `test/rules-ball-save.test.ts`'s "drain inside grace" test (in the AC 3/AC 4 describe block) drained at `10 + graceTicks` — the exact grace deadline — despite its own name promising a tick "before the grace deadline." This made the `isWithinGrace` `<=`→`<` mutation redden it in addition to the dedicated AC 11 boundary-run's exact-deadline probe, contradicting the spec's "only" claim. Judged accidental (the boundary-run's own probe, with its explicit "distinguishes `<=` from `<`" rationale, is the deliberate owner of that exact case; this test's name states an intent — "before," not "at" — that its own tick did not implement). Fixed by re-pointing the drain to `10 + Math.floor(graceTicks / 2)`, strictly interior to the grace span. Re-ran the mutation: now reddens only the boundary-run's exact probe, restoring the spec's claim to being true. See `## Verification`'s AC 11 entry for the full mutation record.
2. **DW-217, narrowed to this story's own three arms (closed).** `test/contracts.test.ts`'s exhaustive-switch test had three new `case` arms (`ball_save_enabled`, `ball_save_timer_started`, `ball_saved`) with bodies never exercised by any assertion — a wrong field reference would type-check and ship silently. Judged worth closing here: mechanical (three `expect()` calls), cheap, and scoped to this story's own new code rather than the wider pre-existing pattern (which remains deferred, unchanged, per the frontmatter `deferred:` entry). Added three assertions with deliberately distinct field values (`untilTick: 8040` vs `tick: 40`; `player: 3` vs `tick: 8020`) so a field swap is actually caught. Mutation-tested: swapped `event.untilTick` for `event.tick` in the `ball_save_timer_started` arm, observed the new assertion redden (`expected 'ball save timer started until 40' to be 'ball save timer started until 8040'`), reverted from a saved copy.

Both fixes were re-verified against the full suite: `pnpm typecheck` clean across all three tsconfigs; `pnpm test` **114 files / 1854 tests / 0 failed / 0 skipped** (unchanged from the story's own reported baseline — no test cases were added or removed, only two existing tests amended). `git status --short` shows only `test/rules-ball-save.test.ts` and `test/contracts.test.ts` modified; every mutation applied during this pass was reverted from a saved copy (never `git checkout --`/`git stash`) with `git status --short`/`git diff --stat` confirmed unchanged on the mutated source files afterward. `mutations_demonstrated=2` (one re-run of the pre-existing AC 11 mutation after the fix; one new mutation for the DW-217 closure).

## Auto Run Result

This section now holds four completions, in chronological order: the story's original (pre-rework) run, rework iteration 1's, rework iteration 2's, and the build-auto review pass that finalizes it. **The authoritative, current `Status:` / `Blocking condition:` lines are the LAST two lines of this file, under "Build-auto review pass completion" — not any of the three pairs inside the historical blocks below, all marked accordingly.**

### Original completion (2026-09-07, pre-rework, `baseline_revision` `411da335f5fa0ed9ac753c5efa0985447b116c8f`)

**Summary of implemented change.** `machine.ballSave` is now a live, machine-scoped device owned by `src/sim/rules/ball-controller.ts` (AD-18): enabled with the timer stopped at `ball_starting`, armed on `ball_launched` via a new `src/sim/rules/ball-save.ts` (`armBallSave`/`disarmBallSave`/`enableBallSave`/`isRunning`/`isWithinHurryUp`/`isWithinGrace`/`hasGraceLapsed`, inclusive `<=` throughout), projected to a new `l_ball_save` insert through the existing `lampsOf()`/`sim/loop` lamp-diff seam, and consulted at the drain so a live window or its grace re-serves the ball (`ball_saved`, a `c_trough_eject` pulse, the autolaunch coil deferred to the re-served ball's own arrival at `bd_shooter`) instead of ending it. Three new `…Ms` tunables (`ballSaveMs`, `ballSaveHurryUpMs`, `ballSaveGraceMs`) were added to `src/sim/table/tuning.ts`, each reached through `shotWindowTicks()`. All five replay goldens received the header-only refresh this story owed (`header.tableHash`, `header.gameStart.tuning`'s six new entries, `notes` appended) with no state-hash, trajectory, or `assetHash` movement. All 15 execution tasks and all 11 acceptance criteria are implemented and verified; no `Block If` condition was hit.

**Files changed:**
- `src/sim/table/tuning.ts` — three new top-level `…Ms` tunables (`ballSaveMs`, `ballSaveHurryUpMs`, `ballSaveGraceMs`), each with `source`/`confidence`.
- `src/sim/contracts/events.ts` — three new `SemanticEvent` members: `ball_save_enabled`, `ball_save_timer_started`, `ball_saved`.
- `src/sim/rules/ball-save.ts` (new) — pure `BallSaveState` helpers (arm/disarm/enable + running/hurry-up/grace predicates).
- `src/sim/rules/ball-controller.ts` — wires the device: enable at `ball_starting`, arm at `ball_launched`, drain-branch interception into a save, the deferred autolaunch on the re-served ball's arrival; two review-pass fixes (see below).
- `src/sim/rules/index.ts` — threads resolved `tuning` into `createBallController`.
- `src/sim/table/dragonwar.ts` — new `LampSubject` arm `{ kind: 'ball_save' }` and the fifteenth lamp, `l_ball_save`.
- `src/sim/rules/lamps.ts` — `projectBallSave()` (off/lit-1/lit-3 per Tilt/running/hurry-up), wired above the `!player` guard beside `lock`; explicit `never` exhaustiveness tail.
- `src/sim/loop/index.ts` — resolves `ballSaveHurryUpTicks` once and threads it to both `lampsOf()` call sites.
- `tools/make-placeholder-blend.py`, `assets/src/dragonwar.blend`, `public/assets/dragonwar.glb` — the `l_ball_save` insert (bottom-centre), rebuilt and re-exported; `dragonwar.collision.json` unaffected (confirmed empty diff).
- `test/rules-ball-save.test.ts` (new) — headless rules coverage for AC 1–7, 9–11, plus two review-pass regression tests (see below).
- `test/rules-ball-save-integration.test.ts` (new) — AC 8's real-`createLoop` `LampCommand`-stream integration test.
- `test/rules-devices-headless.test.ts` — registers the new headless test file in `ENTRY_FILES`.
- `test/table.test.ts` — fifteen-lamp inventory.
- `test/asset-contract.test.ts` — `subjectSwitchName()` widened to `string | null` with a non-vacuity count assertion; `l_ball_save` placement pin.
- `test/contracts.test.ts` — three new exhaustive-switch `case` arms.
- `test/rules-lifecycle.test.ts`, `test/rules-modes.test.ts`, `test/backglass-frame.test.ts`, `test/backglass-integration.test.ts` — collateral drains moved past the (overridden, near-zero) ball-save window so pre-existing scripts keep pinning what they always pinned.
- `test/replays/*.golden.json` (all five) — header-only refresh (`tableHash`, `gameStart.tuning`, `notes` appended); verified byte-identical elsewhere.
- `test/lighting-scene.test.ts`, `test/lighting-integration.test.ts` — review-pass cosmetic fix: stale "fourteen"-lamp prose updated to "fifteen" (assertions were already lamp-count-derived and unaffected).

**Review findings breakdown.** Four reviewers ran in parallel (blind-hunter, edge-case-hunter, verification-gap, intent-alignment). Two independently converged on the same root cause.
- **Patches applied (3, medium 1 / low 2):**
  - `[medium]` `awaitingSaveLaunch` (the deferred-autolaunch flag) had no ball-lifecycle reset point, unlike `machine.ballSave` itself and unlike the file's own cited `pendingLockLaneClosure` precedent (which self-clears on a timeout). A save whose re-serve never reached `bd_shooter` could leave the flag stuck `true` and silently auto-launch a later, unrelated ball. Fixed in `startBall()` (resets at `ball_will_start`, the same AD-7 boundary `ballSave`/`tilt`/`multiball` already reset at). Two new tests drive `createBallController()` directly and were confirmed red against a saved-copy revert before being restored green.
  - `[low]` The deferred autolaunch's coil pulse had no Tilt guard, unlike the drain branch a few lines below it. Fixed by mirroring the drain branch's `!tilt.tilted` check (still consuming the flag either way). Currently unreachable in production (no tilt event exists before Story 2.11), fixed defensively for consistency with AC 6's "inert while tilted" and covered by the same two new tests.
  - `[low]` Stale "fourteen"-lamp prose in two pre-existing lighting test files, made inaccurate by this story's fifteenth lamp. Cosmetic only; the underlying assertions were already correct (`Object.keys(TABLE.lamps)`-derived). Updated to "fifteen".
- **Deferred (1, low 1):** `test/contracts.test.ts`'s three new exhaustive-switch case-arm bodies are never exercised by that test's one assertion — an existing, unchanged pattern this story extended rather than introduced. Recorded in frontmatter `deferred:`.
- **Rejected (5):** (1) "the ball-save timer re-arms on every subsequent plunge" — verified against AD-18/AD-6's own text in the architecture spine, which unconditionally starts the ball-save timer on every opening of `s_shooter_lane`, including a save's own auto-relaunch; this is the architecture's specified behaviour, not a defect. (2) "Tilt 'disarms all' per AD-18/a pre-existing doc comment, vs. this story's read-only guard" — already explicitly scoped and accepted in the spec's own AC 6 and Design Notes ("Story 2.11 will additionally call disarm(source)... this story ships the read-side guard"). (3) "the 'Between balls' matrix row's isolated reset is never independently observable because `ball_starting`'s enable fuses into the same `startBall()` call" — pre-existing Story 2.5 architecture, correctly implemented and asserted exactly as AC 1 specifies. (4) "`disarm()` of the max-setting source doesn't shrink `untilTick` to the next-longest survivor's" — explicitly documented in the spec's own Design Notes as a decided, accepted limitation of the frozen `{untilTick, sources}` shape. (5) A stale `Status: ready-for-dev` line in this file's own (pre-finalize) Auto Run Result footer, flagged mid-review before this Finalize step had run — an artifact of review timing, not a diff defect.

**Follow-up review recommendation: `true`.** Only this pass's `patch`-triaged findings count (medium 1, low 2): `3 × 1 + 1 × 2 = 5`, which meets the "5 or more" threshold (no patched finding was itself `high`).

**Verification performed.**
- `pnpm typecheck` — clean across all three tsconfigs, both before and after the review-pass patches.
- `pnpm test` — **114 files / 1854 tests, 0 failed, 0 skipped** (2 more than the implementer's own 1852, from the two new review-pass regression tests; the story's cited 112/1824 baseline reflects the epic tree, not this multi-story-agent worktree at hand-off — the delta is fully explained by this story's own new/changed test files).
- `pnpm lint:boundaries` — `OK -- 105 .ts file(s)`, no violations.
- `pnpm check:headers`, `pnpm check:attributions` — both clean.
- `pnpm export:assets` — clean; `l_ball_save` accepted; `dragonwar.collision.json` diff confirmed empty.
- `pnpm check:ad7` — exactly 3 passing. `pnpm check:corridor` — 1 passing. `pnpm check:reachability` — 1 passing (52 cases, ~112 s).
- Golden hash-identity gate — re-verified structurally (per-field JSON comparison against `baseline_revision`, not raw substring grep, since golden `notes` prose itself contains the forbidden field names historically): only `tableHash` and `gameStart.tuning` moved on any of the five goldens; `expectedHash`/`expectedGameStateHash`/`expectedCheckpointHashes`/`assetHash`/`transitions`/`coilPrologue`/`durationTicks` are byte-identical to `baseline_revision` on all five.
- `git diff --stat -- public/assets/dragonwar.collision.json` — empty. `git ls-files --others --exclude-standard test/` — only the two intended new test files.
- Matrix Test Audit — all 17 I/O & Edge-Case Matrix rows confirmed covered by a passing, registered test (16 in `test/rules-ball-save.test.ts`/`-integration.test.ts`, "Between balls" by the pre-existing, story-updated `test/rules-lifecycle.test.ts:174-215`).
- Rule 19 mutations — the implementer's 11 AC mutations (one per AC) were each observed red then reverted (per their own report); this review pass additionally ran and confirmed red both of its own two new regression tests against saved-copy reverts of the tilt-guard and ball-boundary-reset fixes before restoring them green. No `git checkout --`/`git stash` used at any point; `git status --short`/`git diff --stat` confirmed unchanged after every revert-and-restore cycle.

**Residual risks.**
- The deferred-autolaunch Tilt guard is defensively fixed but currently untestable through any real game-triggered tilt event (Story 2.11 hasn't landed); it is exercised only via direct `GameState` seeding, the same technique the existing AC 6 test already uses for the same reason.
- `disarm()`'s "never shrinks `untilTick` below the max-setter's own deadline" behaviour (a spec-documented, accepted limitation) means a future multiball-arming story (3.7) inherits a slightly coarser arbitration semantics than a per-source deadline list would give; this is priced into the spec's own Design Notes, not new information from this pass.
- No automated test in this story observes a rendered pixel (`NullEngine` rasterises nothing, per AD-12 clause 3) — the lamp assertions prove projection and command-stream correctness only, exactly as the spec's own Verification section discloses.

*(Historical, superseded by "Rework iteration 1 completion" below — this was the original implementation's own completion line, not the current one.)* Status: done · Blocking condition: none

### Rework iteration 1 completion (2026-09-07, `baseline_revision` `b8bf11f13a5c424038d60697a62edd967b6c3419` → HEAD at finalize)

**Summary of implemented change.** Rework iteration 1 closes `DW-218`, the one blocking finding left open by Story 2.9's original code review: a ball save re-armed a fresh full ball-save window on its own automatic re-serve (the deferred autolaunch's `c_autolaunch` pulse opens `s_shooter_lane`, emitting the same `ball_launched` event the controller armed on unconditionally), so the ball could never actually end — measured at production tuning, 120,000 ticks, seed 0, no input: 28 `ball_saved`, zero `ball_ended`. The author's decision (option 2 of four put to them): **a player plunge arms the window; a save's own re-serve does not.** `src/sim/rules/ball-controller.ts` implements this with a second closure-held cross-tick discriminator, `awaitingSaveRelaunch`, mirroring the file's own pre-existing `awaitingSaveLaunch` pattern — set only when the deferred-autolaunch coil pulse genuinely fires, consumed by the very `ball_launched` that pulse causes, reset at `ball_will_start`. The controller's arming branch is now also gated on the controller's own source being present in `sources` (`enableBallSave`'s previously-inert gate, now load-bearing), closing a second finding the same review pass named. `BallSaveState`'s serialized `{ untilTick, sources }` shape is untouched — zero state-hash movement — and no golden moved (`git diff --stat -- test/replays/` empty for this entire rework). The fix was independently re-verified by the build-auto stage at the full 120,000-tick protocol specified for it (not merely trusted from the implementer's own report): production tuning now reaches `game_over` with all three balls ending at a natural cadence (`saved=[4276,8575,17120,21419,29964,34263]`, `ended=[12849,25693,38537]`), and the control (`ballSaveMs` 500 / `ballSaveGraceMs` 100) gives zero saves with `ball_ended` at tick 4276 — the same tick production's first save landed, confirming the harness was not blind. Both the production-tuning regression guard and its control are now committed, permanent tests.

**Files changed (this rework pass only):**
- `src/sim/rules/ball-controller.ts` — the `awaitingSaveRelaunch` discriminator (declaration, reset at `ball_will_start`, set at the deferred-autolaunch pulse site, consumed at the `ball_launched` arming site); the arming branch additionally gated on `machine.ballSave.sources.includes(BALL_SAVE_SOURCE)`.
- `test/rules-ball-save.test.ts` — three new headless tests: a full save-and-relaunch cycle leaves `machine.ballSave` byte-identical to what the original plunge set (no second `ball_save_timer_started`); an un-enabled source cannot arm; `awaitingSaveRelaunch` itself resets at `ball_will_start` so a stale flag cannot swallow a later player's genuine plunge.
- `test/rules-ball-save-integration.test.ts` — two new real-`createLoop`/real-physics tests: the "DW-218 regression guard" (production tuning, 60,000-tick budget, asserts the exact measured `saved`/`ended` tick arrays and `game_over`) and its "DW-218 regression control" (a 500 ms/100 ms window, added at the build-auto review stage per the checklist's own instruction to pin the control as a permanent test — asserts zero saves and `ball_ended` at the identical tick the production run's first save landed).
- `_bmad-output/implementation-artifacts/spec-2-9-ball-save.md` — this file: rework checklist checked off, an "implemented" writeup, the stale pre-rework HIGH finding marked resolved (preserved as history), a closing Spec Change Log entry, this Review Triage Log entry, three new `deferred:` frontmatter entries, and this Auto Run Result.

**Review findings breakdown.** Four reviewers ran in parallel (blind-hunter, edge-case-hunter, verification-gap, intent-alignment).
- **Patches applied (3, low 3):**
  - `[low]` The spec's own pre-rework `#### HIGH — unresolved, blocking` section contradicted the now-checked-off rework checklist above it. Fixed: heading changed to `RESOLVED in rework iteration 1` with a resolution note; original finding text preserved verbatim as history.
  - `[low]` `## Spec Change Log` had no closing entry for this rework. Fixed: appended one summarising the fix and its independent re-verification.
  - `[low]` The committed "DW-218 regression guard" test bounded `saved.length` only loosely (`< 10`) despite the exact value (6) being already known and measured identically twice. Found independently by blind-hunter and verification-gap. Fixed: tightened to `toEqual()` against the exact measured `saved`/`ended` tick arrays.
- **Deferred (3, medium 3):** all recorded in frontmatter `deferred:` — (1) the `awaitingSaveRelaunch` discriminator consumes blindly whichever `ball_launched` arrives next without verifying it was caused by the deferred-autolaunch pulse (a player's manual plunge could in principle land in the same narrow window and be misclassified; does not reintroduce the DW-218 defect since the still-live original window continues regardless); (2) `awaitingSaveRelaunch` has no same-ball recovery if the deferred pulse resolves to `eject_failed`, staying stuck `true` for the rest of that ball (low reachability today; relevant once ball search or another story gives a ball a path back through `bd_shooter` mid-ball); (3) the new `enableBallSave` arming gate interacts with grace-lapse — once grace fully lapses without a drain, `sources` is cleared and ball-save eligibility is lost for the rest of that ball (dormant today; Story 3.7's second arming source is positioned to make it reachable). Found independently by blind-hunter and edge-case-hunter (1 and 2); blind-hunter alone (3).
- **Rejected (5):** conflating `review_loop_iteration` (this workflow's own bad-spec-loopback counter, correctly `0`) with the spec body's separate "Rework iteration 1" label; claiming the committed control test's relative comparison is weaker than a hardcoded tick literal (it is deliberately the stronger, non-vacuous, self-updating form); calling the 3-tick gap between the original review's measured tick (4273) and this rework's (4276) unexplained (adequately explained — harness call-sequence difference — and corroborated by two independent modern measurements both landing on 4276); calling the regression guard's hard-coded one-tick inter-ball re-plunge delay fragile (already empirically exercised successfully by two independent real-physics runs, the same established pattern as the pre-existing AC 2 integration test's own hard-coded travel window); the intent-alignment audit's observation that the discriminator lives in a JS closure invisible to the state-hash surface (this is the explicitly author-mandated design, not a defect).

**Follow-up review recommendation: `false`.** Only this pass's `patch`-triaged findings count (low 3, medium 0): `3 × 0 + 1 × 3 = 3`, below the "5 or more" threshold, and no patched finding was `high`.

**Verification performed.**
- `pnpm typecheck` — clean across all three tsconfigs, both after the implementation and after the review-pass patches.
- `pnpm test` — **114 files / 1862 tests, 0 failed, 0 skipped**, both after implementation (1862 already, including the build-auto-stage-added control test) and re-confirmed after the three review-pass patches (patches touched only test assertions and the spec file, no new/removed test cases).
- `pnpm lint:boundaries` — `OK -- 105 .ts file(s)` (unchanged — no new `src/` file this pass).
- `pnpm check:headers`, `pnpm check:attributions` — both clean.
- `pnpm check:ad7` — exactly 3 passing. `pnpm check:corridor` — 1 passing. `pnpm check:reachability` — 1 passing (52 cases, ~113 s).
- Golden hash-identity gate — `git diff test/replays/ | grep -E 'expected(Hash|GameStateHash|CheckpointHashes)|assetHash|"transitions"|coilPrologue|durationTicks'` — **no output**; `git diff --stat -- test/replays/` — **empty** (this rework touches no golden at all, header-only or otherwise, exactly as expected since none of the five ever press `s_start`).
- `git diff --stat -- public/assets/dragonwar.collision.json` — empty (untouched this pass). `git ls-files --others --exclude-standard test/` — empty (no new test files; both new tests landed in already-tracked files).
- Rule 19 mutation — the pre-fix unconditional-arm code was independently re-applied by the build-auto stage (not merely trusted from the implementer's own report) and re-confirmed red: 3 of 29 ball-save tests failed for the expected reason (the regression guard's `finalPhase` stayed `'game'`; both new headless discriminator tests flipped `false`→`true`; the new control test stayed green throughout, by design, since it validates the harness rather than the fix). Restored from a saved copy (never `git checkout --`/`git stash`); `md5sum` and `git diff --stat -- src/sim/rules/ball-controller.ts` confirmed the restored file was byte-identical to the fix both before and after.
- Independent 120,000-tick real-physics re-verification (build-auto stage, throwaway scratchpad harness, never under `test/`, deleted after use) — see Summary above; matched the implementer's own scratchpad tick-for-tick.

**Residual risks.**
- The three deferred findings above (frontmatter `deferred:`): the discriminator's causality assumption, `awaitingSaveRelaunch`'s same-ball staleness on `eject_failed`, and the `enableBallSave` gate's dormant grace-lapse interaction. None reintroduce the measured DW-218 defect; all are narrow, currently-unreachable-or-low-reachability, and named for whichever future story (2.12, 3.7) makes them live.
- All residual risks recorded in the story's original (pre-rework) Auto Run Result remain accurate and are not restated here — see `### Original completion (2026-09-07, pre-rework)` above, its own "Residual risks" subsection, for that list.

*(Historical, superseded by "Rework iteration 2 completion" below — this was rework iteration 1's own completion line, not the current one. The first two of the three findings this section deferred, `DW-224`/`DW-225`, are fixed by rework iteration 2, below; the third, `DW-226`, is routed to Story 3.7 and remains open there.)* Status: done · Blocking condition: none

### Rework iteration 2 completion (2026-09-08, `baseline_revision` `75eef7aa8934d2b99a226b310495f66c7fd96117` → HEAD at finalize)

**Summary of implemented change.** Rework iteration 2 closes the four MED findings code review pass 2 left open (three `rework`-triaged in-story; the fourth, `DW-226`, was already `routed` to Story 3.7 and untouched here). `DW-224` (no causal binding) and `DW-225` (no same-ball recovery) share one root cause and one fix: `src/sim/rules/ball-controller.ts`'s `awaitingSaveRelaunch` is widened from a plain boolean to the exact `{ startTick }` shape `pendingLockLaneClosure` already uses one file over (`sim/rules/devices/index.ts`), self-clearing off the already-resolved `ballSaveGraceTicks` — evaluated at the top of `step()`, before this tick's own device events are read, the same ordering `hasGraceLapsed()` immediately above it and `pendingLockLaneClosure` itself both already use. No new tunable, no new `GameState` field — the record lives in the same controller-instance closure the boolean it replaces already occupied, so this is zero state-hash movement, per the spec's own Block-If. It bounds how long a `ball_launched` may be trusted as the deferred pulse's own relaunch — a timeout, not a true per-pulse correlation check, per the code's own inline comment, but sufficient today given `bd_shooter` holds one ball and `s_shooter_lane` is one switch (build-auto review pass correction, 2026-09-08; see below) — and guarantees a stuck latch eventually releases so a later, genuinely unrelated `ball_launched` arms rather than being silently swallowed for the rest of the ball (the same-ball recovery `DW-225` wanted); it does not and cannot, from `sim/rules` alone, un-strand a ball physically resting in an empty shooter lane after a real `eject_failed` — that remains Story 2.12's ball search, exactly as both findings named. AC 10's `phase === 'game'` arming gate, previously shipped with no test able to discriminate it from the `sources` conjunct beside it, is now pinned by a case seeding the state genuinely reachable after `game_over` (`sources` already holding the controller's own source, phase no longer `'game'`). `ballSaveHurryUpMs`, previously pinned only by a relational assertion any magnitude down to `1` would satisfy, now has a production-magnitude consequence pin mirroring the previous pass's own `ballSaveGraceMs` fix. All three fixes were verified with a Rule 19 mutation, each stated before running, observed red for the stated reason, and reverted from a saved copy with `git diff --stat` confirming byte-identity afterward. The `## Verification` section's AC 10 entry is corrected to record the closure (preserving the "shipped unprotected" measurement as history), and a new subsection records the two mutations belonging to no numbered AC, closing the checklist's own fourth item (the re-walk).

**Files changed (this rework pass only):**
- `src/sim/rules/ball-controller.ts` — `awaitingSaveRelaunch` widened from `boolean` to `{ startTick: number } | null` (a local `AwaitingSaveRelaunch` interface); a bounded-lifetime expiry check added at the top of `step()`, before device events are read; the set site now records `{ startTick: tick }`; the two consume/reset sites now assign `null`. No other behavioural line touched — the `phase === 'game' && sources.includes(BALL_SAVE_SOURCE)` arming condition itself is unchanged (only renumbered by the surrounding diff).
- `test/rules-ball-save.test.ts` — five new tests: an `AC 10` case isolating the phase gate from the sources gate; an `AC 9`-block production-magnitude consequence pin for `ballSaveHurryUpMs`; and three cases in a new `Rework iteration 2 (DW-224 + DW-225)` describe block — the bounded-lifetime recovery case plus two boundary-straddle probes (`>` vs `>=` at the expiry tick). **[Count corrected, build-auto review pass, 2026-09-08 — this bullet originally said "three new tests" and omitted the two boundary probes.]**
- `_bmad-output/implementation-artifacts/spec-2-9-ball-save.md` — this file: the rework iteration 2 checklist checked off, an "implemented" writeup, the MED findings section marked resolved (preserved as history), the `DW-224`/`DW-225` frontmatter `deferred:` entries removed (fixed, not deferred), the `DW-226` entry's stale line number corrected, a closing Spec Change Log entry, the `## Verification` section's AC 10 entry corrected and two new mutation entries added, and this Auto Run Result.

**Verification performed.**
- `pnpm typecheck` — clean across all three tsconfigs (`tsconfig.sim.json`, `tsconfig.app.json`, `tsconfig.node.json`).
- `pnpm test` — **114 files / 1867 tests, 0 failed, 0 skipped** (5 more than the pre-rework 1862: the five new tests above). **[Corrected, build-auto review pass, 2026-09-08 — originally reported as 1865 / "3 more"; re-measured directly.]**
- `pnpm lint:boundaries` — `OK -- 105 .ts file(s)` (unchanged — no new `src/` file this pass).
- `pnpm check:headers`, `pnpm check:attributions` — both clean.
- `pnpm check:ad7` — exactly 3 passing.
- Golden/asset/tuning discipline — `git status --porcelain` shows only `src/sim/rules/ball-controller.ts`, `test/rules-ball-save.test.ts` and this spec file touched; no `test/replays/*.golden.json`, no `public/assets/**`, no `src/sim/table/tuning.ts` (the two tunable-value mutations used to verify the AC 10 and hurry-up tests were both reverted from a saved copy before this check, confirmed by `git diff --stat`).
- Rule 19 mutations — three, each applied, observed red for the stated reason, reverted from a saved copy (never `git checkout --`/`git stash`), with `git diff --stat` (and, for the ball-controller.ts mutations, a byte-for-byte `diff` against the pre-mutation backup) confirming the tree unchanged afterward: (1) deleting `nextState.phase === 'game' && ` reddened the new AC 10 isolating case only, leaving the pre-existing attract-phase case and all five goldens green; (2) setting `ballSaveHurryUpMs` to `1` in `tuning.ts` reddened the new hurry-up production-pin test only; (3) short-circuiting the new bounded-lifetime expiry check reddened the new DW-224/DW-225 case only.
- The eleven pre-existing `## Verification` AC mutation lines (AC 1-9, AC 11) were reviewed against this diff rather than mechanically re-run: none targets code this rework touches (`ball-save.ts`'s predicates, `lamps.ts`'s `projectLamp()`, the drain interception, the deferred-autolaunch timing, and the arming condition's own text are all unchanged by this diff — confirmed by `git diff --stat` naming only `ball-controller.ts` and the two test files), and `pnpm test`'s full green run (114/114 files) confirms none of their own dedicated tests moved either. AC 10's line is the one this rework's diff DOES affect, and its entry is corrected above with a freshly-run mutation.

**Residual risks.**
- `DW-226` (the grace-lapse × `enableBallSave` asymmetry) remains open, `routed` to Story 3.7 — unaffected by and unrelated to this rework's fix; recorded in frontmatter `deferred:`.
- The bounded-lifetime fix closes the RULES-side latch defect only, exactly as both `DW-224` and `DW-225` named: it cannot by itself un-strand a ball physically resting in an empty shooter lane after a real `eject_failed` on the deferred autolaunch. That physical recovery remains Story 2.12's ball search. Until 2.12 lands, a same-ball `eject_failed` on the deferred pulse still produces the game-hang `edge-case-hunter` found (`ballsInPlay` stuck at 0, no parking entry possible) — this rework does not claim otherwise; it only guarantees that if the game is later recovered by some other means (or once 2.12 exists), the ball-save rules state itself is no longer the thing standing in the way.
- All residual risks recorded in the story's original (pre-rework) and rework iteration 1's own Auto Run Result sections remain accurate and are not restated here — see those sections' own "Residual risks" subsections above.

*(Historical, superseded by "Build-auto review pass completion" below — this was rework iteration 2's own completion line, not the current one. It ended its turn with a progress message instead of this Finalize step, per the cycle log's `protocol_violation` entry; the implementation it reports is complete and sound, but its self-reported test count, one line-number citation, and its "causal binding" framing are each corrected in the section below rather than in place, to preserve the historical record of what was originally claimed.)* Status: done · Blocking condition: none

### Build-auto review pass completion (2026-09-08, `baseline_revision` `75eef7aa8934d2b99a226b310495f66c7fd96117` → HEAD at finalize)

**Summary of implemented change.** This pass resumed the spec at its own review step (`status: in-review`, per the completion contract's routing) after rework iteration 2's implementation ended its turn early with a progress message rather than a finalize. The prior stage's tree was verified stable first (liveness re-confirmed; the tree was unchanged from what rework iteration 2 left), so no code was re-derived and the latch fix was not reopened. Four review layers — blind-hunter, edge-case-hunter, verification-gap, intent-alignment — ran in parallel against the full diff since `baseline_revision`. Five patches were applied, all mechanical documentation/spec corrections plus one Rule 19 mutation exercise; no production code changed. See the Spec Change Log entry above ("build-auto review pass") for the itemised list; in summary: a stale line-number citation (`:513` → `:533`) in two places, an undercounted test total (1865 → true 1867, two boundary-straddle tests omitted) in four places, a missing Rule 19 mutation record for those same two boundary tests (now run, observed red, reverted, and recorded), and an overclaiming "causal binding" framing softened to match the code's own honest "timeout, not a true causal binding" comment, in three places. Two findings — both already candidly disclosed in the code's own inline comments, both narrow and currently unreachable, both pointing at Story 3.7 (the first story to add a second ball-save arming source) as where they would become live — were added to frontmatter `deferred:`: the bounded timeout's lack of per-pulse correlation, and its reuse of `ballSaveGraceTicks` (a tunable authored for a different concern) to also bound pulse-travel time. Intent-alignment's observation that no test constructs a literal `eject_failed` event or asserts on `ballsInPlay`/`ball_ended`/`ball_will_start` was reviewed and accepted as already correctly scoped, not a gap: the Residual Risks section already stated plainly that the physical stranding is unfixed pending Story 2.12's ball search, and this story's own layer (`sim/rules`) is what the new tests actually pin — that the latch itself no longer permanently blocks recovery once a later `ball_launched` arrives, however it arises.

**Files changed (this pass only):**
- `_bmad-output/implementation-artifacts/spec-2-9-ball-save.md` — this file: the two stale-line-number citations corrected; the test-count claims corrected in four locations; a third Rule 19 mutation recorded for the two boundary-straddle tests; the "causal binding" language softened in three locations; two new frontmatter `deferred:` entries; this Spec Change Log entry; this Review Triage Log entry; this Auto Run Result.

No source file (`src/sim/rules/ball-controller.ts`, `test/rules-ball-save.test.ts`) changed in this pass — the one Rule 19 mutation this pass ran (`>` → `>=` at `ball-controller.ts:447`) was applied, observed red, and reverted from a saved copy before this file was written.

**Review findings breakdown.** Four reviewers ran in parallel (blind-hunter, edge-case-hunter, verification-gap, intent-alignment).
- **Patches applied (4, medium 3 / low 1):**
  - `[low]` Stale line-number citation (`ball-controller.ts:513` → the true `:533`) in the frontmatter `DW-226` deferred entry and the AC 10 `## Verification` note. Found independently by blind-hunter and verification-gap; confirmed by direct `grep -n`.
  - `[medium]` Test-count undercount ("three new tests" / "1865 tests") across the implemented writeup, Spec Change Log, and Auto Run Result — true count is five new tests / 1867 total. Found independently by blind-hunter and intent-alignment; confirmed by a direct `pnpm test` run matching the cycle log's own prior independent measurement.
  - `[medium]` The two new boundary-straddle tests had no Rule 19 mutation recorded. Found by verification-gap. Fixed: mutation run (`>` → `>=`), observed to redden exactly the "EXACTLY" boundary case, reverted from a saved copy, tree confirmed byte-identical, recorded in `## Verification`.
  - `[medium]` "Closing DW-224 (causal binding)" overclaimed relative to the code's own inline comment ("this is a TIMEOUT, not a true causal binding"). Found by blind-hunter. Softened in three locations to match the code's own framing.
- **Deferred (2, medium 2):** both recorded in frontmatter `deferred:` — (1) the bounded-lifetime fix is a timeout, not a true per-pulse causal binding, so a manual plunge landing inside the bounded window is still silently swallowed as the relaunch (found by blind-hunter and edge-case-hunter, already disclosed in the code's own comment); (2) the fix reuses `ballSaveGraceTicks` — authored for a different concern (drain-to-still-saved duration) — to also bound pulse-travel time, safe today only because production `ballSaveGraceMs` vastly exceeds realistic travel time (found the same way, also already disclosed in-code). Both point at Story 3.7 as the story that would make them live.
- **Rejected (2):** `[low]` `AwaitingSaveRelaunch` is declared inside `createBallController()`'s function body, while the precedent it's compared to (`PendingLockLaneClosure`) is declared at module scope (found by blind-hunter; verified by direct `grep -n`) — no functional consequence (erased at compile time; `tsc` clean) and the code comment's claim is about the shared field shape, not lexical scope, so this is noise, not a defect; intent-alignment's "hang not literally pinned" observation, reviewed and accepted as already honestly and correctly scoped by the existing Residual Risks text rather than a gap needing a new test (this story's layer cannot construct the full physical hang without Story 2.12's ball search, and does not claim to).

**Follow-up review recommendation: `true`.** Only this pass's `patch`-triaged findings count (medium 3, low 1): `3 × 3 + 1 × 1 = 10`, which meets the "5 or more" threshold (no patched finding was itself `high`).

**Verification performed.**
- `pnpm typecheck` — clean across all three tsconfigs, confirmed after this pass's edits (spec/documentation only; no source file touched outside the one reverted mutation).
- `pnpm test` — **114 files / 1867 tests, 0 failed, 0 skipped** — re-measured directly by this pass, matching the cycle log's own independent count taken before this stage started.
- Rule 19 mutation (this pass's own) — `tick > awaitingSaveRelaunch.startTick + ballSaveGraceTicks` changed to `tick >= ...` at `ball-controller.ts:447` → reddened exactly the "startTick + ballSaveGraceTicks EXACTLY" test (`expected true to be false`), leaving the "+1" test and the rest of the suite green; reverted from a saved copy (`ball-controller.ts.bak`); `git diff --stat` and a byte-for-byte `diff` against the backup both confirmed the tree unchanged afterward.
- `git status --porcelain` at this pass's start showed only the three files rework iteration 2 left uncommitted (`ball-controller.ts`, `test/rules-ball-save.test.ts`, this spec file) plus the already-committed bookkeeping commit — matching the dispatch's own description of the tree.
- Golden/asset/tuning discipline unaffected — this pass touched no golden, no tuning value, no table, no event, no lamp file; `git diff --stat` confirms only the spec file changed in this pass.

**Residual risks.**
- The two newly deferred findings above (the timeout-vs-correlation gap and the reused-tunable coupling) are real but narrow and currently unreachable; both are Story 3.7's to account for when it adds the first second ball-save arming source.
- `DW-226` remains open, `routed` to Story 3.7, unaffected by this pass.
- All residual risks recorded in the story's original (pre-rework), rework iteration 1's, and rework iteration 2's own Auto Run Result sections remain accurate and are not restated here.

Status: done
Blocking condition: none
