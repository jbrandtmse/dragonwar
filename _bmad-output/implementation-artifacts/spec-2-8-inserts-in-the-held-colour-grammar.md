---
title: 'Story 2.8: Inserts in the held colour grammar'
type: 'feature'
created: '2026-09-06'
status: 'ready-for-dev'
review_loop_iteration: 0
followup_review_recommended: false
context: []
warnings: [oversized]
deferred: []
---

<intent-contract>

## Intent

**Problem:** The table has no lights. `TABLE.lamps` holds one Story 1.4 placeholder (`l_insert_left`) that nothing drives, `src/sim/rules/lamps.ts` does not exist, and `src/presentation/lighting/` holds only a `.gitkeep` — so the lit lanes, the DRAGON letters and the Lock that Stories 2.5 and 2.7 already track in `GameState` are invisible, and a player cannot read the table without instructions (PRD FR-43, FR-44).

**Approach:** Author the fourteen insert lamps the game already has state for — three Top lanes, four inlane/outlane, six DRAGON letters, the Lock — as `TABLE.lamps` entries and as `l_` lens-and-cup meshes in the Blender source; add `lampsOf(state): LampState` as a pure projection in `src/sim/rules/lamps.ts`; make `sim/loop` emit only the per-step diff as `LampCommand`s; and add `presentation/lighting/grammar.ts` (the one `(role, step)` → RGB/intensity/cadence table) plus the `LampDriver` that drives each insert's emissive material and a dynamic light beneath its lens. Rules speak roles and steps; only presentation knows a colour.

## Boundaries & Constraints

**Always:**
- **AD-9 is the contract.** Lamp state is a *pure projection* `lampsOf(state)` recomputed every rules step from `GameState` alone; `sim/loop` emits the **diff** as `LampCommand`s; `step ∈ {0 off, 1 lit, 2 emphasised, 3 urgent}` is the only progression rules may express; `role ∈ { off, lit, hurryup, quickmb, joust, dragon, special }` — **never a colour**; `presentation/lighting/grammar.ts` is the one `(role, step)` → RGB/intensity/cadence table, with blinking timed by presentation. No mode ever issues a lamp command.
- **AD-11.** `assets/src/dragonwar.blend` owns every position; `TABLE` owns the registry and the wiring; `tools/export.py` is the enforcer. An `l_` node is **lens *and* cup geometry below the surface, never a decal**; it must carry exactly two UV layers, exactly one material slot, a `lightgroup` from `TABLE.lightGroups`, and a name that is a key of `TABLE.lamps` (`export.py:194-195` refuses the export otherwise). The `.blend` is edited **only** through `tools/make-placeholder-blend.py`, never in the UI.
- **AD-10 frames.** Geometry is authored unpitched in the **table frame** (playfield-local millimetres, right-handed: +X right, +Y up-table away from the player, **+Z normal to the playfield toward the glass**). "Beneath the lens" therefore means **table −z** (into the playfield, away from the glass) — the light sits at table z ≈ −4 mm, inside the cup. Presentation converts with `toScene()` **only**; `toPhysics()` (which negates y) must not appear anywhere in this story.
- **AD-12.** Inserts are one of four lighting channels behind one `LampDriver`; early phases drive them as **emissive material plus dynamic lights** behind that same driver. The live dynamic-light budget on the floor is 20 per frame `[ASSUMPTION]` and reaches the code as a `tuning.ts` entry.
- **AD-15 / golden discipline.** Every tunable carries `source` and `confidence`, and its entire serialized output is hashed into every golden header. A new lamp key moves `tableHash()`; a new tunable moves `header.gameStart.tuning`. Both are a **header-only refresh** — the same class Stories 2.3, 2.4, 2.5 and 2.7 each carried. Trace all five goldens before recording. **Append** to each `notes`, never rewrite.
- **AD-16 / boundary lint.** `l_` is a device-name prefix, so a lamp-name string literal outside `src/sim/table/dragonwar.ts` and `test/**` is a lint error — lamp names reach presentation through `LampCommand.lamp` (typed `LampName`) or `Object.keys(TABLE.lamps)`, never a literal. The new colour gate belongs in `tools/boundary-lint.mjs` beside the four existing textual bans, not in a parallel scan and not in dependency-cruiser (which, with `parser: 'swc'` and `tsPreCompilationDeps: false`, cannot see identifiers or string literals at all).
- **DW-149.** Every derived set comes from `Object.keys()` / `Object.entries()` over a `TABLE` block. Never a second hand-typed lamp, lane or letter list.
- **Rule 14.** Non-ASCII characters in source are authored as escapes, never literal bytes.

**Block If:**
- **Any golden's `expectedHash`, `expectedGameStateHash`, `expectedCheckpointHashes`, or `header.assetHash` moves.** This story's change is header-only by construction: none of the five goldens presses `s_start`, so all five stay in `phase: 'attract'`, no mode is ever pushed, `lanes` is never written and `lampsOf` projects all-off throughout. A moved state hash means something other than a lamps-and-tuning change happened — that is a **grant-bound trajectory re-record, not a refresh**. HALT and report; do not re-record around it.
- **`public/assets/dragonwar.collision.json` is not byte-identical after the re-export.** `l_` nodes match none of `export.py`'s three collision prefixes (`col_`, `sw_`, `ballDevices`), so the document cannot change. If it does, a `col_`/`sw_`/`bd_` node was disturbed. HALT.
- **The new colour rule fires on any file that is not a deliberate fixture.** HALT naming the file. Do **not** add an exemption to make the real tree green — an exemption nothing can prove is DW-39's own recorded failure.
- **Blender cannot be resolved or the export fails.** HALT naming the exact error. Do **not** conclude Blender is absent: it is a portable extract at `C:/Users/Josh/tools/blender-5.2.1-windows-x64/blender.exe`, `BLENDER` is set at user scope, and `blender.mjs:169-177` honours it verbatim. A previous runner probed four conventional locations and two registry hives and wrongly reported it missing.
- **Removing `l_insert_left` turns out to have a consumer** beyond `TABLE.lamps`, `tools/make-placeholder-blend.py`, `test/table.test.ts` and `ATTRIBUTIONS.md`. HALT and report the consumer.
- **Any existing test would have to be weakened, skipped, deleted, or have an assertion relaxed** to go green.
- The intended `(role, step)` → colour mapping cannot be satisfied without a colour outside PRD FR-44's six.

**Never:**
- **No other lighting channel.** GI, flashers and architectural lighting are AD-12's other three and belong to Story 4.1; `TABLE.flashers` and `TABLE.shows` stay empty and `GiCommand` gets no producer. No per-group bake, no lightmap authoring, no `vpx_lightmapper` technique — Epic 4.
- **No lamp that has no state driving it.** `hurryup`, `quickmb`, `joust` and `special` are declared in the grammar (PRD FR-44 is a *held* mapping) but `lampsOf` never emits them in Epic 2 — Stories 3.5, 3.7, 3.6 and 3.10 own their producers. **Do not add `l_ball_save`**: it is Story 2.9's, and adding it here would ship a fourteenth-plus lamp with no state.
- **No mode lifecycle events and no mode registry** (Story 3.1, AD-8's recorded phasing). **No Lock arbiter and no `lockCredits` write** (Story 3.2, AD-18) — `players[i].lockCredits` is still never written by anything, so the Lock insert projects from `machine.deviceSlots.bd_lock`, not from `lockCredits`.
- **No third-party anything.** Every RGB triple, intensity and cadence in `grammar.ts` is authored from PRD FR-44's own words. No colour-science snippet, no easing curve, no light-falloff formula, no texture, no node group, no add-on. `vbousquet/pinball-parts`' "Thin Film Interaction" node group is **CC BY-NC-SA and forbidden** (an NC component makes the whole work undistributable under GPL-3.0); `vbousquet/vpx_lightmapper` has **no licence verified at source** and no `ATTRIBUTIONS.md` row, so nothing may be taken from it. If any external material is nonetheless needed, the `ATTRIBUTIONS.md` row lands **before** the file, with the licence read at its source.
- Never emit a lamp command from a mode; never format a colour, hex or colour name anywhere under `src/sim/`.
- Never hand-edit `assets/src/dragonwar.blend` in Blender's UI, or hand-edit `public/assets/dragonwar.glb` / `dragonwar.collision.json`.
- Never re-record a golden to make a suite green; never touch a golden's `transitions`, `coilPrologue`, `durationTicks`, `expectedHash`, `expectedGameStateHash` or `expectedCheckpointHashes` in a header-only refresh.
- Never `git commit`, `git push`, `git reset`, `git rebase`, `git checkout --` or `git stash`; never mutate a remote, trigger CI, or trigger a deploy. Revert a Rule-19 mutation **from a saved copy**, then confirm the tree is byte-identical with `git status --short` and `git diff --stat`.
- Never let a `pnpm-workspace.yaml` into the worktree. If subprocess-spawning tests go red locally while CI is green: `PNPM_CONFIG_STRICT_DEP_BUILDS=false pnpm install --ignore-workspace`.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Attract, no modes | `modes: []` (the boot state, and every golden's whole run) | `lampsOf` returns every lamp `{ role: 'off', step: 0 }`; the loop's seeded previous projection is also all-off, so **zero** `LampCommand`s are emitted | No error expected — this is what keeps `test/flipper-mover.test.ts:73,127` green |
| One lit Top lane, skill shot live | base mode (100) + `skill_shot` (200) on the stack, `lanes.lit.top_2 === true` | `l_top_2` = `{ role: 'lit', step: 2 }`; `l_top_1`/`l_top_3` = `off` | No error expected |
| Same lit lane, skill shot resolved | `skill_shot` no longer in `modes[]`, `lanes.lit.top_2` still true | `l_top_2` drops to `{ role: 'lit', step: 1 }` and **one** `LampCommand` is emitted on the step the mode left | No error expected |
| Lane change | `lane_change_pressed` rotates the lit flag from `top_2` to `top_3` | Exactly two commands on that step: `l_top_2` → `off/0`, `l_top_3` → `lit/2` | No error expected |
| Dropped letters | `players[p].letters === 'DRA'` | `l_dragon_d`, `l_dragon_r`, `l_dragon_a` = `{ role: 'dragon', step: 1 }`; `l_dragon_g/o/n` = `off` | A letter not in `TABLE.dropBankWiring` is ignored, never thrown |
| Lock occupied | `machine.deviceSlots.bd_lock` has any `true` slot | `l_lock` = `{ role: 'dragon', step: 1 }` | No error expected |
| Active player is not index 0 | base mode's `player === 1`, `currentPlayer === 0`, only player 1 has a lit lane | The projection follows the **base mode's `player`**, so player 1's lit lane is shown | If `modes[]` is empty, all lamps are `off` — never a read of a missing player |
| No state change across a step | Identical projection two steps running | Zero commands emitted | No error expected |
| Two changes to one lamp in one frame | Lamp goes `lit/1` then `lit/2` within one 16-tick frame | `FrameOutput.commands` carries **both**, in tick order; the driver applies them in order so the **last wins** | No error expected |
| Grammar lookup, `role: 'off'` | `(off, 0)` | intensity 0, no blink, light disabled, emissive black | No error expected |
| Grammar lookup, blinking step | `(dragon, 3)` at `nowMs` in the second half of the blink period | Emissive black and light disabled for that instant, colour and intensity restored in the first half | No error expected |
| Driver over budget | 14 lamps lit with an injected budget of 2 | At most 2 dynamic lights enabled; every remaining lit insert still shows its emissive colour | No throw — degrade to emissive-only, never exceed the budget |
| Scene missing an insert node | A glb without `l_top_1` | `syncLamps` throws, naming the node, on **every** call (not only the first) | Named `Error` at the call site, per `backglass.ts:59-74` |
| Insert node has no material | glTF node resolved to a `TransformNode` (multi-primitive mesh) | `syncLamps` throws naming the node | Named `Error` — never a silent no-op |

</intent-contract>

## Code Map

**The seam that already exists (do not re-invent it).**
- `src/sim/contracts/commands.ts:25-42` — **`LampRole`, `LampStep`, `LampCommand` are already authored** and already closed to AD-9's exact role set. `LampRole` currently has **no runtime representation**, which is why AC 2's closure test is not satisfiable today. `LampState` does not exist and is added here.
- `src/sim/contracts/index.ts:8` — `export * from './commands'`, a value-and-type star export, so a new `LAMP_ROLES` const is forwarded for free (the `CONTACT_SURFACES` precedent).
- `src/sim/contracts/snapshot.ts:109-121` — `FrameOutput.commands` is already `readonly PresentationCommand<TLamp, ...>[]`. **No contract widening is needed for the loop to push lamp commands.**
- `src/sim/table/names.ts:34` — `export type LampName = keyof typeof TABLE.lamps;` — the union widens automatically with every new key. `:58` binds `LampCommand`, `:64` binds `FrameOutput`.
- `src/sim/loop/index.ts:377` builds the `commands` accumulator, `:419` does `commands.push(...rulesResult.commands)`, `:431` returns it. The wire to presentation is live and has never carried a value. `:266-275` holds the closure-local `let`s, and `currentFrame`/`previousFrame` + `buttonSwitchEdges(previous, current, tick)` at `:151-159` is **the existing previous-vs-current diff idiom to mirror**.
- `src/sim/rules/index.ts:92-115` — `RulesStepResult.commands` is `readonly never[]`; `:229` returns `commands: []`. **Leave both alone.** AD-9 and this story's AC 1 both place the diff in `sim/loop`, so rules exports the projection and the loop diffs it. `:75-82` documents the "loop reaches rules only through the `../rules` barrel" convention and `:82` is the `bootDeviceSlots` re-export to copy.

**State the projection reads.**
- `src/sim/contracts/state.ts:82-85` `PlayerLaneState` (`lit: Record<string, boolean>`, `completedSets`), `:95` `letters: string`, `:96` `lockCredits` (**never written by anything** — do not read it), `:99` `lanes`, `:72` `machine.deviceSlots` (`bd_lock` is `readonly boolean[]` of length 3), `:127-132` `ActiveModeState` (`mode`, `priority`, `player`, plus an open index signature).
- `src/sim/rules/modes/base.ts:39` `BASE_MODE_PRIORITY = 100`; `:41-59` `LANE_SETS`/`ALL_LANES` derived from `TABLE.laneWiring`; `:113-133` `applyLaneEntered`; `:147-164` `step` writes to **`active.player`, not `currentPlayer`**.
- `src/sim/rules/modes/skill-shot.ts:41` `SKILL_SHOT_MODE_PRIORITY = 200`; `:79-90` `start()` writes `lanes.lit[drawn] = true` **once** and never caches the draw (DW-202); the stack entry is `{ mode: 'skill_shot', priority: 200, player, launched }`; `:137` removes itself from `modes[]` on resolution.
- `src/sim/rules/ball-controller.ts:266-282` — `bank_target_down` accumulates into `players[currentPlayer].letters` while `phase === 'game'`. This is the **only** writer a letter projection can read; the bank's own `down` map is closure-private in `drop-bank.ts:49-53` and correctly unreadable.
- `src/sim/rules/modes/index.ts:94-106` — modes start **one tick after** `ball_starting` (deferred start). Any test touching `ball_starting` must read `statesByTick.get(startTick + 1)`.

**Registry and geometry.**
- `src/sim/table/dragonwar.ts:117-127` the `TABLE` doc comment (names "the one Epic 1 lamp"); `:474-482` `laneWiring` (`top_1..3` set `top`; `outlane_l`/`inlane_l`/`inlane_r`/`outlane_r` set `inout`, with `order` left→right); `:414-421` `dropBankWiring` (`d,r,a,g,o,n` → `{ switch, node }`); `:565-576` `lamps` / `flashers` / `shows`; `:581-585` `lightGroups` (`lg_playfield`, `lg_inserts`, `lg_cabinet`); `:177` `s_lock_lane`.
- `tools/make-placeholder-blend.py:3256-3281` — `l_insert_left`'s lens+cup authoring block: lens `(249,492,-1.0)..(265,508,0.5)`, cup `(247,490,-7.0)..(267,510,-1.0)`, joined via an explicit `BMVert`-identity vertex map (**stale `.index` is a measured trap — copy the map, not an index join**), two UV layers, `mat_insert` appended, parented to `playfield_root`, `set_props(lightgroup='lg_inserts')`. `:1481-1500` `new_box_mesh` (the helper to sit the new one beside), `:1503` `set_props`, `:1553` `mat_insert`, `:3312-3320` `presentation_objects`.
- Switch-zone authoring, all in one function and all with the local variables to reuse: `:3153` Top lanes (`TOP_LANE_DIVIDER_XS_MM = (95,195,295,376)`, `TOP_LANE_DIVIDER_T_MM = 8`, y `TOP_LANE_Y0_MM+5 = 955` .. `TOP_LANE_Y1_MM-5 = 999.8`); `:3158-3161` inlanes/outlanes (`left_divider_x0/x1 = 34.9/46.9` at `:2013`, `right_divider_x0/x1 = 421.5/433.5` at `:2029-2030`, `LANE_X0_MM = 468.4`, y 150..200); `:3141-3147` DRAGON bank (`cx = DRAGON_BANK_X0_MM + i*DRAGON_BANK_PITCH_MM + DRAGON_BANK_TARGET_W_MM/2`, i.e. 222.4/233.4/244.4/255.4/266.4/277.4 at **pitch 11.0**, zone y 655.005..685.005); `:3113` `sw_lock_lane` (x `lock_lane_x0+2 = 152` .. `lock_lane_x1-2 = 188`, y 480..540).
- `tools/export.py:36` `MIN_BLENDER_VERSION = (5,2,0)` (5.2.1 passes); `:95-100` `is_presentation_object` — everything **except** `col_`/`sw_` reaches the glb; `:120-123`/`:153-154` one material slot; `:126-130` two UV layers; `:151-152`/`:192-193` `lightgroup` present and known; `:194-195` `l_` name must be a `TABLE.lamps` key; `:446-512` the three collision builders (`col_`, `sw_`, `ballDevices` **only**).
- `tools/export-assets.mjs:50-62` builds the `TABLE` dump `export.py` validates against (it includes `lamps`); `:78-89` the argv. `tools/blender.mjs:166-196` — `BLENDER` short-circuits every other lookup and is honoured verbatim.

**Presentation, and how a test reaches the rendered artefact.**
- `src/presentation/scene/playfield.ts:26-51` **`getRequiredNode(scene, name)`** — the one sanctioned node lookup; counts matches rather than taking the first, throws on 0 or >1. Exported in Story 2.6 precisely so a driver reuses it. `:131-142` `applyPitch` rotates `playfieldRoot` about `pivotPitch.position`.
- `src/presentation/backglass/backglass.ts:48-51` per-scene `WeakMap` cache; `:59-74` **resolve-and-validate before caching** (a failed first call must not leave an orphan); `:82-91` the recorded `emissiveColor` gotcha; `:96-104` the idempotent `sync*` shape. `raster.ts:3-5` is the "pure, Babylon-free tier" precedent for `grammar.ts`.
- `src/presentation/scene/balls.ts:32-35` the `WeakMap<Scene, …>` idiom; `:64` `syncBalls(scene, playfieldRoot, snapshot)` — meshes parented to `playfieldRoot` and positioned with `toScene()`. `:9-20` records the **"geometrically present but not actually SEEN"** class this story is squarely inside.
- `src/presentation/scene/create-engine.ts:261-283` scene construction (one `HemisphericLight` named `light`, the **only** light in the scene today); `:304-317` `onFrame(scene, playfieldNodes)` runs before `scene.render()`; `:334-341` **`loadAndRenderOnceForTests(engine, glbUrl, importOptions?, onFrame?)`** — the entry point every rendered-artefact test uses.
- `src/host/boot.ts:246-256` the sim-side fold (`advanceBackglass` → `renderFrame` → `rasterise`); `:268-283` the render-side blit (`syncBalls`, `applyPitch`, `syncBackglass`). The lamp chain wires into exactly these two places.
- `test/backglass-scene.test.ts:59-87` — **the strongest existing rendered-artefact test**: `new NullEngine()`, `loadAndRenderOnceForTests`, drive the driver, then read the real `PBRMaterial` back off the mesh. Copy this shape.
- `test/asset-contract.test.ts:45-53` `readGlbJson()` (parses the glb JSON chunk), `:56-70` `readCollisionDoc()`, `:110-133` the per-mesh TEXCOORD/lightgroup sweeps, `:183-184` the reference-rectangle measurement idiom. **There is no glb bounding-box helper yet** — this story adds one from POSITION accessor `min`/`max`. The node list is **not** pinned anywhere (every glb assertion is universally quantified or a `Set` membership), so new `l_` nodes pass untouched.

**Gates that will move, and gates that will not.**
- `test/table.test.ts:404-408` — `expect(Object.keys(TABLE.lamps)).toEqual(['l_insert_left'])`. Reddens by construction; update it and its `describe` title. `:410-414` pins `lightGroups` to exactly three — safe, this story reuses `lg_inserts`.
- `test/replay-goldens.test.ts:186-190` each `notes` must still contain `DW-70` **and** `deviceSlots`; `:267-311` `PARITY_INERT` is exactly `nudge-coupling` and `two-ball-collision`, checked in **both** directions.
- `src/sim/loop/replay.ts:145-147` `tableHash()` (whole frozen `TABLE`), `:150-152` `assetHash(doc)` (the **parsed collision document only** — the glb is never hashed), `:239-240` `StaleReplayHeaderError`, `:248-283` the five header checks including whole-tree canonical `resolveTuning()`.
- `test/ad7-device-slots.test.ts:432-465` — a **source-text ratchet** over every `.ts` under `src/sim/loop/`: exactly two assignments to the `state` binding, matched by `/(?<![.\w$])state\s*(?::[^=]*)?=(?!=)/` on comment-stripped lines. A binding named `previousLamps` is safe; anything spelled `state`-lowercase is not.
- `test/rules-devices-headless.test.ts:193-238` — `ENTRY_FILES` is now **self-checking**: every `test/rules-*.test.ts` that is not `*-integration.test.ts` must be listed, or the completeness ratchet reddens.
- `test/module-coverage.test.ts:189-196` — every `src/**` module must be import-reachable from a test entry point. Its own `:22-28` states plainly that reachability is **not execution** — satisfying it is not satisfying Rule 19.
- `test/tuning.test.ts:26-66` `scalarKeys` is a hand-maintained allowlist Story 2.7 flagged as already stale — check whether `liveLightBudget` must be added, and do not widen the gap.
- **Unaffected:** `test/export-py-hull.test.ts`, `test/export-py-version-gate.test.ts`, `test/asset-contract.test.ts`'s existing assertions, and `test/export-py-skip-visibility.test.ts` (its `23` counts stay correct unless an `it()` is added to `export-py.test.ts` — this story adds none).

**Boundary lint (the home for AC 2's gate).**
- `tools/boundary-lint.mjs:817-832` `runBoundaryLint` — a rule is one pattern const, one `check…()` returning `{ rule, file, line, message }`, one spread line. `:189-346` `tokenize` (fails closed on an unterminated span); `:348-362` `maskForCodeOnly` (blanks comments **and** strings — identifier scanning); `:366-387` `extractStringLiterals` (literal inner text — string scanning; **neither ever sees a comment**); `:393-422` the per-line `boundary-lint-disable-next-line <rule>` suppression, opt-in per rule; `:424-451` banned globals (word-boundary idiom); `:453-505` the tick/ms rule (the `new RegExp(X.source, 'g')` per-file idiom — a shared `/g` regex carries `lastIndex` across files); `:507-536` device-name literals (**fully anchored** `^…$`, so a prose literal never fires); `:545-609` non-ASCII (`/u`, multi-line template line arithmetic). Exit **1** for a textual violation, **2** for an import-graph one.
- `test/boundary-lint.test.ts:14-40` the `run([root])` harness; `:290-349` Story 2.4's block — **the four-part non-vacuity contract to copy**: positive fixture flagged, negative fixture not flagged, the exact violating-file **set** asserted, and the real tree still green. `:92-96` is the comment-vs-literal control idiom.
- **Measured false-positive surface, whole of `src/sim/**` (83 files):** with comments excluded, **zero** hits at any scope. With comments included, 11 hits — four inside DW-79-frozen ported bodies (`Green's transform`, `color` in a port-deviation note) that **cannot legally be edited**, two in `commands.ts:26-27`'s own JSDoc stating this very invariant, five "green" meaning CI status. Substring matching is catastrophic (`red` appears 603 times, including 41 GPL `redistribute` lines). Bare `0x` numerics must **not** be banned: all six in `src/sim/**` are 8-hex-digit non-colour constants (`rng.ts:30,35` mulberry32; `hit-kd-node.ts:53,97` a kd mask; `replay.ts:106,109` FNV-1a), and two of them live outside `physics/`, so narrowing scope does not rescue a `0x` ban.

**Provenance.**
- `ATTRIBUTIONS.md:71` (`.blend`) and `:72` (`.glb`) are **append-narrative** rows: every story that touched the blend appended a paragraph naming the nodes, the figures, and whether `tableHash`/`assetHash` moved. `pnpm check:attributions` only checks `package.json` dependency keys, so this is review-enforced discipline, not a script. **`:72` records a stale `tableHash a2f90b52`; the live value is `b6c5ce92`. Do not copy the stale figure forward.**

## Tasks & Acceptance

**Execution:**

1. `src/sim/contracts/commands.ts` — add `export const LAMP_ROLES = ['off','lit','hurryup','quickmb','joust','dragon','special'] as const;` and re-derive `export type LampRole = (typeof LAMP_ROLES)[number];` (the `CONTACT_SURFACES` idiom at `events.ts:30-46`). Add `export interface LampProjectionEntry { readonly role: LampRole; readonly step: LampStep }` and `export type LampState<TLamp extends string = string> = Readonly<Record<TLamp, LampProjectionEntry>>`. *Rationale: AC 2's closure assertion is unsatisfiable while the union has no runtime representation — today's `test/contracts.test.ts:111-115` survives adding an eighth role or deleting six.*
2. `src/sim/table/names.ts` — bind `export type LampState = ContractsLampState<LampName>;` beside the `LampCommand` binding at `:58`. *Rationale: contracts are generic over the name unions and never import `TABLE`.*
3. `src/sim/table/dragonwar.ts` — **remove `l_insert_left`** and author fourteen lamps in its place, each `{ channel: 'insert', group: 'lg_inserts', subject }` where `subject` is `{ kind: 'lane', lane: <laneWiring key> }` (`l_top_1`, `l_top_2`, `l_top_3`, `l_inlane_l`, `l_inlane_r`, `l_outlane_l`, `l_outlane_r`), `{ kind: 'letter', letter: <dropBankWiring key> }` (`l_dragon_d/r/a/g/o/n`), or `{ kind: 'lock' }` (`l_lock`). Constrain the lane and letter keys with `satisfies` against `keyof typeof TABLE.laneWiring` / `keyof typeof TABLE.dropBankWiring` so a typo is a compile error. Update the `:117-127` doc comment. *Rationale: AD-11 says `TABLE` owns lamps with channel and group; `subject` is the wiring that lets both `lampsOf` and the placement test derive from one declaration (DW-149). `l_insert_left` was Story 1.4's placeholder existing only to keep `LampName` inhabited — fourteen real lamps do that job, and a lamp nothing drives is exactly the vacuity this epic hunts.*
4. `src/sim/table/tuning.ts` — add one top-level scalar `liveLightBudget: entry(20, '<AD-12: "the live dynamic-light budget on the floor is 20 per frame [ASSUMPTION]", verified 2026-08-26 at ~23 Babylon clustered-forward lights per WebGL2 batch, re-verify 2026-09-26. No artifact states a measured figure>', 'unverified')`. *Rationale: AC 4 says the budget comes from `tuning.ts`; a key **not** ending in `Ms` avoids `assertNoNestedMsKeys` (`:701-719`) and the derived-`Ticks` machinery entirely.*
5. `src/sim/rules/lamps.ts` — **new.** `export function lampsOf(state: GameState): LampState`. Derive the lamp set from `Object.entries(TABLE.lamps)`. Read the **base mode's `player`** from `state.modes`; if no base mode is on the stack, return every lamp `off/0`. Lane lamps: `lit/1` when `players[p].lanes.lit[lane]`, promoted to `lit/2` while a `skill_shot` entry is in `state.modes` and the lane's set is `top`. Letter lamps: `dragon/1` when the letter is present in `players[p].letters` (case-insensitive). `l_lock`: `dragon/1` when any `machine.deviceSlots.bd_lock` slot is `true`. Everything else `off/0`. *Rationale: AD-9 — a pure projection over `GameState`, recomputed every step; Story 2.7's Design Notes assign the step-2 decision to the presence of `skill_shot` in `modes[]`.*
6. `src/sim/rules/index.ts` — re-export `lampsOf` and `LampState` through the barrel, beside the `bootDeviceSlots` re-export at `:82`. **Do not change `RulesStepResult`.** *Rationale: AD-9 and AC 1 both put the diff in `sim/loop`; the loop reaches rules only through this barrel.*
7. `src/sim/loop/index.ts` — add a closure-local `let previousLamps: LampState = lampsOf(state)` beside the other `let`s at `:266-275` (seeded from the boot state, so the first step diffs to nothing), and after `state = rulesResult.state` at `:415` compute `lampsOf(state)`, push one `LampCommand { type:'lamp', lamp, role, step, tick }` per lamp whose `role` **or** `step` differs, then assign `previousLamps`. **Name the binding `previousLamps` — never anything matching `state`-lowercase**, or `test/ad7-device-slots.test.ts:432-465`'s source ratchet reddens. *Rationale: `FrameOutput.commands` already accepts `LampCommand`; the `previousFrame`/`currentFrame` pair at `:151-159` is the existing diff idiom.*
8. `src/presentation/lighting/grammar.ts` — **new, pure, Babylon-free** (the `raster.ts` tier). Export `LAMP_GRAMMAR` mapping each non-`off` role to an authored RGB triple in 0..1 — `lit` white, `hurryup` red, `quickmb` green, `joust` blue, `dragon` orange, `special` purple (PRD FR-44's held mapping) — and `lookupGrammar(role, step): { r, g, b, intensity, blinkPeriodMs: number | null }` where step 1 = intensity 1.0 / no blink, step 2 = intensity 1.4 / 500 ms, step 3 = intensity 1.8 / 160 ms, and `off` (any step) = all zero, no blink. Export `isLampOnAt(blinkPeriodMs, nowMs)` = `blinkPeriodMs === null || (nowMs % blinkPeriodMs) < blinkPeriodMs / 2`. *Rationale: AC 3 — the one `(role, step)` → RGB/intensity/cadence table, blinking timed by presentation. Keeping it Babylon-free is what made Story 2.6's `raster.ts` unit-testable.*
9. `src/presentation/lighting/lamp-view.ts` — **new, pure, Babylon-free.** `LampView` (a `Partial<Record<LampName, LampProjectionEntry>>`), `INITIAL_LAMP_VIEW`, and `advanceLamps(view, output: FrameOutput): LampView` folding every `type === 'lamp'` command in tick order so the **last command per lamp wins**. *Rationale: mirrors `advanceBackglass`; makes AC 4's latest-wins rule unit-testable without a scene.*
10. `src/presentation/lighting/lamp-driver.ts` — **new, the one Babylon touchpoint** (this module *is* AD-12's `LampDriver`, following the `backglass.ts` / `balls.ts` free-function-plus-`WeakMap` convention this repo uses everywhere). `syncLamps(scene, playfieldRoot, view, nowMs, options?: { budget?: number })`. Per lamp name from `Object.keys(TABLE.lamps)`: resolve the mesh with `getRequiredNode`, **throw naming the node if it carries no `material`**, **`material.clone()` per insert on first use** and assign the clone to that mesh, then set `emissiveColor` from the grammar (black when off or in a blink's off half). Create one `PointLight` per insert, parented to `playfieldRoot`, positioned with `toScene()` at the lens centre offset to **table z = −4 mm (beneath the lens, into the playfield)**, with `includedOnlyMeshes = [that insert mesh]`, `diffuse` from the grammar and `intensity` scaled by it. Enable at most `options.budget ?? TUNING.liveLightBudget.value` lights, counting **enabled** lights only; lit inserts beyond the budget keep their emissive and get no light. Validate before caching (`backglass.ts:59-74`), idempotent on every frame. *Rationale: AD-12's "emissive material plus dynamic lights behind the same driver"; the clone, the `includedOnlyMeshes` and the enabled-only count each close a measured trap — see Design Notes.*
11. `src/presentation/lighting/.gitkeep` — delete. *Rationale: its text names Story 4.1 and is now stale; the directory has real files.*
12. `src/host/boot.ts` — fold `advanceLamps(lampView, output)` beside `advanceBackglass` at `:246-256`, and call `syncLamps(scene, nodes.playfieldRoot, lampView, performance.now())` beside `syncBackglass` at `:268-283`. *Rationale: without this the driver is dead code — Story 2.7's DW-201 is exactly the shape of a fix that exists and never runs.*
13. `tools/boundary-lint.mjs` — add textual rule `sim-no-colour` over `src/sim/**`, registered in `runBoundaryLint` at `:817-832`. Three matchers, all comment-blind by construction: (a) over `maskForCodeOnly`, `\b(colou?r|rgba?|hsla?|tint|hue)\w*\b` and `\b(red|green|blue|white|black|yellow|orange|purple|amber|cyan|magenta|violet|pink|gold|grey|gray)\b`, case-insensitive; (b) over `extractStringLiterals`, a **fully anchored** `^…$` colour name (the device-name idiom, so prose provenance strings never fire); (c) over `extractStringLiterals`, unanchored `#[0-9a-fA-F]{3}\b`, `#[0-9a-fA-F]{6}\b`, `#[0-9a-fA-F]{8}\b` and `\b(rgba?|hsla?)\s*\(`. **No `0x` numeric matcher and no substring matching.** Wire per-line suppression (`:393-422`) for this rule. Fresh `new RegExp(src, flags)` per file. Message names AD-9. *Rationale: the tool already owns this exact class of gate; a parallel scan would duplicate a fail-closed tokenizer that is the only reason the comment/string distinction is right.*
14. `test/fixtures/boundary/colour/src/sim/**` — **new fixture tree**: positive files (an identifier `const RED`, a `colour` identifier, an anchored `'white'` literal, a `'#ff8800'` literal, an `rgba(` literal) and negative files that must **not** fire (a comment naming red/RGB/colour, a prose string literal containing the word "red", a `0x811c9dc5` constant, an identifier containing `redistribute`). *Rationale: `test/boundary-lint.test.ts:290-349`'s four-part non-vacuity contract needs both halves, and the negatives are the measured false-positive surface.*
15. `test/boundary-lint.test.ts` — add a `describe` for `sim-no-colour` copying the Story 2.4 block: exit 1, rule and file named, negatives explicitly `not.toContain`, the exact violating-file **set** asserted, and the real tree green (`run([])` names no `sim-no-colour`). *Rationale: without the set assertion, the negative assertions are satisfied by a rule that fires on nothing.*
16. `tools/make-placeholder-blend.py` — factor `:3258-3281` into `new_insert_mesh(name, lens_min_mm, lens_max_mm, cup_min_mm, cup_max_mm, material, parent)` beside `new_box_mesh` at `:1481` (keeping the `BMVert`-identity vertex map), **remove `l_insert_left`**, and author the fourteen inserts in one loop from the **same local variables the matching `add_switch_zone(...)` call uses at `:3113`, `:3141-3147`, `:3153`, `:3158-3161`** — never a re-typed literal. Each lens centre is its subject zone's x/y centre. Lane and Lock inserts: lens 16 × 16 mm, cup 20 × 20 mm. Letter inserts: lens 9 × 9 mm, cup **10 × 10 mm** — the bank pitch is 11.0 mm, so a wider cup would overlap its neighbour. All: lens z −1.0 .. +0.5, cup z −7.0 .. −1.0, two UV layers, `mat_insert`, `parent=playfield_root`, `set_props(lightgroup='lg_inserts')`. Update `presentation_objects` at `:3314-3317`. *Rationale: AD-11 — Blender owns placement, and deriving from the zone's own variables makes "the insert is under its lane" true by construction rather than by transcription.*
17. Regenerate and re-export, committing both artifacts together: `blender --background --factory-startup --python tools/make-placeholder-blend.py` then `pnpm export:assets`. Expect `assets/src/dragonwar.blend` and `public/assets/dragonwar.glb` to change and `public/assets/dragonwar.collision.json` to be **byte-identical**. *Rationale: `test/export-py.test.ts:111-135` byte-compares a fresh export against both committed artifacts.*
18. `test/table.test.ts` — replace the `:404-408` single-lamp pin with the fourteen names and their `subject` linkage, asserting every lane subject is a `laneWiring` key and every letter subject a `dropBankWiring` key, in both directions. Update the `describe` title. *Rationale: the old pin reddens by construction; the replacement must pin the new contract, not merely be widened.*
19. `test/contracts.test.ts` — add the `LAMP_ROLES` runtime closure test in the `CONTACT_SURFACES` shape (`:65-88`): membership and order asserted against the const, plus a `@ts-expect-error` that an eighth role value is rejected. Add a `LampState` construction case. *Rationale: AC 2.*
20. `test/asset-contract.test.ts` — add a glb bounding-box helper reading each mesh node's POSITION accessor `min`/`max` and converting glb metres → table millimetres by AD-10's permutation (table x = glbX·1000, table y = −glbZ·1000, table z = glbY·1000). **Calibrate it first**: assert `vis_playfield`'s derived table-frame footprint is `(0,0)`..`(TABLE.reference.playfieldMm.w, .h)` within 0.5 mm — it is authored as exactly that box at `make-placeholder-blend.py:3250-3252`, so a wrong conversion reddens here before any insert is measured. Then assert, for every `TABLE.lamps` key: the glb has a mesh node of that name; its lens (the z ≥ −1.0 part of its bbox) centre lies **inside** its subject's `sw_` zone footprint from `dragonwar.collision.json` (resolved by switch name through `laneWiring` / `dropBankWiring` / `s_lock_lane`); and no two inserts' cup footprints overlap. Also assert no `l_` node appears in the collision document (the `vis_` pin at `:1408-1416` has no `l_` equivalent). *Rationale: AC 7 — this is the only assertion that proves the inserts are where the game says they are, and the calibration step is what stops it being vacuous.*
21. `test/rules-lamps.test.ts` — **new, headless.** Drive `lampsOf` directly and through `runRulesScript`: the attract all-off case, the skill-shot step-2 case, the step-2 → step-1 drop when `skill_shot` leaves `modes[]`, lane change moving the lit flag, letters `'DRA'`, an occupied `bd_lock` slot, and **the whole matrix repeated with the base mode's `player === 1` while `currentPlayer === 0`** so the projection is discriminated from both a literal `0` and from `currentPlayer`. *Rationale: AC 1, AC 6. Story 2.7's third vacuity was AD-7 player scoping never being exercised at index ≠ 0.*
22. `test/rules-devices-headless.test.ts` — add `rules-lamps.test.ts` to `ENTRY_FILES` at `:193-200`. *Rationale: the completeness ratchet at `:225-238` reddens otherwise.*
23. `test/lighting-grammar.test.ts` — **new.** Pin every `(role, step)` row of `LAMP_GRAMMAR` and `lookupGrammar`, that intensity and blink rate are both monotonic across steps 1→2→3, that `off` is all-zero at every step, that `isLampOnAt` is true in the first half of a period and false in the second, and that `advanceLamps` applies the **last** command per lamp when a frame carries two for the same lamp. *Rationale: AC 3 and the pure half of AC 4.*
24. `test/lighting-scene.test.ts` — **new.** `new NullEngine()` + `loadAndRenderOnceForTests` against the real committed glb (the `test/backglass-scene.test.ts:59-87` shape). Assert, after driving `syncLamps`: the lit insert's `material.emissiveColor` equals the grammar's RGB; **a second insert's `emissiveColor` is still black** (the shared-material control); the insert's `PointLight` is enabled, in that mesh's `lightSources`, and **not** in `vis_playfield`'s; for every insert mesh, `lightSources.filter(isEnabled).length <= (material as PBRMaterial).maxSimultaneousLights`; the light's world position — read via `light.computeWorldMatrix(true).getTranslation()`, **never `getAbsolutePosition()`** — sits below the insert mesh's `boundingBox.centerWorld` on the pitched playfield; advancing `nowMs` by half a blink period flips a step-2 lamp's emissive to black; and with `{ budget: 2 }` over fourteen lit lamps at most two lights are enabled while every lit insert still shows emissive. *Rationale: AC 4, AC 5. Each assertion targets a measured trap — see Design Notes.*
25. `test/lighting-integration.test.ts` — **new.** Real `createLoop({ collisionDoc, gameStart })`, press `s_start` once, plunge, disable `DISABLED_HAZARD_COILS` as `test/rules-modes-integration.test.ts:35` does, fold every frame's `FrameOutput.commands` through `advanceLamps`, and drive `syncLamps` against a real `NullEngine` scene — then assert the drawn Top lane's insert is genuinely lit on the rendered artefact (non-black `emissiveColor`, enabled light in its own `lightSources`), and that the command stream was non-empty. *Rationale: Rule 1's Integration AC and AC I1 — the whole chain, sim → loop diff → view fold → Babylon, with the assertion on the rendered artefact rather than on the projection that precedes it.*
26. `test/replays/*.golden.json` (all five) — **header-only refresh** of `header.tableHash` and `header.gameStart.tuning` only. Leave `header.assetHash`, `header.tickHz`, `header.physicsVersion`, `transitions`, `coilPrologue`, `durationTicks`, `expectedHash`, `expectedGameStateHash`, `checkpointTicks` and `expectedCheckpointHashes` **byte-identical**; **append** to each `notes`, never rewrite, keeping the `DW-70` and `deviceSlots` literals; serialise `JSON.stringify(doc, null, 2) + '\n'` and **restore `roll-and-drain`'s `checkpointTicks` to one line** (Story 2.7's recorded reformatting trap); keep LF. Re-verify `PARITY_INERT` in both directions. Follow the trace procedure in Design Notes **before** writing anything. *Rationale: `tableHash()` hashes the whole `TABLE` and `resolveTuning()`'s whole output is compared canonically — either alone throws `StaleReplayHeaderError` from all five.*
27. `ATTRIBUTIONS.md` — append a Story 2.8 paragraph to the `.blend` row (`:71`) and the `.glb` row (`:72`) naming the fourteen `l_` nodes, their lens/cup spans, the removal of `l_insert_left`, that `assetHash` is unmoved and `tableHash` moved **from `b6c5ce92`** (not the stale `a2f90b52` the `.glb` row currently records), and that every figure and every RGB triple is authored — none sourced from any commercial machine or third party. *Rationale: `CLAUDE.md`'s provenance rule; `pnpm check:attributions` cannot catch a missing asset-row update, so this is review-enforced.*
28. `test/tuning.test.ts` — add `liveLightBudget` to the `:26-66` `scalarKeys` allowlist if that gate requires it; do not widen the gap Story 2.7 flagged. *Rationale: a hand-maintained allowlist that silently misses a new tunable is the same shape as `ENTRY_FILES` before it was made self-checking.*

**Acceptance Criteria:**

- **AC 1 —** Given a `GameState`, when the rules step, then `lampsOf(state)` returns a `{ role, step }` for **every** `TABLE.lamps` key recomputed from state alone, `sim/loop` emits a `LampCommand` **only** for lamps whose `role` or `step` changed since the previous step, and no mode produces a lamp command anywhere.
- **AC 2 —** Given `LAMP_ROLES` and `LampStep`, when `pnpm lint:boundaries` and `pnpm test` run, then a runtime test asserts the role union is exactly the seven AD-9 members and rejects an eighth, and `sim-no-colour` reports no RGB, hex, `rgb(`/`hsl(` form or bare colour name in any file under `src/sim/` while leaving comments and prose provenance strings untouched.
- **AC 3 —** Given `presentation/lighting/grammar.ts`, when `(role, step)` is looked up, then it yields exactly white / red / green / blue / orange / purple for `lit / hurryup / quickmb / joust / dragon / special`, `off` yields zero intensity at every step, and steps 1 / 2 / 3 yield strictly increasing intensity with a blink cadence of none / 500 ms / 160 ms timed entirely in presentation.
- **AC 4 —** Given a lit insert, when `syncLamps` runs against a real loaded scene, then that insert's own `emissiveColor` equals the grammar's colour, **every other insert's stays black**, and when one frame carries two commands for the same lamp the later one is the state that lands.
- **AC 5 —** Given a lit insert, when `syncLamps` runs, then a dynamic light is enabled beneath the lens — at table z ≈ −4 mm inside the cup, parented to `playfield_root` so it tracks pitch, its **world** position (from `computeWorldMatrix(true)`) below the insert mesh's world bounding-box centre — restricted to that insert alone via `includedOnlyMeshes`, with each insert mesh's enabled `lightSources` never exceeding its material's `maxSimultaneousLights`, and the driver never enabling more than the `tuning.ts` live-light budget.
- **AC 6 —** Given a player at index 1 (with `currentPlayer` 0) holding a lit Top lane, a lit inlane/outlane and letters `'DRA'`, with a ball parked in `bd_lock`, when the projection runs, then the lit lanes read `lit` (white), the three dropped letters and the Lock read `dragon` (orange), the Top lane reads step **2** while `skill_shot` is on the stack and step **1** once it leaves, and every other lamp reads `off`.
- **AC 7 —** Given the re-exported `dragonwar.glb`, when it is measured against `dragonwar.collision.json`, then every `TABLE.lamps` key has a mesh node whose lens centre lies inside its subject's `sw_` switch zone, no two inserts' cups overlap, no `l_` node appears in the collision document, and the glb→table conversion is calibrated against `vis_playfield`'s reference rectangle in the same test.
- **AC I1 (Integration) —** Given the real `sim/loop` driven through a Start, a plunge and a lane entry, when its `FrameOutput.commands` are folded by `advanceLamps` and handed to `syncLamps` on a real Babylon scene loaded from the committed glb, then the drawn Top lane's insert is observably lit **on the rendered artefact** — non-black `emissiveColor` and an enabled light in that mesh's `lightSources` — and the command stream is non-empty.
- **AC 8 —** Given the five goldens, when `pnpm test` runs after the header-only refresh, then all five replay without `StaleReplayHeaderError` and `git diff test/replays/` shows **no** change to `expectedHash`, `expectedGameStateHash`, `expectedCheckpointHashes`, `header.assetHash`, `transitions`, `coilPrologue` or `durationTicks`.

## Spec Change Log

## Review Triage Log

## Design Notes

**[LEAD, 2026-09-06 spec gate] DW-47 was re-owned to this story and the plan did not know it existed.** `DW-47` records that `l_insert_left`'s lens protrudes **0.5 mm above** the playfield surface, against AD-11's "lens and cup geometry below the surface" (`tools/make-placeholder-blend.py` builds the lens box from z -1.0 to **+0.5** while `vis_playfield` spans z -1.0 to 0.0; the cup at z -7 to -1 is correct). It was `routed` to Story 4.2. This spec removes `l_insert_left` — which is defensible — but it authors **fourteen** real `l_` inserts in the same `.blend` with the same tooling, so the live risk is no longer one lip: it is **the same authoring mistake reproduced fourteen times, shipped, and inherited by 4.2 as fourteen entries instead of one**. Rule 17's test is whose gate genuinely fails while the item stands, and that is now this story's.

**Binding on this story:** every authored insert's **lens and cup geometry must sit at or below z = 0** in the table frame (AD-10 — name the frame; `toPhysics()` negates y, so do not reason about this in the physics frame), and the story must carry an assertion that **pins it against the exported artefact**, not against the authoring script's intent. **Rule 19:** the named mutation is to raise one insert's lens to z +0.5 — exactly DW-47's measured defect — and confirm that assertion goes red. An assertion that reads `make-placeholder-blend.py`'s source instead of the exported geometry would pass under that mutation and is the vacuity shape this epic has caught 36 times.

**Removing `l_insert_left` — consumers the plan's Block If does not enumerate.** Verified by the lead at this tree: `tools/make-placeholder-blend.py` (creates it), `test/table.test.ts:405-406` (pins `TABLE.lamps` to exactly `['l_insert_left']`), `ATTRIBUTIONS.md:72`, `epics.md:979` (Story 1.4's own delivered AC — **Epic 1's block, which this runner may not edit**; it is a historical record of what 1.4 shipped, not a live constraint, so leave it) and `DW-47` above. Updating the first three is ordinary work; the fourth is deliberately left alone.

**[LEAD] `tableHash` measured, not copied.** The live value is **`b6c5ce92`** — I read it from `test/replays/full-plunge.golden.json` while the goldens pass, so it matches the live `TABLE`. `ATTRIBUTIONS.md:72` contains the string `tableHash` (`a2f90b52`) inside a **dated historical sentence** about an earlier story's re-export; it was true when written and is **not** to be corrected, but it must **never be copied forward as the current value**. My own epic-context pre-warm made exactly that mistake and reported `a2f90b52` as current; the planner measured and was right.


**Governing architecture decisions (Rule 6).** **AD-9** (closed command union; lamp state is a projection; the role set; `grammar.ts` as the one colour table) is the load-bearing one. Also binding: **AD-7** (player-scoped `lanes`/`letters`, machine-scoped `deviceSlots`), **AD-8** as amended 2026-09-06 (the minimal Epic 2 stack is conforming; modes contribute lamp roles by priority), **AD-10** (the table frame and `toScene()` as the only presentation conversion), **AD-11** (Blender owns placement; `TABLE` owns the registry; `l_` is lens **and** cup), **AD-12** (four channels behind one `LampDriver`; inserts as emissive plus dynamic lights in early phases; the 20-light budget), **AD-15** (tunables carry `source`/`confidence` and are hashed into every golden), **AD-16** (boundary lint owns the textual gates; `l_` is a linted device prefix), **AD-19** (lane *state* is the base mode's; the devices layer only reports entries). **No AC contradicts an AD.** Two readings were checked and both resolve cleanly: AD-9 and AC 1 agree that the **diff lives in `sim/loop`**, not in rules, which is why `RulesStepResult.commands` stays `readonly never[]`; and AD-12 names `LampDriver` as a concept, so `lamp-driver.ts`'s free-function-plus-`WeakMap` shape (the repo's universal presentation convention — there is no class anywhere under `src/presentation/`) is conforming, not a deviation.

**Consumes:** Story 2.5 (`machine.deviceSlots`, `phase`, the ball lifecycle) · Story 2.6 (`getRequiredNode`, the `sync*` driver shape, `loadAndRenderOnceForTests`, the `emissiveColor` gotcha) · Story 2.7 (`players[i].lanes` owned by the base mode, `players[i].letters`, the `skill_shot` stack entry, `runRulesScript`). Every Integration AC exercises these against the real modules, never a mock.

**Consumed-by:** **2.9** ball save — adds `l_ball_save` and drives role `lit` step 1 → step 3; it owes its own `tableHash` header-only re-record for that key, and should budget it at planning. **2.10** end-of-ball bonus — the completed lane sets the multiplier advances on. **2.11** tilt. **3.2** the Lock arbiter — replaces this story's `machine.deviceSlots.bd_lock` reading of `l_lock` with real lock credits. **3.5 / 3.6 / 3.7 / 3.10** — the first producers of `hurryup`, `joust`, `quickmb` and `special`, all of which this story's grammar already declares. **4.1** four channels behind one driver, and the `liveLightBudget` this story authors. **4.2 / 4.8** inserts as lights through cups, then the per-group bake, which AD-12 says replaces this driver's dynamic lights "without touching rules, `TABLE`, or mesh names".

**Ledger inbox: empty.** `LEDGER slice 2-8-inserts-in-the-held-colour-grammar` returns nothing, so there is no entry to address or decline.

**The four measured traps this story is walking into, and the assertion that closes each.** Every one was reproduced under `NullEngine` against the committed glb during planning, and every one is silent — none produces a failing test on its own.
1. **One shared material.** The glb carries four materials, one per mesh, but `make-placeholder-blend.py:3274` appends the single `mat_insert` datablock. Fourteen inserts reusing it means Babylon's glTF loader hands all fourteen meshes the **same `PBRMaterial` instance**, so lighting lamp A lights lamp B identically. A single-lamp test cannot see it. → `material.clone()` per insert, pinned by the "lamp A lit, lamp B still black" control (task 24).
2. **`getAbsolutePosition()` lies under `NullEngine`.** `ShadowLight.getAbsolutePosition()` returns `transformedPosition ?? position`, and `transformedPosition` is only populated by `computeTransformedInformation()`, which the engine calls from the shader-bind path — never under `NullEngine`. Measured: a light parented to a 6.5°-pitched `playfield_root` read `y = −0.004` from `getAbsolutePosition()` against a true world `y = 0.05263` — **57 mm wrong, and plausible-looking**. → assert via `computeWorldMatrix(true).getTranslation()` against the mesh's `boundingBox.centerWorld`.
3. **`maxSimultaneousLights = 4`.** `mat_insert` arrives from the loader with Babylon's PBR default of 4, and `BindLights` silently truncates `Math.min(mesh.lightSources.length, 4)`. Twenty authored lights would render four. Because `NullEngine` compiles no shaders, this is unobservable through the render path. → `includedOnlyMeshes = [own mesh]` (so each insert sees hemispheric + 1) plus the per-mesh `lightSources ≤ maxSimultaneousLights` assertion. This is also why the insert lights deliberately do **not** include `vis_playfield`: FR-43's spill onto adjacent playfield art is AD-12's per-group bake in Epic 4, and adding `vis_playfield` to every insert light would blow the four-light material limit at the fifth lamp.
4. **`scene.lights` counts disabled lights.** `setEnabled(false)` removes a light from `mesh.lightSources` but not from `scene.lights`. A budget assertion written against `scene.lights.length` counts lights that cost nothing and reports a pool size rather than a live count. → count `scene.lights.filter((l) => l.isEnabled())`, and exercise the cap with an injected `{ budget: 2 }` over fourteen lamps, because 14 < 20 makes the real-budget assertion vacuous on its own.

**Why the attract case is load-bearing.** `test/flipper-mover.test.ts:73` and `:127` assert `FrameOutput.commands` `.toEqual([])` while running real frames in attract. Seeding `previousLamps` from `lampsOf(bootState)` (all-off, because `modes[]` is empty) means the first step diffs to nothing and those tests stay green — and it makes them a **real second detector** for AC 1: an implementation that emits the full projection every step instead of the diff reddens them immediately.

**The golden budget, and why the state hashes cannot move.** `tableHash()` hashes the whole frozen `TABLE` in canonical JSON, so fourteen new `lamps` keys move it off `b6c5ce92`; `resolveTuning()`'s entire output — `source` prose and `confidence` included — is compared as whole-tree canonical JSON, so `liveLightBudget` moves `header.gameStart.tuning`. Both are header identity: nothing in the tick loop reads either. `assetHash` hashes the **parsed collision document only**, and `export.py`'s three collision builders filter on `col_`, `sw_` and `ballDevices` — an `l_` node matches none, which Story 2.6 measured twice for the structurally identical `vis_` case and got a byte-identical `collision.json`. And none of the five goldens presses `s_start`: all five stay in `phase: 'attract'` for their whole run, so no mode is ever pushed, `rng` never advances, `lanes` is never written, and `lampsOf` projects all-off from tick 1 to the last. Four prior stories recorded the same outcome, including Story 2.7 on `roll-and-drain`'s `expectedCheckpointHashes`.

**Golden re-record procedure — there is no recording tool, so build it throwaway.** No `pnpm` script, no `--record` flag; `src/host/dev/replay-recorder.ts` is browser-side and has never been the re-record path. Build the harness in the **scratchpad, never under `test/`** (a file matching `test/**/*.test.ts` silently joins `pnpm test`), over the shipped surface `runReplay`, `buildHeader`, `tableHash()`, `assetHash(doc)`, `PHYSICS_VERSION` from `src/sim/loop/replay`. Run it with a **scratchpad vitest config** whose `include` is the absolute harness path (the repo config's `include` is `['test/**/*.test.ts']`, so it will not otherwise be collected) and `testTimeout: 300_000`. In `onTick`, when `tick % 25 === 0 || tick === durationTicks`, write one **full-precision, never quantised** TSV row per ball (`name, tick, ballId, x, y, z`) — a drained ball emits no row, so a shifted drain shows as a **row-count** change, not merely a value change. Run it against the tree before and after the change into `trace-BEFORE.txt` / `trace-AFTER.txt` and `diff` them: **expect no output**. Story 2.6 measured **1,132 sampled rows** across the five (`full-plunge` 80, `nudge-coupling` 32, `roll-and-drain` 371, `hold-and-release` 384, `two-ball-collision` 265) in 2.3 s — a materially different row count means the instrument, not the change, is wrong. Only when all five are bit-identical is this a header-only refresh; if any trajectory genuinely moves, that is the **Block If** — stop and report, do not re-author.

**Insert sizing is constrained by the DRAGON bank, not by taste.** The bank pitch is 11.0 mm at target width 10.0 mm (`make-placeholder-blend.py:991-992`), so the six letter inserts take a 9 × 9 mm lens in a 10 × 10 mm cup — one millimetre of gap between neighbouring cups, exactly the margin the targets themselves keep. The seven lane inserts and the Lock take `l_insert_left`'s authored 16 × 16 mm lens in a 20 × 20 mm cup, which fits every subject zone (the narrowest is `sw_outlane_l` at 30.9 mm wide and `sw_lock_lane` at 36 mm).

**No third-party material enters this story.** Every RGB triple, intensity and blink period in `grammar.ts` is authored from PRD FR-44's own six colour words; the light falloff is Babylon's built-in `PointLight` default, with no formula taken from anywhere. The insert geometry is procedural `bmesh` boxes through `bpy.data.*` under `--factory-startup`, with no node group, no add-on and no texture — the same "Generated, not sourced" basis `ATTRIBUTIONS.md:71-73` already records for the `.blend`, `.glb` and `.collision.json`. Two named traps stay closed: `vbousquet/pinball-parts`' "Thin Film Interaction" node group is **CC BY-NC-SA** and is precisely what one reaches for to make an insert lens look right — it cannot be used; and `vbousquet/vpx_lightmapper` is cited in the research as the prior art for insert modelling but its licence is **not verified at source** (a digest claiming GPL-3.0 is not verification under `CLAUDE.md`) and it has no `ATTRIBUTIONS.md` row, so nothing may be taken from it.

**One honest verification gap, stated rather than papered over.** `src/host/boot.ts` is on `test/module-coverage.test.ts`'s allowlist as unreachable from any test entry point, so task 12's two wiring lines cannot be covered by an automated assertion — and Story 2.7 established that a source-text scan is not verification. AC I1 proves the whole chain **except** those two lines, by driving the real loop and the real driver directly. The boot wiring itself is closed by the lead's per-story browser smoke, and this note exists so review does not mistake the gap for an oversight or accept a fabricated assertion in its place.

## Verification

**Commands:**
- `pnpm typecheck` — expected: exit 0. Also the enforcement point for the `@ts-expect-error` role-rejection case (vitest's esbuild transform strips types and never sees the directive).
- `pnpm lint:boundaries` — expected: exit 0, `[boundary-lint] OK -- <n> .ts file(s) under src/ cruised, no violations`. Specifically proves `sim-no-colour` finds nothing in the real tree, no lamp-name literal outside `dragonwar.ts`, and no banned global in `sim/`.
- `pnpm check:headers` — expected: exit 0 (four new source files need GPL-3.0 headers).
- `pnpm check:attributions` — expected: exit 0.
- `pnpm test` — expected: **112 files (108 + the four new test files), more than 1701 tests, 0 failed, 0 skipped.** Baseline before this story is 108 files / 1701 tests / 0 skipped, with `BLENDER` exported. **There are no known-failing tests; every red is a real red, and this plan predicts none.**
- `pnpm check:ad7 && pnpm check:corridor && pnpm check:reachability` — expected: all exit 0.
- `blender --background --factory-startup --python tools/make-placeholder-blend.py` then `pnpm export:assets` — expected: exit 0; `assets/src/dragonwar.blend` and `public/assets/dragonwar.glb` change; `git diff --stat public/assets/dragonwar.collision.json` is **empty**.
- `git diff test/replays/ | grep -E 'expected(Hash|GameStateHash|CheckpointHashes)|assetHash|"transitions"|coilPrologue|durationTicks'` — expected: **no output**.
- `git status --short` and `git diff --stat` after every reverted mutation — expected: identical to before the mutation.

**Rule 19 mutations — one per acceptance criterion, each named up front, each to be applied, observed red, and reverted from a saved copy (never `git checkout --`, never `git stash`):**
- **AC 1** — in `src/sim/loop/index.ts`, delete the `previousLamps` comparison so every lamp is pushed every step. Expect red: the new diff cases in `test/rules-lamps.test.ts` **and** `test/flipper-mover.test.ts:73,127`, which run real attract frames and assert `commands).toEqual([])`. Two independent detectors, both live today.
- **AC 2** — (a) add `const INSERT_WHITE = '#ffffff';` to `src/sim/rules/lamps.ts`; expect `pnpm lint:boundaries` to exit **1** naming `[sim-no-colour]` and that file. (b) append an eighth member to `LAMP_ROLES`; expect the closure test in `test/contracts.test.ts` red.
- **AC 3** — swap the `hurryup` and `joust` rows in `LAMP_GRAMMAR` (red ↔ blue). Expect red: the grammar row pins in `test/lighting-grammar.test.ts` **and** any driver assertion reading a `hurryup` emissive.
- **AC 4** — remove the per-insert `material.clone()` in `lamp-driver.ts` so all inserts share `mat_insert`. Expect red: the "lamp A lit, lamp B's `emissiveColor` still black" control in `test/lighting-scene.test.ts`. (Second recorded mutation: reverse the iteration order in `advanceLamps` so the *first* command per lamp wins — expect the latest-wins case in `test/lighting-grammar.test.ts` red.)
- **AC 5** — remove `light.parent = playfieldRoot` in `lamp-driver.ts`. Expect red: the world-position assertion in `test/lighting-scene.test.ts`, off by ~57 mm in scene y on the pitched playfield. (Second: delete `includedOnlyMeshes` — expect the per-mesh `lightSources ≤ maxSimultaneousLights` assertion and the `vis_playfield` isolation assertion red. Third: delete the budget cap — expect the `{ budget: 2 }` case red at 14 enabled lights.)
- **AC 6** — in `src/sim/rules/lamps.ts`, read `state.currentPlayer` instead of the base mode's `player`. Expect red: the player-index-1 cases in `test/rules-lamps.test.ts`. (Second: drop the `skill_shot`-presence check so every lit Top lane is step 1 — expect the step-2 case red.)
- **AC 7** — in `tools/make-placeholder-blend.py`, shift one insert's lens 20 mm in x, regenerate and re-export. Expect red: that insert's containment assertion in `test/asset-contract.test.ts`. (Second, proving the test is not vacuous: negate the glb→table y conversion — expect the `vis_playfield` **calibration** assertion red before any insert assertion runs.)
- **AC I1** — in `src/sim/rules/lamps.ts`, return every lamp `off/0` unconditionally. Expect red: the rendered-artefact assertions in `test/lighting-integration.test.ts` (emissive stays black, no light enabled), which is the assertion that would otherwise let a blank playfield ship past a green suite — the Story 2.6 `LEFT_MARGIN_COL` and Story 2.7 dot-row-800 shape.
- **AC 8** — revert one golden's `header.tableHash` to `b6c5ce92`. Expect red: `test/replay-goldens.test.ts` with a named `StaleReplayHeaderError`, proving the refresh was genuinely required rather than cosmetic.

**Manual checks:**
- Confirm `tools/boundary-lint.mjs`'s new rule fires on **each** positive fixture and on **none** of the negatives (a comment naming red/RGB/colour, a prose literal containing "red", a `0x` constant, an identifier containing `redistribute`) by reading the asserted violating-file set, not merely the exit code.
- Confirm `git ls-files` shows no new untracked file under `test/` from the golden harness, and that the harness lives only in the scratchpad.

## Auto Run Result

Status: ready-for-dev
Blocking condition: none
