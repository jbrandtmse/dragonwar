# Epic 2 Context: A Complete Game on the Real Shot Map

<!-- Generated from planning artifacts. Regenerate with compile-epic-context if planning docs change. -->

## Goal

Epic 2 turns Epic 1's placeholder vertical slice into a machine a stranger can play with no instructions. The real playfield geometry is drawn for the first time — the drain triangle, both Loops, the Ramp, the off-centre Dragon with the Lock lane between its legs, the DRAGON drop bank, Top lanes, slings and pops — and every device on it is wired into the table registry with a switch that registers reliably at the fastest speed the physics core can produce. On that geometry sits the whole standard game: Start and Hot seat for one to four players, the ball lifecycle, plunge and Skill shot, lane change, ball save, end-of-ball bonus and multiplier, tilt warnings, Tilt and Slam tilt, ball search, Match, game over and a return to a minimal Attract — all readable from a DMD Backglass with the inserts lit in the table's fixed colour grammar. Geometry is the first story and iterates with the rules that land on it.

## Stories

- Story 2.0: Epic 1 deferred cleanup — the provenance gate's rename and owner *(done)*
- Story 2.1a: The drain triangle, the cradle pocket and the flipper's real dimensions *(done)*
- Story 2.1b: The full shot map and the switch set *(done)*
- Story 2.1c: The Loop returns and the inlane feed *(done)*
- Story 2.1d: Device behaviour and guide terminations *(done)*
- Story 2.1e: Every shot case proves its own start point is reachable *(done)*
- Story 2.1f: The bottom-right corridor — the Ramp and the DRAGON bank made reachable *(done)*
- Story 2.2: Slingshots and pop bumpers as hardware rules *(done)*
- Story 2.3: Drop targets, the spinner and the Lock in physics
- Story 2.4: The devices-and-shots layer
- Story 2.5: Start, Hot seat and the ball lifecycle
- Story 2.6: The DMD Backglass
- Story 2.7: Plunge, Skill shot and lane change
- Story 2.8: Inserts in the held colour grammar
- Story 2.9: Ball save
- Story 2.10: End-of-ball bonus and the multiplier
- Story 2.11: Tilt warnings, Tilt and Slam tilt
- Story 2.12: Ball search
- Story 2.13: Match, game over and return to Attract

Story 2.1 was split into 2.1a and 2.1b as a partition — every acceptance criterion carried over verbatim and in order. Four further stories were then chartered as 2.1b and 2.1c closed: **2.1c** takes the one clause 2.1b could not satisfy inside its own constraints (the Loops' return to the inlanes); **2.1d** takes the device behaviour and guide terminations 2.1b shipped without; **2.1e** makes every shot case in the routing harness prove its own start point is reachable; and **2.1f** re-solves the bottom-right corridor so the Ramp and the whole DRAGON bank can actually be hit. **That run order was discharged — 2.1e ran before 2.1f**, so 2.1f inherited a working reachability gate instead of building one under geometry pressure. **The entire 2.1 geometry chain is now complete.** Epic 2 is nineteen stories and **eight are done: 2.0, 2.1a, 2.1b, 2.1c, 2.1d, 2.1e, 2.1f and 2.2** — the epic has turned from drawing the table to making it play, and **Story 2.3 is the next story to plan**. Any artifact that says "Story 2.1" means the cradle and `DW-72`, which live in 2.1a.

## Requirements & Constraints

- **The shot map is the epic's foundation and is drawn from the reference dimensions alone** — no Bally template. **It is now drawn**: both Loops with entries and exits, one Ramp with an authored height and a decided return inlane, the Dragon off-centre with the Lock lane between its legs and a Mouth eject pose, the six-target DRAGON bank, **three** Top lanes, two slingshots, **three** pop bumpers, two inlanes, two outlanes and the plunger lane, all as `col_` and `sw_` nodes under the prefix contract — with **at least one** `sw_` zone per zone-requiring switch, more than one where a device presents separate faces (`s_dragon_body` carries a zone per Dragon leg). The plunge path is separated from the Loop channel: a plunged ball clears the Loop entrance, crosses the top and reaches the left flipper unaided — measured 3.66 mm closest approach and 438 ticks in the flipper band, against 249.40 mm and zero before. Figures that cannot be measured ship marked `unverified`. **Every clause of that foundation is now delivered.** The Loops' exit paths feeding toward the flippers landed with 2.1c; "guides end at rubber posts, never bare metal" landed with 2.1d, which took the map from **zero** `rubber_post` nodes to **48** and performed the named mutation; and the last one — the Ramp, drawn with its authored height and its return rail but **never reachable by any shot in committed geometry**, a measured fact rather than a regression anything introduced — was closed by 2.1f, by a real driven shot rather than a teleport.
- **The routing harness's pin was repaired before it judged any new geometry, and no later story may weaken it.** `test/shot-routing.test.ts`'s `assertReachesFlipperBandOrLeavesPlay` was `reachedFlipperBand || leftPlay` with `leftPlay` set when the ball **drained**, so the two outcomes the criterion exists to distinguish were indistinguishable; four of its cases also closed their primary switch only because the harness's teleport landed the ball *inside* the zone under test. Both defects are fixed: the pin asserts `reachedFlipperBand` per shot where the criterion requires it, `leftPlay` survives only as a terminal-outcome guard, and a release point is proved clear of every body and zone before it is driven. The repair was demonstrated **red against the geometry it then went on to judge** — the ordering that stopped the `DW-119` failure recurring a third time. **The standing rule is the durable part:** a story that relaxes an assertion here rather than fixing the geometry underneath it is repeating that defect, not resolving it.
- **A completed Loop must return the ball to an inlane, and that routing is its own story.** A ball driven into either Loop at a plausible flipper-shot speed has to arrive playable at the flipper on the corresponding side rather than descending an outlane — the Loops are combo shots, not one-way trips. This is design work, not map-drawing, and it is the one place in the epic where **the drain triangle's dimensional bounds and the Ramp's position are explicitly negotiable**, provided every change is recorded with its measurement and the drain triangle's own behavioural gates still pass. Two prior attempts inside the map-drawing story established the shape of the problem: pure diverter deflection was measured at under 5 mm of net lateral drift where roughly 50 mm is needed, diverters solid enough to respond firmly cap the ball's own ascent instead, and the only other corridor on the table is a 5.4 mm gap against a 13.495 mm ball radius. The likely answer is a multi-surface funnel proven out empirically — the way the funnel under the drain triangle was — or a Ramp position that opens a genuinely passable corridor. The plunge-path separation above must survive the change, the drain switch must be re-verified against the routing that finally lands, and every behavioural gate the shot-map story shipped must still pass, with any golden whose trajectory the routing genuinely changes re-recorded on a trace shown correct rather than merely green. The **Left Loop's north end is also free** and belongs to the same story: shortening `col_loop_top` at its left end for the plunge fix left `col_loop_l` no longer meeting the top connector, so no ball closes both Loops' switches on one orbit and nothing tests that it can.
- **A shot case that starts from a teleported ball proves nothing about whether the shot exists** — so the harness now proves each start point is reachable, and any case added later inherits that without opting in. The harness repositions the ball at each case's release point, so a case proves only what happens *after* that placement; two full code-review passes and the original implementation all missed a shot the table cannot deliver at all, because nothing asked the question. The guarantee is built: every release point lives in one manifest, a single `driveCase(id)` entry point is the only way to drive one — so a coordinate cannot be driven without a declaration — and non-teleporting witness trajectories (a ball originating at a plunge or at a bat, under the real physics pipeline) measure each release point's closest approach. It is a property of the harness rather than of each case, carries an anti-vacuity floor so a run that evaluates zero cases fails loudly rather than reporting success, and is backed by an opt-in dense out-of-process sweep that proves the negative. **Its measured baseline is a fact to preserve, not a number to edit:** `check:reachability` exits 0 over **52 driven cases — 32 proven reachable, 20 recorded unreachable**, measured from **644 swept releases in ~106 s** (51/31/20 before Story 2.2, which added the `descend-pop-1` strand column; 46/23/23 before Story 2.1f, which flipped the Ramp to reachable, added six per-letter DRAGON cases, re-declared `descend-ramp-return-rail` and `descend-sling-r` reachable once a right-bat origin existed, and retired two corridor-column cases into the per-letter set). The release count is the sweep's own recipe count and moves independently of the case manifest — re-measure both rather than deriving one from the other. **The single-origin limitation that qualified this baseline is now closed:** every witness was a plunge or a plunge-then-LEFT-bat until Story 2.1f added a **right-bat witness family**, which is why the Ramp could not be proved reachable at any corridor width; the dense sweep now drives both bat sides and asserts that it does, and the witness-corpus floor is a derived origin-family check — plunge-only, plunge-then-bat-l and plunge-then-bat-r must each be represented, which is the property an `unreachable` verdict actually depends on and the one Story 2.1e's corpus silently failed. The remaining unreachable set is a `DW-138` cohort the lead is harvesting, several of which may still reflect the two axes the 2026-09-04 adjudication left with `burndown` (a SECOND flip, and a `bd_lock`/`c_mouth` origin) rather than the geometry. A story that moves that split must move it by changing geometry, not by re-declaring a case. A case proved unreachable **fails, naming the case and the closest approach achieved**, and is marked against its owning ledger entry rather than deleted, weakened or quietly re-pointed at a reachable start. The same blind spot has a dimensional twin — an inlane feed guide can be shifted 20 mm or deleted outright with every routing case green, because the behavioural observable cannot distinguish "delivered onto the bat" from "still on the rail above it". A proximity record now falsifies that one; the dimensional gates remain the primary catch.
- **The bottom-right approach corridor was too narrow to admit a ball to two of the table's named shots, and both defects shared one cause and one fix. Story 2.1f re-solved it; `DW-137` and `DW-136` are closed.** Measured on the committed collision document: from `col_guide_outer_r`'s east face (279.525) to `col_sling_r`'s west face (314.0) is **34.475 mm**, i.e. **7.485 mm of ball-centre freedom**. Entering the Ramp channel needs a ball centre ≥ 351.495, so the Ramp is **50.990 mm out of reach** (`DW-137`, demonstrated by 256 swept releases closing `s_ramp_enter` zero times); only bank targets overlapping x ≈ 280.5–314 can be struck directly, so **two of six** DRAGON targets are reachable (`DW-136`). Neither is a regression the routing story introduced — the Ramp was already 21.990 mm short at that story's baseline (`43a9c37`) and was deepened to 50.990 mm as the explicitly-granted consequence of widening the Loop lanes for the orbit. The corridor must be **re-solved as a whole** — the slingshot span, both Ramp walls, the DRAGON bank and the Loop lane budget together, not one body at a time — because moving `col_sling_r` back east re-narrows the very lane the orbit needs. **Buying the Ramp by breaking the delivered orbit is a hard stop, not a trade to make silently**, and a dimensional gate must pin the corridor width against its tunable so a later change cannot silently re-narrow it. **Delivered (2026-09-04, Story 2.1f):** the quadrant was re-solved as one chain from the Lock lane's east wall (190.0) to `col_sling_r`'s east face (370.4, unmoved) — `col_sling_r` 314.0 → **332.4** (span 56.4 → 38.0), the Ramp channel 338.0..372.0 → **298.4..354.4** (`RAMP_LANE_CLEAR_MM` 34 → 56, still narrower than the 66 mm Loop lane), the DRAGON bank 240..321 → **217.4..282.4** at pitch 14 → 11 and target width 11 → 10, and `DRAGON_LEG_R_W_MM` 45.0 → **23.4**. The corridor is now **52.875 mm wide (25.885 mm of ball-centre freedom, was 7.485)**, `col_sling_r`'s west face stands **34.000 mm** east of `col_ramp_wall_l`'s east face against a 26.99 mm ball, and `pnpm check:corridor` reads **+34.000** where it read −24.000. The dimensional gate is `test/asset-contract.test.ts`'s corridor-width test, pinned against the new `RAMP_CORRIDOR_CLEAR_MM` tunable; the per-target half is behavioural — six per-letter routing cases, each closing its **own** `s_dragon_<letter>`, replacing the old "at least one of six closes" assertion that had let five unreachable targets pass. The Ramp is reached by a **real 536-segment right-bat witness at 0.458 mm**, driven from a plunge and a bat through the physics pipeline, never teleported. The delivered orbit is intact: all six entry offsets, the single-ball orbit and the plunge path pass with no assertion weakened.
- **The pop-bumper count is three.** No artifact ever stated it — the requirement says only "pop bumpers" while tagging Top lanes at three and slingshots at two — so the author fixed it at three (`s_pop_1..3` / `c_pop_1..3`), matching the Top-lane count and the standard arrangement for this shot density. A wildcard cannot be declared in the registry. Like every other unverified figure it is recorded in `TABLE` with a `source` and a `confidence`, so the provenance is visible rather than looking like a fact somebody found.
- **The switch set completed in `TABLE.switches`** covers `s_loop_l_in/out`, `s_loop_r_in/out`, `s_spinner`, `s_ramp_enter/made`, `s_dragon_[d,r,a,g,o,n]`, `s_dragon_body`, `s_lock_lane`, `s_lock_1..3`, `s_top_1..3`, `s_inlane_l/r`, `s_outlane_l/r`, `s_sling_l/r`, `s_pop_1..3` and `s_drain`, each with its per-device `settleTicks` class — plus the ball device `bd_lock` (capacity 3, ejecting through `c_mouth`, with a `ballSearchOrder`) and the coils `c_sling_l/r`, `c_pop_1..3`, `c_dragon_bank_reset` and `c_mouth`. The export script validates the Blender source against the registry dump and both loaders must load the result. **Switch-edge semantics are settled and now correct on both edges:** the tracker latches a make on the tick it observes the zone, and `settleTicks` gates the **break**, never the make. The residual break-side off-by-one was fixed in 2.1d across **both** trackers in one change — correcting either alone turns the tracker-agreement test red — with `settleTicks = 0` still a fixed point and no golden hash moved.
- **`bd_lock` now behaves, and every later story consumes that delivered state rather than re-deriving it.** Boot occupancy is a **declared per-device property**, so the machine boots **4** balls, not seven: `bd_trough` boots full, `bd_lock` boots empty, physics reads the declaration, and the four-ball invariant is checked *by name* at construction rather than assumed. `c_mouth` ejects exactly one ball that stays out — the Mouth eject pose no longer sits inside a slot zone re-parking it on the same tick. The Lock lane's slot zones were re-sited into the Dragon-leg corridor and sealed from the north by new `col_lock_ceiling` / `col_lock_ceiling_west_fill` bodies, so they are bounded by the lane's own geometry instead of swallowing any passing ball in open field. All five goldens were re-recorded under the author's grant in that same pass, with their `notes` corrected. **Geometry work in the bottom-right must not assume these bodies are absent** — the west fill in particular occupies a band that a naive sweep will read as open field and mis-measure. Story 2.3 takes the Lock in physics on exactly this delivered geometry: the parking, capacity, eject-pose and overflow behaviour are the mechanism it builds on, not something to re-derive.
- **The FR-31 guide-termination gate was widened from a name prefix to a structural selector, and that widening is now the contract.** It selects bodies by *having an unjoined free end* rather than by the `col_guide_` prefix — under the old reading its own Given was the empty set, a criterion that could not fail — with every exemption carrying a named reason, a machine-checked `verify()` re-asserting that reason's coordinate claim on every run, and enforcement in **both directions** so a stale exemption fails too. **Story 2.1f was the first story whose own new guide geometry this gate judged, and it closed all three known gaps in it:**
  - **`col_loop_top`'s two bare 9.5 mm end caps** (`DW-146`) shipped **Branch B** under the author's pre-authorised decision rule. The untried ridge axis *was* attempted: `RIDGE_DROP_MM` was swept at 2.5 / 3.0 / 5.0 / 8.0 / 10.0 / 12.0, re-seeding and re-exporting through the real pipeline at every value. The finding is that **Branch A narrowly exists** — at 12.0 the caps taper to a point (the same shape as the two already-exempt return rails) and all six Loop entry offsets still pass. It was declined on a **measurement, not an argument**: at 12.0 the body stops filling the top channel, changing every trajectory that rides it, at a cost of **12 reachability regressions** plus a drain outside its recorded window, which is changed behaviour rather than a retime. Geometry stays at 2.5, Branch A's availability and full cost are recorded for the lead, and the exemption now carries a `verify()` it never had — deriving both caps from the live footprint and asserting they are still 9.500 mm and still bare, so it cannot go stale silently.
  - **A post buried inside another body contributes no collision surface** (`DW-153`) — closed in both halves. The buried-post census was decided body by body with its measurement recorded, and the gate now asserts that every `rubber_post` presents collision surface **outside** the union of the non-post wall footprints, so a post can no longer satisfy the gate without contributing surface. Its circular `verify()` — which measured a post against its own authored literal — was replaced by a derivation from the live geometry.
  - **The gate's helpers admitted degenerate inputs silently** (`DW-154`) — closed across all four sub-cases: an end strictly interior to another body is now an enumerated declaration rather than a silent skip, a shortest-edge tie throws instead of resolving by vertex order, the staleness catch asserts the throw was the shape-reason throw rather than swallowing the error object, and no post satisfies the gate without protruding. **Seven exemptions now carry a `verify()` predicate making the same coordinate-level claim their prose reason states, re-checked every run** — that is the durable half. The rule generalises beyond this gate: on a degenerate input, **fail loudly**; never resolve it to a pass.
- **Two distinct open questions are answered and recorded in the decisions document. Do not conflate them.**
  - **OQ-6 is the playfield geometry itself** — flipper tip gap, outlane widths, post positions, loop entries, **Ramp height and its return side**, Dragon placement, and now the **spinner model** as well. The Ramp's return-inlane choice is the **OQ-6**/FR-27 decision; it is decided (right inlane) but **not yet delivered**, and its delivery was **moved by amendment out of the routing story and into the corridor story** — the routing story could satisfy it only through the harness's teleport, so the clause went where the reachability it depends on is actually bought.
  - **OQ-5 is whether the Lock lane carries both lock and mode start.** If it does not, a separate `sw_scoop` and `bd_scoop` are added with the Mouth as eject only — a table-and-geometry change, not an architecture change, because the Lock arbiter decides from state.
- **Every shot's miss must come back playable (Lawlor's test), verified per shot.** A feel-test entry lands for each of the seven shots — Left Loop, Right Loop, Ramp, Dragon, Lock lane, bank, Top lanes — recording where the most common miss goes; none may be a centre drain, and any shot that fails is re-drawn before its story closes. Running the ritual is author-owned work at the Reference machine — the pipeline cannot close it, and the miss-destination judgement itself stands explicitly **`pending-author` and unwritten**, so no stage may record it as satisfied.
- **Switches register at maximum ball speed or the geometry is wrong.** Zone tests use the ball's per-tick swept segment, never its end position, and a test asserts that the fastest ball a full-strength plunge and flipper hit can produce misses no pass through a rollover, the bank, the Lock lane or the drain — each switch closing exactly once per pass.
- **Per-player facts stay per player in Hot seat** — score, DRAGON letters, Lock credits, modes played, tilt warnings, bonus and multiplier, extra balls, lit lanes.
- **Ball save has a precise shape**: enabled at launch with the timer starting on the launch event, not on enable; a hurry-up display state before expiry; a grace window past the *displayed* expiry during which a drain still saves; saved balls auto-launch; multiple arming sources stack with the longest live window winning; Tilt disarms all.
- **Tilt keeps the score and forfeits the bonus.** Warnings are spaced by a semantic window (default count 1) and the bob is never reset by command — its physical decay plus a settle window is the settle. On Tilt, flippers, slings, pops and autolaunch are disabled together and the ball ends when the last ball in play drains. Slam tilt ends every player's game, returns the machine to Attract, and lives inside the replay.
- **Ball search never releases locked balls while a mode timer runs**, escalates through each device's declared order, and only at its final stage issues the one command that lets physics despawn balls outside a device.
- **Match draws a multiple of ten from the seeded rules PRNG** at the configured probability (default 8%) and is display-only under free play.
- **English literals live in the Backglass and nowhere else** in the simulation. Rules never format text.
- **`pnpm check:ad7` is deliberately red** (`DW-70`), and stays red through Story 2.4. It is a ledger entry routed to **Story 2.5** as that story's own deliverable — device-slot state must come to be derived inside the ball controller rather than written after the rules step, and the test that currently asserts the failure is updated in the same change. No earlier story should touch it; it is not a defect to repair on sight.
- **`pnpm check:corridor` is now INTENDED GREEN** (`DW-137`, closed by Story 2.1f). It was shipped deliberately red by Story 2.1c's final rework pass, mirroring the `check:ad7`/`DW-70` pattern above, over a measured 50.990 mm shortfall. Story 2.1f re-solved the corridor and the same live arithmetic now passes with a 7.010 mm residual; its in-suite intended-red wrapper (`test/dw137-corridor-gate.test.ts`) was **removed** in the same change, because a wrapper asserting a green check's failure asserts nothing. `check:corridor` is now an opt-in green check like `check:reachability` — run it whenever the committed geometry moves. **`check:ad7` is the only intended-red check left.** A green run of `check:corridor` is not on its own proof the Ramp is a shot: that half is behavioural and lives in the suite (`ramp-return-geometry`, witnessed by `plunge-then-bat-r-3899`).
- **A tunable's `source` *and* `confidence` strings are hashed into every golden replay's `header.gameStart.tuning`** — the tuning resolver's entire serialized output is hashed, not just its numeric values, so correcting provenance prose *in place* invalidates all five goldens before tick 1, exactly as a numeric change would. Measured at Story 2.2: a prose-only fix took the suite from 93 files / 1488 passing to 91 / 1454 with 34 skipped, reproduced 3/3, with reverting the string restoring it every time. **A provenance correction is never free.** Budget a header-only five-golden re-record in the same change, or route the correction to a story that is re-recording anyway — which is why the tip-gap wording goes to Story 2.5 and `popKickMmPerS`'s to Story 2.3. A corrected bound can meanwhile be pinned *falsifiably* without touching the string, through the tuning resolver's override seam, which never mutates the production object and so perturbs no hash.
- **Provenance is a hard gate** and the attribution entry lands before the file does, with the licence verified at its source.
- **A golden is re-recorded only under an explicit author grant, never to make a suite green and never by switching a parity check off.** Three were re-recorded under a narrow grant while the shot map landed, and **all five were re-recorded again under the author's grant with 2.1d's `bd_lock` boot fix**, each traced correct *before* recording with the reasoning written into its own `notes` — that is the binding condition for any future re-record. The `col_spinner_l` → `vis_spinner_l` rename was batched into that same re-export for the same reason: either change alone moves `assetHash` and breaks all five, so batching cost one re-record rather than two. **2.1f and 2.2 each carried one further re-record, and both were header-only** — 2.1f's an `assetHash` refresh with every `expectedHash`, `expectedGameStateHash`, `durationTicks` and transition body confirmed byte-identical; 2.2's riding the `rubber_band`/`rubber_post`/`bumper` material assignment, none of the five recorded trajectories passing near a sling or a pop. **Any story that re-exports geometry or moves a tunable owes one re-record — budget it, and batch every such change into that single pass**, because either kind of change alone moves `assetHash` and breaks all five.
- A tick-rate or solver-constant change is a physics-version bump that re-records every golden.
- **A gate is only evidence if its red was observed for the reason the story claims. Eleven vacuous assertions have now been found across this epic, and every one was caught by a deliberate falsification attempt — not one by a passing run.** The recurring shapes: a mutation recorded as proving a behavioural assertion when a construction-time invariant or a throw actually fired first; a terminal state inferred from a switch-make rather than read from an actual capture; and, most recently, a pop-bumper **kick-direction** test that dotted a physics-frame velocity against a table-frame offset, so the y term carried the wrong sign and the assertion was decided by which axis had the larger magnitude rather than by direction — a full sign inversion of the kick's y axis made all three cases pass **more strongly**. That one had been added by a *previous* review pass specifically to close the gap it then failed to close, which is the warning worth carrying: a test written to close a verification gap is not itself exempt from being verified. When a story records a Rule 19 mutation, the red must be demonstrated *through the observable the criterion names*, in a single coordinate frame, asserting per axis so neither can mask the other — and a mutation that tests direction rather than margin will pass at a value the source itself records as insufficient. Prefer re-deriving a mutation at the **authored source constant**, through the real export and physics pipeline, in an isolated copy rather than in the worktree.
- **Current measured baselines a change is judged against:** the suite is **93 files / 1490 passing / 0 failing** (with `BLENDER` set, so the Blender-gated cases run rather than skip); `check:reachability` exits **0** over its **52** cases and 644 releases in ~106 s; `check:corridor` exits **0**; `check:ad7` exits **1 by design** and is **the only intended-red check** — it stays red through Story 2.4 and is Story 2.5's own deliverable. A story reports these as it found them and explains any movement, and **re-measures at its own tree rather than carrying a figure forward**: Story 2.2 alone needed three separate corrections to stale recorded figures, every one caused by reusing a baseline count instead of re-running the command.

## Technical Decisions

- **The devices-and-shots layer is the only consumer of switch events**; the build fails if anything else under the rules tree imports one. It owns the drop bank (letters and the reset coil, pulsed on ball-will-start and on bank completion), the spinner count, ball-device slots, the shooter lane and the declared shots, and emits only payload-complete device events — shot made and broken, ramp made, target down with its letter, bank completed, dragon hit, lock lane entered, spinner spins, lane entered, lane change pressed, ball launched, device ball entered and left, button pressed. Lane *state* belongs to the base mode; the layer reports entries and presses only.
- **Shots are declared data, not code** — an ordered switch sequence in the registry with a tick window converted from a millisecond tunable. In order inside the window emits made; a broken sequence or an expired window emits broken; a Loop taken the wrong way emits nothing.
- **Hardware rules run inside the physics step.** Slingshots and pop bumpers join the flippers and plunger: switch closes at tick *t*, coil energises inside tick *t*'s own physics step with no rules round trip, gated only by coil enable/disable. Disabled, they act as passive rubber and emit no actuation. Every actuation emits a contact event to presentation so each mechanical sound has exactly one source; rules never receive a contact event. **Story 2.2 delivered both, and they sit at different points in the tick — copy that shape rather than re-deriving it.** The slingshot *is* the ported model, registered per coil through a thin subclass that never edits the frozen port (`DW-79` — wire from outside: construct, subclass, inject), so its parabolic kick fires **inside the collision solve** at contact time. The pop bumper is an authored skirt device that needs a switch *edge*, which exists only after the switch tracker steps, so it fires **after** that call and before the step returns, as an impulse directed radially away from the bumper's own footprint centroid derived from the committed document. Three consequences worth inheriting: kick **magnitude alone cannot break a reflection symmetry** — a ball descending exactly onto a bumper's apex vertex needs a fixed, deterministic lateral floor, never randomness (AD-3); reporting that a coil *branch ran* is not the same as reporting that an impulse was *delivered*; and two `sw_pop_*` skirt zones overlap in committed geometry, so one ball can make two pop switches on a tick and fire two near-cancelling coils — escalated rather than patched, because the root fix is `sw_` geometry and therefore a second five-golden re-record, while the code-side mitigation changes *which* coil fires and so is a product call for the devices-and-shots layer.
- **Physics owns ball bodies and mechanical state; rules own accounting.** The Lock is a parking device (capacity 3, filling the lowest empty slot, ejecting the highest filled slot at the Mouth pose, one ball per pulse); a slightly-off Lock-lane shot hits the Dragon body instead.
- **A ball device's boot occupancy is a declared property of that device, never a global constant** *(amended 2026-09-04)*. The trough boots full, the Lock boots empty, physics reads the declaration, and the machine's four-ball total is asserted by name at construction rather than assumed. This is a registry contract, not a one-story detail: **every future ball device declares its own boot occupancy**, and the device, shots, lifecycle, ball-save and ball-search stories are all named consumers. The shooter lane is non-parking with the served ball resting simulated on the plunger tip, and the *opening* of its switch is the one event that means "plunged". Device counts are the number of closed slot switches and nothing else; a slot beyond capacity surfaces as an overflow event that rules answer with an immediate eject. A dropped target is non-collidable until the bank reset coil raises it. The spinner is a pass-through gate rather than a body the ball strikes — see the next bullet.
- **The spinner is a pass-through gate, not a collision body.** A ball crossing its switch zone imparts rotation proportional to entry speed, and the spinner then closes its switch **once per revolution until it decays**, because the requirement awards per rotation — that clause is deliberately preserved. Thirteen-plus rigid-body variants were measured across both candidate ball paths and every one that genuinely contacted the ball produced a permanent stall, because a static rigid body cannot graze a 13.495 mm-radius ball with the precision those paths allow; the analytic swept-segment zone test is therefore the physically correct model here, not a workaround for a failure. A node nothing collides with is not `col_` under the prefix contract, so the spinner's node was renamed to the visual prefix (`vis_spinner_l`) in the device story — that rename is **done**, and it is a presentation mesh now. **The physics-devices story still owns the spin and decay mechanism**, driven off the zone crossing.
- **Game state is one plain-data tree with fixed ownership scopes** — JSON-serializable, no class instances or closures, mutated only inside the rules step, with player-scoped, machine-scoped and mode-local facts strictly separated. The mode stack is empty between balls: every active mode receives its will-stop before ball-ended; ball-will-start resets ball save, tilt and multiball; ball-starting enables hardware.
- **Single arbiters for the overloaded seams.** The ball controller alone pulses the trough and autolaunch coils and alone mutates balls-in-play; ball save is one machine device it owns, armed and disarmed by source through a rules-internal interface; the Lock arbiter is the only consumer of lock-lane entries. "A multiball is running" is a flag set and cleared in mode lifecycle phases, never derived from balls in play.
- **The command union is closed.** Rules to physics: coil pulse/enable/disable, plus the recover command for ball search alone. Rules to presentation: lamp, GI, flasher and show commands, nothing else.
- **Lamp state is a pure projection** recomputed from state every step; the loop emits only the diff, and no mode issues a lamp command. Roles are a closed union and steps are 0 to 3; no RGB, hex or colour name may appear anywhere in the simulation, and one presentation-side table is the sole role-to-colour mapping.
- **One clock, no wall-clock or unseeded randomness in the simulation.** Timers are authored in milliseconds in the tuning file, converted to ticks once at load, and pace presentation by emitting step events presentation animates to — presentation never reports completion. All rules randomness draws from the seeded PRNG in game state, so the Skill-shot lane and the Match number replay identically.
- **Modes have unique numeric priorities** — base 100, skill shot 200 — and speak the four-phase start and stop convention. Epic 2 runs that minimal stack only.
- **Blender owns placement, the registry owns wiring, the export script enforces the contract.** Node prefixes are load-bearing: collision scaffolding, switch zones, visuals, insert lens-and-cup geometry, mechanisms named as their device. The export validates the source against a dump of the registry and writes both the render asset and a collision file in table-frame millimetres; the simulation never parses the render asset, and both loaders fail fast on a missing node or unknown property.
- **Layering is law and is linted.** Device-name string literals outside the table file and the test tree are errors, so every new switch, coil, lamp and device name enters through the registry. Three complementary provenance gates exist and none may be retired in favour of another: a per-file **presence** check across tracked files, a **structural** provenance test (upstream copyright block intact, provenance sets disjoint, the solver-constant pin, the port-body freeze), and the **import** linter. Presence cannot detect a stripped copyright block; Story 2.0 is the structural gate's documented owner.
- **Tunables carry a source and a confidence.** Material parameters are named entries with scatter at zero. Scoring values stay adjustable through Epic 2 and freeze only after Epic 3's first full playtest.

## UX & Interaction Patterns

- **One fixed authored camera, no camera controls.** Both flippers, the Dragon, both Loops, the Ramp and the DRAGON bank must all be legible from it. The backbox strip that carries the Backglass is **not** part of that framing obligation in the shot-map story — no backglass or backbox geometry exists anywhere yet (`cabinet_root` is authored childless), so Story 2.6 authors the quad and owns any camera consequence.
- **The Backglass is a low-resolution dot-matrix display** — a visible dot grid, no smoothing — showing every player's score with the current player highlighted, the ball number, and the highest-priority mode's name and whichever fields it publishes, converted to display units in presentation.
- **Presentation never joins a tick-*t* event to a later snapshot.** An end-of-ball screen names the player from the event payload, never from the snapshot that has already advanced.
- **The colour grammar is fixed and never varies**: white for a lit or qualifying shot, orange for DRAGON letters and the Lock, red for Hurry-up, green for Quick multiball, blue for Joust, purple for extra ball and special. Progression within a colour is a brightness and blink ladder expressed as the step value, with cadence timed by presentation.
- **Flipper buttons move the lit insert** one position per press across the Top lanes and across the inlane/outlane set, wrapping.
- **Attract in this epic is minimal**: the Backglass cycles the last game's scores with a start prompt and shows the flipper, plunge and Start keys once from the view configuration. The camera walk-up belongs to Epic 4.

## Cross-Story Dependencies

- **Story 2.0 was the epic's opening gate** — renaming the structural provenance test, giving it a documented owner, and reconciling it with the architecture decision rewritten alongside it. It added no product behaviour.
- **2.0, the whole 2.1 chain and 2.2 are all done.** 2.1b drew the rest of the shot map on geometry 2.1a had proved, completed the switch and coil declarations, established the plunge path over the Loop entrance, answered OQ-5 and OQ-6 in the decisions document, settled the make-side switch semantics, and closed the outlane path that bypassed the drain switch entirely. 2.1c then settled the Loop return routing on top of it and delivered the orbit; 2.1e gave the routing harness the reachability guarantee every later geometry story now inherits, touching no geometry, tuning or golden file to do it. 2.1d — which took four rework iterations — made `bd_lock` behave, re-sited and sealed the Lock lane, terminated the guides behind a structurally-selected FR-31 gate, renamed the spinner mesh and carried the five-golden re-record. **2.1f closed the chain**: it re-solved the bottom-right corridor with the reachability gate, the working devices and the widened termination gate all already in hand, and 2.2 then put the first hardware rules on top of that geometry. Every later story in the epic lands on the shots and switches these stories declare.
- **2.1c is the only story allowed to move the geometry underneath itself.** 2.1b was forbidden to move Story 2.1a's dimensional bounds or renegotiate the Ramp position; 2.1c is chartered with exactly those constraints lifted, which is why the Loop-return clause was moved there rather than forced through the shot-map story. 2.1c takes 2.1b's shot map, switch set and plunge path as its prerequisite, repairs the routing pin before touching geometry, must re-verify the drain switch against whatever routing it lands on, and must leave 2.1b's behavioural gates passing — the shot-routing, drain-routing and flipper-sweep-clearance gates. **2.1f inherited the same lifted constraints for the bottom-right quadrant, with one added on top:** it took 2.1c's delivered orbit as a prerequisite it could not break — both Loops at all three entry offsets, the single-ball orbit and the plunge path all still pass with no assertion weakened — and it turned the deliberately-red corridor gate green because the corridor genuinely admits a ball, removing that gate's intended-red documentation in the same change. **Done 2026-09-04.**
- **Story 2.3 is next, and it inherits from both 2.1f and 2.2.** 2.1f bought the corridor that 2.3's own "all six droppable" requirement needs — that corridor was deliberately re-owned out of 2.3 because the DRAGON bank's reachability and the Ramp's are one corridor with one fix, and splitting them across two stories would have had each re-solving the other's constraint. 2.2 closed `DW-148` with the **kick** rather than a geometry change, exactly as its routing required, and left the pop cluster live. **Four things are routed to 2.3 specifically:**
  - **The spinner's spin and decay mechanism**, driven off the zone crossing under AD-6's amended pass-through model — never off a collision with a body.
  - **`DW-134`** — `s_lock_lane` closes for balls that merely wander into the Lock lane from open field, so a lane entry no longer implies a Lock-lane shot. Either the zone needs a discriminating condition or the arbiter must tolerate a non-shot entry; a geometry change here would undo the bevel fix it exists for.
  - **`DW-155`** — two of Story 2.1d's recorded Rule 19 mutations do not demonstrate what its spec says they do: one is throw-based, with a construction-time invariant firing before the behavioural assertion it is credited with proving, and the other infers a `locked` terminal state from switch makes rather than from an actual capture. Routed here because 2.3 owns the Lock in physics and re-touches both observables, so its own gates must distinguish a real capture from a switch-make inference.
  - **`DW-160`** — `popKickMmPerS` ships at 200 with a `source` string whose stated floor is **false** (virtually any positive kick clears `DW-148`, down to 0.5 mm/s, so there is no floor near 180) and whose stated ceiling mechanism is the **wrong one**: the real ceiling is a **221 mm/s cliff**, only 21 mm/s above the shipped value, where `DW-148` re-strands at a *different* equilibrium near (93, 840). Both corrected bounds are already pinned as tests through the tuning resolver's override seam, so what remains is stale provenance prose plus a narrow margin — routed here because this story re-records goldens anyway and, per the amended provenance rule above, the prose fix is otherwise not free. **Widening that 21 mm/s margin is a separate real question and needs its own sweep.**
- **What 2.1a already delivered, and later geometry stories should build on rather than re-author:** a verified-draining drain triangle — two outlanes, two inlanes, a divider and outer guides ending in `rubber_post` nodes, a pocket beside each flipper that holds a cradle for the full 5 simulated seconds (closing `DW-72` on measured evidence), bottom walls sloped by `BOTTOM_WALL_DRAIN_DROP_MM = 10.0`, four below-deck return channels and four `sw_trough_*` zones. It also reconciled the flipper's modelled collision body with its authored box (`flipperRadius = lengthMm - baseRadiusMm - endRadiusMm`).
- **That reconciliation moved Epic 1's flipper numbers, and current geometry governs.** The shorter arm dropped the bat's inertia to roughly 68% of its former value, so Story 1.6's light-tap criterion was re-measured from 30 ms to **10 ms** — the promise (a brief tap moves the flipper partially) was kept, only the number moved. **Anything reasoning about flipper travel, cradling or tap feel must use 10 ms and the current geometry, never Epic 1's figures.** A criterion that has become false is amended by asking first, never narrowed inside the implementing story.
- **2.3 (physics devices) precedes 2.4 (the devices-and-shots layer), which precedes every rules story.** 2.5, 2.7, 2.9, 2.10, 2.11, 2.12 and 2.13 all consume device and shot events and must never reach for a raw switch.
- **Story 2.5 owns the real ball lifecycle** and is where Epic 1's dev-only coil escape hatch goes: each golden replay's declared coil prologue is removed and the goldens re-recorded. It also carries the device-slot ownership fix the currently-red architecture check waits on, and the tuning-provenance wording correction that must ride the same golden-header refresh.
- **Story 2.6 authors the backbox quad it had been assuming.** No backglass geometry exists before it, so 2.6 adds the `vis_` quad to the Blender source, re-exports, and **re-aims the fixed camera if the quad falls outside the current view**. Re-aiming moves Story 1.4 deliverables — `createFixedCamera()`'s hand-picked `y=-700`/`z=1300` absolutes and the assertions at `test/scene-smoke.test.ts:311-336`. Both are in scope there; neither is a surprise to be discovered at review, and no earlier story re-aims that camera as a side effect. 2.6 otherwise consumes what 2.5 establishes; 2.8 consumes the lamp projection over state that 2.5 and 2.7 populate.
- **Epic 2 must not pre-build Epic 3's modes** beyond base and skill shot, nor Epic 4's audio, flashers, GI, walk-up or baked lighting, nor Epic 6's persistence, settings panel, high-score table or platform gate. The Dragon's mouth show and hit reaction are Epic 3's; the art passes that replace placeholder primitives behind the same node contract are Epic 5's.
- **Inherited from Epic 1:** contracts, the table registry, tuning, boundary lint, the export pipeline and both loaders, the fixed-step conductor with trough serve and drain, tick-stamped input, the ported flipper mover and hold-to-charge plunge, the cabinet oscillator, tilt bob and slam counter as sensors closing switches inside physics, five golden replays with CI parity, and the dev tuning panel. The rules that consume the bob and slam sensor are this epic's work.
