---
title: 'Story 2.1b: The full shot map and the switch set'
type: 'feature'
created: '2026-08-31'
status: 'in-progress'
baseline_revision: '6ffc1711f4b1a7a0c44a494ce94cb21a14ece90f'
review_loop_iteration: 0
followup_review_recommended: false
context:
  - '{project-root}/CLAUDE.md'
  - '{project-root}/AGENTS.md'
  - '{project-root}/_bmad-output/implementation-artifacts/epic-2-context.md'
  - '{project-root}/_bmad-output/implementation-artifacts/spec-2-1a-the-drain-triangle-the-cradle-pocket-and-the-flipper-s-real.md'
  - '{project-root}/_bmad-output/planning-artifacts/architecture/architecture-dragonwar-2026-08-26/ARCHITECTURE-SPINE.md'
warnings: ['oversized', 'multiple-goals']
deferred: []
---

<intent-contract>

## Intent

**Problem:** Story 2.1a proved a drain triangle beneath `y = 420 mm`; above that line the playfield is empty. Nothing exists for the Left and Right Loops, the Ramp, the off-centre Dragon with the Lock lane between its legs, the six-target DRAGON bank, the three Top lanes, the slingshots or the pop bumpers, and `TABLE.switches` still declares only Epic 1's eleven switches with `coils` at four and no `bd_lock`. Every later story in Epic 2 and Epic 3 lands on shots and switches that do not yet exist. Two switch classes the shot map introduces (`standup` 8 ms, `drop_target` 20 ms) also make ledger `DW-67` reachable for the first time: `createSwitchTracker` debounces the **make**, so a zone crossing shorter than `settleTicks + 1` ticks emits no edge at all -- which makes FR-11 ("no ball is ever lost by a missed switch at any ball speed the Physics core can produce") false by construction the moment the first drop target is declared.

**Approach:** Draw the rest of the shot map in `tools/make-placeholder-blend.py` above `GUIDE_Y_TOP_MM = 420`, meeting 2.1a's four `*_hi` rubber posts, and re-export; complete `TABLE.switches`, `TABLE.coils` and `TABLE.ballDevices.bd_lock`; place one `sw_` zone per zone-requiring switch; make the switch tracker latch on the make and debounce the break so a maximum-speed pass registers exactly once; close both directions of the registry/document validation; and record the two geometry decisions the epic charters to this story in a new `docs/decisions.md`. The seven-shot Lawlor ritual (FR-32) is author-owned and ships `pending-author`.

## Boundaries & Constraints

**Always:**

- Geometry is authored **only** by editing `tools/make-placeholder-blend.py` and re-running it headlessly, then `pnpm export:assets`. The `.blend` is a generated binary; every commit that ever changed it also changed that script, in lockstep. Blender is reached **only** through the `BLENDER` env var -- never write an executable path into a tracked source, config or test file.
- The `.glb`, the `.collision.json` and the `.blend` are regenerated and committed **together**. CI has no Blender and consumes the committed artifacts.
- Every figure this story authors above `y = 420` is an authored decision with **no source anywhere in the repository** (verified: the research digests contain no ramp, orbit, drop-target, pop, sling, Top-lane, post-radius or switch-zone dimension of any kind). Anything that reaches `tuning.ts` ships `confidence: 'unverified'` with an honest `source` string (AD-15, SPEC.md:241).
- Table frame, millimetres, right-handed, origin at the bottom-left playfield corner, **authored unpitched** (AD-10). Playfield is `x in [0, 514.4]`, `y in [0, 1066.8]`.
- Every `col_`/`sw_` node is a MESH with an **identity object transform** -- angle is authored in mesh vertices, not in the object matrix (`export.py`'s rotation guard, `AXIS_ALIGNED_EPSILON = 1e-9`). `col_lane_deflector` is the prototype to copy in shape.
- Every wall footprint is **convex**. Decompose an L, U or notched shape into several convex `col_` nodes; a curve is a chain of convex prisms.
- Every guide's free end terminates at a node whose `surface` is `rubber_post`, never bare metal (FR-31, CAP-31, AD-11).
- New geometry near the flippers must respect the two tight bounds 2.1a measured: the drain-end throat is **0.137 mm** (27.1272 mm against the 26.99 mm ball) and `BOTTOM_WALL_DRAIN_DROP_MM = 10.0` has an unrecorded **lower** bound of 9.863 mm. Both are pinned by `test/flipper-sweep-clearance.test.ts`.
- Device names enter through `src/sim/table/dragonwar.ts` only; a device-name string literal anywhere else under `src/` is a lint error (AD-16, `tools/boundary-lint.mjs:507`).
- Non-ASCII in source is authored as an escape sequence, never as a literal byte (Rule 14).

**Block If:**

- **RESOLVED 2026-08-31 (author).** AC 8's Backglass clause is **removed from this story** and the obligation moved to **Story 2.6 AC 1**, which now authors the `vis_` backbox quad, re-exports, and re-aims the fixed camera if the quad falls outside the current view (`epics.md` amended on both stories). AC 8 here ends at "...are legible". **Do not author backglass or backbox geometry in this story, and do not touch `createFixedCamera()` or `test/scene-smoke.test.ts:311-336`** -- re-aiming a Story 1.4 camera is Story 2.6's scope now, deliberately, so that a shot-map story does not move it as a side effect.
- **RESOLVED 2026-08-31 (author).** The pop-bumper count is **three**: `s_pop_1..3` / `c_pop_1..3` (`epics.md` amended at the geometry and declaration criteria). Three matches the Top-lane count already assumed and is the standard arrangement for this shot density. **Record the number in `TABLE` with `source` and `confidence` exactly as the other unverified figures are recorded** (AD-15), so its provenance is visible rather than reading as a sourced fact. Note the golden-header consequence: the switch and coil sets feed `tableHash`, so fixing the count re-records all five golden headers in this story's own pass.
- The drawn Lock lane cannot carry both the lock and the mode start -- the OQ-5 fallback (`sw_scoop` + `bd_scoop`) rewrites acceptance criteria in Stories 2.3, 2.4, 3.2 and 3.4. HALT rather than adopt the fallback unilaterally.
- Any change to `BOTTOM_WALL_DRAIN_DROP_MM`, the flipper boxes, the pivot positions, or the drain aperture (`x 200 -> 314.4`) would be needed to fit new geometry.
- A golden's `durationTicks`, `PARITY_INERT` membership, or per-golden scenario assertion has to change to keep it green.

**Never:**

- Never touch `DW-70` / `pnpm check:ad7` (Story 2.5). It deliberately exits 1 and `test/ad7-device-slots.test.ts` passes by asserting that failure naming `AD-7`, `DW-70` and `bd_trough`. **A green `check:ad7` is a regression.**
- Never touch `NOTICE`'s vpinball claim (`DW-82`, Story 6.7), or the `flipperTipGapMm` provenance wording (`DW-113`, Story 2.5).
- Never declare `TABLE.shots`, and never write shot-sequence or window logic -- shots are declared data owned by **Story 2.4** (AD-19). This story declares switches, coils and `bd_lock` only.
- Never implement device *behaviour*: slingshot and pop actuation is Story 2.2; drop-target drop/reset, spinner revolutions and Lock parking/eject are Story 2.3. This story authors their bodies, zones and registry entries.
- Never re-record a golden. Headers are **refreshed** (`assetHash`, `tableHash`, `gameStart.tuning`, and `expectedHash`/`expectedGameStateHash` only where a recorded trajectory genuinely reaches new geometry); recorded input (`transitions`, `coilPrologue`) stays byte-identical. Story 2.5 owns the real re-record.
- Never delete, skip or weaken a test to reach the suite baseline.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Max-speed pass, zero-settle zone | ball crosses `sw_top_2` at the measured maximum speed, present in the zone for 1-2 ticks | exactly one `closed: true` on the first intersecting tick, exactly one `closed: false` after it leaves | No error expected |
| Sub-settle crossing, non-zero settle (`DW-67`) | ball crosses `sw_dragon_r` (`drop_target`, 20 ms) in 2 ticks | one `closed: true` at the crossing tick; `closed: false` only after the raw test has been false for `settleTicks` consecutive ticks | Today this emits **nothing at all** -- the defect this story closes |
| Contact bounce on the break | raw value flickers false-then-true inside the break window | no edge pair: the break window restarts, the switch stays reported closed | No error expected |
| Concave wall footprint (`DW-68`) | a `col_` wall whose plan outline has a reflex vertex | `export.py` fails naming the node and the number of vertices the hull dropped | non-zero exit; neither artifact written (both are built in memory then `os.replace`d) |
| Registry device absent from the document (`DW-69`) | `bd_lock` in `TABLE.ballDevices`, no matching `devices[]` entry | `loadCollision()` throws at load time naming `bd_lock` | boot error panel (AD-17), not a permanent runtime `eject_failed` |
| Zone-requiring switch with no `sw_` node | `s_drain` declared, no `sw_drain` object in the `.blend` | `export.py` fails naming `s_drain` | non-zero exit |
| Ball above the guides (`DW-53`) | ball at `z = 200 mm` driven laterally at the measured maximum speed | stays inside `x in [0, 514.4]`, `y in [0, 1066.8]` | No error expected |
| Draining ball below the deck (`DW-120`) | snapshot ball at table `y = -50` (inside the return channel) | not drawn; it reappears only when a device serves it back | No error expected |
| Non-English / non-`C:` Windows (`DW-46`) | `BLENDER` unset, `env.ProgramFiles` names a localized Program Files directory | `resolveBlender()` finds Blender under it | `BlenderNotFoundError` naming every candidate tried |
| Integration: the real conductor | `createLoop({ collisionDoc })` with the completed `TABLE` and regenerated document | boots with no load-time throw; a ball driven through a new zone surfaces a `SwitchEvent` for that switch in `FrameOutput` | a missing node or unknown property throws at load, before the first step |

</intent-contract>

## Code Map

Every anchor below was read during planning at `862ca17` (tree clean, branch `DW-1-epic2`). **2.1a's own Code Map line numbers are stale** -- these are current.

**Asset authoring -- the only sanctioned path into the geometry**

- `tools/make-placeholder-blend.py` (869 lines) -- generates `assets/src/dragonwar.blend`. Its header still claims to be a one-shot seeding tool superseded by AD-11; **git history contradicts it** (`424c0b8`, `a93d375`, `da129bf`, `494113f`, `046af7a`, `0ae3eed` each changed script and `.blend` together). Correct that header in this story.
  - Regeneration: `blender --background --factory-startup --python tools/make-placeholder-blend.py` (line 6). Not an npm script, not in CI.
  - `:45-124` constants (`PLAYFIELD_W_MM 514.4`, `PLAYFIELD_H_MM 1066.8`, `BALL_MM 26.99`, `WALL_T_MM 12`, `WALL_H_MM 50`, `GLASS_Z_MM 400`, `LANE_X0_MM 468.4`, `DRAIN_X0_MM 200`, `DRAIN_X1_MM 314.4`, and 2.1a's `GUIDE_T_MM 12`, `OUTER_GUIDE_T_MM 6`, `OUTLANE_WIDTH_MM 34.9`, `POST_RADIUS_MM 4`, **`GUIDE_Y_TOP_MM 420`**, `DIVIDER_Y_BOTTOM_MM 120`, `POCKET_OFFSET_*`). New shot-map constants go here.
  - `:213-287` below-deck and drain-ramp constants, with the long rationale blocks at `:128-260` and `:237-287`. **`BOTTOM_WALL_DRAIN_DROP_MM = 10.0` at `:287`** -- do not move it.
  - `:290-298` `octagon_points_mm()`; `:317-340` `_box_bmesh()`; `:343-365` `_prism_bmesh()` (arbitrary plan polygon extruded in z); `:368-383` `new_prism_mesh()`; `:386-405` `new_box_mesh()`; `:408-410` `set_props()`.
  - `:443-449` the three top-level empties -- **`cabinet_root` is created childless and `_ = cabinet_root` at `:449` is the standing marker for the first cabinet-mounted object.**
  - `:478-487` the plain-wall table of `(name, min_mm, max_mm)` tuples -- axis-aligned boxes go here.
  - `:519-529` `add_bottom_wall_quad()` and its two calls; `:531-552` `col_lane_deflector`, **the angled-prism prototype** (identity object transform, angled mesh vertices) and the node `DW-58` retires.
  - `:595-598` `add_guide_wall()`; `:600-603` `add_rubber_post()` (`surface='rubber_post'`); `:605-638` `add_channel_rail()`; `:640-664` `add_outlane_return_channel()`; `:666-727` `add_drain_triangle_side()` (docstring `:667-685` explains why the pocket closes at the tip).
  - `:757-787` the switch-zone block (`sw_shooter_lane`, `sw_trough_1..4`) -- **new `sw_` zones go here**; `:789-805` the `bd_` empties at their eject poses; `:807-812` `vis_playfield`; `:814-839` `l_insert_left`; `:842-849` the presentation selection list.
  - **Insertion point for the shot map: after `:755`, before the switch-zone block at `:757`.**
- `tools/export.py` (609 lines) -- validation is generic by prefix and `col_shape`, so new `wall` nodes need no export change. `:38` `COL_SHAPES`; `:95-100` `is_presentation_object()` (`col_`/`sw_` never reach the glb); `:103-117` names/uniqueness; `:157-197` `validate_properties()`; `:200-213` `validate_col_shapes()`; `:215-258` `validate_col_geometry_reducible()`; `:269-273` `validate_node_presence()`; **`:276-285` `validate_ball_devices_present()` -- the shape to copy for the new switch-zone completeness check**; `:304-328` `_convex_hull_2d()`; **`:340-386` `wall_footprint_mm()` -- line 378 computes the hull and nothing compares it to the input (`DW-68`)**; `:389-413` `build_collision_nodes()`; `:416-429` `build_switch_zones()`; `:432-455` device poses; `:500-503` the Blender version gate; `:508-519` validation order; `:522-571` the write path (in-memory build, `newline='\n'`, `os.replace`).
- `tools/export-assets.mjs` -- `pnpm export:assets`. `:50-62` `buildTableDump()` emits `{reference, switches, coils, ballDevices, lamps, lightGroups, physMaterials, nodes, surfaces}` -- **not `shots`/`flashers`/`shows`**; `:78-89` the Blender argv; `:110-136` the temp-file lifecycle.
- `tools/blender.mjs` -- `:44-54` `isExecutableCandidate()`; `:61-66` `pathCandidates()`; `:69-82` `expandBlenderFoundationDir()`; **`:84-104` `conventionalCandidates()` -- the `DW-46` defect: `'C:\\Program Files\\Blender Foundation'` hardcoded at `:86`, and the darwin (`:95`) / Linux (`:98-103`) literals reachable through no env hook**; `:112-142` `resolveBlender()`.

**The registry**

- `src/sim/table/dragonwar.ts` (254 lines) -- one `deepFreeze({...} as const)`; entry types are **inferred**, there is no `Table` interface and **no `satisfies` clause on `switches`**. `:36-43` `SettleClass` (`rollover | standup | drop_target | bumper_skirt | tilt_bob | button | slam`); `:58-62` `DeepReadonly`; `:109` `TABLE`; `:111-116` `reference`; **`:118-130` `switches` (11 today)**; **`:135-140` `coils` (4 today)**; **`:142-187` `ballDevices` -- `bd_trough` (parking, cap 4, `slots` in fill order, `ejectCoil`, `ballSearchOrder`, `servesInto`) and `bd_shooter` (non-parking, `entry`, no `ejectCoil` -- the launch coil is derived from the first `pulse` in `ballSearchOrder`)**; `:202-207` `lamps`/`flashers`/`shows`/**`shots: {}`**; `:218-231` the comment explaining why `col_`/`sw_`/`vis_` fall outside the device-name grammar; `:232-240` `nodes`; `:250-253` `physMaterials`.
- `src/sim/table/names.ts:32-39` -- `SwitchName`, `CoilName`, `BallDeviceName` etc. are `keyof typeof TABLE.*`; `:54-77` binds every seam contract to them. Adding a switch widens `SwitchName` everywhere for free.
- `src/sim/table/tuning.ts` (590 lines) -- `:33-44` `TuningEntry<T>` (`value`, `source`, `confidence`) and `entry()`; **`:171-187` `switchSettleMsByClass`, `satisfies Readonly<Record<SettleClass, TuningEntry<number>>>` -- adding a `SettleClass` member is a compile error until this gains it**; `:280` `troughEjectSpeedMmPerS` and `:292` `autolaunchSpeedMmPerS: 2500` (**the full-strength plunge speed**); `:331-354` 2.1a's three `unverified` geometry tunables; `:390-405` the `...Ms -> ...Ticks` types; `:407-430` `msToTicks()`; **`:465-483` `assertNoNestedMsKeys()` -- a new duration must be authored at TUNING top level**; `:485-542` `resolveTuning()`; `:560-579` `plungerSpeedByHoldMs()` (clamped lerp `plungerMinSpeedScale 0.3 -> plungerMaxSpeedScale 1.0` across `[0, 500]` hold ticks, scaling `autolaunchSpeedMmPerS`).

**Switches, zones and devices**

- `src/sim/physics/geometry.ts:29-57` `segmentIntersectsBox()` -- the shared slab method; doc `:17-21` states the swept-segment contract. **The mechanism AC 5 names already exists**; this story proves it at speed and fixes the debounce on top of it.
- `src/sim/physics/switches.ts` -- `:31-35` `BallMovement`; `:51-61` `parkingDeviceOwnedSwitches()` (derived from `TABLE.ballDevices`, so `bd_lock`'s `s_lock_1..3` are excluded automatically); `:86-141` `createSwitchTracker()`; **`:108-110` the swept call; `:112-133` the debounce -- `pendingSince` is set on the first differing tick and `elapsedTicks >= settleTicks` is tested the same tick, so a new value must hold `settleTicks + 1` consecutive ticks (`DW-67`)**; `:137-139` `currentState()`.
- `src/sim/physics/cabinet/index.ts:176-177` -- a **second, independent** `LevelTracker` with the same `reported`/`pendingSince`/`pendingValue`/`settleTicks` fields for `s_tilt_bob`/`s_slam_tilt`. Any `DW-67` fix must cover both or they diverge.
- `src/sim/physics/machine.ts:224-280` -- `beforeMm` captured at `:233` before `physics.step()` (`:237`), `afterMm` at `:275`, tracker stepped at `:279`, `detectEntries` at `:280`; `:290-308` `deviceSlots`.
- `src/sim/physics/devices.ts` -- `:65-76` failure events; `:88-108` `DeviceMechanics`; `:112` `primaryPulseCoil()`; `:302` the `parked` Set (added in anticipation of a second parking device); `:312` the same `segmentIntersectsBox` call for device entry.
- `src/sim/rules/devices.ts:47-71` `processSwitchEvents()` -- the only consumer of `SwitchEvent` (AD-19). Resolves everything by scanning `TABLE.ballDevices`, never by literal, so `bd_lock` is picked up with no edit.
- `src/sim/physics/loader/index.ts` -- `:65` `TOLERANCE_MM`; `:82-100` `CollisionNodeDoc` / `SwitchZoneDoc`; `:133-141` `LoadedCollision` (**exposes `physics`, `switchZones`, `devices`, `flippers` -- not node bboxes**); `:181-195` `assertHandshake`; `:293-305` `findNode()` (missing and duplicate both throw); `:320-353` `assertReferenceDimensions()`; `:477-492` `assertConvexCcwFootprint()`; `:719-800` `loadCollision()`; **`:771-784` document-switch -> TABLE and `:786-797` document-device -> TABLE, the one-way checks `DW-69` names**; `:748-756` the plane-node whitelist.

**Presentation**

- `src/presentation/scene/balls.ts` `syncBalls()` -- renders every `snapshot.balls` entry at `toScene(ball.pos)` with **no culling and no z drop** (`DW-120`).
- `src/presentation/camera/fixed-camera.ts` -- `:35` reads `TABLE.reference.playfieldMm`; `:51-52` `CAMERA_POSITION_MM = {x: w/2, y: -700, z: 1300}`, `CAMERA_TARGET_MM = {x: w/2, y: h/2, z: 0}`; `:58-72` `createFixedCamera()` (`FreeCamera`, default FOV, `minZ 0.05`, `maxZ 100`). **The standoff and height are hand-picked absolutes; the comment at `:10-17` says so.** No matrix helper is exported -- a test takes `getViewMatrix()`/`getProjectionMatrix(true)` off the returned camera.
- `src/presentation/scene/create-engine.ts:255-275, 334` -- `loadAndRenderOnceForTests()`, the one sanctioned headless presentation seam (AD-15).
- `src/presentation/scene/playfield.ts:26-44, 55-61, 84-135` -- `getRequiredNode()`, `resolvePlayfieldNodes()`, `applyPitch()` and its preconditions.

**Recorded artifacts this story invalidates**

- `test/replays/*.golden.json` (5) -- all carry `assetHash: "3806f64"`, `tableHash: "5985b9f7"`. **Both move**: `assetHash` because the collision document changes, `tableHash` because `TABLE` gains ~30 switches and 4+ coils; `gameStart.tuning` moves if any tunable is added. `src/sim/loop/replay.ts:213-253` throws `StaleReplayHeaderError` naming the field, before any hash is computed. `assetHash()` at `:144-146` hashes the parsed document, never file bytes. There is deliberately **no** re-record script (`src/host/boot.ts:265-268`); expected values are re-derived from the failing assertion.
- `test/replay-goldens.test.ts` -- `:107-117` the on-disk completeness gate (do not add a sixth golden), `:189-262` the falsifiability matrix and `PARITY_INERT`, plus the per-golden scenario `describe`s. A refresh that requires editing a scenario assertion, `durationTicks` or `PARITY_INERT` is a scenario change, not a refresh -- Block If.
- `test/switch-zones.test.ts:153-233` -- **currently pins the make-debounce as intended** (`:164` "a filtered bounce, not a delayed one", `:191`, `:213`). These are the assertions `DW-67` inverts; rewrite them deliberately, do not delete them.
- `test/export-py-skip-visibility.test.ts:64, 78, 91, 113` -- structurally pins **exactly 22** `it()` cases in the one Blender-gated `describe`, and the `expectedSkips` formula. Adding a Blender-gated case turns this red; update the pin deliberately.
- `test/asset-contract.test.ts:189-205, 209-245, 248-284, 341-370, 429-446` -- per-bat x extent, trough-zone tiling `200 -> 314.4`, deflector/wall-footprint invariants, the three 2.1a tunable gates, and the `DRAIN_DROP_MM = 10.0` gate. **`col_lane_deflector`'s invariants move when `DW-58` retires it.**
- `test/flipper-sweep-clearance.test.ts:247, 255-269` -- the drain-end throat gate and the `BOTTOM_WALL_DRAIN_DROP_MM` lower-bound derivation. Slingshot bodies land near this.
- `test/drain-routing.test.ts:56-63 (nodeBboxMm), 127, 175, 225-229, 264, 313` -- AC 7/8/10 blocks and the full-span sweep. New geometry above the guides must not change these outcomes.
- `test/collision-loader.test.ts:62, 75, 268-373, 487-497` -- **hand-built minimal documents** that the `DW-69` reverse check will reject until they gain their `devices` entries.
- `test/machine-serve-drain.test.ts:409-418` -- the `468.4` literal (`DW-65`), twice.
- `test/scene-smoke.test.ts:311-336, 345-353` -- the eight-corner NDC projection and the drain-vs-far orientation pin; the shape AC 8's framing check copies.
- `test/feel-test-docs.test.ts:38-46, 55-56, 70-72, 92-94, 101-102, 110-130, 138-143, 146, 157-158` -- hard-pins the literal measured numbers, the item names, the ISO date heading, a real linked golden per item, and the `## Verdict` line.
- `test/table.test.ts:152-153, 161` -- `giChannels` exactly three; `TABLE.shots` exactly `{}` (**keep it so**).
- `test/ad7-device-slots.test.ts:48-52, 66-73` -- passes by asserting `check:ad7` fails naming `DW-70`, `AD-7`, `bd_trough`, `[true,true,true,true]` and `[true,true,true,false]`. Untouchable.

**Read-only evidence (no edit required)**

- `col_` and `sw_` nodes never reach the glb (`export.py:95-100`), so every shot this story draws is **invisible in the browser** -- the committed glb holds only `playfield_root`, `cabinet_root`, `pivot_pitch`, `vis_playfield`, `l_insert_left`, `bd_trough`, `bd_shooter`. A framing check must project the **collision document's** `bboxMm` entries, exactly as 2.1a's manual check was corrected to read the collision document rather than the glb.
- `l_insert_left` occupies table `x 247-267, y 490-510` -- dead centre of the free space. Draw around it or move it deliberately.
- Free space today: `y in [420, 950] x in [0, 468.4]` is completely empty; `y in [950, 1066.8]` is empty but for `col_lane_deflector` (`x 480.4-514.4, y 976-1010`). `col_wall_lane` stops at `y = 950` so a launched ball can cross into the field.
- The four posts the shot map must meet: `col_post_divider_l_hi` (40.9, 420), `col_post_outer_l_hi` (237.9, 420), `col_post_outer_r_hi` (276.5, 420), `col_post_divider_r_hi` (427.5, 420).
- `CONTACT_SURFACES` (`src/sim/contracts/events.ts:30-43`) already carries unused members `rubber_band`, `ramp`, `target`, `bumper`, `dragon` -- new surfaces validate with **zero** code change. `ContactKind` (`:47-55`) already reserves `drop_target_down`, `bank_reset`, `spinner_tick`.
- `TABLE.physMaterials` has only `default` and `flipper_rubber`, pinned against `Object.keys(TUNING.materials)` by `test/asset-contract.test.ts`. A new named material must be added in both places (Story 2.2 owns the sling/pop/rubber material assignment; prefer `default` here).
- The `DW-46` fix is testable: `test/blender-resolve.test.ts` runs **unconditionally** and already injects through the `env` parameter.

**Driving a ball and observing it -- read at the 2026-08-31 re-plan, for the behavioural tests the author front-loaded**

- **`SwitchEvent` is observable at exactly one tier, and it is not the one the frozen I/O matrix names.** `createMachine(collisionDoc, tuning)` (`src/sim/physics/machine.ts:140`) returns `MachineStepResult { switchEvents, contactEvents, semanticEvents }` from `machine.step(tick, frame, commands)` (`:73-84`). `createLoop({ collisionDoc })` (`src/sim/loop/index.ts:186-206`) builds `switchEvents` locally at `:340`, feeds them straight to `rulesStep()` and **discards them** -- `FrameOutput` is `{ snapshot, events, contactEvents, commands }` (`src/sim/contracts/snapshot.ts:109-129`) with no `switchEvents` channel. See Design Notes; this corrects the frozen matrix's last row and re-sites the Integration AC.
- `test/drain-routing.test.ts:77-125` `releaseBall()` -- the canonical hand-driven harness and the shape 2.1a converged on. Per call: `loadCollision(loadDoc())`, `physics.setGravity(...)`, `resolveTuning()`, `createFlipperMechanics`, `createDeviceMechanics`; clear the trough first (`:89-92`); place a ball via `new Ball(0, data, state, new Vertex3D(vx,vy,vz), TABLE_DATA)` -- **the 4th argument is the initial velocity**; then per tick `applyFrame` -> capture `beforeMm` -> `physics.step()` -> capture `afterMm` -> `detectEntries`. The file header `:17-21` records why `createLoop` cannot be used: it has no "place a ball anywhere" hatch, by design (AD-6, devices spawn balls).
- `test/elasticity-falloff.test.ts:100-138` -- the only worked conversion of mm/s into the constructor's VU/T velocity. `drain-routing` always passes `(0,0,0)`; a shot test must not.
- `test/machine-serve-drain.test.ts:442-535` -- driving `createMachine` + `rulesStep` by hand, including repositioning a live registered ball mid-test. `:506-518`: reset `ball.hit.vel`, `.angularVelocity` **and** `.angularMomentum`, or residual spin walks the ball sideways under friction. Sibling hardcoded literals to the `DW-65` pair: `:348-351`, `:493`.
- `test/flipper-collision.test.ts:48-79` (`buildFlipperHarness`, `spawnBallAt`, `ballSpeed`, `ballPosMm` -- file-local, not shared) and `:449-477` (**the flipper speed assertion is a ratio in VU/T, `maxSpeed > speedBefore * 10`; no absolute mm/s figure exists anywhere in the repo**).
- Units: `ballSpeed()` is VU/T; `snapshot.balls[i].speed` is mm/s (`src/sim/contracts/snapshot.ts:27-35`); `physicsVelocityToTableMmPerS()` at `src/sim/loop/index.ts:168-172`; `MM_PER_VU = 0.53975` at `src/sim/table/frames.ts:51`; `mm/s = VU/T * MM_PER_VU * 100`.
- Determinism: no seed hook and none needed -- `deterministicScatterUnit()` returns 0 (`src/sim/physics/ball/ball-hit.ts:67-70`) and both `Math.random() < 0.5` branches in `player-physics.ts:307, 390` are pinned. **Gravity has no x-component**, so a symmetric release drifts a structural 0.00 mm and symmetry assertions are vacuous (`test/drain-routing.test.ts:319-336` demotes one for this reason).
- `test/switch-zones.test.ts:20-22` (synthetic `zone()` helper -- this suite never touches real geometry), `:154-162` (`tuningWithButtonSettleTicks(n)`), `:164-241` (the three settle cases). `BallMovement` is `{ before, after }` with **no `ball` field**, unlike `devices.ts`'s `BallStepMovement`.
- `src/sim/contracts/time.ts:37` `TICK_HZ = 1000` (1 tick = 1 ms; AD-3 forbids naming it outside that file -- express budgets in ticks). `vitest.config.ts`: `environment: 'node'`, `testTimeout: 60_000` **per case**. Sweep bound convention: 4000 ticks, 8000 for a full sweep.
- `DW-77` spawn clearance is the dominant defect class in ball-driving tests: `test/drain-routing.test.ts:188-237, 339-346` (three occurrences), `:239-266` with controls at `:306-310` (how to bound an excluded band so it fails loudly).

**Projecting geometry through the fixed camera (AC 8) -- also read at the re-plan**

- `test/scene-smoke.test.ts:311-360` -- `new NullEngine()` (512x256, and that size **is** the framing contract), committed glb as a base64 data URL (`:35-39`), `loadAndRenderOnceForTests(...)`, camera as **`scene.activeCamera`** (`:317`), `viewProj = getViewMatrix().multiply(getProjectionMatrix(true))` (`:326-328`), `Vector3.TransformCoordinates` then NDC x/y in `[-1, 1]` (`:330-336`). **`:338-353` is the orientation clause added after review found that in-viewport alone left all 32 corner assertions green with the camera behind the far edge or under the playfield** -- the precedent for AC 8's second and third clauses.
- `src/presentation/scene/create-engine.ts:255-321` / `:334-341` -- returns `{ scene, firstFrameMs, playfieldNodes }`; the camera is created at `:269` and **its return value is discarded**, so `scene.activeCamera` is the only reach. `applyPitch` runs at `:283`, so `playfieldNodes.playfieldRoot.computeWorldMatrix(true)` is the transform that lifts a table-mm point into pitched world space. `onFrame` (`:259, :306`) is the seam for exercising the real per-frame path.
- `src/presentation/camera/fixed-camera.ts:51-52` -- `CAMERA_POSITION_MM` / `CAMERA_TARGET_MM` are **module-private**; `createFixedCamera(scene)` is the only export and there is no matrix helper. A projection test needs neither: it reads the realised matrices. **Read-only in this story.**
- `test/asset-contract.test.ts:20` (`COLLISION_PATH`), `:55-65` (`CollisionDocForTest`), `:67-69` (`readCollisionDoc()`, called fresh inside every `it()`), `:341-371` (the gate shape to imitate), `:373-390` (the convention for justifying a gate with a recorded mutation line), `:413-425` (the three-assertion variant with a clearance clause). Document shape confirmed: 28 `col_` nodes all carrying `bboxMm` + `footprintMm` + `zLowMm`/`zHighMm`; **`sw_` entries live in `switchZones` with `minMm`/`maxMm` and have no `bboxMm`**.
- `test/ball-render.test.ts:12-29, 49-64, 78-92, 110-116` -- the NullEngine ball-observation pattern for `DW-120`; `:87`'s material assertion exists because NullEngine never rasterises, so visibility is a property, not a pixel. `:200-203` notes the suite calls `syncBalls()` directly rather than through `onFrame`.
- `src/sim/loop/replay.ts:139-140` -- **`tableHash()` hashes the whole `TABLE`**, so anything added to the registry (including a provenance string) is inside the golden identity. `assetHash()` at `:142-145` hashes the parsed document, never bytes.
- `src/sim/table/tuning.ts:33` `Confidence`, imported from `./tuning` by `src/host/dev/tuning-panel.ts:22` and `test/tuning.test.ts:13` -- the two call sites a re-export must keep working.

## Tasks & Acceptance

**Execution:**

1. `tools/make-placeholder-blend.py` -- correct the stale "one-time seeding tool, not a build step" header to state what git history shows: this script is the reviewable record of the `.blend` and the only sanctioned edit point -- so the next contributor does not hand-edit the binary.
2. `tools/make-placeholder-blend.py` -- add the shot-map constants block beside `GUIDE_Y_TOP_MM` (`:121`): loop lane width and outer radius, ramp entrance width / height / gradient, Dragon body footprint and its offset from centre, Lock lane clear width, Mouth pose and aim, drop-target face size and bank pitch, Top-lane width and spacing, sling span, pop nest radius. Every one is authored; give each a one-line comment saying so and naming what would confirm it.
3. `tools/make-placeholder-blend.py` -- draw the **Left Loop** and **Right Loop** as chains of convex prisms from the two `*_divider_*_hi` posts at `y = 420` up each side and across the top, exits feeding straight toward the flippers (CAP-26, the layout rule at `addendum.md:212`). Both lanes join across the top so a full orbit passes both. Free ends terminate at `rubber_post` nodes.
4. `tools/make-placeholder-blend.py` -- place the **spinner gate** in the **Left Loop only** (SPEC CAP-26 success clause and `machine-behaviour.md:72` settle the PRD's `[ASSUMPTION: the Left Loop]`), as a `col_` gate body plus its `sw_spinner` zone. No revolution counting -- that is Story 2.3.
5. `tools/make-placeholder-blend.py` -- draw the **Ramp**: entrance right of centre so the **left flipper** shoots it, an authored height and gradient, a return rail landing in an inlane. Author the ramp bed as convex prisms; nothing may cross the plunger lane.
6. `tools/make-placeholder-blend.py` -- draw the **Dragon body left of centre** (`decisions-rejected.md:14`, `machine-behaviour.md:9`: off-centre so a rejection deflects to a flipper; the **right flipper takes it straight, the left flipper backhands it** -- which places it left of `x = 257.2`), with the **Lock lane between its legs** as a narrow lane admitting a precise shot and a body face that a slightly-off shot strikes (FR-29). Author the `bd_lock` empty at the **Mouth** pose above the body, aimed down-table toward the flippers (AD-6: "the Lock's pose *is* the Mouth").
7. `tools/make-placeholder-blend.py` -- draw the **six-target DRAGON bank** as six `col_` target faces in a bank, legible from the fixed camera, and the **three Top lanes** in the upper field on the launched ball's path. Targets are ordinary collidable bodies here; drop/reset is Story 2.3.
8. `tools/make-placeholder-blend.py` -- draw **two slingshot** bodies above the inlanes and a nest of **exactly three pop bumpers** (`col_pop_1..3`, author-decided 2026-08-31; see ## Boundaries & Constraints -- the count is settled, not a judgment left to implementation). **Verify against `test/flipper-sweep-clearance.test.ts` that no sling body enters the bat's swept envelope or narrows the 0.137 mm drain-end throat.**
9. `tools/make-placeholder-blend.py` -- **`DW-58`**: delete `col_lane_deflector` (`:531-552`) and let the Right Loop's upper arc turn the launched ball into the field, which is what a real machine does; record in the script's comments that `bd_trough`'s eject pose at the lane foot is the real kicker position and is required by AD-6 to lie inside `sw_shooter_lane` (`test/device-eject-pose.test.ts:63-64`), so it is derived, not invented. Update `test/asset-contract.test.ts:248-284`'s deflector invariants accordingly.
10. `tools/make-placeholder-blend.py` -- **`DW-53`**: introduce `PERIMETER_WALL_H_MM = GLASS_Z_MM` (derived, not invented) and raise `col_wall_left`, `col_wall_right`, `col_wall_top` and `col_wall_lane` to span `z 0 -> PERIMETER_WALL_H_MM`, leaving interior guides at `WALL_H_MM = 50` so the Ramp can pass over them. Comment the reason at the constant.
11. `tools/make-placeholder-blend.py` -- add one `sw_` zone per zone-requiring switch in the block at `:757-787`, following the `set_props(col_shape='box', switch=..., surface=..., phys_material='default')` pattern. **`sw_` zones cannot be rotated** (`export.py:252-256`); size each so a maximum-speed ball's per-tick swept segment cannot straddle it.
12. `src/sim/table/dragonwar.ts` -- complete `switches` with `s_loop_l_in/out`, `s_loop_r_in/out`, `s_spinner`, `s_ramp_enter/made`, `s_dragon_d/r/a/g/o/n`, `s_dragon_body`, `s_lock_lane`, `s_lock_1..3`, `s_top_1..3`, `s_inlane_l/r`, `s_outlane_l/r`, `s_sling_l/r`, `s_pop_1..3` and `s_drain`, each with its `settleClass` (see Design Notes for the mapping and its rationale); complete `coils` with `c_sling_l/r`, `c_pop_1..3`, `c_dragon_bank_reset` and `c_mouth`; add `ballDevices.bd_lock` as `kind: 'parking'`, `capacity: 3`, `slots: ['s_lock_1','s_lock_2','s_lock_3']`, `ejectCoil: 'c_mouth'`, `ballSearchOrder: [pulse c_mouth, pulse c_mouth, recover]`, **no `servesInto`** (the Mouth ejects onto open playfield, not into a zone). Add the new `col_`/`sw_`/`bd_` names to `nodes` **only** where a loader resolves them by name -- generic `col_` geometry is validated by grammar and shape alone (`dragonwar.ts:218-231`).
12a. `src/sim/table/dragonwar.ts` and `src/sim/table/tuning.ts` -- **record the authored pop-bumper count in `TABLE` with `source` and `confidence`** (AD-15; the author's 2026-08-31 instruction), so `3` reads as an authored decision rather than a sourced fact. `TABLE` has no provenance mechanism today (switch entries are `{ settleClass }`; coil entries are typed `{} as Record<string, never>`, which **forbids** adding a field to them), so: move the `Confidence` union from `tuning.ts:33` **down** into `dragonwar.ts` beside `SettleClass` and re-export it from `tuning.ts` (`tuning.ts` already imports from `dragonwar.ts`, so the reverse direction would be a cycle; re-exporting keeps `src/host/dev/tuning-panel.ts:22` and `test/tuning.test.ts:13` importing from `./tuning` unchanged). Then add a top-level `TABLE.authoredCounts` block whose entries carry `{ value, source, confidence }` in the exact `TuningEntry` shape, with `popBumpers: { value: 3, source: '<authored 2026-08-31: no artifact states a pop count; three matches the Top-lane count and the standard arrangement for this shot density>', confidence: 'unverified' }`. **Do not add it to `buildTableDump()`** (`tools/export-assets.mjs:50-62` emits a fixed key set; a provenance record is not geometry and must not reach `export.py`). Note the consequence at the block: `tableHash()` hashes the **whole** `TABLE` (`src/sim/loop/replay.ts:139-140`), so this text is inside the golden identity -- later prose edits to `source` re-break all five headers. That is intended (the count is part of the table's identity), and the comment must say so.
12b. `test/table.test.ts` -- pin the provenance record against the declared set: `TABLE.authoredCounts.popBumpers.value` equals the number of `s_pop_*` keys in `TABLE.switches` **and** the number of `c_pop_*` keys in `TABLE.coils`, and its `confidence` is `'unverified'`. Without this the record can silently drift from the switch set it documents, which would be worse than not recording it.
13. `src/sim/physics/switches.ts:112-133` -- **`DW-67`**: make the tracker **latch on the make and debounce the break**. A raw `true` that differs from the reported value emits `closed: true` on that same tick; a raw `false` emits `closed: false` only after the raw test has been false for `settleTicks` consecutive ticks, and any raw `true` inside that window cancels it with no edge pair. Document the reasoning at the function, citing FR-11 and AD-2's "per-switch hysteresis".
14. `src/sim/physics/cabinet/index.ts:176-177` -- apply the identical semantics to the cabinet `LevelTracker` so the two implementations cannot diverge, and add a test asserting they agree for a given raw sequence.
15. `test/switch-zones.test.ts:153-233` -- rewrite the three settle assertions to pin the new semantics (immediate make; break held for `settleTicks`; a bounce inside the **break** window emits no edge pair), keeping their discriminating power. Add the case that was previously impossible: a crossing shorter than `settleTicks + 1` ticks against a `drop_target` (20 ms) switch **does** emit exactly one make. This suite synthesises its own zones (`:20-22`) and injects synthetic tuning via `tuningWithButtonSettleTicks(n)` (`:154-162`, spreading `TUNING` and overriding `switchSettleTicksByClass.button.value` while `TABLE.switches.s_start.settleClass` stays real) -- keep that pattern and extend it to the classes this story makes reachable. Note **why this matters**: `standup` (8 ms), `drop_target` (20 ms) and `bumper_skirt` (2 ms) are used by no Epic 1 switch, so this file is the **only** coverage of the `settleTicks > 0` branch in the whole suite. Also keep the existing case's discrimination that a segment merely *starting* inside a box still reads closed -- a genuine exit needs both endpoints outside.
16. `test/switch-max-speed.test.ts` (new) -- **AC 5**. Measure the fastest ball the sim produces on a legitimate input path (full-strength plunge = `plungerSpeedByHoldMs` at max hold = `autolaunchSpeedMmPerS`, plus what a driven flipper adds -- measure it, do not assume), record both figures, then drive a ball through **every** zone-requiring switch's zone at or above that speed along its own approach direction and assert exactly one make per pass, one break after, and no missed pass. Derive every position from the collision document, never a literal (switch zones are in `doc.switchZones` as `minMm`/`maxMm`, **not** in `doc.nodes`, and carry no `bboxMm`).
    **Harness:** `createMachine(collisionDoc, tuning)` (`src/sim/physics/machine.ts:140`) -- `machine.step()` returns `switchEvents` (`:73-84`) and is the **only** tier where a `SwitchEvent` is observable at all (see Design Notes). Reposition a served ball with `test/machine-serve-drain.test.ts:442-535`'s recipe, which resets `ball.hit.vel`, `.angularVelocity` **and** `.angularMomentum` -- omit any one and residual spin walks the ball sideways under friction.
    **Units trap, and it is the likeliest way to get this wrong:** `ballSpeed(ball)` (`test/flipper-collision.test.ts:73-75`) is raw physics **VU/T**; `snapshot.balls[i].speed` is table **mm/s**. The conversion is `mm/s = VU/T * MM_PER_VU (0.53975) * 100` (`src/sim/loop/index.ts:168-172`, `src/sim/table/frames.ts:51`); the `* 100` undoes VP's 1 T = 10 ms convention. Record the measured maximum in **mm/s** and say which figure each recorded number is.
    **Measurement recipe:** plunge leg -- `createLoop({ collisionDoc })`, `pulseCoil('c_trough_eject')`, settle ~300 ticks, hold `plunger` past `plungerMaxHoldMs` (500), release, poll `out.snapshot.balls[0].speed` per tick for a maximum (`test/plunger.test.ts:80-115` is the shape; full strength is `autolaunchSpeedMmPerS = 2500 mm/s`). Flipper leg -- **nothing in the repo records it**: `test/flipper-collision.test.ts:449-477` asserts only `maxSpeed > speedBefore * 10` in VU/T with no absolute figure and no ceiling. Measure it and record it; that measurement is a deliverable of this task, not a lookup.
16a. `test/shot-routing.test.ts` (new) -- **AC 1's behavioural pin, and the single most important test in this story.** One `describe` per shot (Left Loop, Right Loop, Ramp, Dragon body, Lock lane, DRAGON bank, Top lanes, both slingshots, the three pops): drive a ball into the shot at a plausible shot speed along the shot's own approach direction and assert the **observables**, never the geometry -- (a) the shot's switches close in approach order (`machine.step().switchEvents`), (b) the shot's exit or its most common miss arrives **playable at a flipper**: the ball reaches the flipper-reachable band above the bats with a downward velocity, rather than draining between them or coming to rest, and (c) the ball is not still in play at the tick bound, which is how a strand reads.
    This is the `test/drain-routing.test.ts` shape (`:77-125` `releaseBall()`), which is the shape that finally worked on 2.1a, and **not** its three-sample-point predecessor: sweep several entry offsets per shot rather than one, so a shot that only works down its exact centreline fails here instead of at smoke. Harness and traps, all of which cost 2.1a real iterations:
    - **Launch at a chosen speed** via `Ball`'s 4th constructor argument; convert mm/s to VU/T as `test/elasticity-falloff.test.ts:100-138` does. `drain-routing` passes `(0,0,0)` and cannot shoot a ball up a loop.
    - **Spawn clearance is the dominant defect class here (`DW-77`)**: a ball spawned inside a body measures the solver *ejecting* it, and the ejection usually biases toward the expected answer. Verify every release point clear of every node footprint by more than a ball radius using the hoisted `nodeBboxMm()` (task 21); `test/drain-routing.test.ts:188-237, 339-346` records three separate occurrences.
    - **Gravity has no x-component** (`src/sim/physics/ball/ball-hit.ts:67-70`; scatter is pinned to 0 and there is no seed hook). A symmetric release therefore drifts a structural 0.00 mm and any symmetry-based assertion is vacuous -- `test/drain-routing.test.ts:319-336` demotes one for exactly this reason. Assert arrival, not symmetry.
    - **Equilibrium traps are real**: a ball released on a flipper pivot x, or on any flat top face, never resolves under x-free gravity. If a shot needs an excluded band, bound it with explicit control cases at the exclusion edge (`:239-266` with its four controls at `:306-310`), so a widening dead zone fails loudly instead of being silently skipped.
    - Bound each case (`SWEEP_MAX_TICKS = 8000`, i.e. 8 s at `TICK_HZ = 1000`), embed `lastPosMm` in every failure message, and hoist the document parse -- `nodeBboxMm()` re-reads and re-parses `dragonwar.collision.json` on **every** call, and `vitest.config.ts`'s `testTimeout` is 60 s **per case**.
17. `tools/export.py` -- **`DW-68`**: in `wall_footprint_mm()` (`:340-386`), after `_convex_hull_2d()` at `:378`, `fail()` when the hull has fewer points than the mesh's distinct plan-view outline, naming the node and how many vertices were dropped, so a concave footprint can never export silently as a filled block.
18. `tools/export.py` -- add `validate_switch_zones_present()` beside `validate_ball_devices_present()` (`:276-285`): every `TABLE.switches` entry that requires a zone (derived -- see Design Notes) must have at least one `sw_` object naming it. Call it in `run()`'s validation order at `:508-519`.
19. `src/sim/physics/loader/index.ts:786-797` -- **`DW-69`**: add the reverse check -- every key of `TABLE.ballDevices` must appear in `document.devices`, throwing at load time naming the missing device, so a wired-looking coil can never degrade to a permanent runtime `eject_failed` (AD-17: load-time faults reach the error panel). Update the hand-built documents in `test/collision-loader.test.ts:62, 75` and add the paired negative test.
20. `tools/blender.mjs:84-104` -- **`DW-46`**: read `env.ProgramFiles` and `env['ProgramFiles(x86)']` for the Windows base directories (keeping the current literal as a last-resort fallback), and route the darwin and Linux candidate lists through an injectable base so `test/blender-resolve.test.ts` can exercise them on any platform. **No Blender executable path enters a tracked file.** Extend that test to cover a localized Windows Program Files and both POSIX branches.
21. `test/machine-serve-drain.test.ts:409-418` -- **`DW-65`**: replace both `468.4` literals with a value read from the committed collision document. Hoist `nodeBboxMm()` (`test/drain-routing.test.ts:56-63`) into `test/util/` and use it from both files, keeping its named-throw behaviour. **`test/util/` does not exist yet** -- create it; it gains four consumers in this story (`drain-routing`, `machine-serve-drain`, and the new `shot-routing` and `switch-max-speed`), so give it the parse-hoisting the per-call re-parse currently lacks. The sibling literals at `:348-351` (the `484.4`/`510.4`/`10`/`60` shooter-lane rest box) and `:493` are the same defect class but are **not** named by `DW-65`: derive them too if the hoisted helper makes it a one-liner, and say in the commit which ones were left, rather than silently widening or silently skipping.
22. `src/presentation/scene/balls.ts` -- **`DW-120`**: stop drawing balls below the playfield's front edge (table `y < 0`), which is where the below-deck return runs. Add a NullEngine-tier test in `test/ball-render.test.ts`'s existing shape (`:12-29` engine + committed-glb data URL, `:49-64` `buildSnapshot(overrides)`): a snapshot ball at `y = -50` is not visible, one at `y = +50` is. NullEngine never rasterises, so "visible" is observed as a **property**, not a pixel -- either `scene.getMeshByName('ball_0')` returning null or `mesh.isVisible === false`; pick one and assert it both ways round. (`:87` is the precedent: a material assertion added because a mesh can pass every geometry check and still render invisibly.) Record at the call site why culling was chosen over a real Z drop (see Design Notes).
23. `docs/decisions.md` (new file) -- **AC 7**. Create it in the four-column form of `_bmad-output/specs/spec-dragonwar/decisions-rejected.md` (`| Decision | Chosen | Rejected / deferred | Why |`, with the `(YYYY-MM-DD)` suffix in the *Chosen* cell). Record: the **Ramp's return inlane** (FR-27's `[ASSUMPTION: return side]`, SPEC `CAP-27`, under OQ-6 -- see the labelling note in Design Notes); the **OQ-5** Lock-lane decision with its evidence; the spinner on the Left Loop; the Dragon's side; and the **pop-bumper count of three** (author, 2026-08-31 -- answered, no longer pending; its *Why* cell says plainly that no artifact stated a count and three was chosen to match the Top-lane count and the standard arrangement for this shot density, which is the same honesty the `TABLE.authoredCounts` record carries in code).
24. `docs/feel-test.md` -- **AC 6**. Add a dated entry with one `### <Shot>` section per shot (Left Loop, Right Loop, Ramp, Dragon, Lock lane, DRAGON bank, Top lanes), each opening with the token **`pending-author`**, following the established entry shape and never rewriting the entries above it. Append this story's re-measurement date to the `## Environment` Date bullet. Update any build-side measured number this story's geometry moves, and keep every existing pinned literal in `test/feel-test-docs.test.ts` true.
25. `test/asset-contract.test.ts` -- add dimensional gates over the committed collision document for the load-bearing new figures (loop lane clear width, ramp entrance width, Lock lane clear width, Dragon offset from centre, bank width, Top-lane spacing, perimeter wall height), in the same shape as the existing tip-gap (`:341-350`) and outlane-width (`:352-370`) gates -- `readCollisionDoc()` fresh inside each `it()`, `doc.nodes.find(n => n.name === ...)` with a named `toBeDefined()` guard, `toBeCloseTo(..., 1)` -- plus the switch-zone completeness assertion (read from `doc.switchZones`' `minMm`/`maxMm`; `sw_` entries are **not** in `doc.nodes` and have no `bboxMm`). Follow `:373-390`'s convention of a comment block naming the review date, why the behavioural test alone was not falsifying, and the `// mutation: ... -> ...` line. **A dimensional gate alone is not enough either, and neither is a behavioural pin alone** -- 2.1a shipped both failure modes: AC 8 and AC 10 had to gain dimensional gates at review, and AC 1's dimensional checks all passed over outlanes that could not route a ball. The two halves close AC 1 together; task 16a is the other half.
26. `test/vertical-containment.test.ts` (new) -- **AC 9, `DW-53`**: a ball at `z = 200 mm` driven laterally at the measured maximum speed stays inside the playfield bounds on every side, and a control at `z = 25 mm` does too.
26a. `test/shot-map-legibility.test.ts` (new) -- **AC 8**, now that the Backglass clause has moved to Story 2.6. This is the **first** test to project collision-document geometry through the fixed camera, so it bridges two existing conventions: `test/asset-contract.test.ts:67-69`'s `readCollisionDoc()` for the feature bboxes, and `test/scene-smoke.test.ts:311-360`'s NullEngine recipe for the camera. Mechanics: `new NullEngine()` (512x256 -- that size **is** the framing contract, `fixed-camera.ts:37-50`), `loadAndRenderOnceForTests(engine, glbDataUrl(bytes), { pluginExtension: '.glb' })`, take the camera as **`scene.activeCamera`** (`:317`) -- `createFixedCamera()` is never imported and its return value is not exposed by `create-engine.ts` anyway -- build `viewProj = camera.getViewMatrix().multiply(camera.getProjectionMatrix(true))`, and for each named feature lift its `bboxMm` corners into world space through `playfieldNodes.playfieldRoot.computeWorldMatrix(true)` (which carries `applyPitch`, `create-engine.ts:283`) before `Vector3.TransformCoordinates`. **`col_` nodes never reach the glb** (`is_presentation_object()`, `export.py:95-100`), so `scene.getMeshByName` cannot find a single shot-map feature -- the collision document is the only source, which is precisely why this test does not exist yet.
    Assert all three clauses of AC 8, not just the first: corners inside NDC `[-1, 1]`; each feature's projected span at least `MIN_LEGIBLE_NDC_SPAN`; and the vertical ordering (drain end below far end). **The second and third clauses are what make it falsifying** -- `test/scene-smoke.test.ts:338-353` records that in-viewport-alone left all 32 corner assertions green while the camera sat behind the far edge or under the playfield, and that finding is the reason its orientation clause exists. Author `MIN_LEGIBLE_NDC_SPAN` with a comment naming the reasoning and what would confirm it (AD-15's discipline applied to a test constant, in the shape of `test/asset-contract.test.ts:373-390`). **Do not touch `createFixedCamera()` or `test/scene-smoke.test.ts:311-336`.**
27. `test/replays/*.golden.json` (5) -- refresh `assetHash`, `tableHash` and `gameStart.tuning` from the failing `StaleReplayHeaderError`; move `expectedHash`/`expectedGameStateHash` **only** where a recorded trajectory genuinely reaches new geometry, and say so in that golden's own `notes`. Then re-read each per-golden scenario `describe` and confirm the described event still happens at the described tick.
28. `test/export-py-skip-visibility.test.ts:64, 91, 113` -- if any Blender-gated case is added (tasks 17, 18 each add a `fail()` path worth a case), update the structural pin and the `expectedSkips` formula deliberately, as its own message instructs.
29. `ATTRIBUTIONS.md` -- re-date the author-made `.blend` entry for this story's authoring pass, per CLAUDE.md's provenance rule.

### Rework -- iteration 2 (lead, 2026-09-01): AC 1's behavioural half FAILS; the four red goldens are the messenger, not the problem

The implement stage halted believing the four red tests in `test/replay-goldens.test.ts` were golden drift it was forbidden to fix (a Block If). **That diagnosis was wrong, and the lead's own investigation disproved it.** The goldens are correctly reporting that the new shot map does not route a ball. Re-recording them would have frozen "the plunged ball never touches a flipper" as this table's reference behaviour. Nothing here needs an author decision: **AC 1's behavioural half already requires exactly what is missing**, so this is a defect in the delivered geometry, fixed under the story's existing mandate.

Measured evidence (lead-side, against the committed collision document at `3a0688f`, contrasted with the previous table at `573650a`):

- **The ball never reaches the flippers.** Nine plunge strengths, 14000-tick budget: every plunge that leaves the shooter lane runs `s_loop_r_out -> s_loop_r_in -> s_outlane_r -> s_trough_4` and never enters the flipper band (`y in (40,145)` and `x in (140,375)`). Closest approach to the left bat tip is **159.7 mm** (roll-and-drain) / **159.9 mm** (hold-and-release) / **160.5 mm** (full-plunge); on the old table those were **68.0 / 67.9 / 85.7 mm**.
- **Both Loops return into an outlane, not an inlane.** The Right Loop return channel (`x 433.5-468.4`, bounded by `col_loop_r` / `col_ramp_return_2` against `col_wall_lane`) *is* the right outlane -- `s_inlane_r` never closes on any loop return. `col_loop_l_funnel` narrows the left loop lane to `x 0-34.9`, which is the left outlane. This directly contradicts AC 1's "exits whose exit paths feed straight toward the flippers".
- **Eleven new bodies permanently trap a ball on a flat top face** -- confirmed frozen to 0.1 mm at 120000 ticks, bouncing in place at 33-125 mm/s: `col_sling_l`, `col_sling_r` (top face `y = 455`, ball parks at `y = 468.5`), `col_dragon_leg_l`, `col_dragon_leg_r` (`y = 620`, parks `633.5`), the six `col_dragon_*` bank targets (`y = 708`, parks `721.5`), and `col_ramp_wall_l` (`y = 825`, parks `838.5`). Every one is a plain axis-aligned rectangle whose north edge is exactly perpendicular to gravity, so there is zero tangential force. **This is verbatim the `DW-119` defect Story 2.1a already fixed** by reshaping `col_wall_bottom_l/_r` from a dead-flat top face into a genuine slope; the new bodies never got that treatment.
- **Release sweeps.** At-rest grid, 63 clearance-checked points, 14000 ticks: **48/63 (76.2%) reach `bd_trough`, 15 never drain**, and only 27/63 reach the flipper band -- against **63/63** on the old table. Clearance-checked descents from `y = 980`, 12 columns, 30000 ticks: **6/12 trough, 6/12 permanent stall, 2/12 reach the flipper band** -- against **12/12** on the old table.
- **Attribution.** `DW-53`'s wall raise (`zHighMm` 50 -> 400) is **innocent**: rebuilding the document with the four walls back at 50 reproduces both goldens bit-identically. Deleting `col_lane_deflector` (`DW-58`) was **correct and required**: `col_loop_r_deflector` is the same triangle shifted +6.8 mm in y and now does that job; restoring the old node jams the lane exit entirely. First >1 mm divergence from the old table is **tick 5576/5577**, 555 ticks after `c_autolaunch`, in the top-right corner where `col_loop_r_deflector` / `col_loop_r` / `col_loop_top` now stand. Both goldens went parity-inert **because the recorded button presses became irrelevant to a ball that never comes near a bat** -- a symptom of the defect, not drift.

Rework tasks -- work these, then re-run the full suite:

- [x] [Rework] **Slope or bevel the north face of every flat-topped new body** in `tools/make-placeholder-blend.py`, so a landed ball gains a lateral component and cannot bounce in place forever: `col_sling_l`, `col_sling_r`, `col_dragon_leg_l`, `col_dragon_leg_r`, `col_ramp_wall_l`, and the six `col_dragon_*` bank targets. Follow the remedy `DW-119` already applied to `col_wall_bottom_l/_r` -- a genuine slope, not a cosmetic bevel. Keep every footprint convex. Re-export. **DONE (2026-09-01):** `col_sling_l/r`, `col_dragon_leg_l/r` and `col_ramp_wall_l` sloped via a new `add_box_wall_sloped()` helper. The six bank targets are individually only 11 mm wide -- under the ball's own radius -- so a per-target bevel does not work (tried and measured stuck four ways); reverted them to plain flat bodies and added one new wide `col_dragon_bank_backstop` wall spanning the whole bank instead, which is what actually resolved it. Verified by the new drop-from-above sweep in `test/shot-routing.test.ts` (item (e) below).
- [~] [Rework] **MOVED OUT OF THIS STORY (author, 2026-09-01) — do not attempt.** The Loop-return-to-inlane requirement is now **Story 2.1c: The Loop returns and the inlane feed**, chartered between 2.1b and 2.2 with the Story 2.1a dimensional bounds and the Ramp position explicitly negotiable — the Block Ifs that stopped two iterations here are lifted *there*, not here. `epics.md` is amended on both stories and all of this pass's measured evidence is carried into 2.1c's change log. **2.1b no longer owns this clause.** Leave the Loop return geometry exactly as it stands.
- [x] [Rework] **Fix `test/shot-routing.test.ts`, which passes today and could not have caught any of this** (Rule 19: a pin whose red has never been observed in the way it claims is not evidence). Concretely: (a) `assertNotStranded` is `leftPlay || finalSpeedMmPerS > 20`, and **every stall found above bounces in place at 33-125 mm/s and passes it** -- assert *positional progress* over a window, not speed; (b) it is applied to only 5 of the 10 `describe` blocks -- Ramp, Lock lane, Top lanes and all three pops assert switch closure and nothing else; (c) tick budgets are 500-2500 while a real descent from `y = 980` to the trough takes 2000-6600, so the budget ends before the ball's fate is known; (d) **there is no flipper-band assertion anywhere in the file** even though AC 1 requires one and the file's own header claims "reaches a flipper-reachable band OR ..." -- only the OR branch is implemented; (e) every case shoots upward from `y >= 380` with `NO_FRAME`, so nothing descends onto the new bodies from above, which is the only direction that produces the stalls. Add a descending-release sweep and a flipper-band assertion, and demonstrate each new mutation red. **DONE (2026-09-01):** all five weaknesses addressed -- positional-progress-over-a-window replaces the speed-only check; the flipper-band-or-leaves-play assertion now applies to all 10 `describe` blocks; tick budgets raised to 4000-6600; a new descending-release-from-above sweep exercises the rebevelled bodies. All 24 cases pass (independently reconfirmed by the lead).
- [x] [Rework] **Correct the factually false root-cause text this pass wrote into all five `test/replays/*.golden.json` `notes` fields.** They claim "no existing node's own geometry changed" and blame "Blender's alphabetical `bpy.data.objects` ordering reshuffling every existing `col_` node's array position" with an "order-sensitive broadphase". Both claims are false and were disproved twice: four existing walls **did** change (`zHighMm` 50 -> 400) and `col_lane_deflector` was **deleted**; the existing nodes' *relative* order is in fact preserved (the document is alphabetically sorted and the 28 new names merely interleave); and rebuilding with the walls reverted reproduces the goldens bit-identically, so the walls are not the cause either. A false diagnosis committed next to a golden misleads Story 2.5, which owns the real re-record. State the actual cause, or say plainly that the trajectory changed because the shot map changed. **DONE (2026-09-01):** the false claim is corrected in all five goldens' `notes`, with the disproof stated plainly; the true mechanism was not isolated by either pass, and the notes say so rather than guessing further.
- [x] [Rework] **Then re-run the full suite.** If the four `test/replay-goldens.test.ts` failures do not clear once a ball genuinely reaches the flippers and drains again, do NOT edit a golden's `durationTicks`, `PARITY_INERT` membership or scenario assertion, and do NOT re-record: that is a genuine Block If. HALT and report the residual, with the measured drain tick against the recorded window, and the lead will put it to the author. **DONE (2026-09-01):** re-ran the full suite (independently reconfirmed by the lead: 86 files / 1182 tests / 1178 passing / 4 failing, identical count). The four failures did not clear -- confirmed unrelated to the Loop-return item above (both affected goldens' balls stay below `y = 450` the entire run). Per this item's own instruction, neither golden was edited nor re-recorded; both are left red with the measured residual in their own `notes` fields. See `## Auto Run Result` below.


### Rework -- iteration 3 (lead, 2026-09-01): the author's answers; this is the last iteration before the cap

Five answers came back. Three change this story's scope, one adds work, one is a process correction. **AC 1's Loop-return clause is no longer this story's** (see the struck task above).

**A. Separate the plunge path from the loop-shot path.** This is the core geometry answer, and it is neither of the two options the lead offered. On a real machine these are two different paths: **a plunge crosses the top and descends the LEFT side**, while a **right-Loop shot -- hit off the right flipper -- returns to the right inlane for combos**. The ~1300-tick rattle this pass measured in the top-right pocket is not a designed plunge path; it is the plunge falling into the Loop channel by accident.

So: **re-site the deflector so a plunged ball CLEARS the Loop entrance and crosses the top of the playfield**, restoring a left-side descent onto the left flipper. **Keep the right Loop returning toward the right inlane** for flipper-shot combos -- do not undo the Loop geometry to achieve the plunge fix. `epics.md` Story 2.1b's first criterion is amended to require exactly this, and nothing more about Loop exits.

The pre-2.1b behaviour is the reference for what "crosses the top and descends left" means, measured on the old table: the ball crossed leftward (t=6000 at `(226, 1025)`, t=6400 at `(22, 880)`), reached the flipper band for ~120 ticks, and came within **29.61 mm** of the left bat. On the current table it is **249.40 mm** away with **zero** ticks in the band. That gap is the thing to close.

**B. Fix `DW-121` here (author's decision, not escalated).** A ball descending the right outlane bypasses `s_drain` entirely: it crosses `y = 0` at `x ~ 445-448`, outside `s_drain`'s x span (`200 -> 314.4`), then runs along `y ~ -53`, below its y span (`-5..15`), and reaches `bd_trough` slot 4 with **no drain edge ever emitted**. Rule 15 makes a HIGH in-story finding fix-now and this is FR-11 class -- a drain the drain switch never sees. The substance is switch-set work and `s_drain`'s span is AC 3's territory, so it belongs here whichever routing wins. Story 2.1c will re-verify it against the final outlane path; that obligation is already written into 2.1c's block.

**C. A narrow golden re-record exception is GRANTED -- read the condition, it is binding.** Scoped **explicitly and only** to `roll-and-drain` and `hold-and-release`, **in this story only**. No other golden, no other story; the `Never re-record a golden` rule stands everywhere else and Story 2.5 still owns the real re-record. The reasoning that decided it: both were recorded on the placeholder table -- `hold-and-release`'s own description says "this placeholder table has no pocket geometry beside either flipper" -- so they pin behaviour on geometry this story exists to replace.

**The binding condition: show the new traces are CORRECT before recording, not merely green.** A re-recorded golden that captures a wrong trajectory is worse than a red test, because it launders the defect into the reference. Concretely, before you record either one: trace the ball's actual path and its full switch-closure sequence, satisfy yourself the behaviour is what this table *should* do (a plunge that crosses the top and reaches a flipper; a drain that closes `s_drain`), and **write that reasoning into the golden's own `notes` alongside the new hashes**. Note the re-record in `## Spec Change Log` citing this exception. If a trace looks wrong, do **not** record it -- HALT and report.

**D. This is rework iteration 3 of 3.** If it does not converge, HALT and report the residual rather than pushing for convergence; a controlled stop with findings in hand is the intended outcome, not a failure.

Rework tasks:

- [ ] [Rework] **Re-site the deflector so the plunge clears the Loop entrance and crosses the top**, descending the left side to the left flipper, per answer A. Keep the right Loop returning toward the right inlane. Verify with a real trace, not a dimensional check: report the plunged ball's max `y`, its switch-closure sequence, its ticks in the flipper band (`y in (40,145)`, `x in (140,375)`) and its closest approach to the left bat, against the pre-2.1b reference of ~120 band ticks and 29.61 mm.
- [ ] [Rework] **Fix `DW-121`** per answer B: `s_drain` must close for every ball that reaches `bd_trough` on this story's geometry. Pin it with a test that goes red if a drain path bypasses the zone, and demonstrate that red (Rule 19).
- [ ] [Rework] **Re-record `roll-and-drain` and `hold-and-release` ONLY**, under the exception in answer C and subject to its binding condition. Do not touch the other three goldens beyond the header refreshes they already carry. Record the correctness reasoning in each golden's `notes` and cite the exception in `## Spec Change Log`.
- [ ] [Rework] **Re-run the full suite.** AC 10's floor is >= 80 files and >= 1047 passing with `BLENDER` exported, no test deleted, skipped or weakened, and `pnpm check:ad7` still exiting 1 naming `AD-7` / `DW-70` / `bd_trough`. The four `test/replay-goldens.test.ts` failures should now clear -- two by the routing fix, two by the authorised re-record.


**Acceptance Criteria:**

- **AC 1** -- Given `assets/src/dragonwar.blend` with 2.1a's drain triangle, when the shot map is drawn, then the Left and Right Loops have entries and exits whose exit paths feed straight toward the flippers, one Ramp has an authored height and a decided return inlane, the Dragon body is off-centre with the Lock lane between its legs and a Mouth eject pose aimed at the flippers, and the six-target DRAGON bank, three Top lanes, two slingshots, **three** pop bumpers, two inlanes, two outlanes and the plunger lane all exist as `col_` and `sw_` nodes following the prefix contract, with every guide's free end terminating at a `rubber_post` surface. **(`DW-58`: `col_lane_deflector` is gone and the Right Loop's arc turns the launched ball into the field.)**
  **And -- the behavioural half, which is what "feed straight toward the flippers" actually asserts** -- when a ball is driven into each shot at a plausible shot speed, then that shot's switches close in their approach order, the shot's exit or its most common miss arrives **playable at a flipper** rather than draining down the middle or stranding, and no drivable release point inside a shot's own mouth leaves the ball permanently at rest. Node names, lane widths and post surfaces are task 25's dimensional half of this criterion; **neither half alone closes AC 1** (Story 2.1a shipped a drain triangle that passed every name-and-dimension check while both outlanes dead-ended on solid wall).
- **AC 2** -- Given every device drawn in AC 1, when the `.blend` is exported, then the committed collision document carries exactly one `sw_` zone per zone-requiring switch, each axis-aligned and sized so a maximum-speed ball's per-tick swept segment cannot straddle it, and `export.py` fails naming the switch if any is missing.
- **AC 3** -- Given `src/sim/table/dragonwar.ts`, when `TABLE.switches`, `TABLE.coils` and `TABLE.ballDevices` are completed, then every switch named in `epics.md:927` is declared with its `settleClass` -- including exactly three `s_pop_1..3` -- the coils `c_sling_l/r`, `c_pop_1..3`, `c_dragon_bank_reset` and `c_mouth` exist, `bd_lock` is a parking device of capacity 3 with slots `s_lock_1..3`, eject coil `c_mouth` and a `ballSearchOrder`, `TABLE.authoredCounts.popBumpers` records the value 3 with a `source` and `confidence: 'unverified'` that agree with the declared `s_pop_*`/`c_pop_*` sets (AD-15), and `TABLE.shots` is still exactly `{}`.
- **AC 4** -- Given the completed registry, when `pnpm export:assets` runs, then `export.py` validates the `.blend` against the `TABLE` dump in **both** directions and both loaders load the result; a concave `col_` wall footprint fails the export naming the node and the vertices its hull dropped (**`DW-68`**), and a `TABLE.ballDevices` entry with no document counterpart throws at load naming the device rather than degrading to a runtime `eject_failed` (**`DW-69`**).
- **AC 5** -- Given the fastest ball a full-strength plunge and a flipper hit can produce -- measured and recorded, not assumed -- when it passes through each rollover, the DRAGON bank, the Lock lane and the drain, then every switch closes **exactly once per pass** because zone tests use the per-tick swept segment, and no pass is missed at that speed. **(`DW-67`: the tracker latches on the make and debounces the break, so a crossing shorter than the settle window registers instead of vanishing.)**
- **AC 6** -- **`pending-author`.** Given the geometry loads, when the author runs the feel ritual on each shot from the fixed camera against the Reference machine, then `docs/feel-test.md` gains a per-shot Lawlor entry (Left Loop, Right Loop, Ramp, Dragon, Lock lane, DRAGON bank, Top lanes) recording where the most common miss goes, none is a centre drain, and any shot that fails is re-drawn before the story closes (FR-32). **The pipeline cannot close this**; this story lands the seven entries with the `pending-author` token and the build-side geometry they will be judged against. The `sprint-status.yaml` action item already exists.
- **AC 7** -- Given the drawn geometry, when the two decisions this epic charters to this story are made, then `docs/decisions.md` records the Ramp's return inlane (FR-27 / OQ-6) and whether the Lock lane carries both the lock and the mode start (OQ-5), each with its date and its reasoning; if OQ-5 is answered no, a separate `sw_scoop` and `bd_scoop` are added with the Mouth as eject only -- which is a Block If, not a unilateral choice.
- **AC 8** -- Given the fixed camera from Story 1.4, when the full geometry is in view, then both flippers, the Dragon, both Loops, the Ramp and the DRAGON bank are **legible** -- operationally: each named feature's eight projected bbox corners land inside NDC `[-1, 1]` in x and y, each feature's projected bbox spans at least `MIN_LEGIBLE_NDC_SPAN` of the viewport so no feature is a dot, and the projected vertical ordering matches the table layout (the drain end below the far end), so a camera that framed everything by retreating cannot pass. **The camera is read, never written**: the test takes `scene.activeCamera` after `loadAndRenderOnceForTests()` and never imports or modifies `createFixedCamera()`. (The Backglass clause moved to Story 2.6 AC 1 by the author on 2026-08-31; `epics.md` amended on both stories. `createFixedCamera()` and `test/scene-smoke.test.ts:311-336` are **out of scope here** -- re-aiming that camera is deliberately Story 2.6's, so a shot-map story does not move it as a side effect.)
- **AC 9** -- Given a ball above the interior guides, when it is driven laterally at the measured maximum speed at `z = 200 mm`, then it stays inside the playfield on every side, because the perimeter walls now reach the glass (**`DW-53`**).
- **AC 10** -- Given the full command suite, when it runs after the change, then it reports **no fewer than 80 files and 1047 passing** with `BLENDER` exported and no test deleted, skipped or weakened to reach it; any change to the skip count is explained by a deliberately added Blender-gated test with `test/export-py-skip-visibility.test.ts`'s pin and `expectedSkips` formula updated; all five golden headers are **refreshed, not re-recorded**, with no golden's `durationTicks`, `PARITY_INERT` membership or scenario assertion edited; and `pnpm check:ad7` still exits 1 naming `AD-7`, `DW-70` and `bd_trough`.

## Spec Change Log

- 2026-09-01 (lead, applying the author's five answers; rework iteration 3): **AC 1's Loop-return-to-inlane clause moved out to the newly chartered Story 2.1c**, which is permitted to move the 2.1a bounds and renegotiate the Ramp position; `epics.md` amended on 2.1b and 2.1c with the measured evidence carried across. 2.1b's first criterion now requires instead that a plunged ball **clears the Loop entrance and crosses the top of the playfield** -- the author's separation of the plunge path from the loop-shot path. **`DW-121` is fixed here**, not escalated (Rule 15 fix-now, FR-11 class, and `s_drain`'s span is AC 3 territory). **A narrow golden re-record exception is granted for `roll-and-drain` and `hold-and-release` in this story only**, binding on the condition that each new trace is shown correct before recording and the reasoning written into that golden's `notes`. Status reset to `in-progress`. No frozen section was touched.

- 2026-09-01 (lead, rework iteration 2): the implement stage's `blocked` halt was **misdiagnosed**. It read the four red `test/replay-goldens.test.ts` failures as forbidden golden drift (a Block If); the lead's own investigation established they are the correct report of a real routing defect -- the plunged ball never reaches a flipper (159.7 mm closest approach against 68.0 mm before), both Loops return into outlanes rather than inlanes, and eleven new bodies strand a ball permanently on a dead-flat top face, the same `DW-119` failure Story 2.1a already fixed. AC 1's behavioural half already requires what is missing, so no amendment and no author decision was needed: the rework tasks are added under `## Tasks & Acceptance` and `status` is reset to `in-progress`. `DW-53`'s wall raise is exonerated and `DW-58`'s deflector deletion confirmed correct. No frozen section was touched.

- 2026-08-31 (lead, applying the author's answers to the plan stage's two intent gaps; re-dispatch protocol): **Gap 1** -- AC 8's Backglass clause is removed from this story and the obligation moved to Story 2.6 AC 1, which now authors the quad and owns any camera re-aim; `epics.md` amended on both stories with `[AMENDED 2026-08-31]` markers and change-log entries. **Gap 2** -- the pop-bumper count is **three** (`s_pop_1..3` / `c_pop_1..3`), recorded in `TABLE` with `source` and `confidence` per AD-15; `epics.md` amended at both the geometry and the declaration criteria. Also corrected upstream: `epics.md:923` relabelled the Ramp return-side decision OQ-5 -> OQ-6 (the spine defines OQ-6 as the playfield geometry including ramp height; OQ-5 is the Lock-lane question this story's own later criterion uses it for). Frontmatter reset to `draft` so build-auto re-plans around the preserved `<intent-contract>` block.
- 2026-08-31 (author, planning direction): **this story stays whole** -- the ten ACs describe one coherent deliverable and the switches are the shot map's own sensors, so a split would cut through that coupling. Two conditions carried into the plan: (a) if the implement/review loop reaches the 3-iteration rework cap, the lead STOPS and surfaces rather than pushing for convergence -- a controlled stop with findings in hand is the intended outcome, not a failure; (b) **front-load what cost Story 2.1a its four iterations**: 2.1a converged slowly because AC 1 pinned node names, lane widths and post surfaces while nothing tested behaviour, so the real defects (outlanes that dead-ended, 165 mm of flat ledge that stranded the ball) surfaced at code review and smoke instead of at planning. Every AC here that is behavioural must have a **behavioural** pinning test, and every AC's Rule 19 mutation must be named in `## Verification` at plan time rather than discovered later. A shot map is exactly the kind of thing that passes a checklist and fails a ball.

- 2026-08-31 (plan, re-dispatch after the author's clarification): re-planned around the preserved `<intent-contract>`, building on the previous pass rather than restarting. **Gap 1 applied:** AC 8 rewritten from `blocked` to a closable criterion with an operational definition of "legible" (corners inside NDC, a minimum projected span, and the layout ordering), a new task 26a (`test/shot-map-legibility.test.ts`) and two named mutations; `createFixedCamera()` and `test/scene-smoke.test.ts:311-336` marked explicitly out of scope. **Gap 2 applied:** the pop count is three throughout (AC 1, tasks 8, 12, 23), plus new tasks 12a/12b recording it in `TABLE.authoredCounts` with `source`/`confidence` per AD-15 and pinning it against the declared `s_pop_*`/`c_pop_*` sets -- the shape is argued in Design Notes, since `Record<string, never>` forbids per-entry provenance on coils. **Author condition (a)** honoured: the story stays whole; no split proposed. **Author condition (b)** honoured: AC 1 gained its behavioural half and task 16a (`test/shot-routing.test.ts`, the `drain-routing` sweep shape), every behavioural AC now names a behavioural pin, and every AC's Rule 19 mutation is named here at plan time -- including the ledger fixes, which have no AC of their own. **One correction reported, not applied silently (Rule 5, apply-and-report tier):** the frozen I/O matrix's last row names `SwitchEvent` in `FrameOutput`, which does not exist -- `FrameOutput` has no `switchEvents` channel and `createLoop` discards them at `src/sim/loop/index.ts:340`. The intent-contract is preserved verbatim; the Integration AC is re-sited in Design Notes onto `createMachine().step().switchEvents` plus a genuine `FrameOutput` observable (`bd_lock` parking in `snapshot.mechanisms.devices`). The row was the previous plan pass's own wording, not a shipped criterion -- `epics.md:933-935` never mentions `FrameOutput` -- so no shipped criterion was narrowed. Flagged for the lead. Also folded in: the `epics.md:923` OQ-5 -> OQ-6 relabel is now done upstream, so that Design Note moved from "flagged" to "resolved"; and the Code Map gained the ball-driving, unit-conversion, camera-projection and determinism anchors this pass read.

## Review Triage Log

## Design Notes

**Governing architecture decisions (Rule 6).** **AD-11** (the `.blend` is the sole owner of every position, mesh and switch zone; `TABLE as const` is the sole registry; `export.py` validates against a dump of `TABLE`; node prefixes `col_`/`sw_`/`vis_`/`l_`; `sw_` zones are analytic tests against the ball's per-tick swept segment, never the end position; both loaders fail fast) governs AC 1, AC 2, AC 3 and AC 4, and names **OQ-5 and OQ-6** in its own `Binds:` line -- it is the AD that owns both open questions. **AD-10** (table frame in mm, origin at the bottom-left corner, geometry authored unpitched, `TABLE.reference` asserted by the loader) governs AC 1 and AC 9. **AD-2** (switches are edges with per-switch hysteresis and `settleTicks` by class; rules never debounce) governs AC 5 and the `DW-67` fix. **AD-6** (physics owns ball bodies and mechanical state; `bd_lock` is a parking device of capacity 3 with slots `s_lock_1..3` and eject `c_mouth`, and the Lock's pose *is* the Mouth) governs AC 3. **AD-19** (the devices-and-shots layer is the only consumer of `SwitchEvent`; shots are declared in `TABLE.shots` as ordered switch sequences) governs the boundary that keeps `TABLE.shots` empty here. **AD-5** (slings and pops are hardware rules inside the physics step behind their coils) is touched only in that this story authors their bodies and declares their coils; actuation is Story 2.2. **AD-15** (tunables carry `source` and `confidence`; do-not-invent numbers ship `unverified`; goldens are first-class) governs every authored figure and the header refresh. **AD-17** (a malformed collision document reaches the boot error panel, never a silent runtime degradation) governs `DW-69`. **AD-16** (device-name literals outside `sim/table/dragonwar.ts` and `test/**` are errors) governs how ~30 new names enter the codebase. **AD-1** (one table, fixed dependency direction) governs the `bd_lock` addition. No AC contradicts an AD's Rule.

**Rule 20 -- one candidate spine write, for the lead.** Fixing `DW-67` settles a direction **AD-2's Rule leaves implicit**: its Rule says "per-switch hysteresis and `settleTicks` from `TABLE.switches`" without saying which edge the window gates. The ledger entry says explicitly that this story is where that is decided. FR-11 decides it -- "no ball is ever lost by a missed switch at any ball speed the Physics core can produce" is false under a make-debounce the moment a `standup` or `drop_target` switch exists -- and a latch-on-make/debounce-the-break tracker is what "hysteresis" already means, so this moves the code **toward** AD-2 rather than away from it. No AD amendment is strictly required, but a one-clause clarification in AD-2's Rule would stop the next contributor re-deriving it. That write belongs to the lead, at the moment of decision, not to this story.

**The `settleClass` mapping, and why.** `rollover` (0 ms): `s_loop_l_in/out`, `s_loop_r_in/out`, `s_spinner`, `s_ramp_enter/made`, `s_lock_lane`, `s_lock_1..3`, `s_top_1..3`, `s_inlane_l/r`, `s_outlane_l/r`, `s_drain`. `standup` (8 ms): `s_dragon_body`, and `s_sling_l/r` -- a slingshot leaf blade against a rubber is the same mechanical class as a standup target's leaf, and no artifact names a class for it; a dedicated `sling_leaf` class was rejected because `SettleClass` is compile-bound to `switchSettleMsByClass` and would introduce a duration figure nothing sources. `drop_target` (20 ms): `s_dragon_d/r/a/g/o/n`. `bumper_skirt` (2 ms): the pop skirts (Story 2.2's AC names this class explicitly). `s_spinner` stays `rollover` deliberately -- any settle would swallow revolutions, and revolution counting is Story 2.3's mechanical state, not a debounce.

**Which switches require a zone.** Derived, not declared, so `TABLE`'s switch-entry shape is unchanged: a switch requires an `sw_` zone unless it is a **button** switch (`settleClass: 'button'` -- emitted by `sim/loop` from `InputFrame`, AD-2), a **cabinet mechanism** switch (`tilt_bob`, `slam` -- emitted by the cabinet oscillator), or a **parking-device slot** switch (already excluded by `parkingDeviceOwnedSwitches()`, `switches.ts:51-61`, which reads `TABLE.ballDevices` and therefore picks up `bd_lock` with no edit). Everything else must have one. AD-2 names exactly those three sources, so the derivation is the AD's own partition rather than a new convention.

**Why the switch-zone completeness check lives in `export.py` and an asset-contract test, not in `loadCollision()`.** `DW-69` is specifically about `TABLE.ballDevices`, and the loader fix is scoped to it (two hand-built fixture documents gain their `devices` entries). Putting a ~28-switch completeness requirement into `loadCollision()` instead would force every hand-built minimal document in `test/collision-loader.test.ts` to carry the whole zone set, which turns a focused loader unit suite into a fixture-maintenance problem for no additional safety -- the Blender-side check catches the same mistake at the moment it is made, and the asset-contract test catches it in CI, where there is no Blender.

**`DW-120`: culling, not a Z drop.** The ledger names two fixes. A real below-deck subway is not expressible in the current collision model: `col_playfield` is a **single plane node** (`loader/index.ts:748-756` permits exactly two plane nodes, `col_playfield` and `col_glass`), so a ball dropped below `z = 0` would be pushed straight back up by the playfield plane, and punching a hole in it is an AD-11 primitive-set change well beyond this story. Presentation-side culling is the ledger's own second option, is contained to `src/presentation/scene/balls.ts`, does not disturb the drain routing 2.1a took four iterations to prove, and is a rendering decision rather than game state derived from position (AD-1, AD-2 both intact).

**The `epics.md:923` OQ label -- RESOLVED upstream 2026-08-31, no longer an open flag.** The lead relabelled the Ramp return-side decision OQ-5 -> **OQ-6** at `epics.md:923` and the epic context now agrees (`epic-2-context.md:35`: "The Ramp's return-inlane choice is the **OQ-6**/FR-27 decision"; `:36` keeps OQ-5 for the Lock lane). Task 23 and AC 7 already use those labels. The original analysis is kept below because it is the evidence for the relabel, not because anything is still outstanding.

**(Historical, now actioned.)** The previous pass found that `epics.md`'s AC 1 called the Ramp's return inlane "the OQ-5/FR-27 decision". OQ-5 is the **mode-start device** (SPEC.md:303: "Does the Lock lane carry both the lock and the mode start cleanly in the drawn geometry?"); the Ramp's return side is FR-27's `[ASSUMPTION: return side]`, tracked as SPEC `CAP-27` (SPEC.md:296, "decided in epic 2 geometry") under **OQ-6**. The Epic 2 overview (`epics.md:272`, `:810`) and AR-37 (`:192`) both have it right; AC 4's own "(OQ-5)" for the Lock lane is right too. `epic-2-context.md` had propagated the same conflation. Both decisions are recorded either way, so the work was always identical -- the label was the only thing wrong, and the lead has since corrected it in both files.

**OQ-5's answer is already effectively determined, and the fallback is not a local change.** Stories 2.3, 2.4, 3.2 and 3.4 are all written against `lock_lane_entered`, `bd_lock` and `c_mouth`; 3.4's multi-candidate mode select has the ball **parked in `bd_lock`** during `modeSelectMs` before the Mouth ejects; AD-18's Lock arbiter has four outcomes and no scoop variant; and no `bd_scoop`/`sw_scoop`/`s_scoop`/`c_scoop` token exists anywhere in `epics.md` outside this story's own fallback clause. The spine rates the fallback as cheap *architecturally* ("a `TABLE` and geometry change, not an architecture change") and it is -- but the epic text is not neutral, so choosing it would rewrite acceptance criteria in four downstream stories. Hence the Block If: record the confirmation with its evidence, or halt.

**What "a replay of the fastest ball" means here.** AC 5's own Then names **a test** ("and a test asserts no pass is missed at the maximum speed"), and no single trajectory can pass through every rollover, the bank, the Lock lane and the drain. The form is therefore a test that drives a ball through each zone at or above the measured maximum, in the shape `test/drain-routing.test.ts` already uses for its full-span sweep -- **not** a sixth golden, which `test/replay-goldens.test.ts:107-117`'s completeness gate would reject and which Story 2.5 owns anyway. The maximum itself is currently unrecorded anywhere in the repository: a full-strength plunge is `autolaunchSpeedMmPerS = 2500 mm/s` (2.5 mm/tick) via `plungerSpeedByHoldMs` at max hold, and what a driven flipper adds has never been measured in mm/s (`test/flipper-collision.test.ts:449-477` asserts only a ratio). Measuring and recording it is part of the task.

**Integration ACs (Rule 1) -- and a correction to the frozen I/O matrix's last row, made in the open rather than silently.** This story introduces a shared registry surface (~30 switches, 6+ coils, `bd_lock`) and a changed shared component (`createSwitchTracker`). **AC 5 is the Integration AC.**

The frozen `<intent-contract>` matrix's last row says the observable is "a ball driven through a new zone surfaces a `SwitchEvent` for that switch in **`FrameOutput`**". **That is not reachable, and the row is wrong about this codebase** -- not about the intent. `FrameOutput` is `{ snapshot, events, contactEvents, commands }` (`src/sim/contracts/snapshot.ts:109-129`): there is no `switchEvents` channel. `createLoop().advance()` builds the switch events locally at `src/sim/loop/index.ts:340`, hands them to `rulesStep()`, and discards them; only the resulting `SemanticEvent`s survive. Every test in the repo that reads a `SwitchEvent` reads it off `createMachine().step()`. The row was written by the previous plan pass, **not** by `epics.md` and not by the author -- the shipped criterion (`epics.md:933-935`) names only "a test asserts no pass is missed at the maximum speed" and never mentions `FrameOutput`. So this is Rule 5's *apply-and-report* tier (an AC citing a wrong API, restated so it is reachable with the intent unchanged), not an intent gap, and **not** a criterion narrowed to fit behaviour. The intent-contract block is preserved verbatim as the workflow requires; the correction lives here, in the non-frozen section, per Rule 2. **Flagged for the lead**, who owns the matching planning-side write-up.

The Integration AC therefore observes at **two** consumer tiers, both real, neither inspecting tracker internals:

- **`createMachine(collisionDoc, tuning)`** (`src/sim/physics/machine.ts:140`) -- the tier that owns `SwitchEvent`. `machine.step()` returns `switchEvents`, and every new zone-requiring switch surfaces exactly one make and one break per pass at the measured maximum speed (task 16).
- **`createLoop({ collisionDoc })`** -- the real conductor, which is what "boots on the regenerated document and completed `TABLE`" genuinely tests, plus one end-to-end observable that *is* in `FrameOutput`: a ball driven into the Lock lane parks in the newly declared `bd_lock` and the change appears in `snapshot.mechanisms.devices.bd_lock.slots`. This needs **no new behaviour code** -- `createDeviceMechanics` is generic over `TABLE.ballDevices`, and `parkingDeviceOwnedSwitches()` (`switches.ts:51-61`) picks `bd_lock` up by reading the registry -- so it stays inside this story while the Lock *rules* (the arbiter, mode start, the Mouth eject) remain Story 2.3's.

**AC 4** is a third consumer-side check: `export.py` and `loadCollision()` are the two gates that read the registry.

**Consumed-by:** Story 2.2 (slingshot and pop actuation behind `c_sling_l/r` and `c_pop_1..3`, against the bodies drawn here); Story 2.3 (drop-target drop/reset against `s_dragon_*` and `c_dragon_bank_reset`, spinner revolutions on `s_spinner`, Lock parking and Mouth eject on `bd_lock`/`c_mouth`); Story 2.4 (declares `TABLE.shots` over the switch sequences this story names, and is the only consumer of `SwitchEvent`); Story 2.5 (the golden re-record and `DW-70`); Story 2.7 (Skill shot and lane change over `s_top_1..3` and the inlane/outlane set); Story 3.2 and Story 3.4 (the Lock arbiter and mode start over `s_lock_lane`). **Consumes:** Story 2.1a's drain triangle, pocket posts, reconciled flipper boxes and `BOTTOM_WALL_DRAIN_DROP_MM`; Story 1.4's export pipeline, both loaders and the fixed camera; Story 1.5's `bd_trough`, `sw_trough_*` zones and `createDeviceMechanics`; Story 1.6's ported `FlipperMover`; Story 1.8's goldens and the `assetHash`/`tableHash` handshake; Story 2.0's renamed structural provenance gate.

**Ledger inbox (Rule 17).** All eight owned entries are addressed; none is declined. `DW-46` -> task 20. `DW-53` -> AC 9 + tasks 10, 26. `DW-58` -> AC 1 + task 9. `DW-65` -> task 21. `DW-67` -> AC 5 + tasks 13, 14, 15, 16. `DW-68` -> AC 4 + task 17. `DW-69` -> AC 4 + task 19. `DW-120` -> task 22.

**Human-only work.** **AC 6** is `pending-author`: the seven-shot Lawlor ritual (FR-32) is judged against the physical Reference machine (Stern *Dungeons & Dragons*) and the pipeline cannot close it. The precedent is Story 1.9 AC 5, and Story 2.1a's own Design Notes already name "Story 2.1b AC 6" as that precedent's sibling -- which is why the acceptance list above is numbered so the ritual lands at 6. The `sprint-status.yaml` action item `epic-2-retro-item-10` exists and stays open. No other AC in this story needs the Reference machine or Apple hardware. **AC 8 is no longer blocked**: its missing referent was the Backglass quad, and the author moved that clause to Story 2.6 on 2026-08-31, leaving a playfield-feature legibility criterion this story fully owns and can close in software (task 26a). It was never a human judgment and must never be relabelled `pending-author` -- `pending-author` is for physical-feel judgments only, and inventing a software proxy for one, or relabelling a closable AC to look like one, are the two failure modes this paragraph exists to prevent.

**Recording the pop count in `TABLE` (AD-15), and why it takes the shape it does.** The author fixed the count at three and instructed that it be recorded in `TABLE` with `source` and `confidence` "exactly as the other unverified figures are recorded". `TABLE` has no provenance mechanism today -- the other unverified figures live in `TUNING` as `TuningEntry<T>` (`tuning.ts:33-44`). Three shapes were considered. **Per-entry provenance on the pop switches and coils is impossible**: coil entries are typed `{} as Record<string, never>` (`dragonwar.ts:135-140`), a type that admits no property at all, so `c_pop_1` cannot carry a `source` without changing the coil type for every coil. **A `TUNING` entry is the established shape but the wrong home**: the count is a registry fact -- it is exactly "how many `s_pop_*` keys exist" -- not a tunable, and a tunable count could silently disagree with the switch set it claims to describe. So: a top-level `TABLE.authoredCounts` block carrying the `{ value, source, confidence }` triple verbatim, with `Confidence` moved down into `dragonwar.ts` (the import already runs `tuning.ts -> dragonwar.ts`, so the reverse would be a cycle) and re-exported so both existing call sites are untouched. Task 12b pins the recorded value against the actual `s_pop_*` and `c_pop_*` key counts, because a provenance record that can drift from what it documents is worse than none. Two consequences, both deliberate and both commented at the block: `tableHash()` hashes the whole `TABLE` (`replay.ts:139-140`), so the `source` prose is inside the golden identity and later edits to that sentence re-break all five headers -- correct, since the count is part of the table's identity; and the block is **not** added to `buildTableDump()`, because a provenance record is not geometry and `export.py` has no business validating it.

**Why this spec front-loads behavioural tests, and what "behavioural" has to mean here (the author's condition (b)).** Story 2.1a converged in four iterations for one diagnosable reason: its AC 1 pinned node names, lane widths and post surfaces -- a checklist -- while nothing tested what a ball did. Every check passed while both outlanes dead-ended on solid wall, and after that was fixed, 165 mm of flat bottom ledge per side still stranded the ball on the game's own default path. Both defects surfaced at code review and smoke, not at planning. A shot map is the archetype of the thing that passes a checklist and fails a ball, so:

- **Every behavioural AC here carries a behavioural pin, named at plan time**: AC 1 -> `test/shot-routing.test.ts` (task 16a), AC 2 and AC 5 -> `test/switch-max-speed.test.ts` (task 16), AC 4 -> the two failure-path tests, AC 9 -> `test/vertical-containment.test.ts`. Dimensional gates (task 25) are the *second* half of AC 1, never the whole of it, and the spec says so at both ACs.
- **The shape matters as much as the presence.** `test/drain-routing.test.ts`'s release-point sweep is the shape that finally worked on 2.1a; its three-sample-point predecessor is the shape that did not, because three points can all miss the defect. Every shot in task 16a sweeps entry offsets for the same reason.
- **A test that cannot go red is not evidence** (Rule 19). Every AC's mutation is named in `## Verification` below at plan time rather than discovered at review -- and where an AC's mutation could not be named, that is stated rather than papered over. Two specific vacuity traps are called out at their tasks because this codebase has already produced them: symmetry assertions are structurally 0.00 under x-free gravity, and in-viewport-alone left 32 camera assertions green with the camera behind the playfield.

**Scope.** `warnings: ['oversized', 'multiple-goals']`. This story carries the whole shot map, the whole switch and coil set, a shared-component semantics change, eight ledger entries across five subsystems, two recorded decisions, a new document and a five-golden header refresh. Story 2.1a -- smaller than this -- was `oversized` and took four rework iterations. The warnings are the honest signal; splitting is the lead's call, not this workflow's.

## Verification

**Commands:**

- `"$BLENDER" --background --factory-startup --python tools/make-placeholder-blend.py` -- expected: exit 0; rewrites `assets/src/dragonwar.blend`. Export `BLENDER` in the shell; never write the path into a tracked file.
- `BLENDER="$BLENDER" pnpm export:assets` -- expected: exit 0; rewrites `public/assets/dragonwar.glb` and `public/assets/dragonwar.collision.json`. Commit all three artifacts together.
- `pnpm typecheck` -- expected: all three projects clean.
- `pnpm lint:boundaries` -- expected: exit 0; no device-name literal outside `src/sim/table/dragonwar.ts` and `test/**`.
- `pnpm check:headers` and `pnpm check:attributions` -- expected: exit 0.
- `pnpm test` -- expected: at or above **80 files / 1047 passing / 0 skipped** with `BLENDER` exported, zero failures, no test deleted, skipped or weakened to reach it. Without `BLENDER`: the same pass count less the Blender-gated skips, with `test/export-py-skip-visibility.test.ts`'s `expectedSkips` formula updated if any gated case was added.
- `pnpm check:ad7` -- expected: **exit 1**, naming `AD-7`, `DW-70` and `bd_trough`. A green run is a regression.
- `pnpm build && pnpm check:dist && pnpm check:size` -- expected: exit 0 for each.

**Mutations (Rule 19 -- one per AC, applied, observed red, reverted, tree verified byte-identical via `git status --short` and `git diff --stat`):**

- AC 1 -- **two mutations, because AC 1 has two halves and 2.1a proved the dimensional half alone is not evidence.** (a) *Behavioural, the primary pin:* close one Loop's exit with a temporary `col_` wall in the seeding script and re-export -> `test/shot-routing.test.ts` goes red on that Loop, reporting the ball's `lastPosMm` where it dead-ended, while every other shot's case stays green. A second, sharper variant if the first is too coarse: shift the Ramp's return rail so it lands outside the inlane -> the "arrives playable at a flipper" clause goes red for the Ramp alone. (b) *Dimensional:* change one shot-map guide-end post's `surface` from `rubber_post` to `metal` in the seeding script and re-export -> the guide-end gate in `test/asset-contract.test.ts` goes red naming that guide. **If (a) cannot be made to go red, the shot-routing test is not yet evidence and this AC is not closable** -- say so rather than falling back on (b).
- AC 2 -- delete one `sw_` zone object from the seeding script and re-export -> `export.py`'s new switch-zone completeness check fails naming the switch; with the check bypassed, `test/switch-max-speed.test.ts` goes red on that switch's pass.
- AC 3 -- change one new switch's `settleClass` from `drop_target` to `rollover` -> the `switchSettleTicksByClass` assertion in `test/table.test.ts` goes red naming the switch.
- AC 4 -- (a) `DW-68`: author a temporary L-shaped `col_` wall in the seeding script and re-export -> `export.py` fails naming the node and the dropped vertex count; without the fix it exports silently as a filled block. (b) `DW-69`: remove `bd_lock` from the collision document handed to `loadCollision()` -> throws at load naming `bd_lock`; before the fix it loads and the first `c_mouth` pulse returns `eject_failed`.
- AC 5 -- replace `segmentIntersectsBox(movement.before, movement.after, ...)` at `src/sim/physics/switches.ts:109` with an end-position-only test (`movement.after, movement.after`) -> the max-speed passes go red naming the missed switch, while a slow-crossing control stays green. Second, for `DW-67` specifically: revert the tracker to the make-debounce -> the `drop_target` sub-settle crossing case in `test/switch-zones.test.ts` goes red with **no edge emitted at all**, which is the defect's own signature.
- AC 6 -- no mutation; `pending-author`. The build-side pin is `test/feel-test-docs.test.ts`: `mutation: remove one shot's section from docs/feel-test.md -> the per-shot presence assertion goes red naming that shot.`
- AC 7 -- delete the OQ-5 row from `docs/decisions.md` -> the decisions-document test goes red naming the missing decision.
- AC 8 -- **resolved 2026-08-31; the mutation is now nameable.** Move the Dragon body 400 mm up-table in the committed collision document (the Blender-free mutation form `test/asset-contract.test.ts:388-389` already uses) so it projects past the far edge -> `test/shot-map-legibility.test.ts` goes red naming `col_dragon_body` and the NDC value that left `[-1, 1]`. **Second mutation, which is the one that proves the test is not merely a bounds check:** shrink one named feature's `bboxMm` until its projected span falls under `MIN_LEGIBLE_NDC_SPAN` -> the span clause goes red naming that feature while every corner stays inside the viewport. Without the second, this test would pass with every feature framed as a dot, which is the failure `test/scene-smoke.test.ts:338-353` was written to close.
- AC 3 (provenance half) -- change `TABLE.authoredCounts.popBumpers.value` from `3` to `4` without touching the switch set -> task 12b's consistency assertion goes red naming the mismatch; and delete one `s_pop_*` entry -> the same assertion goes red from the other direction.
- AC 9 -- revert `col_wall_left` to `WALL_H_MM` in the seeding script and re-export -> `test/vertical-containment.test.ts` goes red reporting the ball's exit x, while the `z = 25 mm` control stays green.
- AC 10 -- no mutation (it is the gate itself). Guardrails: `check:ad7` -- `mutation: none -- this is a preservation check, not an AC.` Goldens -- `mutation: revert one golden's header.assetHash to the pre-change value -> that golden goes red with StaleReplayHeaderError naming assetHash, before any hash is computed.`

**Ledger-entry mutations (no AC of their own -- Rule 19 scopes to ACs, but these are code changes and an unfalsified fix is not a fix):**

- `DW-46` -- set `env.ProgramFiles` to a localized path in `test/blender-resolve.test.ts` and revert the fix -> resolution fails; with the fix it finds Blender. Both POSIX branches likewise, through the injectable base.
- `DW-65` -- move the lane divider 5 mm in the committed collision document -> the derived value in `test/machine-serve-drain.test.ts` tracks it and the test still passes, where the `468.4` literal would have gone red for the wrong reason. (This one falsifies the *literal*, not the fix: the observable is that a legitimate geometry change no longer breaks an unrelated test.)
- `DW-120` -- revert the cull in `src/presentation/scene/balls.ts` -> the `y = -50` case goes red because the mesh is present/visible, while the `y = +50` control stays green. The control is what stops a fix that culls everything.

**Manual checks:**

- Confirm the shot map reads as a shot map -- two Loops whose exits point at the flippers, one Ramp, a Dragon left of centre with a lane between its legs, a six-target bank, three Top lanes -- **against `public/assets/dragonwar.collision.json`, not the `.glb`.** Every `col_`/`sw_` node is stripped from the glb by design, so a reviewer looking at the rendered scene sees an unchanged empty playfield. This is the check that caught `DW-119` on Story 2.1a, and it caught it by reading the collision document.
- Re-read each of the five goldens' per-golden scenario assertions after the header refresh and confirm the described event still happens at the described tick, rather than only that the hashes agree.
- Confirm no tracked file contains a Blender executable path (`git grep -i "blender-5\|Program Files.*Blender" -- ':!tools/blender.mjs'` should find nothing, and `tools/blender.mjs` should contain only the generic fallback literal).

## Auto Run Result

Status: ready-for-dev

Blocking condition: none

Planning completed on the 2026-08-31 re-dispatch. Both intent gaps below were answered by the author, applied into the preserved `<intent-contract>`, and are now carried through the whole spec (see the newest `## Spec Change Log` entry). The invocation directed `Halt after planning.`, so the run halts here with the spec ready for the lead's validation gate; nothing was implemented and nothing was committed.

One item is **reported, not blocking**, for the lead: the frozen I/O matrix's last row names a `SwitchEvent` in `FrameOutput`, an API that does not exist. It was the previous plan pass's own wording rather than a shipped criterion, so the intent-contract is preserved verbatim and the Integration AC is re-sited under `## Design Notes` onto the tier that actually owns `SwitchEvent`. See the Integration ACs note.

### Implement-stage halt, 2026-09-01

Status: blocked

Blocking condition: implementation verification failed

The implementation subagent completed all 29 tasks (12a/12b/16a/26a included), the geometry, registry, `DW-67` switch-tracker fix, and documentation this spec calls for. Re-run independently by the implement-stage orchestrator: `pnpm typecheck`, `pnpm lint:boundaries`, `pnpm check:headers` and `pnpm check:attributions` all pass clean. `pnpm check:ad7` still correctly exits 1 naming `AD-7`/`DW-70`/`bd_trough` -- no regression.

`pnpm test` (with `BLENDER` exported) reports **86 files / 1175 tests, 4 failing, 1171 passing** -- all four failures in `test/replay-goldens.test.ts`, against the `roll-and-drain` and `hold-and-release` goldens:

- `roll-and-drain`: the ball no longer drains within the golden's recorded `durationTicks` (still in play at the tick bound; confirmed by extending the run, it drains ~50-100 ticks past the bound).
- `hold-and-release`: the raised flipper bat no longer measurably deflects the ball against the no-flipper control (observed divergence 0.0269 mm; the pinned threshold is >5 mm).
- Both goldens also now fail the AC 5 parity-hash falsifiability check: their `finalGameStateHash` no longer changes when `transitions` are stripped -- they have become parity-inert without being declared so on `PARITY_INERT`.

Root cause, per the implementation subagent's diagnosis: Blender's `bpy.data.objects` iteration order is alphabetical and reshuffles on every regeneration, which moves every *existing* collision node's array position in the exported document even though none of those nodes' own geometry changed. The ported physics broadphase is order-sensitive to that reshuffle for these two chaotic (multi-contact) scenarios, so byte-identical recorded input (`transitions`/`coilPrologue`, unchanged as required) now plays out a genuinely different trajectory.

The only way to turn these four tests green is one of the three things this story's own `## Boundaries & Constraints` -> `Block If` list names as a stop condition: change a golden's `durationTicks`, its `PARITY_INERT` membership, or a per-golden scenario assertion. The `Never` list separately forbids re-recording a golden ("Story 2.5 owns the real re-record"), and AC 10 requires the full suite to report zero failures. This is a verification failure the story cannot resolve within its own mandate -- not a judgment call to paper over with a code comment or a `deferred:` entry.

All other `## Verification` commands pass. All 44 changed/added files (implementation subagent's diff plus this frontmatter/result update) are left **uncommitted** in the working tree for the lead -- no commit was made; build-auto commits only at finalize, which this run never reached.

Left for the lead to decide: (a) treat the golden drift as in-scope groundwork for Story 2.5 and pull the affected header/duration/`PARITY_INERT` updates forward with an explicit amendment here, or (b) investigate stabilising `tools/export.py`'s collision-node ordering (e.g. a stable sort key independent of Blender's `bpy.data.objects` iteration) so unrelated existing nodes' array indices do not shift when new geometry is added -- not explored by the implementation subagent and not a task this spec named.

### Rework iteration 2 halt, 2026-09-01

Status: blocked

Blocking condition: implementation verification failed

The lead's own investigation (recorded above under `### Rework -- iteration 2`) disproved the previous halt's diagnosis: the four red `test/replay-goldens.test.ts` failures were not forbidden golden drift but the correct report of a real routing defect, so `status` was reset to `in-progress` and five `[Rework]` tasks were added under `## Tasks & Acceptance`. A fresh implementation subagent worked all five (see the `**DONE**`/`**NOT DONE**` annotations added to each item above); the lead independently reconfirmed every result from a clean shell before this halt.

**Three of five rework tasks landed clean, independently reconfirmed:**

1. **Flat-topped-body traps fixed.** `col_sling_l/r`, `col_dragon_leg_l/r` and `col_ramp_wall_l` gained a sloped north face (`add_box_wall_sloped()`, the same DW-119 mechanism 2.1a proved on `col_wall_bottom_l/_r`). The six DRAGON-bank targets are individually only 11 mm wide -- under the ball's own 13.495 mm radius -- so an individual bevel does not work (tried and measured stuck four different ways: outward-from-centre, reversed on one target, uniform, and a full-depth triangular bevel); reverted them to plain flat bodies and added one new wide `col_dragon_bank_backstop` wall spanning the whole bank instead, which is what actually resolved it. Verified by the new drop-from-above sweep in `test/shot-routing.test.ts`.
3. **`test/shot-routing.test.ts` rewritten against all five named weaknesses**: positional-progress-over-a-window replaces the speed-only stall check (`assertNotStranded` no longer passes a ball bouncing in place at 33-125 mm/s); the flipper-band-or-leaves-play assertion is now real and applies to all 10 `describe` blocks, not 5; tick budgets raised to 4000-6600 ticks to match measured real descent times (were 500-2500); a new descending-release-from-above sweep exercises the rebevelled bodies, the only direction that produced the stalls. All 24 cases pass.
4. **False root-cause text corrected** in all five `test/replays/*.golden.json` `notes` fields. The "Blender's alphabetical `bpy.data.objects` ordering reshuffling every existing node's array position" claim was investigated and disproved twice: the collision document is alphabetically sorted throughout (the ~28 new names interleave rather than reshuffling relative order), and rebuilding with the wall-height (`DW-53`) and deflector-deletion (`DW-58`) changes individually reverted reproduces the two affected goldens bit-identically either way, ruling both out individually. The notes now state plainly that the true mechanism was not isolated by either pass, rather than repeating the disproved claim.

**Two of five rework tasks remain genuinely blocked, each matching a `Block If` this spec's own `## Boundaries & Constraints` names explicitly:**

- **Loop return -> inlane (item 2).** Multiple angled-diverter designs were built and measured against the real physics pipeline at the shot-routing tests' own plausible shot speed (2200 mm/s): thin and thick `add_channel_rail()`-style diverters at several angles and heights (both straight-down and true-perpendicular backing offsets), and a solid triangular wedge. Every version either (a) produced negligible net lateral drift -- under 5 mm across the whole ~500 mm remaining descent, confirmed by direct vx/vy telemetry showing an initial deflection decaying to a near-straight fall within 10-30 ticks -- nowhere near the ~50 mm needed to clear the divider; or (b), built with enough solid depth for a firmer response, instead capped the ball's own unobstructed ascent well short of its peak (the DW-119 mechanism in reverse: a wide backing shelf under a steep face); or (c), when routed through the only other physically available corridor (the Ramp's own channel -- the gap between `col_ramp_wall_r` and the loop's own lower rail is 5.4 mm, under the ball's own radius, so there is no other path into the interior there), measurably broke the Ramp's own passing shot (`s_ramp_made` stopped closing in `test/shot-routing.test.ts`'s own Ramp case). No version separated the loop return from the shared `*_divider_*_hi` post gap without regressing already-proven geometry. Per this task's own text ("if new geometry cannot fit without moving [the 2.1a bounds], that IS a Block If: HALT and say so") this is reported rather than forced. Left at the original (pre-rework-iteration-2) full-height rail geometry. A working fix likely needs either a multi-surface funnel proven out empirically (the way the existing bottom funnel under the drain triangle was) or a renegotiated Ramp position opening a genuine >= 27 mm corridor -- both beyond a rework pass's own mandate.
- **The residual `test/replay-goldens.test.ts` failures (item 5).** Re-running the full suite did not clear them: **86 files / 1182 tests / 1178 passing / 4 failing**, independently reconfirmed by the lead from a clean shell (`BLENDER` exported), identical to the implementation subagent's own count. Confirmed unrelated to the Loop-return item above: neither affected golden's ball trajectory (`roll-and-drain`, `hold-and-release`) ever leaves `y < 450`, well below every rebevelled body and the new bank backstop. `roll-and-drain`: the served ball is still in play at the golden's recorded `durationTicks` (8200); measured directly by extending the identical recorded transitions past the bound, the ball keeps descending along the sloped bottom-wall face (`DW-119`'s own mechanism, working as designed) and drains for real between tick 8640 and tick 8650 -- **440-450 ticks (~5%) past the recorded window**. `hold-and-release`: the raised bat's measured deflection is **0.027 mm** against the golden's own pinned **>5 mm** threshold, essentially unchanged from the original implement-stage halt's own measurement. Per this story's own `Block If` ("A golden's `durationTicks`, `PARITY_INERT` membership, or per-golden scenario assertion has to change to keep it green") and the `Never` rule ("Never re-record a golden... Story 2.5 owns the real re-record"), neither test was edited and neither golden was re-recorded; both are left red with the measured residual recorded in their own `notes` fields, exactly as this rework task's own instruction specifies.

**All other `## Verification` commands pass, independently reconfirmed by the lead from a clean shell:** `pnpm typecheck` (clean, all three projects), `pnpm lint:boundaries` (clean, 83 files under `src/`), `pnpm check:headers` / `pnpm check:attributions` (clean), `pnpm check:ad7` (still exits 1, naming `AD-7`/`DW-70`/`bd_trough` -- no regression), `pnpm build && pnpm check:dist && pnpm check:size` (all clean; 0.844 MB measured against a 2.75 MB budget). AC 10's numeric floor (>= 80 files / >= 1047 passing) is exceeded (86 files / 1178 passing), but the `## Verification` command's own "zero failures" expectation is not met. The Matrix Test Audit is satisfied: every I/O & Edge-Case Matrix row's covering test (switch-max-speed, switch-zones, export-py's concave-footprint and device-presence cases, collision-loader, vertical-containment, ball-render, blender-resolve) ran and passed; the four failures are confined to `test/replay-goldens.test.ts`, outside the frozen `<intent-contract>`'s matrix.

No commit was made; all changes (the bevel/backstop geometry and its regenerated `.blend`/`.collision.json`, the rewritten `test/shot-routing.test.ts`, the corrected golden `notes`, the refreshed `assetHash` headers, and the re-dated `ATTRIBUTIONS.md` rows) are left in the working tree, uncommitted, for the lead. `baseline_revision` above (`6ffc1711f4b1a7a0c44a494ce94cb21a14ece90f`) is the tree at the start of this rework pass.

**Left for the lead / author to decide:** whether AC 1's Loop-return-to-inlane requirement needs to move (which would mean relaxing a 2.1a bound this rework was told not to move unilaterally) or whether a further rework attempt should try a different diverter mechanism (a proven-out multi-surface funnel, or a renegotiated Ramp position); and whether the two residual golden failures are accepted as this story's own to close (`durationTicks` widened, or the divergence investigated further to find the true mechanism) or handed to Story 2.5 as recorded groundwork for the real re-record. The author's own planning-stage condition (a) anticipates exactly this outcome: "if the implement/review loop reaches the 3-iteration rework cap, the lead STOPS and surfaces rather than pushing for convergence -- a controlled stop with findings in hand is the intended outcome, not a failure." This halt reports rework iteration 2's residual findings for that decision.

---

### Historical -- the two intent gaps this re-dispatch resolves (RESOLVED 2026-08-31 by the author; retained as the evidence behind the amendments, not as open questions)

**Gap 1 -- AC 8's Backglass quad has no referent, and no story before 2.6 creates one.**

`epics.md:941` requires "the Backglass quad occupies a strip at the top of the view". No backglass or backbox geometry exists anywhere in the repository: `cabinet_root` is authored childless and commented as such (`tools/make-placeholder-blend.py:447-449`, `_ = cabinet_root`); the committed `public/assets/dragonwar.glb` holds only `playfield_root`, `cabinet_root`, `pivot_pitch`, `vis_playfield`, `l_insert_left`, `bd_trough`, `bd_shooter`; the only "backglass" token under `src/` is `src/presentation/backglass/.gitkeep`, whose entire contents read "Filled by Story 2.6 (the DMD Backglass)"; and `gi_backbox` is a GI lighting-channel name, not geometry. Story 1.4 promised **one** `vis_` mesh and never mentions a Backglass. Story 2.6 **presupposes** the quad -- "mapped onto **the backbox quad visible at the top of the fixed view**" -- and authors no geometry. Story 5.3 models the backbox, in Epic 5, which `epics.md:290` marks "allowed to trail, never blocking epics 1-4". So the clause is unmeasurable as worded, and two readings are equally defensible with nothing in the intent to select between them:

- **(a) Story 2.1b authors a placeholder `vis_backglass` quad under `cabinet_root`** so the framing criterion has something to assert against and Story 2.6 has its surface. This is in keeping with AD-11's placeholder doctrine ("Epic 1 ships a placeholder `.blend` of primitives ... the pipeline, not the art, is the deliverable"), but it may also require **re-aiming the fixed camera** -- `createFixedCamera()`'s standoff (`y = -700`) and height (`z = 1300`) are hand-picked absolutes, and a backbox rising in `+z` beyond `y = 1066.8` projects above the far playfield edge, which is already near the top of the frame. Re-aiming changes Story 1.4's shipped deliverable and would move `test/scene-smoke.test.ts:311-336`.
- **(b) The Backglass clause defers to Story 2.6**, which authors the quad as part of rendering onto it, and Story 2.1b's AC 8 asserts only the playfield-feature legibility half.

**Recommended amendment (author's call, Rule 5 "ask first" -- it defers scope between stories and touches Story 5.3's material):** take **(b)** and amend `epics.md:941` to end at "... are legible", moving the Backglass-strip clause into Story 2.6's first acceptance criterion together with an explicit instruction to author the placeholder `vis_backglass` quad under `cabinet_root` and to re-aim the fixed camera if the strip does not fit. That keeps 2.1b's camera check to geometry it actually draws, gives the quad a single owner before its first consumer, and leaves the Walk-up (Story 4.6) and Story 5.3 unaffected. If **(a)** is preferred instead, say so and state whether re-aiming the fixed camera is in scope for 2.1b.

**Gap 2 -- the pop-bumper count is stated nowhere, and `TABLE` cannot declare a wildcard.**

AC 3 requires `TABLE.switches` to declare "`s_pop_*`" and `TABLE.coils` to declare "`c_pop_*`". Every artifact in the repository uses that literal wildcard and no count: FR-31 (`prd.md:211`) and CAP-31 (`SPEC.md:139`) both say only "pop bumpers", and **the same sentence tags the neighbouring Top lanes `[ASSUMPTION: count]` and settles them at three, and settles the slingshots at two** -- so the omission is conspicuous rather than implicit. AD-5 (`ARCHITECTURE-SPINE.md:116`), AR-12 (`epics.md:149`) and `reconcile-brief.md:53` all carry the same wildcard. The brief never lists pop bumpers among the designed shots at all; its only mention is "pop bumper and slingshot placement" in the geometry to-do list (`addendum.md:136`). The research digests contain no pop-bumper diameter, body size, skirt size or placement -- only a coil part number and an MPF example pulse time.

The count is not a detail an implementer can pick: it fixes the switch and coil sets (and therefore `tableHash` and all five golden headers), the geometry drawn, the space left for the Loops and the bank, and the work in Story 2.2. Getting it wrong costs a Blender re-run, a re-export, five golden headers and every dimensional gate.

**Recommended amendment (author's call -- Rule 5 "ask first", a numeric target with product meaning):** state the count and the names in `epics.md` Story 2.1b AC 2 -- **three pops named `s_pop_1`, `s_pop_2`, `s_pop_3` with coils `c_pop_1..3`** is the conventional modern nest and fits the free space above the Ramp, but it is a product decision and this workflow will not make it. Record the answer in `docs/decisions.md` alongside the OQ-5 and Ramp-return decisions (task 23 already provides the row).

**Everything else is planned.** This spec is complete apart from the two clauses above: the Code Map, all 29 tasks, AC 1-7 and AC 9-10, the Design Notes (governing ADs, the `settleClass` mapping, the zone-requirement derivation, the `DW-120` and `DW-69` scoping rationales, the `epics.md:923` OQ mislabel, Rule 1/2 lists, the Rule 17 inbox with all eight entries addressed and none declined) and the Verification section with per-AC mutations are all written and were verified against the working tree at `862ca17`. To resume: answer both questions, apply the amendments, reset this spec's `status` to `draft`, re-run the epic-context pre-warm if `epics.md` changed, commit, and re-dispatch -- step-01 resumes a `draft` spec in place and preserves the intent contract verbatim.
