---
title: 'Story 2.11: Tilt warnings, Tilt and Slam tilt'
type: 'feature'
created: '2026-09-08'
status: 'done'
baseline_revision: '217fa70aa2152ade3e44a7e0e42cb5691a341110'
baseline_commit: '217fa70aa2152ade3e44a7e0e42cb5691a341110'
review_loop_iteration: 0
followup_review_recommended: true
context: []
warnings: ['oversized']
deferred:
  - summary: >-
      Tilt (and, already today, game over) does not disable the manual
      plunger, contradicting AD-5's own enumeration of the hardware set
      "Tilt, game over and Attract disable together". The manual plunge
      shares one coil gate with the autolauncher, and that gate is
      structurally excluded from HARDWARE_COILS.
    evidence: |-
      src/sim/physics/machine.ts:306 gates the manual plunge on
      `coilEnabled.c_autolaunch`: `plungerMechanics.applyFrame(tick, frame,
      coilEnabled.c_autolaunch)`. There is no `c_plunger` coil in
      TABLE.coils (src/sim/table/dragonwar.ts:301-318). `HARDWARE_COILS`
      (src/sim/rules/ball-controller.ts:163-172) filters out every coil any
      ball device names as an eject coil or a `ballSearchOrder` pulse step,
      and `bd_shooter.ballSearchOrder` pulses `c_autolaunch`
      (dragonwar.ts:389-392), so `c_autolaunch` can never be in the batch.
      Measured consequence at this tree: the one production disable sweep
      that exists (game over, ball-controller.ts:727-730) leaves
      `coilEnabled.c_autolaunch` true, so a player can still manually plunge
      a ball resting in the shooter lane after game over. Story 2.11 adopts
      the same batch for Tilt and therefore inherits the same gap. It is
      NOT newly created by this story, and closing it is not a one-line
      change: disabling `c_autolaunch` on Tilt turns DW-222's stalled
      shooter-lane ball into an unrecoverable hang (no autolaunch AND no
      manual plunge) until Story 2.12's ball search lands, and it requires
      a matching re-enable in `startBall()` that today enables only
      `HARDWARE_COILS`.
    location: >-
      src/sim/physics/machine.ts:306 · src/sim/rules/ball-controller.ts:163-172, 727-730
    severity: medium
  - summary: >-
      Adding `tiltWarnings` to TUNING makes the dev tuning panel render a
      hot-appliable row for it, but a hot-apply cannot change a sim
      adjustment: `GameStart.adjustments` is built by the host at boot and
      `hostLoop.reset({ tuning })` does not rebuild it, so the row is inert.
    evidence: |-
      src/host/dev/tuning-panel.ts:54-69 (`enumerateTuningRows`) walks every
      leaf `TuningEntry` in the whole TUNING tree with no key-name, suffix or
      allowlist filter, and every row wires to `hotApply()` ->
      `buildOverriddenTuning()` -> `resolveTuning()` ->
      `deps.hostLoop.reset({ tuning: resolved })` (tuning-panel.ts:79-95,
      179, 208-249). `reset()` passes only `tuning`; `GameStart.adjustments`
      is a separate literal built once at `src/host/boot.ts:276-281`. AD-14's
      preset/override layer is explicitly deferred
      (src/sim/contracts/replay.ts:13-15, "preset (deferred)"), so there is
      no layering step a hot-applied table default could flow through. The
      value itself is correct and provenance-carrying where AD-15 says it
      belongs; only the panel row is misleading.
    location: >-
      src/host/dev/tuning-panel.ts:54-69 · src/host/boot.ts:276-281
    severity: low
  - summary: >-
      `describeEvent()`'s remaining unexercised arms shrink from eight to
      zero in this story, but the pattern that produced them is unguarded -
      nothing forces a NEW SemanticEvent member to gain an executing
      assertion, only a compiled case arm (the `never` tail is a typecheck
      gate, not a `pnpm test` gate).
    evidence: |-
      test/contracts.test.ts:240-277 (`describeEvent`) and its
      `const neverEvent: never` tail at :270-275. The tail fails
      `pnpm typecheck` when a union member has no case, but a case body with
      a wrong field reference still typechecks and ships unless some
      assertion executes it - which is DW-217's and DW-223's shared root
      cause, now fixed twice by hand (Story 2.9's three arms, Story 2.10's
      two) and a third time here. A completeness ratchet over the union
      (every member appears in at least one executing `describeEvent(...)`
      assertion) would fix the class rather than the instance, in the shape
      of `test/tuning.test.ts`'s own scalarKeys ratchet, but it needs a
      runtime enumeration of a type-level union and is not a two-way door.
    location: >-
      test/contracts.test.ts:240-277
    severity: low
  - summary: >-
      A `ball_ended` event arriving while the `tilt_warning` screen is
      already SHOWING silently replaces it with no memory of the warning --
      the one direction DW-247's carry does not cover (a NEW ball_ended
      racing an ALREADY-DISPLAYED warning, rather than a NEW warning racing
      an already-armed ball_ended hold).
    evidence: |-
      `src/presentation/backglass/frame.ts`'s `ballEndedEvent` arming
      branch is checked first in `advanceBackglass()`, unconditionally,
      regardless of `view.screen`, and its `pendingTiltWarning` carry is
      `view.pendingTiltWarning || Boolean(tiltWarningEvent)` -- both false
      when the warning is already being SHOWN (not merely pending) and no
      new `tilt_warning` event arrives on the interrupting frame. This is
      pre-existing, not introduced by this iteration's DW-247 fix: the
      arming branch's unconditional priority over whatever screen is
      currently showing predates the smoke rework (Story 2.11 task 10), so
      a live WARNING interrupted by a drain was equally lost before this
      fix landed. Found by the Edge Case Hunter review layer during this
      iteration's code review; verified by tracing that the pre-fix code
      exhibits the identical behaviour.
    location: >-
      src/presentation/backglass/frame.ts (the `ballEndedEvent` arming
      branch, near the top of `advanceBackglass()`)
    severity: low
---

<intent-contract>

## Intent

**Problem:** Nothing in the shipped product can set `machine.tilt.tilted`. Physics already emits both cabinet-sensor switches — `createCabinetMechanics()` level-tracks the plumb bob's `isOverThreshold` and the slam detector's windowed nudge count and pushes `s_tilt_bob` / `s_slam_tilt` edges into the same `switchEvents` array `sim/loop` hands `rules.step()` (`src/sim/physics/cabinet/index.ts:204-207, 248-255`) — but the devices layer's `buildPlayfieldSwitches()` **excludes both settle classes by construction** (`src/sim/rules/devices/index.ts:189`) and no branch of its six-stage dispatch (`devices/index.ts:249-384`) matches either switch, so both are **silently dropped** and emit no device event of any kind. AD-19 forbids anything outside `src/sim/rules/devices/` from reaching for a raw `SwitchEvent`, so no other layer can pick them up. The consequence is a whole band of already-shipped read-side code that no test can reach in play: Story 2.9's ball-save disarm guard and deferred-autolaunch guard (`ball-controller.ts:676-678, 642`), Story 2.9's `l_ball_save` inertness while tilted (`lamps.ts:78-83`), and Story 2.10's bonus forfeit (`ball-controller.ts:694-695`) — Story 2.10's own record says its tilted ball was *"only ever seeded via `initialState`, never reached in play"*. `SemanticEvent` (`src/sim/contracts/events.ts:246-260`) carries no tilt member; `DmdScreen` (`src/presentation/backglass/frame.ts:62`) carries no TILT screen, though its own comment already names one as this story's; `disarmBallSave()` (`src/sim/rules/ball-save.ts:73-79`) has **zero** production callers; `tiltWarningSpacingTicks` and `tiltSettleTicks` resolve on every `resolveTuning()` and are read by **nothing**; and `GameAdjustments.tiltWarnings` has no table default at all (`DW-36`), even though AD-15's own Rule names `tiltWarnings` in `sim/table/tuning.ts`'s inventory.

**Approach:** Give the two cabinet sensors a device-event seam and put one owner behind it. `src/sim/rules/devices/index.ts` gains a rules-side structural derivation of the two switch names from their unique `SettleClass` — mirroring the file-private `switchNameForSettleClass()` physics already uses (`cabinet/index.ts:118-126`), which `sim/rules` may not import (AD-1) — and Stage 3's closed-edge loop emits two new `DeviceEvent`s, `tilt_bob_closed` and `slam_tilt_closed`. `PLAYFIELD_SWITCHES` is untouched: a bob closure is neither "the ball reached the playfield" nor "has anything closed?". A new `src/sim/rules/tilt.ts` holds `createTiltController(adjustments, tuning)`, constructed by `createRules()` alongside the ball controller and stepped from `rules.step()` **before** the ball controller so a Tilt on tick *t* is already true for every guard the controller runs on tick *t*. It owns the two semantic windows (`tiltWarningSpacingMs` for eligibility, `tiltSettleMs` for the warning cool-down) as closure state — never `GameState`, because `machine.tilt` is present in all five goldens' attract snapshots and widening it is a state-hash re-record — writes `players[currentPlayer].tiltWarnings`, `machine.tilt`, `machine.hardwareEnabled` and (through `disarmBallSave`, AD-18's own named interface) `machine.ballSave`, and emits `tilt_warning { player, remaining }`, `tilt { player }`, `slam_tilt` plus the `CoilCommand disable` batch. `DW-36` is closed by transcribing FR-14's default into `TUNING.tiltWarnings` — the one place AD-15 names — and having both `DEFAULT_ADJUSTMENTS` and `src/host/boot.ts`'s real `GameStart` read it, so the figure has exactly one definition. The Backglass gains two `DmdScreen` members driven by the two different truths AD-9 allows: the transient warning from the event, the TILT condition from the snapshot.

## Boundaries & Constraints

**Always:**
- `src/sim/rules/devices/` stays the only consumer of `SwitchEvent` (AD-19, enforced textually by `tools/boundary-lint.mjs`'s `rules-no-switch-event-outside-devices` check at `:661-771`). The tilt controller consumes `DeviceEvent`s and never a switch.
- The two switch names are derived **structurally from their unique `SettleClass`**, never typed as literals: `DEVICE_NAME_PATTERN` (`tools/boundary-lint.mjs:97`) matches `s_tilt_bob` and `s_slam_tilt`, and the only path-level exemption under `src/**` is `src/sim/table/dragonwar.ts` (`boundary-lint.mjs:560-565`). Exactly one `TABLE.switches` entry carries `settleClass: 'tilt_bob'` and exactly one `'slam'` (`dragonwar.ts:249-250`); anything else throws as a `TABLE` authoring defect rather than degrading (`cabinet/index.ts:118-126` and `ball-controller.ts:192-198` are the two in-tree shapes).
- `sim/rules/**` never imports from `sim/physics/**` (AD-1's dependency graph). Note this direction has **no** dependency-cruiser or boundary-lint rule today — it holds by convention and by the fact that nothing does it — so the rules-side derivation is authored independently rather than by reaching for the physics helper (which is file-private in any case).
- `GameState` mutates only inside `rules.step()` (AD-7), gated at source by `test/ad7-device-slots.test.ts:445-489`, which permits exactly two writes to `sim/loop`'s `state` binding. `tiltWarnings` is player-scoped; `tilt`, `hardwareEnabled` and `ballSave` are machine-scoped.
- Every folding stage returns the **same `GameState` reference** when nothing changed (`src/sim/rules/bonus.ts:138-146, 187-195` is the precedent, and `rules/index.ts:229-234` records why structural sharing is load-bearing for DW-70's identity gate).
- Both semantic windows are read through the resolved tuning, at production magnitude, and every test offset is authored as a literal at the probe — never read back from the tunable (epic vacuity #43; Story 2.9 shipped `ballSaveGraceMs` and `ballSaveHurryUpMs` each settable to `1` with the whole suite green, twice).
- Every window mark the tilt controller holds is **reset-safe against a restarted timeline**: `src/host/loop.ts`'s `reset()` (tuning-panel hot-apply, replay playback, the two dev hatches in `boot.ts`) rebuilds the sim with `createLoop()` and restarts `tick` at 0 while the surrounding closures survive. `src/presentation/backglass/frame.ts:180-192` records the same failure once already — a stale deadline froze the panel for minutes. A recorded mark strictly greater than the current `tick` is a mark from a different timeline and must be discarded, not compared against.
- `TUNING.tiltWarnings` is a top-level `entry(value, source, confidence)` with a non-empty `source` and a `confidence` from `high | medium | low | unverified`, **and** is listed by name in `test/tuning.test.ts`'s `scalarKeys` — the completeness ratchet at `test/tuning.test.ts:92-108` derives the declared set from `TUNING` itself and reddens on any unlisted top-level entry. It carries **no** unit suffix (a count, following `skillShotAward` at `tuning.ts:637-648`), and therefore derives no `…Ticks` sibling.
- Every drain-driving test runs at a **non-saving** tuning (`NO_BALL_SAVE_TUNING`, five byte-identical file-local definitions) or seeds the device disarmed, and asserts `ball_ended` actually arrived. A live ball save returns from `ball-controller.ts:680-685` before the entire end-of-ball path — "a parking entry taking `ballsInPlay` to 0 ends the ball" is false; there are two exits.
- Any harness built to refresh the goldens lives in the scratchpad, never under `test/` (a file matching `test/**/*.test.ts` silently joins `pnpm test`).

**Block If:**
- Any golden's `expectedHash`, `expectedGameStateHash`, `expectedCheckpointHashes`, `checkpointTicks`, `transitions`, `coilPrologue`, `durationTicks`, `header.assetHash`, `header.tableHash`, `header.physicsVersion`, `header.tickHz`, `header.physicsSeed`, or `header.gameStart.{seed,adjustments,highscores}` moves. Only `header.gameStart.tuning` (one new block) and an **appended** `notes` may differ. A moved body field or state hash means something other than a tuning-registry change happened: **HALT and report**; a trajectory or state-hash re-record needs the author's explicit grant and is out of this story's budget.
- Any widening of `TiltState`, `MachineState`, `PlayerState` or `GameState`'s shape would be required. `machine` is present in all five goldens' `attract` snapshots, so a new machine-scoped field moves `expectedGameStateHash` — a state-hash re-record.
- Any change to `TABLE` would be required. `tableHash()` hashes the whole frozen `TABLE` (`replay.ts:145-147`); this story's seam is derived from switches that already exist, so `header.tableHash` must stay `8838dd46`.
- The implementation would require `src/sim/loop/**` to write any `GameState` field, or a third write to its `state` binding.
- `HARDWARE_COILS`'s derivation (`ball-controller.ts:163-172`) would have to be widened to include `c_autolaunch`. It is the complement of every ball-serving coil precisely so a `disable` cannot land in the same `coilCommands` batch as a serve pulse and swallow it (`DW-74`, pinned by `test/coil-enable.test.ts:77-88`). See `## Design Notes`, "Why AC 3's autolaunch clause is met by the guard, not the batch".
- The suite shows a red `check:ad7`, `check:corridor` or `check:reachability`. Every gate is expected green; a red one is a regression, not the status quo.

**Never:**
- Never emit a device event for `s_tilt_bob` or `s_slam_tilt` through `playfield_switch_closed`, and never add either switch to `PLAYFIELD_SWITCHES`. Its membership is **derived by subtraction** and is Story 2.12's answer to "has anything closed?" and Story 2.9's to "did the ball reach the playfield?"; a bob closure is neither. The set stays at **28** switches.
- Never reset the tilt bob by command, and never add a command or event that does (AD-7's Rule states this explicitly: *"The bob is never reset by command — its physical decay plus `tiltSettleMs` is the settle"*).
- Never let an uppercase `TILT` or `WARNING` appear anywhere under `src/sim/**`, in identifiers or literals, outside a comment. `test/backglass-frame.test.ts:796-811`'s negative control does a plain `contents.includes(literal)` over comment-stripped source, so a constant named `TILT_…` in `sim/rules` would redden the AD-9 "rules never format text" gate. Name rules-side identifiers in `camelCase`/`snake_case` (`tiltWarningSpacingTicks`, `'tilt_warning'`). Measured at this tree: the only occurrence of an uppercase `TILT` anywhere under `src/sim/**` today is the word `UNTILTED` inside a doc comment at `src/sim/contracts/events.ts:202`, which `stripComments()` removes before the scan — so the gate is clean *now* and one careless constant would break it. `WARNING` has zero occurrences.
- Never add a `GameState` field, a lamp, a GI channel, a flasher or a show. Flashers on Tilt (FR-45) and the `show_tilt` audio cue (FR-48) are **Epic 4's**, not this story's.
- Never add a `tilt` phase to `GamePhase`. Tilt is a machine condition (`machine.tilt`), not a phase.
- Never make any lamp other than `l_ball_save` tilt-aware. It is today the only tilt-aware lamp (`lamps.ts:106-108`); whether the other fourteen inserts should go dark under Tilt is undecided, not decided, and is not this story's call.
- Never build a second game-over → Attract path. Story 2.13 owns the general transition; Slam tilt writes the minimal state change that reaches the same Attract (see `## Design Notes`).
- Never decide `DW-204` (does the paying skill-shot lane freeze at `ball_launched`), `DW-212` (`l_lock` between balls and in Attract), `DW-226` (ball-save eligibility after a grace lapse), `DW-232` (is a saved ball entitled to a second skill shot from an automatic launch), `DW-236` or `DW-237` (the end-of-ball screen's count direction and its score line). All six are `decision-pending` on the author's decision sheet. `DW-232` in particular is tilt-adjacent — a saved ball's second skill-shot attempt — and this story neither builds on it nor pre-empts it: nothing here reads or writes the skill-shot mode.
- Never re-own `DW-219` (disarm never shrinks `untilTick` to a surviving source's window). Its routing to Story 3.7 was re-confirmed on 2026-09-08 with a corrected premise: with a single arming source a disarm empties `sources` and resets `untilTick` correctly, so this story cannot reach it however many times it calls `disarm`.
- Never compute a test's expected value from `tiltWarningSpacingMs`, `tiltSettleMs` or `TUNING.tiltWarnings` by re-importing it. Never derive a non-vacuity guard's expected count from the helper it is guarding (vacuity #44).
- Never assert "no event is emitted" without first establishing, in the same test, that something *could* have been emitted (vacuity #51 — two Story 2.10 assertions held only because nothing had been armed earlier in those runs). This story is full of negative assertions: no coil fires, no bonus is paid, no autolaunch, no second warning.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Bob closure reaches rules | `s_tilt_bob` `closed: true` in `switchEvents` | devices layer emits `tilt_bob_closed { tick }`; **no** `playfield_switch_closed`, no `button_pressed`, no `lane_entered` | No error expected |
| Bob opening | `s_tilt_bob` `closed: false` | **no** device event at all (Stage 3 is closed-edge only) | No error expected |
| Slam closure reaches rules | `s_slam_tilt` `closed: true` | devices layer emits `slam_tilt_closed { tick }`, and nothing else | No error expected |
| Derivation is structural | a hypothetical `TABLE` with two `settleClass: 'tilt_bob'` entries | module construction **throws** a named `TABLE`-authoring error, never degrades silently | Load-time throw (Conventions: load paths throw, step paths never do) |
| First warning | `phase: 'game'`, `currentPlayer: 0`, `players[0].tiltWarnings: 0`, `adjustments.tiltWarnings: 1`, first ever `tilt_bob_closed` at tick `T` | `players[0].tiltWarnings` 0 → 1; `tilt_warning { player: 0, remaining: 0, tick: T }`; `machine.tilt.tilted` still `false`; hardware still enabled; no `CoilCommand` | No error expected |
| Burst debounce (spacing) | the same run, further `tilt_bob_closed` at `T+15`, `T+30`, `T+45` (the bob's own oscillation; Story 1.7 measured the longest over-threshold run at **15 ticks**) | every one is **ignored** — no warning, no tilt, no event — because each falls inside `tiltWarningSpacingMs` (500 ticks) of the previous closure | No error expected |
| Eligible again | quiet from `T+45`, then `tilt_bob_closed` at `T+600` with `tiltWarnings: 1` and `adjustments.tiltWarnings: 1` | count already equals the adjustment → `tilt { player: 0, tick: T+600 }`, `machine.tilt.tilted` true | No error expected |
| Settle gates the next warning | `adjustments.tiltWarnings: 2`; warning at `T`; quiet; `tilt_bob_closed` at `T+600` | eligible (spacing satisfied) but **ignored**: `tiltSettleMs` (3000 ticks) has not elapsed since the last counted warning. `tiltWarnings` stays 1, no event | No error expected |
| Settle elapsed | the same run, `tilt_bob_closed` at `T+3100` | `tiltWarnings` 1 → 2, `tilt_warning { player: 0, remaining: 0 }` | No error expected |
| Zero-warning machine | `adjustments.tiltWarnings: 0`, `tiltWarnings: 0`, first eligible `tilt_bob_closed` | `tilt` fires immediately with **no** warning first (count already equals the adjustment) | No error expected |
| Tilt's hardware batch | the tilt tick | one `CoilCommand { action: 'disable' }` for **every** coil in `HARDWARE_COILS`, and `machine.hardwareEnabled` false | No error expected |
| Tilt disarms **all** ball-save sources | `machine.ballSave.sources: ['ball-controller', 'seeded-second']`, `untilTick` set | `sources` is `[]` and `untilTick` is `null` — every source removed, not just the controller's | No error expected |
| Tilt with nothing armed | `machine.ballSave: { untilTick: null, sources: [] }` | the disarm fold is a no-op returning the same `BallSaveState` reference | No error expected |
| A tilted drain does not save | live save window, `machine.tilt.tilted` true, drain takes `ballsInPlay` to 0 | **no** `ball_saved`, **no** trough pulse; `ball_ended` with `tilted: true` and `total: 0`; `players[i].score` unchanged; no `bonus_count_step` | No error expected |
| Tilted ball end pays nothing | untilted-earned bonus (letters 2, loops 1, multiplier 3), then Tilt, then drain, `NO_BALL_SAVE_TUNING` | `ball_ended` carries the **real** `bonusByCategory` and `multiplier` beside `total: 0` (specified, not a defect); the score does not move | No error expected |
| Next ball clears the tilt | the drain rotates to the next ball on the same tick | `ball_will_start` → `machine.tilt` back to `{ tilted: false, slamTilted: false }`; `ball_starting` → `hardwareEnabled: true` and an `enable` for every `HARDWARE_COILS` coil, **in the same batch as and after** the tilt's disables, so the enables win | No error expected |
| Warnings survive the ball | the same rotation | `players[i].tiltWarnings` is **unchanged** by `ball_will_start` — warnings are per player per game, like `letters` | No error expected |
| Warnings are per player | two players; player 0 collects a warning; play rotates | `players[1].tiltWarnings` is 0 and `players[0].tiltWarnings` is 1; a warning collected by player 1 moves only `players[1]` | No error expected |
| Tilt while nothing is in play (`DW-222`) | live save; drain at `T` → `ball_saved`, trough pulse, `awaitingSaveLaunch` set; Tilt at `T+50`; the re-served ball reaches `bd_shooter` at `T+200` | **no** `c_autolaunch` pulse (the controller's `!tilt.tilted` guard at `ball-controller.ts:642`); `awaitingSaveLaunch` is consumed either way, preserving the anti-double-fire property the 2.9 review added; the ball rests in the shooter lane and stays **manually plungeable** (`machine.ts:306` gates the plunge on `coilEnabled.c_autolaunch`, which this story never disables) | No error expected |
| …and the ball is still playable | the same run: `s_shooter_lane` **opens** at `T+400` (a manual plunge is exactly that, from rules' point of view) | `ball_launched` → `ballsInPlay` 1; ball save does **not** re-arm (the tilt emptied `sources`, and 2.9's arming gate requires the controller's own source to be present); the ball's drain produces `ball_ended` with `tilted: true`, `total: 0` — the run never ends with `ballsInPlay` at 0 and no `ball_ended` | No error expected |
| Untilted control for the above | the identical script with no tilt | the `c_autolaunch` pulse **is** emitted on arrival — which is what makes "no pulse" above discriminating rather than accidental (vacuity #51) | No error expected |
| Slam tilt in a game | `phase: 'game'`, two players mid-game, `slam_tilt_closed` | `slam_tilt { tick }`; `phase` → `'attract'`; `modes` → `[]`; `machine.hardwareEnabled` false with a `disable` for every `HARDWARE_COILS` coil; `machine.tilt` → `{ tilted: false, slamTilted: true }`; **no** `ball_ended`, no bonus paid, no score change; `players[]` **kept** so Attract can cycle the last game's scores | No error expected |
| Slam tilt in Attract | `phase: 'attract'`, `slam_tilt_closed` | nothing at all — no event, no command, the same state reference. There is no game to end | No error expected |
| Slam tilt after game over | `phase: 'game_over'` | nothing (same as Attract). "A game running" is `phase === 'game'` | No error expected |
| Warnings need a player | `phase: 'game'` with no `players[currentPlayer]` | the fold returns the same state reference and emits nothing | No error expected |
| Bob closure in Attract | `phase: 'attract'`, `tilt_bob_closed` | no warning, no tilt, same state reference — but the closure **does** update the spacing mark, because the bob's history is physical and does not care about phase | No error expected |
| Restarted timeline | a recorded window mark at tick 9000, then `tick` restarts at 0 (`hostLoop.reset()`) | the mark is discarded as belonging to a different timeline; the next closure is eligible immediately rather than being blocked for the rest of the session | No error expected |
| Backglass shows the warning | `FrameOutput` carrying `tilt_warning` | the panel switches to a `tilt_warning` screen for `TILT_WARNING_HOLD_TICKS`, rendering a `WARNING` row that lights dots in its own declared band; re-folding the identical frames with `events: []` never produces it | No error expected |
| Backglass shows TILT | a snapshot with `phase: 'game'` and `machine.tilt.tilted` true | the panel shows a `tilt` screen whose `TILT` row lights dots in its own band, and the same fold over the same snapshot with `tilted: false` shows the score screen with a different dot set | No error expected |
| The ball's end still wins the panel | a `ball_ended` event on a tilted ball | the `ball_ended` screen arms as it does today and holds for `BALL_ENDED_HOLD_TICKS`; the TILT screen does not pre-empt it | No error expected |
| `l_ball_save` goes dark | a live save window, then a Tilt | `lampsOf()` projects `l_ball_save` off — Story 2.9's guard, reachable in play for the first time | No error expected |
| The table default (`DW-36`) | `resolveTuning()` | `tiltWarnings` is present as a top-level entry with `source` and `confidence`; `DEFAULT_ADJUSTMENTS.tiltWarnings` and `src/host/boot.ts`'s real `GameStart` both read that one entry; the figure `1` appears once | No error expected |
| Goldens | the change is complete | all five carry the same `header.tableHash 8838dd46` / `assetHash ab163ff` / `physicsVersion v1-ce6772ef`; only `header.gameStart.tuning` (one new block) and an appended `notes` differ | `StaleReplayHeaderError` if the refresh is missed |

</intent-contract>

## Code Map

**Physics — read-only evidence, never imported by rules**
- `src/sim/physics/cabinet/index.ts` — `switchNameForSettleClass()` `:118-126`, **file-private**, throws when `matches.length !== 1`; called at `:204-205`. `stepLevel()` `:161-193` collapses each sensor's level into edges; `applyFrame()` pushes them at `:248-255` with independent `LevelTracker`s (`:206-207`), so the bob and the slam detector share no state. `src/sim/physics/cabinet/plumb-bob.ts:170-191` sets `isOverThreshold`; `src/sim/physics/cabinet/slam.ts:30-52` reads `tuning.slamNudgesPerWindow.value` and `tuning.slamNudgeWindowTicks.value` directly (physics reads resolved `…Ticks` fields; `shotWindowTicks()` is the `sim/rules/**` reader).
- `src/sim/physics/machine.ts:306` — `plungerMechanics.applyFrame(tick, frame, coilEnabled.c_autolaunch)`. **The manual plunge and the autolaunch pulse share one gate.** `:251` defaults `coilEnabled.c_autolaunch` true; `:266-276` writes the enable map for every `enable`/`disable` in a batch before any pulse is filtered (DW-74's mechanism, and the reason a same-tick disable-then-enable resolves to the last write).

**Rules — where the work lands**
- `src/sim/rules/devices/index.ts` — `buildPlayfieldSwitches()` `:186-209` with the exclusion at `:189`; `PLAYFIELD_SWITCHES` `:219` (test-only export, the `HARDWARE_COILS` precedent). `step()` `:249-384`: Stage 1 `:262-308`, Stage 2 `:310-325`, **Stage 3 `:327-355`** (`if (!event.closed) continue;` at `:330-332` — the closed-edge stage where the two new emissions belong, beside `dragon_hit` `:334` and `playfield_switch_closed` `:341-343`), Stage 4 `:357-368`, Stages 5/6 `:370-374`. `DevicesLayerStepResult` `:67-70`; `return { events, coilCommands }` `:383`.
- `src/sim/rules/devices/events.ts` — `DeviceEvent` union `:159-173`; `PlayfieldSwitchClosedEvent` `:114-118`, `ButtonPressedEvent` `:121-125`, `LaneEnteredEvent` `:85-89` are the shapes to follow (literal `type` discriminant, `tick: number`, payload typed off a `TABLE`-derived alias, never a hand-typed second list — the file header `:1-18` records why, DW-149).
- `src/sim/rules/index.ts` — `RulesStepResult` `:106-146`; `createRules(tuning, adjustments = DEFAULT_ADJUSTMENTS)` `:213`, `createBallController(adjustments, tuning)` `:215`. `step()` `:223-284`: `devicesLayer.step` `:227`, accounting `:235-240`, `creditBonusFromDeviceEvents` `:249`, `ballController.step` `:251`, `modeStack.step` `:260`, `advanceBonusMultiplier` `:271`, `nextState` `:273`, events merge `:275`, `coilCommands` merge `:281`. `DEFAULT_ADJUSTMENTS` `:199-204` with the 2026-09-06 review-correction comment `:177-187` that names `DW-36` and this story by id.
- `src/sim/rules/ball-controller.ts` (768 lines) — `hardwareCoils()` `:163-172` and `HARDWARE_COILS` `:172` (test-only export); `ballServingCoils()` `:148-161`; `BALL_SAVE_SOURCE` `:182`; `shooterLaunchCoil()` `:192-198` (**the same-layer structural-derivation precedent**); `emptyPlayer()` seeds `tiltWarnings: 0`. `startBall()` `:430-499` — latch resets `:438, :447`, `players` map `:456-458`, `machine` object `:468-474` (the `ball_will_start` tilt reset at `:471`), `coilCommands` `:483-486` (serve pulse + `HARDWARE_COILS` enables). `step()` `:501-764` — bonus drain `:507-518`, grace lapse `:527-529`, `awaitingSaveRelaunch` expiry `:531-540`, letters `:542-558`, Start `:560-588` (the only `phase: 'game'` write, `:564`), device-event loop `:615-655` (**the deferred-autolaunch arrival branch `:629-654`, with `awaitingSaveLaunch = false` at `:641` before the `!tilt.tilted` check at `:642`** — DW-222), drain branch `:657-761`, ball-save early return `:668-685` (the `!tilt.tilted` conjunct at `:677`), `ball_ended` `:714-722` (tilt forfeit at `:694-695`), game over / rotation `:724-739` (the only `phase: 'game_over'` write and the only `disable` sweep, `:727-731`), bonus arm `:741-760`.
- `src/sim/rules/ball-save.ts` (99 lines) — `EMPTY_BALL_SAVE` `:35`, `enableBallSave` `:42-47`, `armBallSave` `:49-63`, **`disarmBallSave` `:65-79` (zero production callers today)**, `hasGraceLapsed` `:96-99` whose doc comment names *"Story 2.11's later per-source Tilt `disarm()`"*.
- `src/sim/rules/bonus.ts:138-146, 187-195` — the two folds' `phase === 'game'` + `players[currentPlayer]` guard shape and same-reference-on-no-op contract.
- `src/sim/rules/lamps.ts:78-83, 106-108` — `projectBallSave(ballSave, tilted, tick, hurryUpTicks)`, the **only** lamp that reads `machine.tilt`; `lampsOf(state, ballSaveHurryUpTicks = 0)` `:146`.

**Contracts and table**
- `src/sim/contracts/state.ts` — `GamePhase` `:27` (`'attract' | 'game' | 'highscore_entry' | 'game_over'`), `BallSaveState` `:40-43`, `TiltState` `:45-54`, `MachineState` `:56-73`, `PlayerState` `:96-127` (`tiltWarnings` `:106`), `GameState` `:154-167`. **Frozen shapes — none may be widened (Block If).**
- `src/sim/contracts/events.ts` — the growth-convention comment `:86-93`; `BallSaveTimerStartedEvent` `:151-162`, `BallSavedEvent` `:164-174`, `BallEndedEvent` `:183-192` are the payload idioms; `SemanticEvent` union `:246-260` (13 members, **no tilt member**).
- `src/sim/contracts/replay.ts` — `GameAdjustments` `:17-22` (`tiltWarnings` `:19`), `GameStart` `:32-37`, and the `preset (deferred)` note `:13-15`.
- `src/sim/table/dragonwar.ts` — `SettleClass` `:36-43`; `s_tilt_bob` / `s_slam_tilt` `:249-250`; `coils` `:301-318` (**no plunger coil**); `bd_shooter` `:383-396` (`ballSearchOrder` pulses `c_autolaunch` at `:390` — the reason `HARDWARE_COILS` excludes it).
- `src/sim/table/tuning.ts` — `entry()` / `TuningEntry`; `slamNudgesPerWindow` / `slamNudgeWindowMs` `:306-307` with the doc block `:299-305`; **`tiltWarningSpacingMs` `:367` (500) and `tiltSettleMs` `:368` (3000)** with the doc block `:360-366`; `skillShotAward` `:637-648` (the suffix-less shape a count follows); `assertNoNestedMsKeys()` `:780-798`; `resolveTuning()` `:800-838` with the `…Ms → …Ticks` loop `:804-823` (the sibling copies `source`/`confidence` **verbatim** at `:822`); `TuningMsKey` `:705-707` and `ShotWindowMsKey` `:866` — a **computed** type over `typeof TUNING`, so `shotWindowTicks('tiltWarningSpacingMs', tuning)` and `shotWindowTicks('tiltSettleMs', tuning)` **already typecheck with no type edit**; `shotWindowTicks()` `:889-896` (its doc `:869-878` restricts it to `sim/rules/**` callers). **`tiltWarningSpacingTicks` and `tiltSettleTicks` already resolve and have zero readers anywhere in `src/`.**

**Loop, host and presentation**
- `src/sim/loop/index.ts` — `initialMachineState()` `:213-223` (`tilt` booted `:218`, `hardwareEnabled` false `:216`); `buttonSwitchByAction()` `:125-142` (a third derivation precedent, `sim/loop`, not importable from rules); `createLoop(...)` `:225-253`; `createRules(tuning, options.gameStart?.adjustments)` `:266`. **Read-only in this story.**
- `src/host/boot.ts` — the **real** `GameStart` `:276-281` (`adjustments: { …, tiltWarnings: 1, …, matchProbability: 0.08 }` at `:279`); the **dev replay recorder's** separate `GameStart` `:403-413` (`tiltWarnings: 3, matchProbability: 0` at `:407`) — the source of the goldens' recorded header figures, and `DW-185`'s subject, routed to 3.7 and **not** this story's to touch; `backglassView` `:227`; the sim-chain fold `:291-292`; `reset()` `:359-367` (deliberately does not re-seed `backglassView`, `:340-358`).
- `src/host/dev/tuning-panel.ts:54-69` — `enumerateTuningRows()`, an unconditional recursive walk of every leaf `TuningEntry` with no key or suffix filter; hot-apply at `:79-95, 179, 208-249`.
- `src/presentation/backglass/frame.ts` — `DMD_COLS/DMD_ROWS` `:39-40`; `DmdRow` `:54-59`; **`DmdScreen` `:62`, whose own comment names *"2.11's TILT"* as a future member**; `BackglassView` `:91-96`; `BALL_ENDED_HOLD_TICKS` `:118`; `advanceBackglass()` `:164-222` (the `ball_ended` arming `:168-179`, the reset-safe half-open hold guard `:180-213` with its rationale `:184-192`, the `bonus_count_step` fold `:208-212`, the attract branch `:215-219`, the score fallthrough `:221`); `LEFT_MARGIN_COL` `:224`, `LINE_PITCH_ROWS` `:226`; `formatScore()` `:229-233`; `buildScoreRows()` `:325-346`; `buildBallEndedRows()` `:367-376`; `renderFrame()` `:383-410` with the `never` tail `:395-409` that turns a new `DmdScreen` member into a compile error here.
- `src/presentation/backglass/raster.ts:39-69` — `rasterise()`, with the two silent bounds checks at `:49` (`destRow >= DMD_ROWS`) and `:59` (`destCol >= DMD_COLS`) that drop an out-of-range row's every pixel. This is vacuity #50's mechanism.

**Tests and gates — read-only evidence**
- `test/util/switch-script.ts` — `close()`/`open()` `:26-83` typed by `SwitchName`, so `close('s_tilt_bob')` and `close('s_slam_tilt')` are directly expressible; `RunRulesScriptOptions` `:170-179` (`tuning`, `adjustments`, `durationTicks`, `initialState`); `RunRulesScriptResult` `:181-192` (`finalState`, `statesByTick`, `events`, `coilCommands`, `modeEvents`); `runRulesScript()` `:228` with the out-of-range throw `:232` / `:91-97`.
- `NO_BALL_SAVE_TUNING` — **five** byte-identical file-local definitions (`test/backglass-frame.test.ts:42`, `test/backglass-integration.test.ts:52`, `test/rules-bonus.test.ts:53`, `test/rules-lifecycle.test.ts:34`, `test/rules-modes.test.ts:47`), each overriding **only** `ballSaveMs` → 1 and `ballSaveGraceMs` → 0. For a `createLoop` test it must be passed as `createLoop()`'s own `tuning` option, not `GameStart.tuning` alone (`backglass-integration.test.ts:47-73`).
- `test/rules-devices-headless.test.ts` — `ENTRY_FILES` `:193-206`; the completeness ratchet `:220-241` (`readdirSync` picks up any new `test/rules-*.test.ts` that is not `*-integration.test.ts`, so a new headless file reddens until listed); `FORBIDDEN_FAMILIES` `:169-176`; the transitive-closure scan `:131-161` and its assertion `:267-278`.
- `test/cabinet-bob.test.ts:33-42` — `burstFrames()`, ten `nudge_up` rising edges at the fastest achievable spacing (`on` at `1 + 2i`, `off` the next tick); `:119-122` pins the measured longest consecutive over-threshold run at **K = 15 ticks**. `test/cabinet-slam.test.ts:39-75` and `:100-136` prove the two sensors' independence in both directions.
- `test/cabinet-integration.test.ts:72-140, 165-186` — nudges injected into a real `createLoop` as `InputTransition`s (`{ tick, frame: { ...NO_FRAME, nudge_l: true } }`), and the `for (…) out = loop.advance(1, [])` idiom.
- `test/rules-lifecycle-integration.test.ts:27-84` — the canonical end-to-end shape: `createLoop({ collisionDoc, gameStart })`, press `s_start` via an `InputTransition` `:59`, the trough eject on the following tick `:70`, `loop.pulseCoil('c_autolaunch')` `:74`, then `advance(1, [])` in a loop until `ballsInPlay` reaches 1 `:84`.
- `test/rules-lifecycle.test.ts:80-90` and `:245-262` — the two `HARDWARE_COILS` loop assertions (enable at `ball_starting`, disable at game over), each with a `HARDWARE_COILS.length > 0` non-vacuity guard. **This is DW-181's precedent and the shape the Tilt disable batch must follow.**
- `test/coil-enable.test.ts:77-88` — DW-74's same-tick disable-then-pulse pin.
- `test/contracts.test.ts:240-277` — `describeEvent()` with the `const neverEvent: never` tail `:270-275` (a **typecheck** gate, not a `pnpm test` gate); executing arms `:278-308`. **Six of thirteen arms have executing assertions; eight do not** (`sim_time_discarded`, `ball_starting`, `ball_started`, `ball_launched`, `ball_missing`, `eject_failed`, `broken`, `device_overflow`) — that residual is `DW-223`.
- `test/tuning.test.ts:27-82` — `scalarKeys` (**31** entries); `:83-90` the per-entry `source`/`confidence` check; `:92-108` the completeness ratchet with its `> 20` floor; `:255-256` pins that a derived `…Ticks` sibling inherits `source`/`confidence`.
- `test/backglass-frame.test.ts:84-92` `litDotRows()`; `:148` `const BANDS = [0, 8, 16, 24]`; **`:390-415` the differential band assertion for Story 2.10's BONUS row — the standing precedent this story must reuse**; `:699-811` the English-literal scan, `DISPLAY_LITERALS` `:707`, positive control `:788-794`, negative control `:796-811` with its own `> 20` non-vacuity floor.
- `test/backglass-integration.test.ts:153-191` the whole-window row scan; `:194-220` the `events: []` re-fold control.
- `test/backglass-raster.test.ts:26-36` — `litDots()`.
- `test/ad7-device-slots.test.ts:445-489` — the `sim/loop` two-writes gate; `:190-206, :263-274` — the nested-harness reporter contract with its exact passing count of **3**.
- `src/sim/loop/replay.ts` — `tableHash()` `:145-147`, `assetHash()` `:150-152`, `PHYSICS_VERSION` `:214`, `buildHeader()` `:228-237`, `StaleReplayHeaderError` `:240`, `assertHeaderMatchesLiveEnvironment()` `:248-283` (the tuning comparison `:275-282`; **it never compares `header.gameStart.adjustments`**), `runReplay()` `:336` whose `createLoop({ collisionDoc })` at `:381` passes neither `tuning` nor `gameStart`. **There is no re-recording tool in the repository.**
- `test/replay-goldens.test.ts:123` `GOLDEN_NAMES`, `:153-163` the on-disk ratchet, `:165-175` the hash assertions; `test/golden-line-endings.test.ts:23-29` a **second** five-name list that must stay in sync.
- `test/replays/*.golden.json` — five files, all carrying `header.tableHash` **`8838dd46`**, `header.assetHash` **`ab163ff`**, `header.physicsVersion` `v1-ce6772ef`, measured at this tree. **None presses `s_start`** (verified: zero occurrences in any `transitions` array), so none reaches `phase: 'game'`, none starts a ball and none can read `adjustments.tiltWarnings`. Each header records `adjustments.tiltWarnings: 3` from the dev recorder while the live `DEFAULT_ADJUSTMENTS` carries 1 — that is `DW-185`, inert for exactly this reason, routed to 3.7 and out of scope here.
- **Baseline at `4b4388c`, measured with `BLENDER` exported: 115 files / 1906 tests / 0 failing / 0 skipped, ~89 s wall. `pnpm lint:boundaries`: `OK -- 106 .ts file(s)`.**

## Tasks & Acceptance

**Execution:**

1. `src/sim/rules/devices/events.ts` — add `TiltBobClosedEvent { readonly type: 'tilt_bob_closed'; readonly tick: number }` and `SlamTiltClosedEvent { readonly type: 'slam_tilt_closed'; readonly tick: number }`, and add both to the `DeviceEvent` union `:159-173`. No payload beyond `tick`: the subject is the discriminant, so no `SwitchName` needs to escape and no literal is spelled. *Rationale: AD-19 — the devices layer emits payload-complete device events and is the only consumer of `SwitchEvent`. **This extends AD-19's binding event enumeration and owes a spine amendment the LEAD must write before implementation — see `## Design Notes`, "The AD-19 amendment this story owes".***

2. `src/sim/rules/devices/index.ts` — add a file-private `switchNameForSettleClass(settleClass)` that filters `TABLE.switches` and throws a named error unless exactly one entry matches, resolve `tiltBobSwitch` and `slamTiltSwitch` once at module level beside `PLAYFIELD_SWITCHES` `:219`, and emit the two new events from Stage 3's closed-edge loop (`:329-355`), beside the existing `dragon_hit` and `playfield_switch_closed` pushes. **Name both constants in `camelCase`, exactly as `cabinet/index.ts:204-205` names its own pair** — a `SLAM_TILT_SWITCH` under `src/sim/**` would put an uppercase `TILT` in scanned source and redden `test/backglass-frame.test.ts:796-811`'s negative control the moment task 11 adds `'TILT'` to `DISPLAY_LITERALS` (see `## Boundaries & Constraints`, Never). **Leave `buildPlayfieldSwitches()` `:186-209` exactly as it is** — the two settle classes stay excluded and `PLAYFIELD_SWITCHES` stays at 28. *Rationale: AD-16 and `tools/boundary-lint.mjs:97, 557-586` make `'s_tilt_bob'` a lint error anywhere under `src/` but `dragonwar.ts`; AD-1 forbids importing the physics helper (which is file-private regardless). `shooterLaunchCoil()` (`ball-controller.ts:192-198`) is the same-layer precedent; `cabinet/index.ts:118-126`'s exactly-one throw is the stricter shape to copy, because two `tilt_bob` entries would silently pick one.*

3. `src/sim/contracts/events.ts` — add `TiltWarningEvent { type: 'tilt_warning'; player: number; remaining: number; tick: number }`, `TiltEvent { type: 'tilt'; player: number; tick: number }` and `SlamTiltEvent { type: 'slam_tilt'; tick: number }`, and add all three to the `SemanticEvent` union `:246-260`. *Rationale: AD-9 payload-completeness — presentation renders a warning from the event alone and never joins it to a later snapshot; `remaining` is what lets the panel say how much rope is left without reading `players[i]`. Follow `BallSavedEvent` `:164-174` for the `player`+`tick` shape.*

4. `src/sim/table/tuning.ts` — add a top-level `tiltWarnings: entry(1, "PRD FR-14: 'up to the Settings count (default 1 [ASSUMPTION: default; the research gives only the Competition preset value of 2])'", 'unverified')` beside the tilt window block `:360-368`. **No unit suffix** (a count, following `skillShotAward` `:637-648`), so it derives no `…Ticks` sibling and adds exactly **one** block to `resolveTuning()`'s serialized output. *Rationale: **`DW-36`.** AD-15's Rule names `tiltWarnings` explicitly in `sim/table/tuning.ts`'s inventory of table tunables; AD-14 makes the table default the first layer of a sim adjustment. Keep the `source` tight — it is hashed into every golden header.*

5. `test/tuning.test.ts` — add `'tiltWarnings'` to `scalarKeys` `:27-82` (32 entries). *Rationale: the completeness ratchet at `:92-108` derives the declared set from `TUNING` itself and reddens on any unlisted top-level entry — this is not optional bookkeeping, it is a gate.*

6. `src/sim/rules/index.ts` — make `DEFAULT_ADJUSTMENTS.tiltWarnings` `:201` read `TUNING.tiltWarnings.value` instead of the literal `1`, and update the `:177-187` review-correction comment to record that `DW-36` is now closed and where the figure lives. `src/host/boot.ts:279` — make the real `GameStart`'s `tiltWarnings` read the same entry. **Leave the dev replay recorder's `:407` literal `3` alone** — it is deliberate, it is what the goldens recorded, and `DW-185` owns that divergence for Story 3.7. *Rationale: one definition of the default. `DEFAULT_ADJUSTMENTS.pitchDeg` already reads `TABLE.reference.pitchDeg` `:200`, so a module-level read of the table layer from `sim/rules` is the established shape and a permitted import direction (`rules --> table`).*

7. `src/sim/rules/tilt.ts` — **new file**, GPL-3.0 header. `createTiltController(adjustments: GameAdjustments, tuning: ResolvedTuning): TiltController` with `step(state, deviceEvents, tick): { state, events, coilCommands }`. Resolve `tiltWarningSpacingTicks = shotWindowTicks('tiltWarningSpacingMs', tuning)` and `tiltSettleTicks = shotWindowTicks('tiltSettleMs', tuning)` once at construction (`ball-controller.ts:260-261` is the pattern). Hold two closure marks, `lastBobClosureTick` and `lastWarningTick`, both `number | null`, both **discarded when `tick` is strictly less than the recorded mark** (a restarted timeline). Behaviour, in order:
   - **`slam_tilt_closed`** and `state.phase === 'game'` → emit `slam_tilt { tick }`; return a state with `phase: 'attract'`, `modes: []`, `machine.hardwareEnabled: false`, `machine.tilt: { tilted: false, slamTilted: true }`, `players` and `currentPlayer` untouched; and a `disable` `CoilCommand` for every coil in `HARDWARE_COILS`. Any other phase: nothing, same state reference.
   - **`tilt_bob_closed`**: always update `lastBobClosureTick`, whatever the phase. Then, only when `state.phase === 'game'` and `state.players[state.currentPlayer]` exists and `machine.tilt.tilted` is false: the closure is **eligible** iff `lastBobClosureTick` was `null` or `tick - lastBobClosureTick >= tiltWarningSpacingTicks` (evaluated against the mark's value *before* this closure updated it). An eligible closure with `player.tiltWarnings >= adjustments.tiltWarnings` **tilts**; otherwise it counts a **warning** only if `lastWarningTick` is `null` or `tick - lastWarningTick >= tiltSettleTicks`.
   - A **warning** increments `players[currentPlayer].tiltWarnings`, sets `lastWarningTick = tick`, emits `tilt_warning { player, remaining: max(0, adjustments.tiltWarnings - newCount), tick }`, and changes nothing else.
   - A **tilt** emits `tilt { player, tick }`, sets `machine.tilt.tilted` true, `machine.hardwareEnabled` false, disarms **every** ball-save source, and pushes a `disable` for every coil in `HARDWARE_COILS`. It does not touch `tiltWarnings`.
   - Return the **same `GameState` reference** whenever nothing changed.
   - Import `HARDWARE_COILS` from `./ball-controller` rather than re-deriving it (one definition, DW-149), and **update its doc comment at `ball-controller.ts:171`, which currently reads "Test-only export"** — this story gives it a second, production reader, and a comment that misstates a symbol's reach is how the next story re-derives it by hand.
   *Rationale: AD-5 (Tilt disables the hardware set together, through the coil-enable channel), AD-7 (`tiltWarnings` player-scoped, `tilt`/`hardwareEnabled`/`ballSave` machine-scoped, mutated only inside `rules.step`), AD-18 (`Tilt disarms all`), AD-3 (the two rules-side windows are semantic and authored in ms, converted once at load), AD-15 (the window values stay on the production path). Closure state rather than `GameState` because `machine` is present in all five goldens' attract snapshots — a new field there is a state-hash re-record and a Block-If.*

8. `src/sim/rules/tilt.ts` (same file) — the disarm is a fold of `disarmBallSave` over a **snapshot** of `machine.ballSave.sources`, not a single call for the ball controller's own source and not a whole-device `EMPTY_BALL_SAVE` reset. *Rationale: AD-18's clause is "Tilt disarms **all**", and `disarmBallSave` (`ball-save.ts:73-79`) removes **one** source. With one source today the two are behaviourally identical, which is exactly why a single-source implementation would be untestable; the fold is what a seeded two-source state can falsify. This makes Story 2.11 `disarmBallSave()`'s first production caller, as `ball-save.ts:96`'s own comment predicted. **`DW-219` is not reached and is not re-owned**: with the sources emptied, `untilTick` returns to `null` correctly.*

9. `src/sim/rules/index.ts` — construct the tilt controller in `createRules()` beside `createBallController` `:215`, call `tiltController.step(stateAfterAccounting, deviceResult.events, tick)` between `:240` and the bonus credit at `:249`, thread its state into `creditBonusFromDeviceEvents`, merge its events into `:275` **before** the controller's, and merge its `coilCommands` into `:281` as `[...deviceResult.coilCommands, ...tiltResult.coilCommands, ...controllerResult.coilCommands]`. *Rationale: the tilt must be established before any stage that reads it — the ball controller's save guard (`:677`), its autolaunch guard (`:642`) and its bonus forfeit (`:694`) all run inside `ballController.step` and must see this tick's tilt. Placing it after the accounting fold and before the bonus credit mirrors `applyDeviceEvents` `:235`, the in-tree precedent for a stage folding this tick's device events straight from this file. The coil order matters: on a tick where a Tilt ends the ball and rotation starts the next one, the tilt's `disable`s must precede `startBall()`'s `enable`s so the enables win (`machine.ts:266-276` writes the map in batch order).*

10. `src/presentation/backglass/frame.ts` — add `'tilt_warning'` and `'tilt'` to `DmdScreen` `:62` (its own comment already names the second). Add `TILT_WARNING_HOLD_TICKS = msToTicks(2000)` beside `BALL_ENDED_HOLD_TICKS` `:118` — a **presentation constant, not a tunable**, exactly as its neighbour is, so it moves no golden. In `advanceBackglass()` `:164-222`, in this order: the existing `ball_ended` arming; the existing `ball_ended` hold branch; **then** `if (game.phase === 'game' && game.machine.tilt.tilted) return { screen: 'tilt', … }`; **then** a `tilt_warning` event arming with a hold; **then** the live warning hold, guarded by the same reset-safe half-open window as `:180-192`; then the existing attract and score branches. Add `buildTiltRows()` and `buildTiltWarningRows()` — one row each, `TILT` and `WARNING`, at `col: LEFT_MARGIN_COL, row: 0, emphasis: false` — and the two matching `case`s in `renderFrame()` `:383-410`. *Rationale: AD-9 — the transient warning is animated from a payload-complete event, the persistent TILT condition is continuous display state read off the snapshot (which is what the snapshot is for). Ordering: an ended ball's screen still wins, because the ball ending is the more specific event; the TILT condition supersedes a live warning hold, because a tilt always follows warnings. Each screen replaces the panel wholesale rather than adding a row to `buildScoreRows()` `:325-346`, whose combined line budget is already over-subscribed and is `DW-197`, routed to Story 2.13.*

11. `test/backglass-frame.test.ts` — append `'TILT'` and `'WARNING'` to `DISPLAY_LITERALS` `:707` (uncovered otherwise), and assert both new rows with the **differential band form** of `:390-415`: derive the band from the row's own declared `row`/`GLYPH_H`, require it non-empty on the frame that should show it, and empty (or a demonstrably different dot set) on a control frame that should not. *Rationale: Story 2.6's standing precedent, reproduced as vacuity #50 by Story 2.10 — a whole-buffer `some(d => d === 1)` predicate is satisfied by any other row, and `raster.ts:49, :59` drops an out-of-range row's every pixel in silence.*

12. `test/backglass-integration.test.ts` — a real-`createLoop` case that folds a tilted ball's frames exactly as `src/host/boot.ts:291-292` does, asserting the `WARNING` row appears at the warning tick and the `TILT` row after the tilt, plus the **`events: []` re-fold control** in the shape of `:194-220` proving the warning screen reads the event and not the snapshot. *Rationale: Rule 1's Integration AC. The control is what distinguishes "the Backglass reads the event stream" from "the Backglass re-derives from `snapshot.game.machine.tilt`" — which for the warning would be wrong, since a warning is not a state.*

13. `test/rules-tilt.test.ts` — **new**, headless, added to `ENTRY_FILES` (`test/rules-devices-headless.test.ts:193-206`) in the same change or the completeness ratchet `:220-241` reddens. Covers every rules-side row of the I/O matrix through `runRulesScript` with `close('s_tilt_bob')` / `close('s_slam_tilt')` scripts, each scenario run at **both** `currentPlayer` 0 and 1, with `initialState` for mid-game setups and `NO_BALL_SAVE_TUNING` for anything that drains. Pin `tiltWarningSpacingMs` and `tiltSettleMs` at **production** magnitude with every tick offset authored as a literal at the probe, never read back from the tunable; use the `adjustments` option (never a tuning override) to reach the `tiltWarnings: 0` and `tiltWarnings: 2` cases. *Rationale: Rule 8, Rule 19, and epic vacuity #43 — Story 2.9 shipped two tunables each settable to `1` with the whole suite green because every behavioural assertion ran under an override.*

14. `test/rules-tilt-integration.test.ts` — **new**, `-integration`-suffixed so the headless ratchet excludes it. Drive the bob through **real nudge input** end to end: `createLoop({ collisionDoc, gameStart, tuning })`, press `s_start`, serve and launch a ball, then apply `burstFrames()`-shaped `nudge_up` transitions (`test/cabinet-bob.test.ts:33-42`) and assert a `tilt_warning` arrives in `FrameOutput.events`; then a second burst after the spacing window and assert `tilt` arrives, `HARDWARE_COILS` receive their `disable`s, and the ball's drain produces `ball_ended` with `tilted: true` and `total: 0`. *Rationale: this is the story's load-bearing test. Story 2.10's own record says its tilted ball was "only ever seeded via `initialState`, never reached in play", and the epic's standing lesson is that a headless test which **scripts** the consequence it means to observe proves nothing about whether the consequence happens. Nothing else in this story proves the physics→devices→rules chain is actually connected.*

15. `test/rules-devices.test.ts` — assert `tilt_bob_closed` and `slam_tilt_closed` are emitted on the closed edge and **not** on the open edge, and that neither produces a `playfield_switch_closed`. The membership half is **already pinned** and must simply stay green: the existing AC 8 block at `:631-684` asserts the exact 28-member set (`:664-665`) and loops over the excluded switches asserting each is absent (`:684`) — do not duplicate it. *Rationale: the derived membership is Story 2.12's and 2.9's substrate; a regression that quietly admitted a cabinet switch would corrupt both.*

16. `test/contracts.test.ts` — add the three new arms to `describeEvent()` `:240-277`, each with an **executing** assertion using deliberately distinct field values in the shape of `:278-308`. In the same pass add executing assertions for the **eight** arms that still have none (`sim_time_discarded`, `ball_starting`, `ball_started`, `ball_launched`, `ball_missing`, `eject_failed`, `broken`, `device_overflow`). *Rationale: **`DW-223`**, `wontfix-accepted` with a `reopen_if` that fires exactly here — *"a story touches that switch for another reason and can close the pattern cheaply while it is already there"*. Eight one-line assertions is cheap, and it takes the file from six of today's thirteen arms exercised to **all sixteen** (thirteen plus this story's three).*

17. `test/replays/*.golden.json` (five) — **header-only** refresh: `header.gameStart.tuning` gains exactly **one** block (`tiltWarnings`, no derived sibling) and `notes` is **appended**. Leave `header.tableHash` (`8838dd46`), `header.assetHash` (`ab163ff`), `header.physicsVersion`, `header.tickHz`, `header.physicsSeed`, `header.gameStart.{seed,adjustments,highscores}`, `transitions`, `coilPrologue`, `durationTicks`, `expectedHash`, `expectedGameStateHash` and (on `roll-and-drain` only) `checkpointTicks` / `expectedCheckpointHashes` byte-identical. Build the harness **in the scratchpad**, never under `test/`, over the shipped `runReplay` / `buildHeader` / `tableHash()` / `assetHash(doc)` / `PHYSICS_VERSION` surface, and preserve `resolveTuning()`'s natural **declaration** key order with `value`/`source`/`confidence` per entry (`DW-213`'s `reopen_if` fires if the order flips to sorted or `confidence`-first). Verify **structurally, by parsing each file per field at `4b4388c` and at HEAD** — never by grepping the diff. *Rationale: AD-15's 2026-09-05 amendment — `resolveTuning()`'s entire serialized output is hashed into `header.gameStart.tuning`, so one new tunable invalidates all five goldens with `StaleReplayHeaderError` before tick 1. This is a **header-only refresh and needs no author grant** (precedents: Story 2.4 `b9f5431`, Story 2.9, Story 2.10, all three of which moved *more* than this one does — they moved `tableHash` as well).*

**Acceptance Criteria:**

- **AC 1 — a bob closure warns the current player, and the bob's own swing does not warn twice.** **Given** `phase: 'game'`, a player at `currentPlayer` with `tiltWarnings: 0` and `adjustments.tiltWarnings: 1`, **when** `s_tilt_bob` closes at tick `T` and again at `T+15`, `T+30` and `T+45` (the bob's own measured oscillation — Story 1.7 pinned the longest consecutive over-threshold run at 15 ticks), **then** exactly **one** `tilt_warning { player, remaining: 0 }` is emitted, at `T`; `players[currentPlayer].tiltWarnings` reads 1; `machine.tilt.tilted` is still `false`; `machine.hardwareEnabled` is still `true`; and **no** `CoilCommand` of any kind was emitted — with the run establishing, before the burst, that a closure *can* produce a warning, so the three silences are discriminating (vacuity #51).

- **AC 2 — the settle window gates the next warning, at production magnitude.** **Given** the same run with `adjustments.tiltWarnings: 2` (an adjustment override, never a tuning override), a warning counted at `T`, and the bob quiet thereafter, **when** `s_tilt_bob` closes at `T+600` — past `tiltWarningSpacingMs` but inside `tiltSettleMs` — **then** nothing is emitted and `tiltWarnings` stays 1; and **when** it closes again at `T+3100`, **then** `tiltWarnings` reads 2 and a second `tilt_warning { remaining: 0 }` is emitted. The offsets 600 and 3100 are authored as literals at the probe, never read back from `tiltSettleMs`.

- **AC 3 — the tilt disables the hardware set, disarms every ball-save source, and suppresses the autolaunch.** **Given** `players[currentPlayer].tiltWarnings` already equals `adjustments.tiltWarnings` and `machine.ballSave.sources` holds **two** seeded sources with a live `untilTick`, **when** an eligible `s_tilt_bob` closure arrives, **then** `tilt { player }` fires; `machine.tilt.tilted` is `true`; `machine.hardwareEnabled` is `false`; **every** coil in `HARDWARE_COILS` receives exactly one `disable` `CoilCommand` on that tick (asserted by looping over `HARDWARE_COILS` with a `length > 0` non-vacuity guard, the `test/rules-lifecycle.test.ts:245-262` shape — never a `length > 0` assertion on the batch); `machine.ballSave` is `{ untilTick: null, sources: [] }`; and no `c_autolaunch` pulse is emitted on that tick or any later tick of the run, against a control run in which the same script untilted **does** produce one.

- **AC 4 — a tilted ball ends, pays nothing, and the next ball starts clean.** **Given** a ball at `NO_BALL_SAVE_TUNING` whose player has earned a non-zero bonus, **when** the machine tilts and the ball then drains taking `ballsInPlay` to 0, **then** `ball_ended` **arrives** carrying `tilted: true`, `total: 0` and the real `bonusByCategory`/`multiplier` beside that zero; `players[i].score` is byte-identical to its pre-drain value; **no** `bonus_count_step` is emitted (with the run establishing that an untilted control ball at the same tuning *does* emit them); and on the same tick `ball_will_start` resets `machine.tilt` to `{ tilted: false, slamTilted: false }`, `ball_starting` restores `hardwareEnabled: true` with an `enable` for every `HARDWARE_COILS` coil ordered **after** the tilt's disables in the same batch, while `players[i].tiltWarnings` is **unchanged** by the rotation.

- **AC 5 — warnings are per player.** **Given** two players mid-game, **when** player 0 collects a warning and then a Tilt ends their ball and play rotates to player 1, **then** `players[0].tiltWarnings` is 1 and `players[1].tiltWarnings` is 0; and **when** player 1 subsequently collects a warning, **then** only `players[1].tiltWarnings` moves and `players[0]`'s is untouched — every other assertion in this story re-run at `currentPlayer` 1 as well as 0.

- **AC 6 — Slam tilt ends every player's game and returns the machine to Attract.** **Given** `phase: 'game'` with two players and a mode on the stack, **when** `s_slam_tilt` closes, **then** `slam_tilt` fires; `phase` reads `'attract'`; `modes` is `[]`; `machine.hardwareEnabled` is `false` with a `disable` for every `HARDWARE_COILS` coil; `machine.tilt.slamTilted` is `true`; **no** `ball_ended` is emitted and no player's `score` moves; and `players[]` survives so Attract can cycle the last game's scores. **And given** `phase: 'attract'` or `'game_over'`, **when** `s_slam_tilt` closes, **then** nothing is emitted and the returned state is the **same reference** — with the run establishing, in the same test, that the identical closure in `phase: 'game'` does produce `slam_tilt`.

- **AC 7 — `DW-222`: a Tilt inside a save's re-serve window never strands the ball.** **Given** a live ball save, a drain at `T` that emits `ball_saved` and pulses the trough eject, and a Tilt engaging at `T+50` before the re-served ball reaches `bd_shooter`, **when** that ball arrives at `T+200`, **then** no `c_autolaunch` pulse is emitted (the guard at `ball-controller.ts:642`) while the arrival is still consumed exactly once, preserving the anti-double-fire property the Story 2.9 review added; **and when** `s_shooter_lane` subsequently opens at `T+400` — which is precisely what a manual plunge looks like to rules, and the manual plunge remains live because `machine.ts:306` gates it on `coilEnabled.c_autolaunch`, which this story never disables — **then** `ball_launched` fires, `ballsInPlay` reads 1, ball save does **not** re-arm (the tilt emptied `sources`, and Story 2.9's arming gate requires the controller's own source to be present), and that ball's drain produces `ball_ended` with `tilted: true` and `total: 0`. The run must assert `ball_ended` **arrived** — not merely that nothing failed.

- **AC 8 — `DW-36`: the tilt-warning default has one definition, in the file AD-15 names.** **Given** the shipped tree, **when** `resolveTuning()` runs, **then** `tiltWarnings` is present as a top-level entry with a non-empty `source` and a `confidence` from the sanctioned set, `test/tuning.test.ts`'s completeness ratchet passes with `scalarKeys` at 32, and `DEFAULT_ADJUSTMENTS.tiltWarnings` and `src/host/boot.ts`'s real `GameStart` both resolve to that one entry's value — with **no** second literal `1` for this figure anywhere outside the tuning entry (the dev replay recorder's deliberate `3` at `boot.ts:407` excepted and left alone, per `DW-185`).

- **AC 9 — Integration (Rule 1): the Backglass shows the warning from the event and TILT from the state.** **Given** the `FrameOutput` frames of a tilted ball folded through `advanceBackglass()` and `renderFrame()` exactly as `src/host/boot.ts:291-292` does, **when** the warning tick passes, **then** a `WARNING` row lights dots in its **own declared band** (derived from that row's `row`/`GLYPH_H`), and when the identical frames are re-folded with `events: []`, that row never appears anywhere in the hold window; **and when** the tilt tick passes, **then** a `TILT` row lights dots in its own band, while the same fold over the same snapshot with `machine.tilt.tilted: false` produces a different dot set with no `TILT` row; **and** an ended ball's `ball_ended` screen still wins over the TILT screen for its hold.

- **AC 10 — the bob is reached through real nudge input, end to end.** **Given** a real `createLoop` with a ball in play, **when** `nudge_up` transitions in the measured `burstFrames()` shape are applied until the plumb bob crosses its threshold, **then** a `tilt_warning` appears in `FrameOutput.events` with no `machine.tilt` ever seeded by the test; and **when** a second burst is applied after `tiltWarningSpacingMs` has elapsed, **then** `tilt` appears, every `HARDWARE_COILS` coil receives a `disable`, and the ball's eventual drain produces `ball_ended` with `tilted: true` and `total: 0`.

- **AC 11 — the goldens moved for exactly one header reason.** **Given** the change is complete, **when** each of the five goldens is parsed **per field** at `4b4388c` and at HEAD and compared, **then** `header.gameStart.tuning` and `notes` are the **only** fields that differ on any of them — `header.tableHash` still reads `8838dd46`, `header.assetHash` still reads `ab163ff`, and every body field is deeply equal — and `pnpm test` passes with no `StaleReplayHeaderError`.

- **AC 12 — the devices seam is derived, not typed, and does not widen the playfield set.** **Given** the shipped devices layer, **when** `s_tilt_bob` or `s_slam_tilt` closes, **then** exactly one `tilt_bob_closed` / `slam_tilt_closed` is emitted and no `playfield_switch_closed` is; **when** either opens, **then** nothing is emitted; **and** `PLAYFIELD_SWITCHES` still holds exactly 28 switches with neither cabinet switch among them, `pnpm lint:boundaries` reports `OK` with no `no-device-name-literal` violation, and the switch names are resolved from their unique `SettleClass` such that a `TABLE` carrying two entries of either class throws a named authoring error rather than silently choosing one.

### Review Findings

_Code review 2026-09-10 (`bmad-code-review`, full mode). Scope: `f2fd48f..HEAD` plus QA's uncommitted working tree; there were no untracked files. Review tier: full. `_bmad/custom/model-overrides.yaml` is absent, so every layer inherited the reviewer's model. Four layers ran: blind-hunter, edge-case-hunter, verification-gap and acceptance-auditor. None failed, and containment was verified after them: the tree and the unpushed commits were unchanged. After dedup: 16 patch findings (all applied), 8 deferred under Rule 15, 9 dismissed, and 0 decision-needed left open. Rule 3: the real-runtime tests are `test/rules-tilt-integration.test.ts` and `test/backglass-integration.test.ts`; panel pixels remain the lead's smoke. Rule 1: AC 9 is the Integration AC. Rule 6: AD-1, AD-3, AD-5, AD-7, AD-9, AD-15, AD-18 and AD-19 (as amended) were verified. The one AD-5 tension is the pre-existing `DW-241`, and the slam ball-save patch below brings the slam into line with AD-18. Every mutation below was applied from a saved copy, observed through vitest's JSON reporter, and restored byte-identical. After the patches, with `BLENDER` exported, the measured results were:_
- _`pnpm typecheck`: exit 0._
- _`pnpm test`: 117 files / 1959 tests / 0 failed / 0 skipped. That is 1952 + 7 review-added tests._
- _`pnpm lint:boundaries`: OK, 107 files._
- _`pnpm check:headers`: OK._

**Patch (applied):**
- [x] [Review][Patch] (med) **Slam tilt during a save's re-serve autolaunched the ball into Attract.** The deferred-autolaunch guard read only `tilt.tilted`, which a slam leaves `false`. It is now also gated on `phase === 'game'`. [src/sim/rules/ball-controller.ts:653] fix-risk low: one conjunct, and the flag is only ever set in a game.
  - mutation: drop `&& nextState.phase === 'game'` → rules-tilt "a Slam tilt inside a save's re-serve window never autolaunches into Attract" RED.
- [x] [Review][Patch] (low) **Slam tilt left `machine.ballSave` armed, so `l_ball_save` stayed lit in Attract.** `lampsOf()` has no phase gate. The slam now disarms every source (AD-18, "Tilt disarms all"). This reverses the implement-stage triage's rejection 3, whose "Attract never reads it" held for the drain branch only. [src/sim/rules/tilt.ts:146] fix-risk low.
  - mutation: remove the slam's `ballSave:` disarm → rules-tilt "AC 6 … l_ball_save is dark in Attract" and the same-tick collision test RED.
- [x] [Review][Patch] (low) **The `ball_ended` hold survived a slam into Attract.** The end-of-ball/BONUS screen could stay up for as long as `BALL_ENDED_HOLD_TICKS` after the game had ended. The hold is now gated on `game.phase !== 'attract'`. [src/presentation/backglass/frame.ts:225] fix-risk low.
  - mutation: drop that gate → backglass-frame "a live ball_ended hold is abandoned the moment phase becomes attract" RED.
- [x] [Review][Patch] (med, Rule 19, AC 3) **AC 3's "no `c_autolaunch` pulse" could not fail.** Its run had no drain and no re-serve, and its control was a different script, so removing the ball controller's `!tilt.tilted` guard left AC 3 green. It is rewritten on a reachable path: a two-source save re-serve with the tilt landing before the arrival. The control is the IDENTICAL script minus the bob closure. [test/rules-tilt.test.ts:379]
  - mutation: drop the `!nextState.machine.tilt.tilted` conjunct at ball-controller.ts:653 → AC 3 and both AC 7 tests RED.
- [x] [Review][Patch] (med, Rule 19, AC 4) **"`tiltWarnings` unchanged by the rotation" read the wrong player.** It read the ENDING player, whom `startBall()` never touches. A reset written into `startBall()` hits the STARTING player, so both twins stayed green. The starting player is now seeded nonzero and asserted in both twins. [test/rules-tilt.test.ts:539]
  - mutation: `tiltWarnings: 0` in `startBall()`'s players map → both AC 4 twins RED.
  - re-walk (vacuity #48): swap `tiltResult`/`controllerResult` in `rules/index.ts`'s `coilCommands` merge → both AC 4 twins RED.
- [x] [Review][Patch] (med, Rule 19, AC 6) **AC 6's "no score change" did not discriminate "without bonus".** Both players carried a zero bonus. Both are now seeded with nonzero bonuses. [test/rules-tilt.test.ts:671]
  - mutation: the slam branch pays each player's bonus → the AC 6 in-game test RED.
- [x] [Review][Patch] (med, Rule 19, AC 8) **The check that `DEFAULT_ADJUSTMENTS` reads the one entry compared a value with itself.** Reverting `rules/index.ts` to a literal `1` left every test green. A new `vi.doMock` test moves `TUNING.tiltWarnings` to 7 and re-imports `sim/rules`. [test/host-game-seed.test.ts:140]
  - mutation: `tiltWarnings: 1,` in `DEFAULT_ADJUSTMENTS` → that test RED.
- [x] [Review][Patch] (med, Rule 19, AC 9) **Nothing pinned the TILT screen's `phase === 'game'` gate.** A tilted last ball leaves `tilted` true through game over. [test/backglass-frame.test.ts:493]
  - mutation: drop the gate → "a tilted snapshot in phase game_over does NOT show the TILT screen" RED.
- [x] [Review][Patch] (med, Rule 19, AC 9) **"An ended ball still wins over TILT for its hold" was pinned on the arming frame only.** [test/backglass-frame.test.ts:509]
  - mutation: move the TILT branch between the `ball_ended` arming and its hold → "a live ball_ended hold keeps the panel through a LATER tilted frame" RED.
- [x] [Review][Patch] (med, Rule 19) **Nothing pinned `tilt.ts`'s already-tilted guard.** Without it, every eligible closure during a tilted ball re-emits `tilt` and a second disable batch. [test/rules-tilt.test.ts:977]
  - mutation: `if (!player || nextState.machine.tilt.tilted)` → `if (!player)` → "an eligible closure while ALREADY tilted emits no second tilt" RED.
- [x] [Review][Patch] (low) **Three test titles claimed more than their tests asserted.** They are retitled, or strengthened with a direct-controller identity check and a tilt-happened sanity check. [test/rules-tilt.test.ts:439, :969]
- [x] [Review][Patch] (low) **The reset-safety guard's stated premise was false.** `hostLoop.reset()` rebuilds `createRules()`, and with it a fresh tilt controller, so no mark survives a reset. The comments now call the guard defence in depth; the guard and its test stay, per Boundaries. [src/sim/rules/tilt.ts:26, and the restarted-timeline comments in test/rules-tilt.test.ts]
- [x] [Review][Patch] (low) **The WARNING phase gates carried the wrong rationale.** One `FrameOutput` aggregates every owed tick's events, so a tick-k warning can share a frame with a slam's Attract snapshot from a later tick. The comments now say the gates are load-bearing. [src/presentation/backglass/frame.ts:259, test/backglass-frame.test.ts]
- [x] [Review][Patch] (low) **Stale and contradictory comments corrected.** [src/sim/rules/index.ts:180, frame.ts:66, test/backglass-integration.test.ts:224]
  - `rules/index.ts`'s 2026-09-06 correction still said the REAL boot `GameStart` carries 3.
  - The line references `boot.ts:407`, `boot.ts:291-292` and `frame.ts:180-192` were out of date.
  - `DmdScreen`'s doc still listed ARM YOURSELF as a future screen.
- [x] [Review][Patch] (low) **The settle-class authoring error named `createDevicesLayer()` but throws at module load.** [src/sim/rules/devices/index.ts:244, test/rules-devices.test.ts:662]
- [x] [Review][Patch] (low) **Nothing pinned the "two-edge burst stays under the slam count" measurement.** A tuning drift would have failed as "no warning". Both integration tests now assert that no `slam_tilt` fired. [test/rules-tilt-integration.test.ts:116, test/backglass-integration.test.ts:264]
  - mutation: `slamNudgesPerWindow` 3 → 2 → both rules-tilt-integration tests RED.

**Defer (dispositioned under Rule 15; ledger ids):**
- [x] [Review][Defer] (med) **Start in Attract after a slam has no balls-home gate.** The voided game's ball drains and ends the NEW game's unplunged ball 1, then a second ball is served into the occupied shooter lane; a scratch probe confirmed both. [src/sim/rules/ball-controller.ts:569] — `escalated`, **DW-244**, for the decision sheet.
  - fix-risk high: the fix sets what Start means when balls are not home (refuse, or reuse the resting ball), and it is coupled to DW-241's live plunger, 2.12's ball search and 2.13's attract/new-game path.
  - The naive refusal gate trades corruption for a machine that cannot start.
- [x] [Review][Defer] (med) **`slamNudgesPerWindow` (3 per 500 ms, unverified) lets three quick nudge taps void every player's game.** Before this review the only record was a test comment. [src/sim/table/tuning.ts:306] — `decision-pending`, **DW-245**, `human=feel_ritual`.
- [x] [Review][Defer] (med) **A tilted ball still earns.** It collects DRAGON letters and bonus credit, and a skill-shot award on a DW-222 manual plunge; FR-15 is silent on scoring under Tilt. [src/sim/rules/ball-controller.ts:558, src/sim/rules/modes/skill-shot.ts:124] — `decision-pending`, **DW-246**. This is a product call, and the skill shot stays fenced by DW-232/DW-204.
- [x] [Review][Defer] (low) **A slam during a pending bonus count-up keeps emitting `bonus_count_step` in Attract.** [src/sim/rules/ball-controller.ts:518] — occurrence on **DW-235** (routed to 2-13). Its visible symptom is closed by the `ball_ended` hold patch above.
- [x] [Review][Defer] (low) **AC 5's sequential single-run scenario is untested.** Because the marks are machine-wide, player 0's settle window gates player 1's first warning. — occurrence on **DW-240** (decision-pending: what the windows are measured from).
- [x] [Review][Defer] (low) **Every `GameStart` carries the tilt-warning count twice.** `tuning.tiltWarnings` is read by nothing and sits beside `adjustments.tiltWarnings`, so the dev recorder's header states two different counts. — occurrence on **DW-242**.
- [x] [Review][Defer] (low) **A `tilt_warning` arriving during a live `ball_ended` hold is never shown.** [src/presentation/backglass/frame.ts:217-245] — `by-design`, **DW-247**: spec task 10 specifies that order.
- [x] [Review][Defer] (low) **`adjustments.tiltWarnings` is not validated (NaN, negative or fractional).** [src/sim/rules/tilt.ts:161] — `wontfix-theoretical`, **DW-248**: it becomes real once a Settings UI or a save can supply the value.

**Dismissed (9):**
- AC 7's "arrival consumed exactly once": already pinned. Story 2.9's `rules-ball-save` test goes RED when the flag is consumed only inside the untilted branch.
- The golden `notes` parenthetical: the reason the note relies on, the phase guard, is correct.
- Record drift in the spec and cycle log (1943 vs 1952 tests, `4b4388c`): these are point-in-time records for their stages, and the cycle log is the lead's.
- `describeEvent()` assertions exercise test code: that is DW-243's pattern, already ledgered.
- `boot.ts` has no executed test host: this predates the story, and the established regex pins it textually.
- Prose Rule 19 bullets instead of `mutation:` lines: the lead's AD gate ran all 17.
- AC 9's integration test checks row text rather than a rasterised band: the own-band checks live at frame level, and the named AC 9 mutation reddens.
- DW-242: already ledgered.
- Test fixtures that author `tiltWarnings: 1`: literals at the probe are this story's own anti-vacuity rule.

### Smoke rework (iteration 1, 2026-09-11)

- [x] [Smoke] **DW-247: a `tilt_warning` that lands while a `ball_ended` hold is live is swallowed for good. The player never sees WARNING, and the next eligible closure tilts them with no visible warning.** [`src/presentation/backglass/frame.ts` `advanceBackglass()`: both the `ball_ended` arming branch and the live-hold branch `return` above the warning arming, and the warning screen is armed only from the one frame that carries the event.] The browser smoke failed on this on 2026-09-11 (S1: a burst shortly after a drain shows no WARNING, and the next burst TILTs). A red-first fold test confirmed the mechanism. A real-browser run with in-page instrumentation showed it is the ONLY cause: on a clean ball with no hold live, WARNING arms at the event and holds its full 2 s.
  **What the fix must do.** Keep task 10's ordering and its intent: an ended ball's screen still wins the panel for its hold, and TILT still supersedes a warning. But never drop the warning.
  - A `tilt_warning` that arrives on the `ball_ended` arming frame, or while the hold is live, is carried in `BackglassView`. That is presentation state, never `GameState`, so no golden moves.
  - It is shown when the hold ends, for the full `TILT_WARNING_HOLD_TICKS` measured from the hold's end.
  - It is dropped if, when the hold ends, the machine is tilted (TILT shows instead) or `phase` is no longer `'game'`.
  - Every existing ordering and phase-gate test stays green.
  **Tests (Rule 19, each with a named mutation recorded in `## Verification`):**
  1. A `tilt_warning` folded into a live `ball_ended` hold. WARNING appears on the first frame at or after the hold's end and holds for its full window. Control: the same fold without the event never shows WARNING.
  2. The same case on the `ball_ended` ARMING frame itself: both events in one `FrameOutput`, because one frame carries every owed tick's events.
  3. A pending warning is superseded by TILT when the machine is tilted at the hold's end, and dropped when `phase` has left `'game'`.
  4. One case built from REAL frames: a `runRulesScript` or `createLoop` run in which a ball drains and `s_tilt_bob` closes within the following hold, folded through `advanceBackglass()`/`renderFrame()` exactly as `src/host/boot.ts` does. Assert the `WARNING` row lights dots in its own declared band.
  The expected red for dropping the carry is tests 1, 2 and 4. Deliver this to close DW-247 (owned by this story again, reopened `by=smoke`), and do not re-file it.

**Implement re-spawn (2026-09-11): done.** `src/presentation/backglass/frame.ts` gains `BackglassView.pendingTiltWarning: boolean` (presentation state, never `GameState` -- no golden moves). Set `true` by (a) the `ball_ended` arming branch, OR'd with any already-pending flag, when a `tilt_warning` lands on the SAME frame as the arming, and (b) the hold-continuation branch, when a `tilt_warning` lands while the hold is already live. Read by the warning-arming branch (`game.phase === 'game' && (tiltWarningEvent || view.pendingTiltWarning)`), which measures the shown hold's `TILT_WARNING_HOLD_TICKS` from `tick` at the moment control reaches it -- i.e. from the `ball_ended` hold's own end, never from when the event originally arrived. Explicitly cleared to `false` by the TILT branch (a genuine Tilt supersedes a carried warning exactly as it supersedes a showing one) and implicitly dropped by every other branch (Attract, the final score/game_over fallback), each of which returns a fresh view literal with no `pendingTiltWarning` carried forward -- so a carried warning whose game is no longer `'game'` by the time the hold releases is dropped by omission rather than needing its own clearing step. Task 10's ordering and every existing phase gate are unchanged. Five new tests in `test/backglass-frame.test.ts` (`describe('smoke rework (DW-247) ...')`, nested under the existing "Story 2.11 -- AC 9" block to reuse its `gameInPlay`/`litDotRows` fixtures): the two Rule-19-mutation-confirmed carry cases (in-hold arrival; same-frame-as-arming arrival), the TILT-supersedes and game_over-drops cases, and a REAL-frames case built from a `runRulesScript` drain (`ball_ended` at tick 20) plus a genuine `s_tilt_bob` closure at tick 25 (within the following 3000-tick hold), folded through `advanceBackglass()`/`renderFrame()`/`rasterise()` to confirm the WARNING row lights dots in its own declared band once the hold's real end tick (3020) is reached. Two pre-existing `BackglassView` literals in this file (the two "armed on a PREVIOUS timeline" reset tests) gained the new required field to keep typechecking.

## Spec Change Log

- 2026-09-11, **smoke rework iteration 1** (lead). Browser smoke S1 failed: WARNING was never shown after a drain. The diagnosis is DW-247, reopened from `by-design`. It is the single cause: a red-first fold test confirms it, and an instrumented real-browser run shows that a clean ball 1 displays WARNING for its full hold. The orchestrator's unverified clean-run miss was capture latency. The `[Smoke]` task above was added. Task 10's ordering and intent are kept: this is a tier-1 amendment that adds the guarantee that the warning is shown and preserves the priority that the ended ball wins its hold. The frozen intent block is untouched. Status reset from `done` to `in-progress` for the implement re-spawn. The frontmatter `deferred:` list already carries three items the lead harvested at the first `dev_complete` (DW-241, DW-242, DW-243); append only NEW items.

## Review Triage Log

### 2026-09-08 — Review pass
- intent_gap: 0
- bad_spec: 0
- patch: 6: (high 1, medium 3, low 2)
- defer: 0
- reject: 3
- addressed_findings:
  - `medium` `patch` A nudge violent enough to cross BOTH the plumb bob's and the slam detector's thresholds in the SAME tick (physically plausible, and deterministic per `sim/physics/cabinet/index.ts`'s own fixed bob-before-slam push order) let a bob-triggered `tilt_warning`/`tilt` land on a tick the slam had already ended, and separately let a live warning hold outlive a LATER slam tilt for up to `TILT_WARNING_HOLD_TICKS` (2000 ticks) -- the Backglass could show WARNING after the game had already returned to Attract. Found independently by Blind Hunter and Edge Case Hunter (four convergent findings, deduplicated to one root cause). Fixed in `src/sim/rules/tilt.ts` (slam_tilt_closed now processed to completion in its own pass, before any tilt_bob_closed in the same tick, regardless of array order) and `src/presentation/backglass/frame.ts` (both the tilt_warning arming branch and its hold-continuation branch now gated on `game.phase === 'game'`, mirroring the TILT branch's own existing gate) -- defense in depth at both layers. Three new tests in `test/rules-tilt.test.ts` and two in `test/backglass-frame.test.ts`; all five confirmed to redden against the pre-fix code and pass against the fix.
  - `high` `patch` AC 4's "no bonus_count_step" pinning test ran for only 40 ticks (`tiltAndDrainTick + 30`) while `bonusCountTicks` is 400 at production magnitude -- the earliest possible `bonus_count_step` could not have appeared inside that window regardless of whether the tilted branch correctly skipped `armBonusCountSchedule()`, an unfalsifiable assertion (Rule 19). The test was also missing the untilted control the spec's own AC 4 text explicitly requires ("with the run establishing that an untilted control ball at the same tuning DOES emit them"). Found by the Verification Gap Reviewer, with exact tick math traced and confirmed independently against `sim/rules/ball-controller.ts`'s `armBonusCountSchedule()`. Fixed: `test/rules-tilt.test.ts`'s AC 4 test now runs `tiltAndDrainTick + 1300` (matching `test/rules-bonus.test.ts`'s own precedent for the identical claim) and a new companion `it()` runs the identical script and duration minus the tilt closure, asserting `bonus_count_step` DOES fire for the same earned bonus. Confirmed the strengthened test reddens under the reviewer's own suggested mutation (removing the `if (tilted)` gate on `armBonusCountSchedule()`) and the original 40-tick version does not.
  - `medium` `patch` The same AC 4 test's "tiltWarnings is UNCHANGED by the rotation" assertion seeded `tiltWarnings: 0` and checked it was still 0 after `ball_will_start` -- indistinguishable from a broken reset that zeroes `tiltWarnings` on every rotation (vacuity #51's shape). Found by the Intent Alignment Auditor. Fixed: seeded `tiltWarnings: 3` instead (harmless to the tilt outcome, since `adjustments.tiltWarnings: 0` tilts regardless of the seed's exact value), now genuinely discriminating.
  - `medium` `patch` The "Bob closure in Attract" test (I/O matrix row) proved only that no EVENT fires during Attract, which holds regardless of whether `lastBobClosureTick` is genuinely updated there, since the phase gate short-circuits before the spacing check either way -- the matrix row's own specific claim ("the closure DOES update the spacing mark ... because the bob's history is physical") was unobserved. Found by the Intent Alignment Auditor. Fixed: a new test drives `createTiltController()` directly across two `.step()` calls -- an Attract-time closure, then a later closure in phase 'game' inside the spacing window -- and asserts the later closure is still gated ineligible, which is only true if the Attract-time mark was genuinely recorded.
  - `low` `patch` The two tilt windows' `>=` boundary comparisons (`tiltWarningSpacingMs`/`tiltSettleMs`) were never tested AT the exact tick (`T+500`, `T+3000`) -- every existing offset sat comfortably past the boundary, so an off-by-one regression (`>` swapped for `>=`) could slip past the suite. Found by Blind Hunter. Fixed: two new boundary tests added (one per window, each isolated from the other window's own effect), both confirmed to redden under a `>=`-to-`>` mutation of their respective comparison and pass against the shipped `>=`.
  - `low` `patch` A test title in `test/backglass-frame.test.ts` claimed "the SAME band is dark on the arming input with no event," but the test body never rendered an event-stripped frame -- it compared against the score screen's differing dot pattern instead (the event-stripped control it described is a separate test just below). Found by the Verification Gap Reviewer. Fixed: retitled to describe what the test actually checks.
  - `reject` Blind Hunter and the Intent Alignment Auditor both independently claimed `l_ball_save`'s tilted-inertness lamp branch (`lampsOf()`, `projectBallSave()`) has zero test coverage, before or after this story. Verified false: `test/rules-ball-save.test.ts:346` ("AC 6 -- Tilt makes the device inert") already seeds a live window plus `tilted: true` and asserts `lampsOf(...).l_ball_save` projects `{ role: 'off', step: 0 }` -- a pre-existing Story 2.9 test, part of the passing suite, confirmed directly by re-running it and by observing it redden under an unrelated mutation earlier in this same review pass. Both reviewers likely searched only the diff text, and this file is untouched by this story's diff.
  - `reject` Blind Hunter flagged that `machine.tilt` is not reset when a Tilt (not Slam) ends the LAST ball of the LAST player, staying stale through `phase: 'game_over'` and the following Attract until the next `startBall()`. Verified: this is NOT a new asymmetry -- `machine.ballSave` and `machine.multiball` already follow the identical convention (reset only at the NEXT `startBall()`, never at game over itself, per that function's own "AD-7: ball_will_start resets ballSave, tilt and multiball" comment). `machine.tilt` merely joins a pre-existing, deliberate reset-boundary convention its two siblings already use. No current consumer of `machine.tilt.tilted` lacks its own `phase === 'game'` gate, so no observable consequence.
  - `reject` Edge Case Hunter flagged that a Slam tilt does not disarm `machine.ballSave`, leaving it armed with a stale `untilTick` through Attract. Verified harmless: `startBall()` unconditionally resets `machine.ballSave` to `enableBallSave(EMPTY_BALL_SAVE, BALL_SAVE_SOURCE)` on every Start (first game or rotation), and Attract phase never processes a drain (the ball-save guard only runs inside the `phase === 'game'` drain branch), so the stale value is never read before being overwritten. Pinned as a regression guard in the new same-tick collision test (`test/rules-tilt.test.ts`) rather than "fixed," since it is the CORRECT, intentional behavior -- AC 6 and the I/O matrix's "Slam tilt in a game" row never mention `ballSave`, and the Design Notes' own "four things deliberately not done" list for Slam tilt is complete as written.

### 2026-09-10 — Review pass (smoke rework iteration, DW-247)

_Scope: `217fa70..HEAD` (the DW-247 implement re-spawn's uncommitted changes to `src/presentation/backglass/frame.ts` and `test/backglass-frame.test.ts`). Four layers ran: Blind Hunter, Edge Case Hunter, Verification Gap and Intent Alignment Auditor. Rule 6: no AD's Rule is touched by this change (AD-9's payload-completeness and continuous-condition distinction, cited by the diff's own comments, is unaffected). The reset-safety reachability claim was independently verified by this stage directly against `src/sim/loop/index.ts:286` (fresh `GameState.phase` always boots `'attract'`) and `src/host/loop.ts:174,299` (`reset()` sets `lastFrameMs = null`, forcing the very next `tick()` call to compute `elapsedMs = 0` and advance zero ticks) before triage, confirming the specific leak Blind Hunter demonstrated is not reachable in the wired product today (an 'attract' frame always intervenes first) even though the underlying field genuinely lacked the file's own reset-safety discipline._
- intent_gap: 0
- bad_spec: 0
- patch: 3: (medium 1, low 2)
- defer: 1: (low 1)
- reject: 3
- addressed_findings:
  - `medium` `patch` `BackglassView.pendingTiltWarning` (this iteration's own new field) had none of this file's established reset-safety discipline: `screen`/`holdUntilTick` are both protected by an explicit half-open tick window (the "armed on a PREVIOUS timeline" tests), but the carried boolean had no bound of its own, so a stale `pendingTiltWarning: true` paired with a stale, far-future `holdUntilTick` -- the exact shape `host/loop.ts`'s `reset()` can leave behind, since it rebuilds the sim but never re-seeds the closure-held `BackglassView` -- would reach the warning-arming branch unfiltered. Found by Blind Hunter (direct repro against the pure function) and corroborated in depth by the Verification Gap Reviewer, who additionally traced that today's actual reachability is blocked by an independent, undocumented invariant (fresh state always boots `phase: 'attract'`, and `reset()` forces the first post-reset frame to advance zero ticks) -- confirmed directly by this stage. Fixed with no new field: the warning-arming branch now additionally requires `view.holdUntilTick !== null && tick >= view.holdUntilTick` before honouring a carried (non-live-event) `pendingTiltWarning` -- reusing the SAME `holdUntilTick` already carried alongside the flag, which is non-null exactly while a legitimate `ball_ended` hold is (or was) live and is null on every other screen. [`src/presentation/backglass/frame.ts`, the `tilt_warning`-arming `if` condition] New test: "a pendingTiltWarning carried from a PREVIOUS timeline does not survive a reset, exactly like its screen/holdUntilTick siblings" [`test/backglass-frame.test.ts`].
    - mutation: revert the bound to the pre-patch `(tiltWarningEvent || view.pendingTiltWarning)` → the new PREVIOUS-timeline test RED (`screen` came back `'tilt_warning'` instead of `'score'`); all other tests in the file unaffected.
  - `low` `patch` Nothing proved the TILT branch's own `pendingTiltWarning: false` clearing is load-bearing -- the existing "superseded by TILT" test only checked the ONE frame TILT fires on. Found by Blind Hunter. Fixed: the same test now folds one more frame, back in a live untilted game with no new event, and asserts the carry did not resurface. [`test/backglass-frame.test.ts`, the "a pending warning is superseded by TILT" test]
    - mutation: replacing the TILT branch's explicit return literal with `{ ...view, screen: 'tilt' }` (a realistic "simplify to a spread" refactor that leaks both `pendingTiltWarning` and the stale `holdUntilTick` together) → the new follow-up assertion RED. (A plain `pendingTiltWarning: view.pendingTiltWarning` leak alone does NOT redden this assertion, because the medium finding's own `holdUntilTick !== null` bound independently blocks it -- confirmed empirically before settling on the compound mutation above; the two fixes are defense-in-depth, not a single point of failure.)
  - `low` `patch` Nothing proved a Slam mid-hold (phase -> `'attract'`) correctly drops a pending warning -- found independently by Blind Hunter and the Intent Alignment Auditor (convergent). Tracing showed the Attract branch already handles this correctly (`game.phase !== 'attract'` gates the hold-continuation branch out, and the Attract branch's own explicit `pendingTiltWarning: false` clears it), but nothing exercised it. Fixed: a new test mirrors this file's own pre-existing "a slam mid-hold must drop the end-of-ball screen for Attract" precedent, extended to assert the carried warning does not resurface on a later live frame either. [`test/backglass-frame.test.ts`, new test under the DW-247 describe block]
    - mutation: replacing the Attract branch's explicit return literal with a spread that omits `pendingTiltWarning: false` (leaking it and the abandoned `holdUntilTick` together) → the new test RED, once the test's own follow-up tick was moved comfortably past the abandoned hold's `holdUntilTick` (the first attempt, at `tick: 33`, passed for the wrong reason -- the leaked `holdUntilTick` was still in the future, so the medium finding's own bound coincidentally blocked it there too; corrected to `armed.holdUntilTick! + 1`).
- deferred: `low` A `ball_ended` arriving while the `tilt_warning` screen is already SHOWING (not merely pending) silently replaces it with no memory of the warning -- the one direction this iteration's carry does not cover. Found by Edge Case Hunter. Verified pre-existing: the arming branch's unconditional top-of-function priority over whatever screen is currently showing predates this smoke rework (Story 2.11 task 10), so a live WARNING interrupted by a drain was equally lost before this fix. Added to the spec's frontmatter `deferred:` list rather than fixed here, being out of DW-247's own charter (which covers a NEW warning racing an already-armed hold, not a NEW drain racing an already-shown warning).
- rejected (3): no player-matching on the carried `tiltWarningEvent` before latching it (found independently by Edge Case Hunter and the Intent Alignment Auditor, deduplicated) -- verified pre-existing (the original arming branch never player-matched either) and harmless (the WARNING screen renders no per-player content, so a cross-player carry has no visible incorrect content); the "REAL frames" test's per-tick `FrameOutput` construction (Intent Alignment Auditor) -- verified to match this file's own established AC 3/AC 8 convention rather than falling short of a genuinely batched multi-tick frame, so not a gap introduced by this diff; a Spec Change Log entry for the routine `in-progress` -> `in-review` status transition (Blind Hunter) -- this is ordinary build-auto stage bookkeeping performed on every review pass, not a substantive decision the file's changelog convention is meant to capture.
- Verification after patches, `BLENDER` exported: `pnpm typecheck` exit 0; `pnpm test` **117 files / 1966 tests / 0 failed / 0 skipped** (1964 before this pass's 2 new tests); `pnpm lint:boundaries` OK -- 107 files; `pnpm check:headers` / `check:attributions` OK; golden pre-filter (`git diff --stat -- test/replays/`) empty; `git ls-files --others --exclude-standard test/` empty.

## Design Notes

**Governing architecture decisions (Rule 6).** **AD-5** is the primary: it puts the tilt bob and the slam detector in physics as switch-closing sensors, gives the hardware set one gate (`CoilCommand enable | disable`), and states that *"Tilt, game over and Attract disable all of them together"*. **AD-19** governs the seam — `src/sim/rules/devices/` is the only consumer of `SwitchEvent`, and its event enumeration became binding at the 2026-09-06 amendment, so the two new device events extend it (see the amendment note below). **AD-7** fixes the scopes: `tiltWarnings` is player-scoped, `tilt`/`hardwareEnabled`/`ballSave` are machine-scoped, all mutated only inside `rules.step`; its Rule also carries *"The bob is never reset by command — its physical decay plus `tiltSettleMs` is the settle"*, which is why nothing here resets the bob and why the window marks are not cleared at a ball boundary. **AD-18** supplies *"Tilt disarms all"* and names the `arm`/`disarm` interface this story is the first production caller of. **AD-2** sanctions the two rules-side semantic windows by name and forbids rules from debouncing a switch — the spacing window is a *semantic* window over device events, not a switch debounce, and `settleClass` for both classes is 0 ms in `TABLE`. **AD-3** requires both windows be authored in ms and converted once at load, and forbids wall-clock. **AD-9** governs the three new payload-complete events and the closed `DmdScreen` union. **AD-15** governs `TUNING.tiltWarnings` and the golden budget; **AD-14** makes it the table-default layer of a sim adjustment that applies at the next game. **AD-6** supplies the drain semantics and the `s_shooter_lane`-opening-means-plunged rule AC 7 leans on. **AD-8**'s phasing means a Slam tilt that clears `modes[]` directly is conforming in Epic 2, not violating. **No AC contradicts any AD's Rule.** One AC's *stated mechanism* is met by an equivalent, and it is recorded below.

**The AD-19 amendment this story owes — a LEAD write (Rule 20), not a stage's.** AD-19's Rule enumerates the devices layer's emissions and the 2026-09-06 amendment made that enumeration binding (*"a shipped event missing from it is a stale seam contract, not a documentation nicety"*). This story ships two events that are not in it. The **exact clause to widen** is AD-19's emission list, which today ends `…, `button_pressed { button }`, and `playfield_switch_closed { switch }`` — it needs `, `tilt_bob_closed`, and `slam_tilt_closed`` added to that list, together with one sentence recording why they are **not** members of the derived `playfield_switch_closed` set: the same amendment's parenthetical already excludes *"the button, tilt-bob and slam classes"* from that derivation and **must stay exactly as it is** (the set stays 28), because a cabinet-sensor closure answers neither "did the ball reach the playfield?" (Story 2.9) nor "has anything closed?" (Story 2.12). Suggested wording for the added sentence: *"The tilt bob and slam sensors emit their own dedicated events rather than joining `playfield_switch_closed`: they are cabinet sensors, not playfield contacts, and both consumers of the derived set reason about a ball."* No other AD needs a write for this story; **`AD-20` remains the next claimable id.**

**Consumes:** Story 1.7 (`createCabinetMechanics()`, the plumb bob, the slam detector, the two `TABLE.switches` entries and their `SettleClass`es, and the measured `K = 15` over-threshold run) · Story 2.4 (the devices-and-shots layer, `DeviceEvent`, `PLAYFIELD_SWITCHES`, the Stage-3 closed-edge dispatch) · Story 2.5 (the ball controller, `startBall()`, `HARDWARE_COILS`, `ball_ended`, rotation and game over) · Story 2.6 (`advanceBackglass()` / `renderFrame()` / `rasterise()`, `DmdScreen`, the reset-safe hold guard and the rendered-dots precedent) · Story 2.8 (`lampsOf`, and `l_ball_save` as the one tilt-aware lamp) · Story 2.9 (`machine.ballSave`, `disarmBallSave`, `BALL_SAVE_SOURCE`, `awaitingSaveLaunch`, the `!tilt.tilted` guards, `NO_BALL_SAVE_TUNING`) · Story 2.10 (the bonus forfeit at `ball_ended`, `bonus_count_step`, and the `BONUS` row's differential band assertion) · AD-15's tuning resolver (`shotWindowTicks()`).

**Consumed-by:** **Story 2.12 (Ball search)** — it is the automatic recovery for the shooter-lane ball AC 7 leaves manually plungeable, and `DW-230`'s own `reopen_if` fires there; it also inherits the `playfield_switch_closed` set this story deliberately did not widen. **Story 2.13 (Match, game over, Attract)** — it owns the general game-over → Attract transition that Slam tilt reaches here by the shortest state change, and it will subsume this story's `phase: 'attract'` write when it generalises the path; it also owns `DW-197`, the Backglass line budget these two new screens deliberately sidestep by replacing the panel rather than adding rows. **Story 3.1 (the mode stack)** — a Tilt that stops modes directly is conforming only until AD-8's four-phase convention becomes real; from 3.1 the slam tilt's `modes: []` write must go through `_will_stop`. **Story 3.7 (Quick multiball)** — the first second ball-save arming source, which is what makes AC 3's two-source disarm a production case rather than a seeded one, and which owns `DW-219` and `DW-185`. **Epic 4** — FR-45's flashers on Tilt and FR-48's `show_tilt` cue consume the `tilt` event this story introduces.

**Integration ACs (Rule 1).** This story introduces one module (`src/sim/rules/tilt.ts`), two device events and three semantic events. Every one carries an integration criterion against a real consumer rather than against internal state. `tilt` / `tilt_warning` → **AC 9**, asserted on the Backglass's rendered DMD dots through the exact fold `src/host/boot.ts:291-292` performs, with the `events: []` re-fold as its control. The whole physics→devices→rules→presentation chain → **AC 10**, asserted through a real `createLoop` driven by genuine nudge input. `machine.tilt` as consumed by already-shipped code → **AC 4** (Story 2.10's bonus forfeit) and **AC 7** (Story 2.9's save and autolaunch guards), both reached in play for the first time. The tilt controller's own state is never the subject of an integration assertion.

**The two windows, and the reading that makes both of them do work.** `epics.md`'s AC 1 states two conditions in one sentence — *"no closure has occurred within `tiltWarningSpacingMs`"* and *"`tiltSettleMs` applies before the next warning can count"* — and no artifact says what each is measured **from**. That matters, because if both are measured from the same origin the shorter one (500 ms) can never bind while the longer one (3000 ms) is in force, and `tiltWarningSpacingMs` would be a tunable that changes nothing — vacuity #43's exact shape, shipped by construction. The reading adopted here gives each its own origin, and it is the one under which every clause of the AC is literally true:
- **`tiltWarningSpacingMs` is measured from the last `s_tilt_bob` closure of any kind**, counted or ignored. This is the AC's own wording (*"no **closure** has occurred"*, not "no warning") and it is the debounce FR-14 demands: `epics.md:52` says *"the bob's continued swing cannot produce two warnings inside the spacing window"*, and the brief addendum (`briefs/…/addendum.md:57`) says *"The bob keeps swinging, so both a debounce window and a settle time are required."* Story 1.7 measured the bob's longest consecutive over-threshold run at 15 ticks for a ten-edge burst, so 500 ticks collapses one violent nudge into exactly one countable closure. It gates warnings **and** the tilt.
- **`tiltSettleMs` is measured from the last counted warning** and gates **only** the next warning — the AC's own scope (*"before the next **warning** can count"*).

Both are then independently falsifiable: dropping `tiltWarningSpacingMs` to 1 lets the bob's own second oscillation tilt the machine from a single nudge burst (AC 1 reddens); dropping `tiltSettleMs` to 1 lets a second warning count 600 ticks after the first (AC 2 reddens). AC 2 needs `adjustments.tiltWarnings: 2` to reach a second warning at all — that is an **adjustment** override, not a tuning override, so both windows remain on the path at their shipped production values, which is precisely the distinction Story 2.9 got wrong twice. The one consequence worth naming: at the shipped default of **one** warning, `tiltSettleMs` never binds in ordinary play, because there is no second warning to gate. It is not inert code — the research's Competition preset is 2 warnings and `GameAdjustments.tiltWarnings` is a real settings axis — but a reviewer should read this as a decision rather than discover it as an accident. **If the author intends `tiltSettleMs` to gate the tilting closure as well** (so a warning is followed by three seconds of immunity), that is a one-conjunct change and a different game; it is recorded here rather than guessed at.

**Why AC 3's autolaunch clause is met by the guard, not the batch.** `epics.md:1598` says flippers, slings, pops *"and autolaunch are disabled together via `CoilCommand disable`"*. `HARDWARE_COILS` (`ball-controller.ts:163-172`) is the complement of every coil any ball device names as an eject coil or a `ballSearchOrder` pulse step, and `bd_shooter.ballSearchOrder` pulses `c_autolaunch` (`dragonwar.ts:389-392`), so `c_autolaunch` cannot be in that batch. The exclusion is deliberate and measured: a `disable` for a serving coil lands in the same `coilCommands` batch as a serve pulse and swallows it (`DW-74`, pinned by `test/coil-enable.test.ts:77-88`). What actually suppresses the autolaunch while tilted is the ball controller's own `!nextState.machine.tilt.tilted` guard at `ball-controller.ts:642` — already shipped by Story 2.9, unreachable until now, and pinned by AC 3 and AC 7 against an untilted control. **The promise is kept exactly; only the mechanism differs, and the mechanism named in the AC is structurally unavailable.**

Two further facts, measured here and worth stating because they are not obvious. First, **the manual plunge shares `c_autolaunch`'s gate**: `src/sim/physics/machine.ts:306` passes `coilEnabled.c_autolaunch` as the plunger's `enabled` argument, and there is no plunger coil in `TABLE.coils`. So AD-5's own clause *"Flippers, the manual plunger, slingshots and pop bumpers … Tilt, game over and Attract disable all of them together"* is **not** satisfied for the manual plunger — not by this story, and not by the game-over sweep that has shipped since Story 2.5. That is a pre-existing AD-5 gap, recorded in this spec's frontmatter `deferred:` list rather than fixed here, because closing it requires a matching re-enable in `startBall()` and, more seriously, would convert AC 7's stalled ball into an unrecoverable hang. Second, and directly downstream: **because this story does not disable `c_autolaunch`, the shooter-lane ball in AC 7 stays manually plungeable**, which is the entire basis of the `DW-222` decision below.

**`DW-222`, decided.** The entry says a Tilt landing between a save's re-serve and the re-served ball's arrival *"suppresses the autolaunch AND consumes the flag, stranding that ball in the shooter lane with no `ball_ended` and no recovery"* until ball search lands in 2.12. Two of those three claims hold and one does not, and the correction is the substance of the decision. (a) The suppression is correct and is kept — AC 3 and AD-5 both require no autolaunch into a tilted playfield, and firing it anyway would be a different game. (b) Consuming `awaitingSaveLaunch` on the tilted arrival is correct and is kept — the Story 2.9 implement-stage review added that placement specifically to stop a later arrival double-firing, the ball genuinely *did* arrive, and leaving the flag `true` would only wait for an arrival that has already happened. (c) **"No recovery" is false at this tree.** `HARDWARE_COILS` excludes `c_autolaunch`; nothing in this story disables it; and `machine.ts:306` gates the manual plunge on exactly that flag. So the player can plunge the ball, `s_shooter_lane` opens, `ball_launched` fires, `ballsInPlay` reaches 1, and the ball's drain runs the ordinary tilted end-of-ball path. **AC 7 pins that whole path end to end, including that ball save does not re-arm on the manual plunge** — because the tilt emptied `sources` and Story 2.9's arming gate requires the controller's own source to be present, which is a genuine composition of this story's disarm with 2.9's gate rather than a restatement of either. The residual is real and is stated plainly: nothing recovers the ball *automatically*, and the player is given no cue to plunge beyond the TILT screen. That residual belongs to Story 2.12's ball search, which is the recovery mechanism the entry itself names. **Recommended ledger trailer for the lead (this stage does not write the ledger, Rule 15(a)):** `resolved-by:2-11-…` with a note correcting the "no recovery" premise and re-pointing the automatic-recovery residual at 2.12.

**`DW-230` is declined, deliberately, and this story does not widen it.** The entry says `awaitingSaveLaunch` never received the bounded lifetime its sibling `awaitingSaveRelaunch` did. It stays `wontfix-accepted` under Story 2.9 with its `reopen_if` naming Story 2.12, for two reasons. First, this story does not create a new way for the flag to strand: the one new path it makes reachable — a tilted arrival — **consumes** the flag (`ball-controller.ts:641`), so the flag's exposure is unchanged. Second, the only bound available is `ballSaveGraceTicks`, and the entry's own note records that reusing it a second time compounds `DW-229`. Adding a bounded lifetime here would be a behavioural change with its own tests and no defect to fix. *Declined `DW-230`: not this story's, unchanged by this story, and its `reopen_if` names Story 2.12.*

**`DW-36`, addressed by task 4 and AC 8, and why a golden refresh is the right price.** The entry is from 2026-08-28 and predates `DEFAULT_ADJUSTMENTS` (Story 2.5), so a reader could reasonably argue it is already closed — there *is* a default of `1`, at `src/sim/rules/index.ts:201`. That reading is declined, because the entry's own words are *"no **table** default even though **AD-15 lists `tiltWarnings` among the table tunables**"*, and AD-15's Rule does name it in `sim/table/tuning.ts`'s inventory, with `source` and `confidence`. Closing the entry by pointing at a bare literal in the rules layer would leave an AD Rule contradicted, which Rule 6 makes a HIGH finding. So the figure moves to the one file AD-15 names and both existing readers point at it. The price is one new block in `header.gameStart.tuning` on all five goldens — the narrowest golden movement of any story this epic, and narrower than the three header-only precedents (2.4, 2.9, 2.10) which each also moved `tableHash`. `header.tableHash` cannot move: this story adds nothing to `TABLE` and derives its seam from switches that already exist.

**Why the tilt controller is a stateful controller and not a pure fold.** The two windows need cross-tick marks. `bonus.ts`'s two folds are stateless because bonus accumulation is; tilt is not. `GameState` is out: `machine` is present in all five goldens' `attract` snapshots, so any machine-scoped field moves `expectedGameStateHash` on every golden — a state-hash re-record, out of budget and a Block-If. A player-scoped field would be hash-free (the goldens' `players[]` is empty) but is the wrong scope: the bob is one physical object, not a per-player fact. So the marks live in a factory closure, the pattern `createBallController` and `createDevicesLayer` already establish four times over (`pendingLockLaneClosure`, `awaitingSaveLaunch`, `awaitingSaveRelaunch`, `pendingBonusCountSteps`). The epic's standing rule for such a latch is two obligations — a reset at `ball_will_start` and a bounded lifetime — and **this one satisfies neither, deliberately**, which is the part worth stating rather than discovering. They are not latches: they are monotone tick marks that can only *delay* an effect, never enable one, so a stale mark cannot hang a game the way `DW-224`/`DW-225` did. They are deliberately **not** reset at `ball_will_start`, because AD-7's Rule says the bob is never reset by command and its physical decay is the settle — a mark cleared at a ball boundary would let the new ball's first swing warn instantly while the bob is still moving. What they *do* owe is the third obligation, which is not in the epic's usual list and which this repository has already been bitten by once: **reset-safety against a restarted timeline.** `src/host/loop.ts`'s `reset()` restarts `tick` at 0 while closures survive, and `frame.ts:180-192` records the DMD freezing for minutes on exactly this. A mark strictly greater than the current tick is from a different timeline and is discarded.

**Why the tilt controller, not the ball controller, performs the ball-save disarm.** AD-18 makes `machine.ballSave` the ball controller's device and says *"a mode calls `ballSave.arm({ ticks, source })` / `disarm(source)` through a rules-internal interface"* — and, in the same clause, *"Tilt disarms all"*. Tilt is not a mode; the AD names it separately as a disarmer, and `ball-save.ts`'s exported pure functions **are** the rules-internal interface (they are what the controller itself uses). Ownership in AD-18 is about single arbitration of *arming policy* — which module decides how long a window lasts and who may extend it — and a disarm removes rather than arbitrates. Doing it in the ball controller instead would require the controller to detect the rising edge of `machine.tilt.tilted`, which is new closure state for no gain. Recorded so a reviewer reads a decision rather than a boundary crossing.

**Why the tilt stage runs before the ball controller.** Three already-shipped guards read `machine.tilt.tilted` inside `ballController.step()` — the ball-save conjunct (`:677`), the deferred-autolaunch guard (`:642`) and the bonus forfeit (`:694`) — and all three must see a Tilt that engaged on the same tick, or a drain on the tilting tick would be saved and paid. Running after the devices-accounting fold (`index.ts:235-240`) and before the bonus credit (`:249`) puts it at the same composition point `applyDeviceEvents` already occupies, with the same "purely a function of this tick's device events" character. The coil ordering falls out of the same placement: on a tick where a Tilt ends the ball and rotation starts the next, the tilt's `disable`s precede `startBall()`'s `enable`s in one batch, and `machine.ts:266-276` writes the enable map in batch order, so the enables win and the new ball is playable. That is asserted, not assumed (AC 4).

**Slam tilt reaches Attract; it does not build a second path there.** Nothing in `src/sim/rules/**` writes `phase: 'attract'` today — the only two phase writes are `attract → game` (`ball-controller.ts:564`) and `game → game_over` (`:731`), and Story 2.13 owns the general game-over → Attract transition. Since that path does not yet exist, this story writes the transition itself, and keeps it to the **minimum state change that reaches the Attract the Backglass already renders**: `phase`, `modes: []`, `hardwareEnabled: false` with the disable batch, and `machine.tilt.slamTilted`. Four things are deliberately *not* done. `players[]` is **kept**, because `attractScreenAt()` cycles the last game's scores when players are present and `ball-controller.ts:564` already replaces `players` wholesale on the next Start. `ballsInPlay` is **not** zeroed, because a ball may still be physically on the playfield; `applyDeviceEvents` decrements it on the parking entry regardless of phase, so it self-corrects, and forcing it to 0 would desync rules from the balls. No `ball_ended` is emitted, because the game is voided rather than a ball being completed — FR-16 says *"all players' games end"*, not "each player's ball ends", and emitting a `ball_ended` would drag the bonus payment path and the Backglass's end-of-ball hold into a slam tilt. `machine.tilt.tilted` is left `false`, because `tilted` is the *ball* condition and there is no ball; `slamTilted` is the flag FR-16 describes, and `startBall()` `:471` already clears both on the next Start. When Story 2.13 generalises the transition, this write is what it subsumes.

**What the two new Backglass screens deliberately avoid.** `buildScoreRows()` (`frame.ts:325-346`) emits one row per player plus a `BALL` row plus the whole mode block into four line slots at dot rows 0/8/16/24, with no budget check anywhere — at four players the `BALL` row is silently dropped by `raster.ts:49`, at three the whole mode block, at two the mode's optional fields. That is `DW-197`, an author-decided product call routed to Story 2.13, and it is **still open**. Adding a warning row to the live score screen would inherit that overflow directly and could ship a warning nobody can see. So both new screens replace the panel wholesale, as `ball_ended` already does, and neither adds a row to the score screen's budget. The trade is stated rather than hidden: while the warning holds, the scores are not on the panel for `TILT_WARNING_HOLD_TICKS`.

**Anti-vacuity plan, by named shape.** *Vacuity #43 and Story 2.9's two repeats* — `tiltWarningSpacingMs` and `tiltSettleMs` could each be set to `1` and a suite whose every expectation re-imported them would stay green. So AC 1's `T+15/T+30/T+45` offsets and AC 2's `600`/`3100` are **authored as literals at the probe**, and no test in this story may override either tunable; the only overrides used are `NO_BALL_SAVE_TUNING` (which touches `ballSaveMs` and `ballSaveGraceMs` and nothing else, leaving both tilt windows on the path at production value) and `runRulesScript`'s **`adjustments`** option, which is a settings axis rather than a tuning override. *Vacuity #50 and Story 2.6's standing precedent* — the `TILT` and `WARNING` rows are pinned by a **differential band** taken from each row's own declared coordinate, required non-empty where the row should appear and empty (or demonstrably different) on a control frame; a whole-buffer `some(d => d === 1)` predicate is forbidden, and it would in fact be *catastrophically* vacuous here, since both screens render a single row and a dropped glyph leaves an entirely blank panel. *Vacuity #51 — the one this story is most exposed to*, because a tilt story is built from negative assertions. Every one of them establishes its precondition in the same run: "no second warning" is paired with a first warning that did arrive; "no `bonus_count_step`" is paired with an untilted control ball at the same tuning that emits them; "no `c_autolaunch` pulse" is paired with an untilted control run in which the pulse fires; "nothing on a slam tilt in Attract" is paired with the identical closure in `phase: 'game'` that produces `slam_tilt`. *Vacuity #44* — any non-vacuity guard must derive its expected count from an independent source (`HARDWARE_COILS.length`, an authored fixture), never from a second call of the helper it guards; the `HARDWARE_COILS` disable assertion follows `DW-181`'s loop-with-a-`length > 0`-guard shape rather than asserting the batch is non-empty. *Vacuity #48* — a mutation is a property of a tree, so every mutation below must be re-walked at the final tree if any line it targets was edited after it was first observed.

**What this story's suite cannot prove.** That any dot on the real panel changed: `NullEngine` rasterises nothing and `RawTexture.readPixels()` returns `null` under it, and AD-15 forbids automated rendering assertions in v1. The TILT and WARNING screens' legibility is the lead's browser smoke. That claim is narrower than usual here, because the epic-level method limit logged at Story 2.10 applies: browser smoke on this project cannot drive *aimed* play, but it **can** drive a nudge — `nudge_l`/`nudge_r`/`nudge_up` are ordinary key bindings — so a smoke that tilts the machine and photographs the panel is genuinely available and does not need a dev hatch. That is the one presentation claim only a rendered frame can settle.

**Decision-pending entries this story does not touch.** **`DW-204`** (does the paying skill-shot lane freeze at `ball_launched`) and **`DW-232`** (is a saved ball entitled to a second skill shot from an automatic launch) are both skill-shot compositions; this story reads and writes nothing in `modes/skill-shot.ts` and no test here composes a tilt with a skill shot. `DW-232` is the closer of the two — it lives on the same save re-serve path as AC 7 — and the fence is explicit: AC 7 asserts only that the autolaunch does not fire and the ball stays playable, never what the skill shot does with it. **`DW-212`** (`l_lock` between balls and in Attract) is a lamp question; this story adds no lamp and the only lamp assertion here is Story 2.9's existing `l_ball_save` guard. **`DW-226`** (ball-save eligibility after a grace lapse) is a ball-save composition on a path AC 7 deliberately keeps off: every drain test here runs at a non-saving tuning or with the device explicitly seeded. **`DW-236`** and **`DW-237`** are the end-of-ball screen's count direction and score line; a tilted ball pays `total: 0` and emits no `bonus_count_step`, so neither question is reached.

**Ledger inbox (Rule 17).** `bash _bmad/scripts/ledger.sh _bmad-output/implementation-artifacts/deferred-work.md slice 2-11-tilt-warnings-tilt-and-slam-tilt` returns exactly two entries at this tree. **`DW-36`** — addressed by **task 4, task 5, task 6 and AC 8**. **`DW-222`** — addressed by **task 7's guard-preserving design, AC 3 and AC 7**, with its "no recovery" premise corrected by measurement above. Nothing is declined from the inbox itself. Two entries outside it are dispositioned here because this story is where their conditions were said to fire: **`DW-223`** is closed by **task 16** (its `reopen_if` names exactly this situation), and **`DW-230`** is *declined* with reasons above. **Bookkeeping note for the lead:** `DW-222` is in this story's slice but was never transcribed into `epics.md`'s Story 2.11 acceptance bullets — only `DW-36` is there (`epics.md:1614`). Rule 17 (1b) asks for the bullet; it is missing, and the plan addressed the entry from the slice regardless.

## Verification

**Commands** (export `BLENDER="C:/Users/Josh/tools/blender-5.2.1-windows-x64/blender.exe"` first, in **every** shell — unset, roughly two dozen Blender-gated cases skip instead of running and the counts change, so state which you did; `0 skipped` is itself the proof it was exported):
- `pnpm typecheck` — expected: exits 0 across all three tsconfigs. Three missing `describeEvent()` arms fail here first (the `never` tail at `test/contracts.test.ts:270-275` is a **typecheck** gate only; `pnpm test` transpiles without it), and so do the two missing `renderFrame()` cases (`frame.ts:395-409`).
- `pnpm test` — expected: 0 failing, **0 skipped**. Baseline at `4b4388c` is **115 files / 1906 tests**, measured with `BLENDER` exported, ~89 s. Report the counts as measured and account for the delta (two new test files plus the new cases). ~23 skips means a broken shell, not a result. If `test/export-py-skip-visibility.test.ts` alone fails with a null status, that is `DW-227`'s load-dependent nested-vitest timeout, not this story.
- `pnpm lint:boundaries` — expected: `OK -- N .ts file(s)`, N grown by one for `src/sim/rules/tilt.ts` (baseline **106**). A literal `'s_tilt_bob'` or `'s_slam_tilt'` anywhere under `src/` outside `src/sim/table/dragonwar.ts` fails here as `no-device-name-literal`, and a `SwitchEvent` reference outside `src/sim/rules/devices/**` fails as `rules-no-switch-event-outside-devices`.
- `pnpm check:headers` and `pnpm check:attributions` — expected: exit 0; `src/sim/rules/tilt.ts` carries the GPL-3.0 header.
- `pnpm check:ad7` — expected: exit 0 with **exactly 3** passing tests. `pnpm check:corridor` — exit 0. `pnpm check:reachability` — exit 0 over its **52** cases (32 reachable / 20 unreachable), roughly two minutes.
- **Golden body-field pre-filter:** `git diff test/replays/ | grep -E '^[-+][[:space:]]*"(expectedHash|expectedGameStateHash|expectedCheckpointHashes|checkpointTicks|assetHash|tableHash|transitions|coilPrologue|durationTicks)"'` — expected: **no output**. Anchored to the field's own key position so the appended `notes` prose cannot trip it. This is a fast pre-filter; **the authority is the structural check below.**
- `git ls-files --others --exclude-standard test/` — expected: only the two intended new test files. The re-record harness must never appear under `test/`.
- `git diff --stat -- public/assets/` and `git diff --stat -- src/sim/table/dragonwar.ts` — expected: **empty** for both. Nothing in this story reaches an exported asset or the `TABLE` registry.

**Manual checks:**
- **Structural golden comparison (the authoritative one).** For each of the five goldens, parse the JSON at `4b4388c` and at HEAD and compare **per field**: assert `header.gameStart.tuning` differs by exactly one added `tiltWarnings` block, `notes` is a strict append, and every one of `header.tableHash` (`8838dd46`), `header.assetHash` (`ab163ff`), `header.physicsVersion`, `header.tickHz`, `header.physicsSeed`, `header.gameStart.seed`, `header.gameStart.adjustments`, `header.gameStart.highscores`, `transitions`, `coilPrologue`, `durationTicks`, `expectedHash`, `expectedGameStateHash`, and (on `roll-and-drain` only) `checkpointTicks` and `expectedCheckpointHashes` is **deeply equal**. Never a substring grep over the diff — Story 2.9's grep-based gate was permanently self-tripping on the goldens' own appended `notes`.
- Inspect the `gameStart.tuning` diff: exactly **one** new block per golden, in `resolveTuning()`'s natural declaration order with `value` / `source` / `confidence` per entry. A large reordering diff means the harness flipped the key order and has tripped `DW-213`'s `reopen_if`.
- Inspect each golden's `notes`: **appended**, never rewritten, with the existing `DW-70` and `deviceSlots` provenance intact.
- Confirm both new lists stay in sync: `test/replay-goldens.test.ts:123` and `test/golden-line-endings.test.ts:23-29` each declare the same five goldens independently.
- Re-read `src/sim/rules/index.ts:177-187`'s review-correction comment after task 6 and confirm it now records where the tilt-warning default lives, rather than predicting it.

**Rule 19 mutations — one per acceptance criterion. State the expected red BEFORE running each one** (a mutation harness can itself be vacuous: at Story 2.7's gate a `head -1` extractor matched vitest's test-*files* counter and under-reported two mutations' red). Apply each, observe red, revert **from a saved copy** — never `git checkout --`, never `git stash` — and confirm `git status --short` and `git diff --stat` are unchanged afterwards. Re-walk any mutation whose target line was edited after it was first observed (vacuity #48).

- **AC 1** — set `tiltWarningSpacingMs` to `1` in `src/sim/table/tuning.ts` → the burst cases redden, because the bob's own `T+15` oscillation now counts and (at the default adjustment) tilts. **This is the mutation that proves the tunable is on a real path**; if the suite stays green, every AC 1 assertion is running under an override and the pin is vacuous — the exact failure Story 2.9 shipped twice.
- **AC 2** — set `tiltSettleMs` to `1` → the `adjustments.tiltWarnings: 2` case reddens at `T+600`, which should have been ignored and now counts a second warning. The AC 1 cases stay green (spacing is unchanged).
- **AC 3** — in `src/sim/rules/tilt.ts`, replace the disarm fold with a single `disarmBallSave(state, BALL_SAVE_SOURCE)` call → the two-source case reddens with the seeded second source still present, while the single-source cases stay green. Separately, drop one coil from the disable loop → the `HARDWARE_COILS` loop assertion reddens naming that coil (the `DW-181` shape: a `disables.length > 0` assertion would **not** catch this, which is why it is forbidden).
- **AC 4** — remove the `tilted` conjunct from the tilt controller's write of `machine.tilt.tilted` (i.e. never set it) → the tilted `ball_ended` case reddens on `total: 0` and on the unchanged score, and the `bonus_count_step` silence flips to three events. The untilted control ball stays green.
- **AC 5** — change the warning increment to write `players[0]` instead of `players[currentPlayer]` → every `currentPlayer: 1` case reddens while every `currentPlayer: 0` case stays green.
- **AC 6** — remove the `phase: 'attract'` write from the slam-tilt branch, leaving the event and the disables → the Attract-return assertions redden while the `slam_tilt` event and hardware assertions stay green. (The pair is the point: it proves they are asserted independently.)
- **AC 7** — remove the `!nextState.machine.tilt.tilted` guard at `ball-controller.ts:642` → the tilted-arrival case reddens because a `c_autolaunch` pulse now appears; the untilted control stays green. Separately, revert the tilt's ball-save disarm → the "ball save does not re-arm on the manual plunge" assertion reddens.
- **AC 8** — set `TUNING.tiltWarnings`'s value to `4` → the AC 8 assertion that `DEFAULT_ADJUSTMENTS.tiltWarnings` and the boot `GameStart` both resolve to the one entry stays green (correctly — they follow it), but the AC 1 default-adjustment tilt case reddens because one warning no longer tilts. That pairing is what proves the entry is genuinely the single definition rather than a decorative duplicate; a second, independent mutation removing `'tiltWarnings'` from `scalarKeys` must redden `test/tuning.test.ts`'s completeness ratchet with its named message.
- **AC 9** — make `advanceBackglass()` derive the warning screen from `snapshot.game.players[i].tiltWarnings` instead of the `tilt_warning` event → **the `events: []` control reddens** (a WARNING row appears with the event stripped). This is the mutation that proves the control is doing work. Separately, give the `TILT` row a `row` of `4 * LINE_PITCH_ROWS` (32, off-panel) → the differential band assertion reddens while a whole-buffer predicate would have stayed green, reproducing and killing vacuity #50's shape.
- **AC 10** — in `src/sim/rules/devices/index.ts`, drop the `tilt_bob_closed` emission → the integration test reddens with no `tilt_warning` ever arriving from real nudge input, proving the physics→devices→rules chain is what the test traverses and not a scripted stand-in.
- **AC 11** — revert one golden's `header.gameStart.tuning` to its `4b4388c` content → `test/replay-goldens.test.ts` reddens with a named `StaleReplayHeaderError` (`header.gameStart.tuning no longer matches the live resolveTuning() output`) on exactly that golden's tests, leaving the other four green.
- **AC 12** — add `s_tilt_bob` to `PLAYFIELD_SWITCHES` by deleting the `'tilt_bob'` clause from `buildPlayfieldSwitches()`'s exclusion at `devices/index.ts:189` → the **pre-existing** AC 8 block at `test/rules-devices.test.ts:631-684` reddens on `size` 29 against its `toBe(28)` (`:665`) and on the exclusion loop (`:684`), and this story's new "no `playfield_switch_closed` on a bob closure" assertion reddens too. That the older block reddens is the point: it proves the derived set is still doing its job rather than having been quietly widened. Separately, make `switchNameForSettleClass` return the first match instead of throwing on a non-unique count → the authoring-defect case reddens.

**QA stage file edits (Rule 8 / file-list completeness).** `test/rules-tilt.test.ts` — edited (QA): nine `currentPlayer: 1` twin tests added (no new file; the headless `ENTRY_FILES` registration in `test/rules-devices-headless.test.ts` was already present from implementation).

**QA stage mutations (Rule 19), this pass.** Task 13's own clause ("each scenario run at both `currentPlayer` 0 and 1") had exactly one executing `currentPlayer: 1` case at implementation hand-off (AC 5's own twin); the mutations below were re-walked against `test/rules-tilt.test.ts` as extended by this stage with nine new `currentPlayer: 1` twins (AC 1 x2, AC 2 x2, the "Eligible again"/"Zero-warning machine" I/O-matrix rows x2, AC 3, AC 4, AC 7). Both reverted from a saved copy (`cp`, never `git checkout --`/`git stash`); `git status --short` and `git diff --stat` on `src/sim/rules/tilt.ts` confirmed unchanged after each.
- **AC 5, re-walked** — `mutation: warning-branch players.map's index === playerIndex → index === 0 (src/sim/rules/tilt.ts:186)` → now reddens **5** tests (up from 1 at hand-off): AC 1's two new `currentPlayer: 1` twins, AC 2's two new `currentPlayer: 1` twins, and AC 5's own pre-existing twin. Every `currentPlayer: 0` case stayed green (QA).
- **AC 5, second clause** — `mutation: tilt-branch events.push({ type: 'tilt', player: playerIndex, tick }) → player: 0 (src/sim/rules/tilt.ts:163)` → reddens **6** tests, none overlapping the mutation above: the AC 1 spacing-boundary `currentPlayer: 1` twin, both "Eligible again"/"Zero-warning machine" `currentPlayer: 1` twins, the AC 3 `currentPlayer: 1` twin, the AC 4 rotation `currentPlayer: 1` twin, and the AC 7 (DW-222) `currentPlayer: 1` twin. Every `currentPlayer: 0` case stayed green (QA).
- **F2 investigated, not a gap** — `mutation: slam-tilt-branch events.push({ type: 'slam_tilt', tick }) removed (src/sim/rules/tilt.ts:119)` → reddens **5** pre-existing tests directly, including AC 6's own primary assertion `expect(result.events).toEqual(expect.arrayContaining([{ type: 'slam_tilt', tick: 1 }]))` (`test/rules-tilt.test.ts:651`) and all three same-tick-collision tests' `slam_tilt`-only event-list assertions. The `slam_tilt` event was already independently falsifiable; no test gap existed and none was added (QA).

**Smoke rework mutation (DW-247, implement re-spawn, 2026-09-11).** One mutation covering both carry sites at once, in `src/presentation/backglass/frame.ts`: change the `ball_ended` arming branch's `pendingTiltWarning: view.pendingTiltWarning || Boolean(tiltWarningEvent)` to `pendingTiltWarning: false`, and the hold-continuation branch's `const pendingTiltWarning = view.pendingTiltWarning || Boolean(tiltWarningEvent)` to `const pendingTiltWarning = false` (i.e. drop both carry sites, reproducing the pre-fix code exactly). Expected red, per the task's own prediction: tests 1, 2 and 4. Observed: tests 1, 2 and 4 reddened exactly as predicted (`atHoldEnd.screen` / `view.screen` each `'score'` instead of `'tilt_warning'`); test 3 (TILT-supersedes / game_over-drops) stayed green, correctly, since it never depends on the carry actually working. Reverted from a saved copy (`cp`, never `git checkout --`/`git stash`); `git diff --stat src/presentation/backglass/frame.ts` and a `grep` for the mutation's own marker comment confirmed the tree was byte-identical to the pre-mutation state afterward, and the full `test/backglass-frame.test.ts` + `test/backglass-integration.test.ts` pair (43 tests) was re-run green post-revert.

**Smoke rework review-pass mutations (DW-247, code review, 2026-09-10).** Three patches landed this pass; each mutation applied from a saved copy of `src/presentation/backglass/frame.ts` (`cp` out, `cp` back -- never `git checkout --`/`git stash`), confirmed `git status --short` / `git diff --stat` unchanged after each revert.
- **Reset-safety bound** (the medium patch) — `mutation: (tiltWarningEvent || (view.pendingTiltWarning && view.holdUntilTick !== null && tick >= view.holdUntilTick)) → (tiltWarningEvent || view.pendingTiltWarning) (frame.ts, the tilt_warning-arming condition)` → the new "a pendingTiltWarning carried from a PREVIOUS timeline does not survive a reset" test RED (`screen` `'tilt_warning'` instead of `'score'`); all 44 other tests in the file stayed green.
- **TILT-branch clearing is load-bearing** (a low patch) — `mutation: return { screen: 'tilt', holdUntilTick: null, ... , pendingTiltWarning: false } → return { ...view, screen: 'tilt' } (frame.ts, the TILT branch)` → the "superseded by TILT" test's new follow-up assertion RED. Also verified: a narrower mutation that leaks ONLY `pendingTiltWarning` (`pendingTiltWarning: view.pendingTiltWarning`, `holdUntilTick: null` left intact) does **not** redden this assertion -- the reset-safety bound above independently blocks it, since it requires `view.holdUntilTick !== null` and TILT always nulls it. The two fixes are confirmed defense-in-depth, not a shared single point of failure.
- **Attract-branch clearing on a Slam mid-hold is load-bearing** (a low patch) — `mutation: return { screen, holdUntilTick: null, attractCycleOriginTick: originTick, heldBallEnded: null, pendingTiltWarning: false } → return { ...view, screen, attractCycleOriginTick: originTick, heldBallEnded: null } (frame.ts, the Attract branch)` → the new "a pending warning is dropped ... when a Slam ends the game mid-hold" test's follow-up assertion RED, once its own post-Attract probe tick was moved to `armed.holdUntilTick! + 1` (a first attempt at `tick: 33` passed for the wrong reason -- the leaked, abandoned `holdUntilTick` from the ball_ended hold was still in the future relative to tick 33, so the reset-safety bound coincidentally blocked the leak there too, the same way it will after a genuine reset).

All three mutations and their reverts were re-verified against a clean tree: `git diff --stat` after the final revert showed only the three intentional files (`frame.ts`, `test/backglass-frame.test.ts`, this spec), and the full `pnpm test` suite (`BLENDER` exported) measured **117 files / 1966 tests / 0 failed / 0 skipped**.

## Auto Run Result

Status: done
Blocking condition: none

### Iteration 2 -- smoke rework (DW-247), 2026-09-10

_This iteration's scope was exactly one item: the `[Smoke]` task under "Smoke rework (iteration 1, 2026-09-11)" above, closing DW-247 (a `tilt_warning` landing during a live `ball_ended` hold was swallowed for good). Everything else in this spec was already implemented, reviewed and verified in Iteration 1 below, and was not touched._

#### Summary of implemented change

`src/presentation/backglass/frame.ts`'s `BackglassView` gained `pendingTiltWarning: boolean` (presentation state, never `GameState` -- no golden moves). `advanceBackglass()` now carries a `tilt_warning` event forward through a live `ball_ended` hold (or a same-frame arming) instead of dropping it, and surfaces it the instant the hold releases, for a fresh `TILT_WARNING_HOLD_TICKS` measured from that release -- unless the machine is genuinely tilted by then (TILT supersedes) or `phase` has left `'game'` (dropped). Task 10's original ordering and every existing phase gate are unchanged. Five tests were added by the implementation subagent (`test/backglass-frame.test.ts`) covering the four scenarios the reopened task named, including one built from real `runRulesScript` frames. Code review (four parallel layers) then found the new field lacked this file's own established reset-safety discipline (unlike its `screen`/`holdUntilTick` siblings) and that two of the field's clearing sites had no load-bearing test; this pass fixed the reset-safety gap (reusing the existing `holdUntilTick` bound, no new field) and added two more regression tests, landing at 117 files / 1966 tests / 0 failed / 0 skipped.

#### Files changed

- `src/presentation/backglass/frame.ts` — `BackglassView.pendingTiltWarning` added; `advanceBackglass()`'s `ball_ended` arming and hold-continuation branches now carry a `tilt_warning` event forward; the TILT branch and the warning-arming branch's own consumption both updated (the latter gated on `view.holdUntilTick !== null && tick >= view.holdUntilTick` for reset-safety, per code review); the Attract and final score/game_over fallback branches explicitly drop the carry.
- `test/backglass-frame.test.ts` — new `describe('smoke rework (DW-247) ...')` block: the four scenarios the reopened task named (in-hold carry, same-frame-as-arming carry, TILT-supersedes, phase-left-'game'-drops) plus a REAL-frames case via `runRulesScript`, all from the implementation subagent; this review pass added a dedicated PREVIOUS-timeline reset-safety test and strengthened the TILT-supersedes and new Slam-mid-hold tests with follow-up-frame assertions proving the carry is actually cleared, not merely outranked for one frame. Two pre-existing `BackglassView` literals gained the new required field.
- `_bmad-output/implementation-artifacts/spec-2-11-tilt-warnings-tilt-and-slam-tilt.md` — `baseline_revision`/`baseline_commit` updated to `217fa70` (this iteration's dispatch HEAD); the `[Smoke]` task checked off with an "Implement re-spawn: done" note; a new `## Review Triage Log` entry; three new `mutation:` records in `## Verification`; frontmatter `deferred:` gained one new low-severity item; this section.

#### Review findings breakdown

Four parallel review layers (Blind Hunter, Edge Case Hunter, Verification Gap, Intent Alignment) produced 8 raw findings this pass, deduplicated to 7 (see `## Review Triage Log`, "2026-09-10 — Review pass (smoke rework iteration, DW-247)", for full detail and evidence):
- **Patches applied: 3** (1 medium, 2 low) — the reset-safety gap on the new field, and two missing load-bearing tests for its clearing sites (TILT branch; a Slam mid-hold's Attract branch). All three confirmed to redden against the pre-fix/pre-test code and pass against the fix, including one case where a first attempted mutation passed for the wrong reason and was corrected.
- **Deferred: 1** (low) — a `ball_ended` arriving while `tilt_warning` is already SHOWING (not pending) silently replaces it; verified pre-existing (predates this smoke rework), added to the spec's frontmatter `deferred:` list.
- **Rejected: 3** — no player-matching on the carried event (pre-existing, no visible consequence since the screen carries no per-player content); the REAL-frames test's per-tick construction (matches this file's own established convention); a changelog-entry request for the routine `in-review` status transition (ordinary stage bookkeeping, not a substantive decision).

#### Follow-up review recommendation

`followup_review_recommended: true` — this pass's patched findings were 1 medium, 2 low. The threshold `3×medium + 1×low >= 5` is met exactly: `3×1 + 1×2 = 5`.

#### Verification performed

- `pnpm typecheck` — clean across all three tsconfigs (re-run after the review patches).
- `pnpm test` (BLENDER exported) — final: **117 files / 1966 tests / 0 failing / 0 skipped** (1959 at this iteration's dispatch HEAD `217fa70`, +5 from implementation, +2 from review patches).
- `pnpm lint:boundaries` — `OK -- 107 .ts file(s)`.
- `pnpm check:headers` / `pnpm check:attributions` — OK.
- Golden pre-filter (`git diff --stat -- test/replays/`) — empty; this iteration touches no golden. `git ls-files --others --exclude-standard test/` — empty, no stray files.
- `git status --short` / `git diff --stat` — exactly three files dirty throughout: `frame.ts`, `test/backglass-frame.test.ts`, this spec.
- **Rule 19 mutations** — one for the implementation subagent's own fix (recorded in `## Verification` under "Smoke rework mutation (DW-247, implement re-spawn, 2026-09-11)"), three more for this review pass's patches (recorded immediately below it), each applied from a saved copy (`cp`, never `git checkout --`/`git stash`) and reverted with the tree confirmed byte-identical afterward.
- **Matrix Test Audit** — DW-247 is not itself an I/O & Edge-Case Matrix row (it is a smoke-discovered gap in the already-audited "Backglass shows the warning" / "The ball's end still wins the panel" rows' interaction); both of those rows remain covered and green, with no regression, confirmed by the full-suite pass above.

#### Residual risks

- All residual risks recorded under Iteration 1 below are unchanged by this iteration (none touched or worsened).
- The one new deferred item (see frontmatter `deferred:`, fourth entry): a `ball_ended` interrupting an ALREADY-SHOWING `tilt_warning` screen still loses it, with no memory carried forward -- the reverse direction from the one this iteration closed. Low severity: the player did see the warning for at least part of its window before the ball ended, unlike the original DW-247 defect where the warning was never shown at all.
- No browser smoke re-run of this specific fix was performed by this stage (no browser tooling available here) -- the lead's own manual smoke, which originally found DW-247, is the appropriate verification of the actual on-panel behavior, per the spec's own "What this story's suite cannot prove."

### Iteration 1 -- original implementation and code review, 2026-09-08 to 2026-09-10

#### Summary of implemented change

Story 2.11 is implemented in full: 17 tasks and 12 acceptance criteria. `src/sim/rules/devices/index.ts` derives `s_tilt_bob`/`s_slam_tilt`'s switch names structurally from their unique `SettleClass` and emits two new `DeviceEvent`s (`tilt_bob_closed`, `slam_tilt_closed`) on the closed edge only, leaving `PLAYFIELD_SWITCHES` at 28. A new `src/sim/rules/tilt.ts` module (`createTiltController`) owns `machine.tilt`, the current player's `tiltWarnings`, and the Tilt-side of `machine.hardwareEnabled`/`machine.ballSave`, stepped before the ball controller on every tick so a same-tick Tilt is already visible to its already-shipped guards (the ball-save disarm conjunct, the deferred-autolaunch guard, the bonus forfeit). `TUNING.tiltWarnings` closes `DW-36`'s duplicate-default gap; `DEFAULT_ADJUSTMENTS` and the real `GameStart` in `src/host/boot.ts` both now read the one entry. The Backglass (`src/presentation/backglass/frame.ts`) gains `tilt_warning`/`tilt` `DmdScreen` members, the former read from the event, the latter from the snapshot condition. Three new `SemanticEvent` members (`tilt_warning`, `tilt`, `slam_tilt`) and their `describeEvent()` arms are added, alongside the 8 previously-unexercised arms this story's own pass closed opportunistically (`DW-223`). All five golden replays received a header-only refresh (exactly one new `tiltWarnings` tuning block plus an appended `notes` entry; `tableHash`/`assetHash`/every body field unchanged), verified structurally per field.

Code review (four parallel layers: Blind Hunter, Edge Case Hunter, Verification Gap, Intent Alignment) found and this pass fixed a real same-tick/cross-tick race between a bob-triggered warning/tilt and a slam tilt (defense-in-depth fix in both `tilt.ts` and `frame.ts`) and a structurally unfalsifiable pinning test on AC 4 (window too short to observe the claim, and missing the untilted control the AC's own text requires) — see `## Review Triage Log` for the full breakdown.

#### Files changed

- `src/sim/rules/tilt.ts` — new: the tilt controller (`createTiltController`), including the code-review fix that processes every `slam_tilt_closed` to completion before any `tilt_bob_closed` in the same tick.
- `src/sim/rules/devices/index.ts` — structural switch-name derivation, Stage 3 emits `tilt_bob_closed`/`slam_tilt_closed`.
- `src/sim/rules/devices/events.ts` — `TiltBobClosedEvent`/`SlamTiltClosedEvent` added to `DeviceEvent`.
- `src/sim/rules/index.ts` — tilt controller wired into `createRules()`/`step()`; `DEFAULT_ADJUSTMENTS.tiltWarnings` reads `TUNING.tiltWarnings.value`.
- `src/sim/rules/ball-controller.ts` — `HARDWARE_COILS`'s doc comment updated (second, production reader); no behavioural change.
- `src/sim/contracts/events.ts` — `TiltWarningEvent`/`TiltEvent`/`SlamTiltEvent` added to `SemanticEvent`.
- `src/sim/table/tuning.ts` — `tiltWarnings` table tunable added (`DW-36`).
- `src/host/boot.ts` — real `GameStart.adjustments.tiltWarnings` reads the one `TUNING` entry.
- `src/presentation/backglass/frame.ts` — `tilt_warning`/`tilt` `DmdScreen` members, `advanceBackglass()`/`renderFrame()` wiring, including the code-review phase-gate fix.
- `test/rules-tilt.test.ts` — new: headless coverage of ACs 1–7 and the I/O matrix's rules-side rows, including all code-review-triggered additions (the same-tick collision describe block, the AC 4 untilted control and extended window, the two window-boundary tests, the Attract-mark test).
- `test/rules-tilt-integration.test.ts` — new: AC 10, real nudge input through a real `createLoop`.
- `test/backglass-frame.test.ts` — AC 9's WARNING/TILT differential-band tests, `DISPLAY_LITERALS`, the two code-review phase-gate regression tests, one corrected test title.
- `test/backglass-integration.test.ts` — task 12's real-`createLoop` WARNING/TILT integration case.
- `test/contracts.test.ts` — the three new `describeEvent()` arms plus the eight previously-unexercised ones (`DW-223`).
- `test/rules-devices.test.ts` — task 15's `tilt_bob_closed`/`slam_tilt_closed` coverage, plus a new standing regression test for `switchNameForSettleClass()`'s load-time throw on a non-unique `SettleClass` (closes a Matrix Test Audit gap: the "Derivation is structural" row had no executing test, only the Rule 19 mutation exercise).
- `test/rules-devices-headless.test.ts` — `rules-tilt.test.ts` added to `ENTRY_FILES`.
- `test/rules-modes.test.ts` — the `s_slam_tilt` "inert" case removed (no longer inert once this story ships).
- `test/tuning.test.ts` — `tiltWarnings` added to `scalarKeys`.
- `test/host-game-seed.test.ts` — regex updated for the new `TUNING.tiltWarnings.value` read.
- `test/replays/*.golden.json` (five files) — header-only refresh (`tiltWarnings` tuning block, appended `notes`).

#### Review findings breakdown

Four parallel review layers (Blind Hunter, Edge Case Hunter, Verification Gap, Intent Alignment) produced 13 raw findings, deduplicated to 9 (see `## Review Triage Log` for full detail and evidence):
- **Patches applied: 6** (1 high, 3 medium, 2 low) — all fixed in this pass, each with a new or strengthened regression test confirmed to redden against the pre-fix code and pass against the fix.
- **Deferred: 0.**
- **Rejected: 3** — two verified false on inspection (pre-existing test coverage the reviewers missed since it lives outside the diff; a "stale ballSave" observation that is harmless because `startBall()` unconditionally resets it and Attract never reads it), one verified to be a pre-existing, deliberate convention this story merely joins (machine.tilt's reset-at-next-Start timing, matching its `ballSave`/`multiball` siblings exactly) rather than a new defect.

#### Follow-up review recommendation

`followup_review_recommended: true` — this pass's patched findings were 1 high, 3 medium, 2 low. Any high-severity patch alone triggers `true` (the threshold `3×medium + 1×low >= 5` is also independently met: 3×3 + 1×2 = 11).

#### Verification performed

- `pnpm typecheck` — clean across all three tsconfigs (re-run after every patch round).
- `pnpm test` (BLENDER exported) — final: **117 files / 1943 tests / 0 failing / 0 skipped**, ~100 s.
- `pnpm lint:boundaries` — `OK -- 107 .ts file(s)`.
- `pnpm check:headers` / `pnpm check:attributions` — OK.
- `pnpm check:ad7` — 3 passing (exact). `pnpm check:corridor` — 1 passing.
- `pnpm check:reachability` — 1 passing over 52 cases, run once before the review-triggered patches; the patched files (`tilt.ts`, `frame.ts`, `ball-controller.ts` comment-only) are outside this gate's surface (geometry/shot reachability), so it was not re-run after patching.
- Golden pre-filter grep (body-field keys) — no output (clean). `git ls-files --others` under `test/` — only the two intended new test files. `git diff --stat` on `public/assets/` and `src/sim/table/dragonwar.ts` — both empty.
- **Structural golden comparison (authoritative)** — all five goldens parsed per field at baseline `4b4388c` and at HEAD: `header.gameStart.tuning` gained exactly one `tiltWarnings` block each (in `resolveTuning()`'s natural declaration order, `value`/`source`/`confidence`), `notes` strictly appended, every other field (`tableHash` `8838dd46`, `assetHash` `ab163ff`, `physicsVersion`, `tickHz`, `physicsSeed`, `gameStart.{seed,adjustments,highscores}`, `transitions`, `coilPrologue`, `durationTicks`, `expectedHash`, `expectedGameStateHash`, and `roll-and-drain`'s `checkpointTicks`/`expectedCheckpointHashes`) byte-identical.
- **Matrix Test Audit** — every I/O & Edge-Case Matrix row covered by an executing, passing test; one genuine gap found and closed ("Derivation is structural" — the load-time throw on a non-unique `SettleClass` had no standing test, only the Rule 19 mutation exercise) with a new regression test using the established `vi.resetModules()` + `vi.doMock('../src/sim/table/dragonwar', ...)` pattern (`test/lock-device-behaviour.test.ts`'s own precedent), confirmed to redden under the corresponding mutation.
- **Rule 19 mutations spot-checked directly by this stage** (beyond the implementation subagent's own 2): AC 1's `tiltWarningSpacingMs=1`, AC 12's exclusion-clause deletion, the `switchNameForSettleClass()` throw removal, the ball-save `!tilt.tilted` guard removal (confirmed the pre-existing `l_ball_save` lamp test catches it), the always-arm `armBonusCountSchedule()` mutation (confirmed the strengthened AC 4 test catches it), and the `>=`-to-`>` mutation on both tilt windows plus the Attract-mark update removal (confirmed the three new tests each catch their own regression). Every mutation reverted from a saved copy (`cp`, never `git checkout --` or `git stash`); tree confirmed byte-identical after each revert via `git diff --numstat` against the pre-mutation state.

#### Residual risks

- **AD-5's manual-plunger gap** (deferred in this spec's own frontmatter `deferred:` list, severity medium): `machine.ts:306` gates the manual plunge on `coilEnabled.c_autolaunch`, which `HARDWARE_COILS` structurally excludes, so neither Tilt nor game-over disable it — pre-existing, not created by this story, and not fixed here (closing it risks converting `DW-222`'s recoverable stall into an unrecoverable hang until Story 2.12's ball search lands).
- **Automatic recovery from `DW-222`'s stranded-ball scenario** is explicitly Story 2.12's (ball search), not this story's — AC 7 pins that the ball stays manually plungeable end to end, but nothing here recovers it automatically.
- **No browser smoke test** of the rendered TILT/WARNING dots on the real panel — `NullEngine` cannot rasterise, and AD-15 forbids automated rendering assertions in v1; this is explicitly the lead's manual smoke per the spec's own "What this story's suite cannot prove."
- **Coil double-disable on a same-tick collision** (the code-review fix's own residual): when a bob-triggered tilt and a same-tick slam both attempt to disable `HARDWARE_COILS`, the slam's disable batch lands first (by design) and the bob branch's own disable is now correctly suppressed (never reached, since `nextState.phase !== 'game'` short-circuits it) — verified by the new collision tests' exact-count assertion (`disabledAt50.length === HARDWARE_COILS.length`, not doubled).
