---
title: 'Story 2.10: End-of-ball bonus and the multiplier'
type: 'feature'
created: '2026-09-08'
status: 'ready-for-dev'
baseline_revision: 'f79fd0c3b66526f83fe72b8e59505aaec3314e46'
review_loop_iteration: 0
followup_review_recommended: false
context: []
warnings: ['oversized']
deferred: []
---

<intent-contract>

## Intent

**Problem:** Every ball's letters and Loops accumulate nothing. `players[i].bonus` exists (`PlayerBonusState { byCategory, multiplier }`), is seeded once at `ball-controller.ts:208` and read once at `ball-controller.ts:612-613`, and **nothing between those two lines ever writes it**; `ball_ended` ships `total: 0` hardcoded; there is no `bonus_count_step` event, no `bonusCountMs`, and no bonus scoring value anywhere in the tree. Separately, `RulesStepResult.modeEvents` — the channel carrying `lanes_completed { set: 'top' }`, the event this story's multiplier advances on — has **no production consumer at all** (`DW-208`): its only reader in the whole repository is `test/util/switch-script.ts:247`. So the ball has a payoff field with no producer, and the multiplier has a trigger the shipped product cannot see.

**Approach:** Introduce `src/sim/rules/bonus.ts` as the single owner of the bonus vocabulary and arithmetic — a closed `BonusCategory` union (`letters | loops | strikes`), the fixed multiplier ladder `1 → 2 → 3 → 5`, and two pure folds. `src/sim/rules/index.ts` calls one fold over `deviceResult.events` immediately **before** the ball controller (so a category credited on the drain tick is in that ball's own payload) and the other over `modeStackResult.events` immediately **after** the mode stack — the second being `DW-208`'s production consumer, at the same composition point where `applyDeviceEvents` and `deriveDeviceSlots` already fold events into state. The ball controller keeps what only it can own: the per-ball reset inside `startBall()`, and — from the pre-rotation `endingPlayer` snapshot it already captures at line 575 — the total, the first `score` write it has ever made, the real `ball_ended` payload, and a closure-held count-up schedule that emits `bonus_count_step` events paced by a new `bonusCountMs`. The Backglass's existing `ball_ended` screen animates to that stream.

## Boundaries & Constraints

**Always:**
- Every bonus write goes through `src/sim/rules/bonus.ts`; the categories, the ladder and the per-category values have exactly one definition each.
- `players[i].bonus` is player-scoped and mutates only inside `rules.step()` (AD-7). `src/sim/loop/**` writes no `GameState` field — gated at source by `test/ad7-device-slots.test.ts:432-490`, which permits exactly two writes to the loop's `state` binding.
- Every new tunable is a `TuningEntry` with `source` and `confidence: 'unverified'` (AD-15). `bonusCountMs` is a **top-level** `…Ms` scalar (`assertNoNestedMsKeys()`, `tuning.ts:739-757`); the three scoring values carry **no** unit suffix, following `skillShotAward` (`tuning.ts:643`).
- `bonus_count_step` is payload-complete (AD-9): presentation renders each step from the event alone and never joins it to a later snapshot. Rules never format text.
- Device names never appear as string literals outside `src/sim/table/dragonwar.ts` (AD-16; `DEVICE_NAME_PATTERN` at `tools/boundary-lint.mjs:97` matches `shot_left_loop` and `shot_left_loop_made`). The shot→category mapping is declared data in `TABLE`, derived at the consumer.
- Every drain-driving test runs at a **non-saving** tuning (`NO_BALL_SAVE_TUNING`) or seeds the device disarmed. A live save returns from `ball-controller.ts:593` before the entire end-of-ball path.
- Any harness built to re-record the goldens lives in the scratchpad, never under `test/` (a `test/**/*.test.ts` file silently joins `pnpm test`).

**Block If:**
- Any golden's `expectedHash`, `expectedGameStateHash`, `expectedCheckpointHashes`, `checkpointTicks`, `transitions`, `coilPrologue`, `durationTicks` or `header.assetHash` moves. That is a trajectory or state-hash re-record, which needs the author's explicit grant and is **out of this story's budget** — HALT and report; never re-record around it and never switch a check off.
- The implementation would require `src/sim/loop/**` to write any `GameState` field (see `## Design Notes`, "Why DW-208's fix is rules-side").
- A `GameState` field would have to be added under `machine` (machine-scoped state is present in all five goldens' `attract` snapshots, so it moves `expectedGameStateHash` — a state-hash re-record).
- The suite shows a red `check:ad7`, `check:corridor` or `check:reachability`. Every gate is expected green; a red one is a regression, not the status quo.

**Never:**
- Never invent a producer for the `strikes` category. Epic 2 has no War mode and no Strike event; `strikes` stays 0 all epic (see `## Design Notes`).
- Never advance the multiplier on `lanes_completed { set: 'inout' }`, and never advance it past the ladder's last rung.
- Never add a `GameState` field for the count-up schedule, and never add a lamp, GI, flasher or show. No new `CoilCommand`. No Backglass screen beyond the existing `ball_ended` one.
- Never compute a test's expected value from `bonusCountMs`, `bonusLetterValue`, `bonusLoopValue` or `bonusStrikeValue` by re-importing it (epic vacuity #43). Never derive a non-vacuity guard's expected count from the helper it is guarding (#44).
- Never write `players[i].score` outside the ball controller's drain branch in this story. Never decide `DW-204`, `DW-212`, `DW-226` or `DW-232`.
- Never touch tilt detection — `machine.tilt.tilted` stays unset until Story 2.11. This story ships only the read-side forfeit.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Letter credited | `phase: 'game'`, `currentPlayer: 0`, `bank_target_down { letter: 'D' }` | `players[0].bonus.byCategory.letters` 0 → 1; `players[0].letters` still advances via the existing accumulator (`ball-controller.ts:451-467`), unchanged | No error expected |
| Loop credited | `shot_left_loop_made`, then `shot_right_loop_made` | `.loops` 0 → 1 → 2 | No error expected |
| Ramp credits nothing | `shot_ramp_made` | Every category unchanged — `TABLE.bonusWiring` has no entry for it | No error expected |
| Multiplier ladder | four `lanes_completed { set: 'top' }` on successive ticks | `multiplier` 1 → 2 → 3 → 5 → 5 (capped at the last rung) | No error expected |
| Wrong lane set | `lanes_completed { set: 'inout' }` | `multiplier` stays 1 | No error expected |
| Not in a game | the same device events with `phase: 'attract'` (or no player at `currentPlayer`) | no bonus write at all | Fold returns the same state reference |
| Untilted ball end | `NO_BALL_SAVE_TUNING`; letters 2, loops 1, strikes 0, multiplier 3; drain takes `ballsInPlay` to 0 | `ball_ended` carries `bonusByCategory { letters: 2, loops: 1, strikes: 0 }`, `multiplier: 3`, `total: 60000`, `tilted: false`; `players[0].score` rises by exactly 60000 | No error expected |
| Tilted ball end | same, with `machine.tilt.tilted: true` seeded | `total: 0`, `tilted: true`, score **unchanged**, and **no** `bonus_count_step` is emitted | No error expected |
| Count-up stream | untilted end at tick `E`, two nonzero categories | three `bonus_count_step` events at ticks `E + N`, `E + 2N`, `E + 3N` (`N = bonusCountTicks`); `step` 1..3, `steps: 3`; the last carries `running === total` | No error expected |
| Zero bonus | untilted end, every category 0 | `total: 0`, score unchanged, **no** `bonus_count_step` | No error expected |
| Next ball | `ball_will_start` fires inside `startBall()` | the starting player's `byCategory` is all-zero and `multiplier` is 1; `letters`, `score`, `lockCredits`, `extraBalls` untouched | No error expected |
| Ball saved instead | drain inside a live save window | `ball_saved`; **no** `ball_ended`, no score write, no reset, no `bonus_count_step` — the early return at `ball-controller.ts:593` still precedes everything this story adds | No error expected |
| Backglass animates | `FrameOutput.events` carries the three `bonus_count_step` events | the `ball_ended` screen gains a BONUS row whose text tracks `running` and ends at `total`; re-folding the identical frames with `events: []` never produces it | No error expected |
| Second player | every scenario above re-run with the ending player at index 1 | identical, scoped to `players[1]`; `players[0]` untouched | No error expected |

</intent-contract>

## Code Map

**Rules — where the work lands**
- `src/sim/rules/index.ts` — the composition point. `RulesStepResult` (105-141) declares `modeEvents` (140), produced only at 257 from `modeStackResult.events`. Stage order: devices `:222` → `applyDeviceEvents` `:230` → `deriveDeviceSlots` `:231-234` → `stateAfterAccounting` `:235` → **ballController.step** `:237` → **modeStack.step** `:246` → `nextState` `:248` → events merge `:250`. The two new folds go at `:236` and `:247`. `:230` is the in-tree precedent for a pure event-fold invoked straight from this file.
- `src/sim/rules/ball-controller.ts` (641 lines) — `emptyPlayer()` `:202-216` seeds `bonus` at `:208`; `startBall()` `:358-421` maps `players` at `:378-380` (today it touches **only** `ballNumber`) and builds `ball_will_start` at `:360`; `shotWindowTicks(...)` reads at `:260-261` are the pattern for `bonusCountTicks`. The letters accumulator `:451-467` is the sibling precedent ("Not a mode, not scoring -- raw per-player state the ball controller already owns the write scope for"). Drain branch: entry `:566-574`, `endingPlayer` captured **pre-rotation** at `:575`, ball-save early return at `:585-594`, ending player's snapshot `:596`, mode teardown `:598-607`, `ball_ended` literal `:609-617` with `total: 0` at `:614`, game-over/rotation `:619-635`. `score` is written **nowhere** here today.
- `src/sim/rules/modes/base.ts` — `applyLaneEntered()` `:113-133` emits `lanes_completed` at `:128` after resetting that set's `lit` flags; `applyLaneChangePressed()` `:134-144` rotates `lit`. `src/sim/rules/modes/events.ts` — `LanesCompletedEvent` `:26-30`, `LaneSetName` `:16` (`'top' | 'inout'`), `ModeEvent` `:41`. Header `:7` and `:23` already name this story as the consumer.
- `src/sim/rules/devices/events.ts` — `BankTargetDownEvent` `:41-46`, `ShotMadeEvent` `:127-136` (`type: \`${ShotName}_made\``), `DeviceEvent` union `:152-175`. Produced at `devices/drop-bank.ts:73` and `devices/shots.ts:104`. `shot_*_made` has **zero** consumers in `src/` today.

**Contracts and table**
- `src/sim/contracts/state.ts` — `PlayerBonusState` `:75-79` (`byCategory: Readonly<Record<string, number>>` — an **open** record, nothing constrains the keys), `PlayerState` `:92-118`, `GameState` `:150-158`, `MachineState` `:64-73`.
- `src/sim/contracts/events.ts` — `BallEndedEvent` `:175-184` (already declares `bonusByCategory`, `multiplier`, `total`, `tilted`); `SemanticEvent` union `:216-229` (13 members); `:85` records the union's growth convention.
- `src/sim/table/dragonwar.ts` — `shots` `:604-620` (`shot_left_loop`, `shot_right_loop`, `shot_ramp`); `laneWiring` / `dropBankWiring` are the wiring-block idiom a new `bonusWiring` follows.
- `src/sim/table/tuning.ts` — `TuningEntry` `:43-47`, `entry()` `:49-51`; `ballSaveMs/HurryUpMs/GraceMs` `:336-358` (the doc-block shape to copy); `skillShotAward` `:637-648` (the **only** scoring value in the game, and the shape a bonus value follows — no unit suffix); `assertNoNestedMsKeys()` `:739-757`; the `…Ms → …Ticks` derivation `:762-792` (the sibling inherits `source` and `confidence` **verbatim**, so a `…Ms` tunable costs two hashed blocks); `resolveTuning()` `:759`; `shotWindowTicks()` `:848`. **No `bonus`, `letter` or loop-scoring key exists anywhere in this file.**

**Loop and presentation**
- `src/sim/loop/index.ts` — `rules.step()` `:428`; reads `.state` `:433`, `.events` `:435`, `.commands` `:437`, `.coilCommands` `:459`; **never** `.modeEvents`. `tuning` resolved once `:256`; `ballSaveHurryUpTicks = shotWindowTicks('ballSaveHurryUpMs', tuning)` `:271` threaded into `lampsOf` at `:306` and `:445` — the AD-9 threading precedent. **Read-only in this story.**
- `src/presentation/backglass/frame.ts` — `isBallEndedEvent()` `:29-31` and the single `input.events.find(...)` at `:139` (the one place `FrameOutput.events` reaches anything today); `BackglassView` `:76-81` with `heldBallEnded` and `holdUntilTick`; `BALL_ENDED_HOLD_TICKS = msToTicks(3000)` `:94`; the reset-safe half-open hold guard `:164-171`; `formatScore()` `:187`; `buildBallEndedRows()` `:317-322`; `renderFrame()` `:329` with its `never` exhaustiveness tail `:351`; `LEFT_MARGIN_COL` `:182`, `LINE_PITCH_ROWS` `:185`.
- `src/host/boot.ts` — `backglassView` `:227`; the sim-chain fold `:291-292` (must stay on the sim chain — the rAF chains are independent, `:220-226`); the `reset()` wrapper `:360-367`, which re-seeds `lampView` but deliberately **not** `backglassView` (`:341-343`: it self-corrects because `renderFrame()` re-derives from the snapshot — **event-driven bonus state does not**).

**Tests and gates — read-only evidence**
- `test/util/switch-script.ts` — `runRulesScript()` `:228`, options `:170-179` (`tuning`, `adjustments`, `durationTicks`, `initialState`), result `:181-201` including `modeEvents` `:190-191`; the loop `:241-250`; out-of-range ticks **throw** `:232`.
- `NO_BALL_SAVE_TUNING` — four byte-identical file-local definitions: `test/rules-lifecycle.test.ts:34-38`, `test/rules-modes.test.ts:47-51`, `test/backglass-frame.test.ts:38-42`, `test/backglass-integration.test.ts:52-56`. For a `createLoop` test it must be passed as `createLoop()`'s **own** `tuning` option, not `GameStart.tuning` alone (`backglass-integration.test.ts:47-51`).
- `test/contracts.test.ts` — the `ball_ended` payload fixture `:196-207`; `describeEvent()` `:223-259` with the `const neverEvent: never` tail `:252-257` (a **typecheck** gate, not a `pnpm test` gate); Story 2.9's three executing arms with deliberately distinct field values `:260-271`. **No executing assertion exercises the `ball_ended` arm today.**
- `test/rules-devices-headless.test.ts` — `ENTRY_FILES` `:193-202` and the completeness ratchet `:216-238`: any new `test/rules-*.test.ts` must be added to `ENTRY_FILES` or the suite reddens; forbidden imports `:169-176` (`sim/loop`, `sim/physics`, `@babylonjs`, `node:fs`).
- `test/ad7-device-slots.test.ts:432-490` — the source-level gate that permits exactly two writes to `sim/loop`'s `state` binding, in **every** `.ts` under `src/sim/loop/`. `:191-199` / `:263-274` — the nested-harness reporter contract and its exact count of **3**, driven by `it()` blocks in `test/fixtures/dw70-ad7/`, untouched by `GameState` field changes.
- `test/backglass-frame.test.ts:489-602` — the English-literal source scan. `DISPLAY_LITERALS` `:497` is a **hand-maintained list**: a new literal is not covered until it is added. Positive control `:578-584`, negative control over `src/sim/**` `:586-601`.
- `test/backglass-integration.test.ts:154-179` — the `events: []` re-fold control that proves the seam reads events rather than the snapshot. `test/backglass-raster.test.ts:26-36` — `litDots(raster)`, file-local, the deepest honest observable (there is no pixel readback: `backglass.ts:14-21`).
- `test/replay-goldens.test.ts` — `GOLDEN_NAMES` `:123`, the on-disk completeness ratchet `:153-162`, the hash assertions `:165-175`. `test/golden-line-endings.test.ts:24-28` carries a **second** five-name list that must stay in sync. `src/sim/loop/replay.ts` — `buildHeader` `:228`, `runReplay` `:336`, `tableHash()` `:145`, `assetHash(doc)` `:150`, `PHYSICS_VERSION` `:214`, `StaleReplayHeaderError` `:240` thrown only from `assertHeaderMatchesLiveEnvironment()` `:248-283`, whose tuning comparison is `:275-277`. **There is no re-recording tool in the repository** — `buildHeader` has three importers and none is under `tools/`.
- `test/replays/*.golden.json` — five files. `header.tableHash` **`bac5816c`**, `header.assetHash` **`ab163ff`**, `header.physicsVersion` `v1-ce6772ef` on all five, measured at this tree. `roll-and-drain.golden.json` alone carries `checkpointTicks` and `expectedCheckpointHashes`.
- Baseline at `f79fd0c`, measured with `BLENDER` exported: **114 files / 1868 tests / 0 failing / 0 skipped**, ~100 s wall.

## Tasks & Acceptance

**Execution:**

1. `src/sim/table/dragonwar.ts` — add a `bonusWiring` block beside `laneWiring`/`dropBankWiring`, mapping each declared shot that credits a bonus category to that category: `shot_left_loop` and `shot_right_loop` → the loops category. `shot_ramp` is deliberately absent. *Rationale: AD-11 and AD-19 — shots are declared data, and `DEVICE_NAME_PATTERN` (`tools/boundary-lint.mjs:97`) makes `'shot_left_loop'` a lint error anywhere but this file, so the mapping cannot live in `bonus.ts`. This moves `header.tableHash`; see task 12.*

2. `src/sim/table/tuning.ts` — add four entries. `bonusCountMs` as a **top-level** scalar (value `400`), and `bonusLetterValue` `5000`, `bonusLoopValue` `10000`, `bonusStrikeValue` `25000` with **no** unit suffix, each `entry(value, source, 'unverified')` with a `source` that says the figure is authored and names what constrains it. `bonusCountMs`'s `source` records the constraint that fixes it: at most four steps × 400 ms = 1600 ms, so a whole count-up fits inside the Backglass's existing 3000 ms `ball_ended` hold (`frame.ts:94`). *Rationale: AD-3 (every display-paced sequence is authored in ms and converted once) and AD-15 (`source` + `confidence`, scoring values adjustable until Epic 3's playtest freeze). Follow `skillShotAward` (`:637-648`) for the no-suffix scoring shape and `ballSaveMs` (`:336-358`) for the doc block. Keep each `source` tight — the `…Ticks` sibling copies it verbatim, so `bonusCountMs`'s prose is hashed twice into every golden header.*

3. `src/sim/contracts/state.ts` — add an exported `BonusCategory = 'letters' | 'loops' | 'strikes'` and narrow `PlayerBonusState.byCategory` from `Readonly<Record<string, number>>` to `Readonly<Record<BonusCategory, number>>`. *Rationale: a total record makes the `Σ` exhaustive by construction and makes a typo'd category a compile error rather than a silently-ignored key. PRD FR-20 names exactly these three.*

4. `src/sim/contracts/events.ts` — narrow `BallEndedEvent.bonusByCategory` to the same `Readonly<Record<BonusCategory, number>>`, and add `BonusCountStepEvent { type: 'bonus_count_step'; player: number; step: number; steps: number; running: number; total: number; tick: number }` to the `SemanticEvent` union. *Rationale: AD-9 payload-completeness — the Backglass must render a step from the event alone; `step`/`steps` let it know it is on the last one without joining to a snapshot.*

5. `src/sim/rules/bonus.ts` — **new file**, GPL-3.0 header. Owns: `BONUS_CATEGORIES` (the declared iteration order), `EMPTY_BONUS` (every category 0, multiplier 1), the ladder `MULTIPLIER_LADDER = [1, 2, 3, 5]`, `bonusTotal(bonus, tuning)` (`Σ byCategory[c] × value(c) × multiplier`), `creditBonusFromDeviceEvents(state, deviceEvents): GameState`, and `advanceBonusMultiplier(state, modeEvents): GameState`. Both folds are gated on `phase === 'game'` and a present `players[currentPlayer]`, return the **same state reference** when nothing changed, and write only `players[currentPlayer].bonus`. Recover a shot name from a `ShotMadeEvent` by stripping the `_made` suffix and indexing `TABLE.bonusWiring`; never spell a shot name. *Rationale: AD-19 names "scoring" as a peer consumer of device events with no file yet; this is that file. One owner for the vocabulary, the ladder and the arithmetic means the cap, the category set and the values each have exactly one definition.*

6. `src/sim/rules/index.ts` — call `creditBonusFromDeviceEvents(stateAfterAccounting, deviceResult.events)` between `:235` and the ball-controller call at `:237`, and `advanceBonusMultiplier(modeStackResult.state, modeStackResult.events)` between `:246` and `:248`. **This is `DW-208`'s fix**: `modeEvents` gains its first production consumer. Update `RulesStepResult.modeEvents`'s doc comment (`:131-140`) to name that consumer instead of predicting it. *Rationale: the device fold runs before the controller so a category credited on the drain tick is inside that ball's own payload and cannot diverge from the letters accumulator at `ball-controller.ts:451-467`, which reads the same event at the same tick position. The mode fold runs after the mode stack because `lanes_completed` does not exist until then. `applyDeviceEvents` at `:230` is the in-tree precedent for both.*

7. `src/sim/rules/ball-controller.ts` — five edits. (a) `emptyPlayer()` `:208` seeds `EMPTY_BONUS`. (b) `startBall()`'s `players` map `:378-380` resets the starting player's `bonus` to `EMPTY_BONUS` alongside the `ballNumber` increment, leaving `letters`, `score`, `lockCredits` and `extraBalls` untouched. (c) resolve `bonusCountTicks = shotWindowTicks('bonusCountMs', tuning)` beside `:260-261`. (d) in the drain branch, after the ball-save early return, compute `total = tilted ? 0 : bonusTotal(player.bonus, tuning)`, add it to the **ending** player's `score` in the same `players` map that runs teardown `:598-607`, and put the real `bonusByCategory` / `multiplier` / `total` into the `ball_ended` literal `:609-617`. (e) arm a closure-held count-up schedule **after** the game-over/rotation block `:619-635` when `total > 0`, and drain it at the top of `step()` — one `bonus_count_step` per nonzero category in `BONUS_CATEGORIES` order carrying the running un-multiplied subtotal, then a final step whose `running === total`, each `bonusCountTicks` after the previous and the first `bonusCountTicks` after `ball_ended`. *Rationale: AD-18 — the ball controller is the single arbiter of the drain, and it alone holds the pre-rotation `endingPlayer` (`:575`) and the pre-teardown player snapshot (`:596`). The schedule is closure state, not `GameState`, because a `machine`-scoped field would move all five goldens' `expectedGameStateHash`. Arming it after `startBall()` is deliberate — `startBall()` runs in the same tick and would otherwise wipe it.*

8. `src/presentation/backglass/frame.ts` — `advanceBackglass()` folds `bonus_count_step` from `input.events` into `heldBallEnded` (extend it with the running bonus and the total), and `buildBallEndedRows()` `:317-322` gains a BONUS row rendered with `formatScore()`. Keep the fold on the existing `ball_ended` hold so it inherits the reset-safe half-open guard `:164-171`; do **not** add a `DmdScreen` member. *Rationale: AD-9 — presentation animates to rules-emitted step events and never reports completion. Reusing `heldBallEnded` avoids new tick-deadline state and keeps `boot.ts:360-367` unchanged.*

9. `test/rules-bonus.test.ts` — **new**, headless, added to `ENTRY_FILES` (`test/rules-devices-headless.test.ts:193-202`) in the same change or the completeness ratchet reddens. Covers every I/O-matrix row that is rules-side, each scenario run at **both** player index 0 and index 1, using `runRulesScript` with `NO_BALL_SAVE_TUNING` for anything that drains and `initialState` for the tilted and seeded-`strikes` cases. Pin `bonusCountMs` at **production** magnitude with the step spacing authored as a literal at the probe, never read back from the tunable. *Rationale: Rule 8 and Rule 19; epic vacuity #43 and Story 2.9's twin `ballSaveGraceMs`/`ballSaveHurryUpMs` failures, where every behavioural assertion ran under an override so the production value was never on the path.*

10. `test/contracts.test.ts` — add the `bonus_count_step` arm to `describeEvent()`, plus an **executing** assertion with deliberately distinct field values, following `:260-271`. Add the missing executing assertion for the existing `ball_ended` arm in the same pass, and update the `:196-207` fixture to the now-total category record. *Rationale: `DW-223` — a dozen-plus arms are compiled but unexercised, so a wrong field reference type-checks and ships.*

11. `test/backglass-frame.test.ts` and `test/backglass-integration.test.ts` — append the new display literal(s) to `DISPLAY_LITERALS` `:497` (uncovered otherwise), assert the BONUS row's text across the step ticks at the row level and at the `litDots` level for one frame, and add the `events: []` re-fold control in the shape of `:154-179`. *Rationale: Rule 1's Integration AC, and the control is what makes the claim "the Backglass reads the event stream" rather than "the Backglass reads the snapshot".*

12. `test/replays/*.golden.json` (five) — **header-only** refresh: `header.tableHash` (task 1 adds a `TABLE` block) and `header.gameStart.tuning` (`bonusCountMs` plus its derived `bonusCountTicks`, and the three scoring values — five new blocks, non-adjacent) only, with **appended** `notes`. Leave `header.assetHash`, `header.physicsVersion`, `header.tickHz`, `header.physicsSeed`, `header.gameStart.seed/adjustments/highscores`, `transitions`, `coilPrologue`, `durationTicks`, `expectedHash`, `expectedGameStateHash`, `checkpointTicks` and `expectedCheckpointHashes` byte-identical. Build the harness **in the scratchpad**, never under `test/`, over the shipped `runReplay` / `buildHeader` / `tableHash()` / `assetHash(doc)` / `PHYSICS_VERSION` surface with a scratchpad vitest config naming the absolute harness path (`test/fixtures/dw70-ad7/vitest.harness.config.ts` is the shape). Trace BEFORE and AFTER on a tick stride into full-precision per-ball TSV and diff — expect no output; the known corpus is ~1,132 sampled rows across the five (80 / 32 / 371 / 384 / 265), and a materially different row count means the instrument is wrong. Serialize `gameStart.tuning` in `resolveTuning()`'s natural declaration order with `value`/`source`/`confidence` per entry — `DW-213`'s `reopen_if` fires if the order flips to sorted or `confidence`-first. *Rationale: AD-15's 2026-09-05 amendment — the whole serialized tuning output, `source` prose included, is hashed. Story 2.4 (`b9f5431`) and Story 2.9 are the precedents; both moved `tableHash` in a header-only refresh and neither needed an author grant.*

**Acceptance Criteria:**

- **AC 1 — categories increment from device events.** Given `phase: 'game'` and a player at `currentPlayer`, when `bank_target_down`, `shot_left_loop_made` or `shot_right_loop_made` arrives, then that player's matching `bonus.byCategory` entry increments by exactly 1 and no other category moves; and given `shot_ramp_made` or `phase: 'attract'`, when the same fold runs, then no category moves at all.
- **AC 2 — the multiplier ladder.** Given a player whose `bonus.multiplier` is 1, when four `lanes_completed { set: 'top' }` arrive on successive ticks, then the multiplier reads 2, 3, 5, 5 after each; and given a `lanes_completed { set: 'inout' }`, when it arrives, then the multiplier is unchanged.
- **AC 3 — the untilted payload and the score.** Given an untilted ball at a non-saving tuning with letters 2, loops 1 and multiplier 3, when the drain takes `ballsInPlay` to 0, then `ball_ended` carries that `bonusByCategory`, `multiplier: 3` and `total: 60000`, and the **ending** player's `score` rises by exactly 60000 while every other player's is untouched.
- **AC 4 — the count-up stream is paced by `bonusCountMs` at its production value.** Given the AC 3 ball ending at tick `E` under `NO_BALL_SAVE_TUNING` — which overrides **only** `ballSaveMs` and `ballSaveGraceMs` and therefore leaves `bonusCountMs` at its shipped production value, so the pacing tunable is genuinely on the path — when the run continues past `E`, then exactly three `bonus_count_step` events arrive, at `E + 400`, `E + 800` and `E + 1200` — the offsets authored at the probe as literals, never read back from the tunable — with `step` 1..3, `steps: 3`, and the third carrying `running === total === 60000`; and given a ball whose total is 0, when it ends, then no `bonus_count_step` is emitted.
- **AC 5 — a tilted ball forfeits the bonus and keeps the score.** Given `machine.tilt.tilted: true` seeded on an otherwise identical ball, when it ends, then `ball_ended` carries `total: 0` and `tilted: true`, the player's `score` is unchanged, and no `bonus_count_step` is emitted.
- **AC 6 — the per-ball reset, and what survives it.** Given a player who ended a ball with a non-empty bonus, when `ball_will_start` fires for their next ball, then their `byCategory` is all-zero and `multiplier` is 1, while `letters`, `score`, `lockCredits` and `extraBalls` are byte-identical to their pre-reset values.
- **AC 7 — `DW-208`: `modeEvents` has a production consumer.** Given the shipped `rules.step()`, when a `lanes_completed { set: 'top' }` is produced by the base mode, then it is consumed inside `rules.step()` by `advanceBonusMultiplier` and the multiplier advance is observable in the returned `GameState` — with no test-only surface on the path and no `GameState` write from `src/sim/loop/**` (`test/ad7-device-slots.test.ts:432-490` stays green with exactly two permitted writes).
- **AC 8 — Integration (Rule 1): the Backglass animates to the stream.** Given the `FrameOutput` frames from an untilted ball end folded through `advanceBackglass()` and `renderFrame()` exactly as `src/host/boot.ts:291-292` does, when the three `bonus_count_step` ticks pass, then the `ball_ended` screen's BONUS row text changes at each step and ends at the formatted total; and when the identical frames are re-folded with `events: []`, then that row never appears.
- **AC 9 — the goldens moved for exactly two header reasons.** Given the change is complete, when each of the five goldens is parsed **per field** at `f79fd0c` and at HEAD and compared, then `header.tableHash`, `header.gameStart.tuning` and `notes` are the only fields that differ on any of them, and `pnpm test` passes with no `StaleReplayHeaderError`.

## Spec Change Log

## Review Triage Log

## Design Notes

**Governing architecture decisions (Rule 6).** **AD-19** is the primary: it names *"modes, **scoring** and the ball controller"* as the consumers of device and shot events, and `src/sim/rules/bonus.ts` is the scoring peer that AD-19 has always described and the tree has never had. **AD-7** fixes the scopes — bonus by category and multiplier are player-scoped, `GameState` mutates only inside `rules.step()` — and is the decisive constraint on `DW-208` (below). **AD-18** puts the drain, the ball end and therefore the payment under one arbiter, the ball controller. **AD-9** governs `bonus_count_step`'s payload-completeness, and its own Rule already names `ball_ended { player, bonusByCategory, multiplier, total, tilted }` as the worked example of a payload-complete event. **AD-3** governs the pacing: *"every display-paced sequence (**bonus count-down**, Match reveal, …) is authored in ms in `tuning.ts`, converted to ticks once at load, and drives presentation by emitting step events; presentation animates to them and never reports completion"* — this story is the first implementation of that clause. **AD-15** governs the four tunables and the golden budget. **AD-11** puts the shot→category mapping in `TABLE`; **AD-16** is why it cannot live anywhere else. **AD-6** supplies the drain semantics the payment hangs off. **No AC contradicts any AD Rule.**

**Consumes:** Story 2.4 (`bank_target_down`, `shot_*_made`, `device_ball_entered`, the devices layer's whole event vocabulary) · Story 2.5 (the ball controller, `startBall()`, `ball_ended`, `ballsInPlay`, player rotation and game over) · Story 2.6 (`advanceBackglass()` / `renderFrame()` / `rasterise()` and the `ball_ended` screen) · Story 2.7 (the base mode, `lanes_completed`, `RulesStepResult.modeEvents`) · Story 2.9 (the ball-save early return, and the `NO_BALL_SAVE_TUNING` override idiom every drain test here depends on) · Story 2.3 (the DRAGON drop bank that produces `bank_target_down`) · AD-15's tuning resolver (`shotWindowTicks()`).

**Consumed-by:** **Story 2.11 (Tilt)** — this story ships the read-side forfeit (`total: 0` when `machine.tilt.tilted`), which nothing sets true until 2.11; 2.11 makes AC 5 reachable in real play. **Story 2.13 (Game over, Match, Attract)** — the final scores it shows include the bonus this story pays, and it owns the Backglass line budget the BONUS row shares. **Story 3.7 / the War (Epic 3)** — the first producer of the `strikes` category. **Story 3.11** — the playtest freeze that turns the four `unverified` tunables into `playtested`.

**Integration ACs (Rule 1).** This story introduces two shared surfaces and both carry an integration criterion against a real consumer, not against internal state. `bonus_count_step` → **AC 8**, asserted on the Backglass's rendered DMD rows through the exact fold `src/host/boot.ts:291-292` performs, with the `events: []` re-fold as its control. `src/sim/rules/bonus.ts` → **AC 7**, asserted on the `GameState` that `rules.step()` returns to a real caller, with the `sim/loop` source gate proving no other layer wrote it. `bonusTotal()`'s third category has no producer this epic, which is stated below rather than faked.

**Why `DW-208`'s fix is rules-side, and not the loop channel its note names.** The entry's root cause — *"`RulesStepResult.modeEvents` has no production consumer"* — is exactly what task 6 closes. Its note goes on to say *"must add the `sim/loop` channel"*, and that half is not implementable as written: `AD-7`'s Rule is *"mutated only inside `rules.step`"*, enforced at source by `test/ad7-device-slots.test.ts:432-490`, which asserts that the **only** two writes to `sim/loop`'s `state` binding in **every** `.ts` under `src/sim/loop/` are the boot construction and `state = rulesResult.state;`. The multiplier is `GameState`, so the loop cannot advance it; a loop that merely forwarded `modeEvents` into `FrameOutput` would satisfy neither AC 2 nor AC 7 and would build a fifth `FrameOutput` channel with no reader — the precise vacuity Story 2.7's own `Never` section cited when it declined to emit `lane_lit`. The tree already says so twice, in `src/sim/rules/index.ts:131-140` (*"its only consumer, Story 2.10's bonus multiplier, is rules-side"*) and `src/sim/rules/modes/events.ts:7`. So `DW-208` is **addressed, not declined** — by AC 7 and task 6 — and its note's suggested mechanism is recorded here as measured against AD-7 and replaced, so the adjudication gate reads a decision rather than a surprise.

**The score-ownership decision, made deliberately.** `players[i].score` has exactly one increment site in the whole codebase today, `src/sim/rules/modes/skill-shot.ts:130`; the ball controller has never written it. This story makes it the second writer, and the reasoning is that no other module *can* be correct here. The payment needs three facts that exist only inside `ballController.step()`'s drain branch: that the ball ended at all (the branch is entered at `:566-574`, and the ball-save early return at `:593` means "a parking entry took `ballsInPlay` to 0" is **not** sufficient), which player it ended for **before** rotation (`endingPlayer`, captured at `:575`, and `startBall()` moves `currentPlayer` at `:629`), and that player's bonus **before** teardown (`:596`). Handing those to a scoring module would create a second owner of "the ball has ended", which is precisely the seam AD-18 exists to keep single. The precedent in the same file is explicit: the letters accumulator at `:451-467` is commented *"Not a mode, not scoring -- raw per-player state the ball controller already owns the write scope for"*. Accumulation stays in `bonus.ts`; **payment** — the one act that needs the pre-rotation, pre-teardown snapshot — is the ball controller's, and it happens in the same `players` map as teardown so the two cannot diverge. Governed by **AD-18** (single arbiter) and **AD-7** (player-scoped, inside `rules.step`).

**Why the two folds sit either side of the ball controller.** `creditBonusFromDeviceEvents` runs at `index.ts:236`, before the controller, so a `bank_target_down` on the drain tick lands in that ball's own payload — the same tick position at which the letters accumulator already reads that event, so the two can never disagree. `advanceBonusMultiplier` must run after `modeStack.step()` at `:246`, because `lanes_completed` does not exist before it and the stack's position after the controller is load-bearing (`index.ts:240-245`). The residual: a `lanes_completed { set: 'top' }` on the **exact** drain tick advances the multiplier after that ball's `ball_ended` was built. It is unreachable in Epic 2 — a Top-lane rollover and a trough entry are two switches one ball cannot close on one tick, and Epic 2 has no multiball — and even when Epic 3 makes it reachable, "a multiplier earned in the same millisecond the ball drained does not pay on that ball" is a defensible answer, not a defect. Recorded so a later reviewer reads a decision rather than discovering an accident.

**Why the count-up schedule is closure state.** `GameState` is hashed (AD-15) and `machine` is present in all five goldens' attract snapshots, so a machine-scoped schedule field would move `expectedGameStateHash` on every golden — a state-hash re-record, which is out of budget and a Block-If. A player-scoped field would be hash-free (the goldens' `players[]` is empty) but is wrong scope: the schedule is a display pacer, not a fact about the player. So it follows the established closure pattern (`pendingLockLaneClosure`, `awaitingSaveLaunch`, `awaitingSaveRelaunch`). The epic's rule for such a latch is two obligations — a reset at `ball_will_start` and a bounded lifetime — and this one satisfies both **by construction rather than by a reset call**, which is the part worth stating: it holds at most `steps` entries, each is emitted once at its own tick and dropped, and arming replaces any previous schedule wholesale. A `ball_will_start` reset would in fact be *wrong* here, because `startBall()` runs on the same tick as `ball_ended` and would wipe the schedule before its first step — which is why task 7(e) arms it **after** the game-over/rotation block.

**The `strikes` category has no producer this epic, and that is not a gap.** PRD FR-20 names three categories (letters, Loops, Strikes) and this story's own AC 1 names event sources for only two. A Strike is the War mode's, and the War is Epic 3: grep confirms the only `strike` in the tree is `strikesRemaining` (a `ModeView` display field no mode sets) and physics' drop-target `DropTargetStrike` collision concept, neither of which is a bonus category. So `strikes` ships as a declared category, seeded 0, with an authored value, and **stays 0 all epic**. No test may fabricate a producer for it. What the tests *do* prove honestly is that the arithmetic covers it: a seeded `initialState` with a non-zero `strikes` count contributes `count × bonusStrikeValue × multiplier` to the total, which pins the `Σ` as exhaustive without inventing an event.

**Anti-vacuity plan for four new tunables (epic vacuity #43, and Story 2.9's two repeats).** `bonusLetterValue`/`bonusLoopValue`/`bonusStrikeValue` could each be set to 0, and `bonusCountMs` to 1, and a suite whose every expectation re-imported them would stay green — which is exactly how `ballSaveGraceMs` and `ballSaveHurryUpMs` each shipped settable to 1 with the whole suite green, twice, because every behavioural assertion ran under an override tuning so the production value was never on the path. So: **AC 3's `total: 60000` is authored as a literal at the assertion**, arithmetically consistent with the shipped values but not read back from them, and **AC 4's three step offsets (400 / 800 / 1200) are authored at the probe**. The one override these two ACs use is `NO_BALL_SAVE_TUNING`, and it is what makes them production probes rather than override probes: it replaces `ballSaveMs` and `ballSaveGraceMs` and **nothing else**, so `bonusCountMs` and all three scoring values are on the path at their shipped values while the ball-save early return at `ball-controller.ts:593` is kept off it. No test in this story may override any of the four new tunables. Any non-vacuity guard added to a loop in this story must derive its expected count from an **independent** source — `BONUS_CATEGORIES.length`, or the authored fixture — never from a second call of the helper the loop consults (vacuity #44). And because a mutation is a property of a **tree**, not of a test (#48), every mutation below must be re-walked at the final tree if any line it targets was edited after it was first observed.

**What the Backglass tests prove, and what they cannot (AD-15, epic vacuity #42).** AC 8 is a claim about `DmdRow` text and, for one frame, the `litDots` dot buffer — both pure functions with no engine, so both are honestly testable headlessly. **None of it proves a pixel changed.** `NullEngine` rasterises nothing and `RawTexture.readPixels()` returns `null` under it (`backglass.ts:14-21`), so there is no pixel readback anywhere in this suite. That the BONUS row is legible on the real panel is the lead's browser smoke, not this story's suite. The `events: []` re-fold control matters for a second reason worth naming: a bonus display could be faked entirely off `snapshot.game.players[i].bonus`, which would look correct in every positive test and would be reading the wrong thing; the control is what distinguishes them.

**Golden budget, measured at this tree (2026-09-08).** All five goldens carry `header.tableHash bac5816c` / `header.assetHash ab163ff` / `header.physicsVersion v1-ce6772ef`, measured directly — **not** the `672573c` recorded in Story 2.9's spec, which is the superseded value. Task 1's `bonusWiring` block moves `tableHash` (it hashes the whole frozen `TABLE`); task 2's four tunables move `header.gameStart.tuning` by five blocks (`bonusCountMs` plus its derived `bonusCountTicks`, which inherits its `source` and `confidence` verbatim, and the three suffix-less scoring values, which derive nothing). `assetHash` cannot move: it hashes the parsed collision document built from the `col_` / `sw_` / `ballDevices` prefixes, and neither a `TABLE` wiring block nor a tunable reaches it. **This is a header-only refresh — routine, no author grant** (precedents: Story 2.4 `b9f5431` and Story 2.9, both of which moved `tableHash` in exactly this way). Note this is one item wider than the dispatch's framing of "two keys in `header.gameStart.tuning`": it is five tuning blocks *plus* `tableHash`, all header fields, so the budget is unchanged. **What would need a grant, and must instead HALT, is any movement in a body field or a state hash.**

**Verify the goldens structurally, never by grepping the diff.** Story 2.9's own Block-If gate was a `git diff | grep` over the golden field names, and it is permanently self-tripping: the appended `notes` prose names six of the seven fields, so the unanchored form emitted 27 lines of false HALT while every body field was in fact byte-identical. Anchor any pre-filter to the field's own key position (`^[-+]\s*"<field>"`) and treat the **per-field JSON parse at `f79fd0c` versus HEAD as the authority**. A gate that fires on its own story's bookkeeping teaches a reviewer to wave it through, which is how a real body move ships.

**Decision-pending entries this story does not touch.** **`DW-204`** (does the paying skill-shot lane freeze at `ball_launched`?) is a skill-shot question: `applyLaneChangePressed` (`base.ts:134-144`) rotates the `lit` flags and therefore does affect *when* `lanes_completed` fires, but AC 2's rule — "advance on each `lanes_completed { set: 'top' }`" — is unchanged whichever way the author decides, so this story neither builds on it nor pre-empts it. **`DW-212`** (`l_lock` between balls and in Attract) is a lamp question; this story adds no lamp and reads no lamp state. **`DW-226`** (ball-save eligibility after a grace lapse) and **`DW-232`** (a saved ball's second skill shot) are both ball-save compositions; every drain test here runs at a non-saving tuning specifically so the save is not on the path, and the one scenario that *is* about a save asserts only that the existing early return still precedes everything this story adds.

**Ledger inbox (Rule 17).** `bash _bmad/scripts/ledger.sh _bmad-output/implementation-artifacts/deferred-work.md slice 2-10-end-of-ball-bonus-and-the-multiplier` returns exactly one entry at this tree: **`DW-208`**, addressed by **task 6** and **AC 7** above. Nothing is declined.

## Verification

**Commands** (export `BLENDER="C:/Users/Josh/tools/blender-5.2.1-windows-x64/blender.exe"` first, in **every** shell — unset, roughly two dozen Blender-gated cases skip instead of running and the counts change, so state which you did):
- `pnpm typecheck` — expected: exits 0 across all three tsconfigs. A missing `bonus_count_step` arm in `test/contracts.test.ts`'s `describeEvent()` fails here first, and so does any `byCategory` fixture that is no longer a total record. This is a **typecheck** gate only; `pnpm test` transpiles without it.
- `pnpm test` — expected: 0 failing, **0 skipped**. Baseline at `f79fd0c` is **114 files / 1868 tests**, measured with `BLENDER` exported; report the counts as measured and account for the delta (one new test file plus the new cases). ~23 skips means a broken shell, not a result. If `test/export-py-skip-visibility.test.ts` alone fails with a null status, that is `DW-227`'s load-dependent nested-vitest timeout, not this story.
- `pnpm lint:boundaries` — expected: `OK -- N .ts file(s)`, N grown by one for `src/sim/rules/bonus.ts`. Any `no-device-name-literal`, `sim-no-literal-ms`, `sim-one-tick-constant` or `sim-no-colour` violation is a failure — in particular, a literal `'shot_left_loop'` or `'shot_left_loop_made'` anywhere outside `src/sim/table/dragonwar.ts` fails here.
- `pnpm check:headers` and `pnpm check:attributions` — expected: exit 0; `src/sim/rules/bonus.ts` carries the GPL-3.0 header.
- `pnpm check:ad7` — expected: exit 0 with **exactly 3** passing tests (the count is over `test/fixtures/dw70-ad7/`'s own `it()` blocks and is not affected by contract changes). `pnpm check:corridor` — exit 0. `pnpm check:reachability` — exit 0 over its **52** cases (32 reachable / 20 unreachable), roughly two minutes.
- **Golden body-field pre-filter:** `git diff test/replays/ | grep -E '^[-+][[:space:]]*"(expectedHash|expectedGameStateHash|expectedCheckpointHashes|checkpointTicks|assetHash|transitions|coilPrologue|durationTicks)"'` — expected: **no output**. Anchored to the field's own key position, so the appended `notes` prose cannot trip it. This is a fast pre-filter; **the authority is the structural check below.**
- `git ls-files --others --exclude-standard test/` — expected: only the intended new test file. The re-record harness must never appear under `test/`.
- `git diff --stat -- public/assets/` — expected: **empty**. Neither a `TABLE` wiring block nor a tunable reaches an exported asset.

**Manual checks:**
- **Structural golden comparison (the authoritative one).** For each of the five goldens, parse the JSON at `f79fd0c` and at HEAD and compare **per field**: assert `header.tableHash` and `header.gameStart.tuning` differ, `notes` is a strict append, and every one of `header.assetHash`, `header.physicsVersion`, `header.tickHz`, `header.physicsSeed`, `header.gameStart.seed`, `header.gameStart.adjustments`, `header.gameStart.highscores`, `transitions`, `coilPrologue`, `durationTicks`, `expectedHash`, `expectedGameStateHash`, and (on `roll-and-drain` only) `checkpointTicks` and `expectedCheckpointHashes` is **deeply equal**. Never a substring grep over the diff.
- **Re-record trace diff.** `trace-BEFORE` versus `trace-AFTER`, tick-stride sampled at full precision: expect **no output**, with a row count of the order of **1,132** across the five (80 / 32 / 371 / 384 / 265). A drained ball emits no row, so a shifted drain shows as a row-count change. A materially different count means the instrument, not the change, is wrong.
- Inspect the `gameStart.tuning` diff: exactly **five** new blocks per golden, in `resolveTuning()`'s natural declaration order with `value` / `source` / `confidence` per entry. A four-figure reordering diff means the harness flipped the key order and has tripped `DW-213`'s `reopen_if`.
- Inspect each golden's `notes`: **appended**, never rewritten, with the existing `DW-70` and `deviceSlots` provenance intact.

**Rule 19 mutations — one per acceptance criterion. Apply each, observe red, revert from a saved copy (never `git checkout --`, never `git stash`), and confirm `git status --short` and `git diff --stat` are unchanged afterwards. State the expected red BEFORE running each one — a mutation harness can itself be vacuous, and at Story 2.7's gate a `head -1` extractor matched vitest's test-*files* counter and under-reported two mutations' red. Re-walk any mutation whose target line was edited after it was first observed (vacuity #48).**
- **AC 1** — in `creditBonusFromDeviceEvents`, drop the `shot_*_made` branch → the loops-credited cases in `test/rules-bonus.test.ts` redden while the letters cases stay green.
- **AC 2** — remove the ladder cap so the multiplier keeps climbing past the last rung → the fourth `lanes_completed { set: 'top' }` assertion (still 5) reddens; the 1→2→3 assertions stay green.
- **AC 3** — revert the `score` write in the drain branch, leaving the payload correct → the score-delta assertion reddens while the `ball_ended` payload assertions stay green. (The pair is the point: it proves the two are asserted independently.)
- **AC 4** — set `bonusCountMs` to `1` in `src/sim/table/tuning.ts` → the production-tuning spacing probe reddens on the authored 400/800/1200 offsets. **This is the mutation that proves the tunable is on a real path**; if the suite stays green, every AC 4 assertion is running under an override and the pin is vacuous (Story 2.9 shipped this exact failure twice).
- **AC 5** — remove the `tilted` guard from the total computation → the tilted case's `total: 0` and unchanged-score assertions redden.
- **AC 6** — drop `bonus: EMPTY_BONUS` from `startBall()`'s `players` map → the next-ball reset assertions redden while the letters-persist assertion stays green.
- **AC 7** — delete the `advanceBonusMultiplier` call from `src/sim/rules/index.ts:247` → every AC 2 assertion reddens through the real `rules.step()` path. Separately, add a third write to the `state` binding in `src/sim/loop/index.ts` → `test/ad7-device-slots.test.ts:432-490` reddens, proving the loop gate is live.
- **AC 8** — make `advanceBackglass()` read the running bonus from `input.snapshot.game.players[...]` instead of the event → the positive row assertions stay green and **the `events: []` control reddens**. This is the mutation that proves the control is doing work.
- **AC 9** — revert one golden's `header.tableHash` to `bac5816c` → `test/replay-goldens.test.ts` reddens with a named `StaleReplayHeaderError`.

**What this story's suite does not prove.** That any dot on the real panel changed (`NullEngine` rasterises nothing; AD-15 forbids automated rendering assertions). That a tilted ball is reachable in play — `machine.tilt.tilted` is only ever seeded here, and Story 2.11 is what sets it. That the `strikes` category ever increments — it has no producer until Epic 3, by design.

## Auto Run Result

Status: ready-for-dev
Blocking condition: none
