/* DragonWar is licensed GPL-3.0. See LICENSE, NOTICE, and ATTRIBUTIONS.md.
   A BLOCK-comment header on purpose: it leaves the "// Ported from " line
   below as the file's FIRST line-comment, so the only thing standing between
   that marker and a whole-file Rule 14 exemption is the real code above it. */
export const declaredBeforeTheMarker = 1;
// Ported from nowhere-real (not-a-real-licence); this marker follows real
// CODE, so it must NOT exempt this file. Fixture only; DragonWar-authored.
export const stillCaught = 'section §';
