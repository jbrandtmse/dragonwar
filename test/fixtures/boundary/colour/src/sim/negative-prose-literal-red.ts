// DragonWar is licensed GPL-3.0. See LICENSE, NOTICE, and ATTRIBUTIONS.md.
// Negative control: a prose provenance string literal that merely CONTAINS
// the word "red", not an anchored bare colour name -- must NOT fire
// sim-no-colour (AD-9). Mirrors a real TUNING entry's own `source` prose.
export const provenance = 'authored: no artifact states this figure; not sourced from any red herring';
