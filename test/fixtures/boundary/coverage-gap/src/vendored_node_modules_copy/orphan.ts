// DragonWar is licensed GPL-3.0. See LICENSE, NOTICE, and ATTRIBUTIONS.md.
// Deliberate coverage-gap fixture: dependency-cruiser's own
// `doNotFollow: { path: 'node_modules' }` (tools/dependency-cruiser.config.mjs)
// is an UNANCHORED substring match against the resolved path -- a directory
// merely containing "node_modules" anywhere in its name (not only a
// directory literally called that) is excluded from the cruise result
// entirely, verified empirically during this story's implementation, even
// though it is a real `.ts` file under this root's src/. This directory name
// is deliberately NOT literally "node_modules" so it is not also caught by
// the repository's own .gitignore (`node_modules/`, unanchored, matches only
// a directory whose name is exactly that). tools/boundary-lint.mjs's
// coverage guard (task 10b) must catch exactly this: a `.ts` file
// dependency-cruiser silently never saw.
export const orphanValue = 1;
